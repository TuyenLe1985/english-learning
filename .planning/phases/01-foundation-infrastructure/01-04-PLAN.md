---
phase: 01-foundation-infrastructure
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-03
files_modified:
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/nest-cli.json
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
  - apps/api/src/health/health.controller.ts
  - apps/api/src/health/health.module.ts
  - apps/api/vitest.config.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "GET http://localhost:3001/api/health returns {status: ok, timestamp: ...} with HTTP 200"
    - "NestJS boots in under 5 seconds with ValidationPipe globally applied"
    - "Type-check passes for apps/api with no errors"
  artifacts:
    - path: "apps/api/src/main.ts"
      provides: "NestJS bootstrap with global prefix and ValidationPipe"
      contains: "setGlobalPrefix"
    - path: "apps/api/src/health/health.controller.ts"
      provides: "GET /api/health endpoint"
      contains: "status: 'ok'"
    - path: "apps/api/nest-cli.json"
      provides: "SWC compiler config"
      contains: "\"builder\": \"swc\""
  key_links:
    - from: "apps/api/src/main.ts"
      to: "localhost:3001"
      via: "app.listen()"
      pattern: "PORT.*3001"
    - from: "apps/api/tsconfig.json"
      to: "packages/tsconfig/nestjs.json"
      via: "extends"
      pattern: "@repo/tsconfig/nestjs.json"
---

<objective>
Scaffold the NestJS 11 API skeleton: app module, health controller, global ValidationPipe, SWC compiler config. This delivers the first half of the walking skeleton — a running NestJS API that responds to HTTP requests.

Purpose: The NestJS bootstrap pattern (SWC compiler, emitDecoratorMetadata, global API prefix) must be correct from day one. Changing the global prefix later breaks all client URLs across all phases.

Output: GET /api/health returns 200 with JSON body. pnpm type-check passes. pnpm lint passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md

<interfaces>
From packages/tsconfig/nestjs.json (created in Plan 01):
  extends: "./base.json"
  compilerOptions: module "CommonJS", moduleResolution "node", experimentalDecorators true, emitDecoratorMetadata true

From packages/database/src/index.ts (created in Plan 01):
  export const prisma: PrismaClient
  export * from '../generated/client'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NestJS app scaffold — package.json, tsconfig, nest-cli.json, app module, health module</name>
  <read_first>
    - .planning/phases/01-foundation-infrastructure/01-RESEARCH.md (Pattern 2: NestJS 11 bootstrap, SWC config, health controller; anti-patterns: NodeNext module in NestJS tsconfig)
    - .planning/phases/01-foundation-infrastructure/01-CONTEXT.md (D-01: @repo/database import; D-11: PORT env var in apps/api/.env.local)
    - CLAUDE.md (version pins: @nestjs/core ^11.1.26, @nestjs/common ^11.1.26, @nestjs/platform-express ^11.1.26)
  </read_first>
  <files>
    apps/api/package.json,
    apps/api/tsconfig.json,
    apps/api/nest-cli.json,
    apps/api/.env.local,
    apps/api/src/main.ts,
    apps/api/src/app.module.ts,
    apps/api/src/health/health.controller.ts,
    apps/api/src/health/health.module.ts
  </files>
  <action>
    Create the NestJS 11 API skeleton. This is the Phase 1 shell — no database queries in the health endpoint (those come in Phase 2). Just bootstrapped NestJS with ValidationPipe and a simple health JSON response.

    APPS/API/PACKAGE.JSON: name "@repo/api", version "0.0.1", private true. scripts: build "nest build", dev "nest start --watch", start "node dist/main", type-check "tsc --noEmit", lint "eslint src --ext .ts". dependencies: @nestjs/common@^11.1.26, @nestjs/core@^11.1.26, @nestjs/platform-express@^11.1.26, @nestjs/config@^4.0.4, @repo/database (workspace:*), @repo/shared (workspace:*), reflect-metadata@^0.2.2, rxjs@^7.8.1. devDependencies: @nestjs/cli@^11.0.0, @nestjs/schematics@^11.0.0, @types/express@^5.0.0, @types/node@^20.0.0, @swc/core@latest, @swc/cli@latest, vitest@^2.x, typescript@^5.4.0, @repo/tsconfig (workspace:*), @repo/eslint-config (workspace:*).

    APPS/API/TSCONFIG.JSON: extends "@repo/tsconfig/nestjs.json" (the NestJS-specific config with emitDecoratorMetadata: true and CommonJS module). compilerOptions: outDir "./dist", rootDir "./src", paths (empty for now). include ["src/**/*"]. exclude ["dist", "node_modules"].

    APPS/API/NEST-CLI.JSON: schema "https://json.schemastore.org/nest-cli", collection "@nestjs/schematics", sourceRoot "src", compilerOptions: builder "swc", typeCheck true. CRITICAL: builder "swc" enables SWC for dev builds (4-5x faster than tsc); typeCheck true keeps TypeScript errors visible.

    APPS/API/.ENV.LOCAL: JWT_SECRET=change_me_in_production, PORT=3001. This is .gitignored (apps/api/.gitignore should be created too: node_modules, dist, .env.local).

    APPS/API/SRC/MAIN.TS: Import NestFactory from @nestjs/core; ValidationPipe from @nestjs/common; AppModule. bootstrap() function: create app with NestFactory.create(AppModule), app.setGlobalPrefix('api') — ALL routes will be under /api/ prefix, app.useGlobalPipes(new ValidationPipe({ whitelist: true, transform: true })) — whitelist: true strips unknown DTO properties (security: prevents property injection attacks), transform: true auto-transforms plain objects to DTO class instances, app.enableShutdownHooks(), await app.listen(process.env.PORT ?? 3001). Call bootstrap().

    APPS/API/SRC/APP.MODULE.TS: @Module decorator with imports: [HealthModule], controllers: [], providers: []. Import HealthModule from ./health/health.module. Use ConfigModule.forRoot({ isGlobal: true }) if @nestjs/config is available. Keep it minimal for Phase 1.

    APPS/API/SRC/HEALTH/HEALTH.MODULE.TS: @Module with controllers: [HealthController], providers: []. Exports []. Import HealthController from ./health.controller.

    APPS/API/SRC/HEALTH/HEALTH.CONTROLLER.TS: @Controller('health') class HealthController. @Get() method check() returns { status: 'ok', timestamp: new Date().toISOString() }. This is a simple JSON response — no database call needed in Phase 1 (Terminus integration added in Phase 2 when auth service actively queries the DB).
  </action>
  <verify>
    <automated>
      cd /path/to/apps/api &amp;&amp; pnpm --filter @repo/api run type-check 2>&amp;1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - apps/api/nest-cli.json contains "builder": "swc" and "typeCheck": true
    - apps/api/tsconfig.json extends "@repo/tsconfig/nestjs.json"
    - apps/api/src/main.ts contains app.setGlobalPrefix('api') and ValidationPipe with whitelist: true
    - apps/api/src/health/health.controller.ts returns {status: 'ok', timestamp: ...}
    - pnpm --filter @repo/api run type-check exits 0 with no errors
    - apps/api/package.json contains @nestjs/common@^11.1.26 (NOT ^10 or ^12)
    - reflect-metadata is in dependencies (required by NestJS DI container)
  </acceptance_criteria>
  <done>NestJS skeleton created; all files written; type-check passes; SWC builder configured; ValidationPipe with whitelist:true globally applied; health controller returns status:ok JSON</done>
</task>

<task type="auto">
  <name>Task 2: Vitest config + smoke test for NestJS health endpoint</name>
  <read_first>
    - .planning/phases/01-foundation-infrastructure/01-RESEARCH.md (Validation Architecture section: Wave 0 requirements — apps/api/vitest.config.ts with SWC transformer)
    - .planning/phases/01-foundation-infrastructure/01-VALIDATION.md (Per-task verification map: smoke-api-health test)
    - CLAUDE.md (Testing stack: Vitest 2.x, @vitest/coverage-v8 for NestJS)
  </read_first>
  <files>
    apps/api/vitest.config.ts,
    apps/api/src/health/health.controller.spec.ts
  </files>
  <action>
    Create the Vitest config (required by VALIDATION.md Wave 0) and a unit test for HealthController. This is the test scaffold that CI will invoke.

    APPS/API/VITEST.CONFIG.TS: defineConfig with test: { globals: true, environment 'node', include ['src/**/*.spec.ts', 'src/**/*.test.ts'], coverage: { provider 'v8', reporter ['text', 'json'] } }. No SWC plugin needed for Vitest — Vitest handles TypeScript natively; SWC is for the NestJS CLI build tool (nest build), not for Vitest.

    APPS/API/SRC/HEALTH/HEALTH.CONTROLLER.SPEC.TS: Import { Test, TestingModule } from @nestjs/testing; import HealthController. describe('HealthController') block: beforeEach creates testing module with controllers: [HealthController]. Test "check() returns status ok": call controller.check(), expect result.status to equal 'ok', expect result.timestamp to be a string. This satisfies smoke-api-health from VALIDATION.md.

    Add "test" script to apps/api/package.json scripts: "test": "vitest run", "test:watch": "vitest".
  </action>
  <verify>
    <automated>
      pnpm --filter @repo/api run test 2>&amp;1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - apps/api/vitest.config.ts exists
    - apps/api/src/health/health.controller.spec.ts exists with at least 1 test
    - pnpm --filter @repo/api run test exits 0 with "1 test passed" (or similar)
    - Test asserts result.status === 'ok'
    - Test file is NOT in the src build output (vitest config uses include pattern, not part of nest build)
  </acceptance_criteria>
  <done>Vitest config created; health controller unit test passes; pnpm --filter @repo/api run test exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → NestJS API | All incoming request bodies must pass through ValidationPipe |
| NestJS → database | No database calls in Phase 1 health endpoint; safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-10 | Tampering | ValidationPipe missing | mitigate | ValidationPipe({ whitelist: true, transform: true }) applied globally in main.ts — strips unknown DTO fields before reaching controllers; prevents property injection |
| T-1-11 | Tampering | NestJS DI failure (silent undefined) | mitigate | nestjs.json tsconfig sets emitDecoratorMetadata: true and module: CommonJS — required for decorator-based DI; missing this causes providers to inject as undefined |
| T-1-12 | Information Disclosure | PORT env var default | accept | Default PORT 3001 in main.ts fallback is acceptable for dev; production uses .env.local PORT override |
| T-1-SC | Tampering | @nestjs/* package installs | mitigate | All @nestjs packages are [VERIFIED] (official NestJS organization, github.com/nestjs/nest); pinned to ^11.x |
</threat_model>

<verification>
After both tasks complete:

1. `pnpm --filter @repo/api run type-check` exits 0
2. `pnpm --filter @repo/api run test` shows 1 test passed
3. `grep 'whitelist: true' apps/api/src/main.ts` confirms ValidationPipe config
4. `grep '"builder": "swc"' apps/api/nest-cli.json` confirms SWC builder
5. Start API: `pnpm --filter @repo/api run dev &` then `curl -sf http://localhost:3001/api/health` returns `{"status":"ok","timestamp":"..."}`
6. `grep 'setGlobalPrefix' apps/api/src/main.ts` confirms /api prefix
</verification>

<success_criteria>
- GET http://localhost:3001/api/health returns {"status":"ok","timestamp":"..."} with HTTP 200
- pnpm --filter @repo/api run type-check exits 0
- pnpm --filter @repo/api run test exits 0 (1 test passing)
- ValidationPipe({ whitelist: true, transform: true }) applied globally
- NestJS uses SWC builder (nest-cli.json builder: swc)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-infrastructure/01-04-SUMMARY.md` when done
</output>
