---
phase: 01-foundation-infrastructure
plan: 05
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-03
files_modified:
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.js
  - apps/web/tailwind.config.ts
  - apps/web/postcss.config.js
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/app/api/health/route.ts
  - apps/web/vitest.config.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "GET http://localhost:3000/api/health returns {status: ok, timestamp: ...} with HTTP 200"
    - "Next.js 14 app starts without errors"
    - "pnpm --filter @repo/web run type-check passes"
    - "Tailwind CSS 3.x (NOT v4) is configured"
  artifacts:
    - path: "apps/web/src/app/api/health/route.ts"
      provides: "Next.js App Router health endpoint"
      contains: "status"
    - path: "apps/web/next.config.js"
      provides: "Next.js 14 config with transpilePackages for @repo/*"
      contains: "transpilePackages"
    - path: "apps/web/tailwind.config.ts"
      provides: "Tailwind 3.x config"
      contains: "content"
  key_links:
    - from: "apps/web/tsconfig.json"
      to: "packages/tsconfig/nextjs.json"
      via: "extends"
      pattern: "@repo/tsconfig/nextjs.json"
    - from: "apps/web/src/app/api/health/route.ts"
      to: "Response.json"
      via: "Next.js App Router route handler"
      pattern: "Response.json"
---

<objective>
Scaffold the Next.js 14 App Router skeleton: root layout, placeholder homepage, health API route, Tailwind 3.x config, and Vitest config. This delivers the second half of the walking skeleton — a running Next.js app that responds to HTTP requests.

Purpose: Next.js 14 + Tailwind 3.x + React 18 must be pinned from day one. Installing "latest" would get Tailwind 4 (breaks shadcn/ui) or React 19 (breaks Next.js 14 compatibility). These pins carry through all 8 phases.

Output: GET /api/health returns 200. pnpm type-check passes. pnpm lint passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md

<interfaces>
From packages/tsconfig/nextjs.json (created in Plan 01):
  extends: "./base.json"
  compilerOptions: jsx "preserve", module "ESNext", moduleResolution "Bundler", allowImportingTsExtensions true, noEmit true

From packages/database/src/index.ts (created in Plan 01):
  export const prisma: PrismaClient (globalThis singleton — safe to import in RSCs)
  export * from '../generated/client'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js 14 app scaffold — package.json, configs, root layout, placeholder page, health route</name>
  <read_first>
    - .planning/phases/01-foundation-infrastructure/01-RESEARCH.md (Code Examples: Next.js 14 health route; anti-patterns: React 19 pin, Tailwind v4 pin)
    - .planning/phases/01-foundation-infrastructure/01-CONTEXT.md (D-01: @repo/database import; D-05: apps run natively)
    - CLAUDE.md (version pins: next@^14.2.35, react@^18.3.1, tailwindcss@^3.4.19 — ALL critical)
  </read_first>
  <files>
    apps/web/package.json,
    apps/web/tsconfig.json,
    apps/web/next.config.js,
    apps/web/tailwind.config.ts,
    apps/web/postcss.config.js,
    apps/web/.gitignore,
    apps/web/.env.local,
    apps/web/src/app/layout.tsx,
    apps/web/src/app/page.tsx,
    apps/web/src/app/api/health/route.ts,
    apps/web/src/app/globals.css
  </files>
  <action>
    Create the Next.js 14 App Router skeleton with exact version pins. This is a bare skeleton — no UI components, no data fetching, no auth.

    APPS/WEB/PACKAGE.JSON: name "@repo/web", version "0.0.1", private true. scripts: build "next build", dev "next dev --port 3000", start "next start", type-check "tsc --noEmit", lint "next lint". dependencies: next@^14.2.35 (CRITICAL: NOT 15 or 16 — App Router API changed in Next.js 15), react@^18.3.1 (CRITICAL: NOT 19 — requires Next.js 15+), react-dom@^18.3.1, @repo/database (workspace:*), @repo/shared (workspace:*). devDependencies: @types/node@^20.0.0, @types/react@^18.0.0, @types/react-dom@^18.0.0, tailwindcss@^3.4.19 (CRITICAL: NOT v4 — shadcn/ui incompatible with Tailwind 4), postcss@^8.0.0, autoprefixer@^10.0.0, eslint@^9.0.0, @repo/tsconfig (workspace:*), @repo/eslint-config (workspace:*), vitest@^2.x, @testing-library/react@^16.0.0, jsdom@^25.0.0.

    APPS/WEB/TSCONFIG.JSON: extends "@repo/tsconfig/nextjs.json". compilerOptions: paths for @/* -> ./src/*. include ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"]. exclude ["node_modules"].

    APPS/WEB/NEXT.CONFIG.JS: module.exports = { transpilePackages: ['@repo/database', '@repo/shared'] }. transpilePackages is REQUIRED for monorepo internal packages that export TypeScript directly (just-in-time pattern). Without this, Next.js won't compile the TypeScript source in @repo/* packages. Also add: experimental: {} (empty for now), eslint: { ignoreDuringBuilds: false }.

    APPS/WEB/TAILWIND.CONFIG.TS: type: Config. content: ["./src/**/*.{js,ts,jsx,tsx,mdx}"]. theme: { extend: {} }. plugins: []. This is the Tailwind 3.x config format. Do NOT use the Tailwind 4 CSS-first config format.

    APPS/WEB/POSTCSS.CONFIG.JS: module.exports = { plugins: { tailwindcss: {}, autoprefixer: {} } }.

    APPS/WEB/.ENV.LOCAL: NEXTAUTH_SECRET=change_me_in_dev, NEXTAUTH_URL=http://localhost:3000, GOOGLE_CLIENT_ID=CHANGE_ME, GOOGLE_CLIENT_SECRET=CHANGE_ME. Also add apps/web/.gitignore: node_modules, .next, .env.local.

    APPS/WEB/SRC/APP/LAYOUT.TSX: Root layout. Import type Metadata from next; import './globals.css'. export const metadata: Metadata = { title: 'English Learning Platform', description: 'Adaptive English learning for B1-C1 learners' }. export default function RootLayout({ children }: { children: React.ReactNode }) with html lang="en" and body. Add className with basic Tailwind utilities.

    APPS/WEB/SRC/APP/PAGE.TSX: Minimal homepage placeholder. export default function HomePage() returns a div with "English Learning Platform — Coming Soon". Not a feature — just a skeleton page so the app doesn't 404 at /.

    APPS/WEB/SRC/APP/GLOBALS.CSS: @tailwind base; @tailwind components; @tailwind utilities; (Tailwind 3.x directives). These three lines are the entire file.

    APPS/WEB/SRC/APP/API/HEALTH/ROUTE.TS: export async function GET() { return Response.json({ status: 'ok', timestamp: new Date().toISOString() }); }. Use Response.json() not NextResponse.json() — the Web API Response object works in Next.js 14 App Router route handlers without importing next/server. Keep it simple.
  </action>
  <verify>
    <automated>
      grep -q 'next@' apps/web/package.json &amp;&amp; grep -q '14.2' apps/web/package.json &amp;&amp; grep -q 'tailwindcss' apps/web/tailwind.config.ts &amp;&amp; grep -q 'transpilePackages' apps/web/next.config.js &amp;&amp; echo "Next.js config OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json contains next@^14.2.35 (NOT ^15 or ^16)
    - apps/web/package.json contains react@^18.3.1 (NOT ^19)
    - apps/web/package.json contains tailwindcss@^3.4.19 (NOT ^4)
    - apps/web/next.config.js contains transpilePackages with @repo/database and @repo/shared
    - apps/web/tsconfig.json extends "@repo/tsconfig/nextjs.json"
    - apps/web/src/app/api/health/route.ts exports GET function returning Response.json with {status: 'ok'}
    - apps/web/src/app/globals.css contains @tailwind base/components/utilities directives
    - pnpm --filter @repo/web run type-check exits 0
  </acceptance_criteria>
  <done>Next.js 14 skeleton created; exact version pins applied; health route exports GET; transpilePackages configured; Tailwind 3.x configured; type-check passes</done>
</task>

<task type="auto">
  <name>Task 2: Vitest config for Next.js (Wave 0 requirement) + health route unit test</name>
  <read_first>
    - .planning/phases/01-foundation-infrastructure/01-RESEARCH.md (Validation Architecture: apps/web/vitest.config.ts; Testing Stack: Vitest 2.x + Testing Library)
    - .planning/phases/01-foundation-infrastructure/01-VALIDATION.md (Wave 0 requirements list — apps/web/vitest.config.ts mandatory)
  </read_first>
  <files>
    apps/web/vitest.config.ts,
    apps/web/src/app/api/health/route.test.ts
  </files>
  <action>
    Create Vitest config for Next.js and a unit test for the health route. This satisfies VALIDATION.md Wave 0 requirement for apps/web/vitest.config.ts.

    APPS/WEB/VITEST.CONFIG.TS: defineConfig with test: { globals: true, environment 'jsdom' (Next.js components run in browser environment), include ['src/**/*.test.{ts,tsx}', 'src/**/*.spec.{ts,tsx}'], setupFiles [] (empty for now — Phase 2 adds MSW setup), coverage: { provider 'v8', reporter ['text', 'json'] } }. No complex transform needed — Vitest handles TypeScript natively.

    APPS/WEB/SRC/APP/API/HEALTH/ROUTE.TEST.TS: Import { GET } from './route'. describe('Health route') block. Test "returns status ok": await GET(), then call .json() on the result, expect the json to have status 'ok' and timestamp to be a string. Note: Response.json() returns a Response object; in Vitest/jsdom environment, call response.json() as a Promise.

    Add test scripts to apps/web/package.json: "test": "vitest run", "test:watch": "vitest".
  </action>
  <verify>
    <automated>
      pnpm --filter @repo/web run test 2>&amp;1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - apps/web/vitest.config.ts exists with environment: 'jsdom'
    - apps/web/src/app/api/health/route.test.ts exists
    - pnpm --filter @repo/web run test exits 0 with at least 1 test passing
    - Test asserts response.status === 'ok'
  </acceptance_criteria>
  <done>Vitest config created for Next.js; health route unit test passes; Wave 0 requirement satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js App Router | Route handlers receive untrusted HTTP input |
| Next.js RSC → @repo/database | Direct DB access from server components bypasses NestJS guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-13 | Tampering | React 19 accidental install | mitigate | package.json pins react@^18.3.1 explicitly — Next.js 14 is incompatible with React 19; this pin prevents semver resolution to 19.x |
| T-1-14 | Tampering | Tailwind v4 accidental install | mitigate | package.json pins tailwindcss@^3.4.19 — shadcn/ui requires Tailwind 3; v4 uses incompatible CSS-first config that breaks @apply directives |
| T-1-15 | Information Disclosure | .env.local in git | mitigate | apps/web/.gitignore includes .env.local; NEXTAUTH_SECRET must never be committed |
| T-1-SC | Tampering | npm package installs | mitigate | next, react, tailwindcss all [VERIFIED] in Package Legitimacy Audit; exact versions from RESEARCH.md used |
</threat_model>

<verification>
After both tasks complete:

1. `pnpm --filter @repo/web run type-check` exits 0
2. `pnpm --filter @repo/web run test` shows 1 test passed
3. `grep '14.2' apps/web/package.json` confirms Next.js 14 pin
4. `grep '18.3' apps/web/package.json` confirms React 18 pin
5. `grep '3.4.19' apps/web/package.json` confirms Tailwind 3 pin
6. Start web: `pnpm --filter @repo/web run dev &` then `curl -sf http://localhost:3000/api/health` returns `{"status":"ok","timestamp":"..."}`
7. `grep 'transpilePackages' apps/web/next.config.js` confirms @repo/* packages transpiled
</verification>

<success_criteria>
- GET http://localhost:3000/api/health returns {"status":"ok","timestamp":"..."} with HTTP 200
- pnpm --filter @repo/web run type-check exits 0
- pnpm --filter @repo/web run test exits 0 (1 test passing)
- next@^14.2.35, react@^18.3.1, tailwindcss@^3.4.19 pinned in package.json
- transpilePackages includes @repo/database and @repo/shared
</success_criteria>

<output>
Create `.planning/phases/01-foundation-infrastructure/01-05-SUMMARY.md` when done
</output>
