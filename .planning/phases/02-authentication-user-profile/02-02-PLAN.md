---
phase: 02-authentication-user-profile
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components.json
  - apps/web/src/app/globals.css
  - apps/web/src/components/ui/.gitkeep
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/(auth)/layout.tsx
  - apps/web/vitest.config.ts
  - apps/api/vitest.config.ts
  - apps/api/src/auth/auth.service.spec.ts
  - apps/api/src/users/users.service.spec.ts
  - apps/api/src/profile/profile.service.spec.ts
  - apps/api/src/auth/jwt.guard.spec.ts
  - packages/shared/src/user.dto.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-04, PROF-01, PROF-02]

must_haves:
  truths:
    - "shadcn is initialized (components.json exists) with New York style + zinc base + CSS variables"
    - "The (auth) route group renders a centered 400px-max card shell with Inter font"
    - "Wave 0 RED test scaffolds exist for the auth service, JWT guard (AUTH-05/06), users/profile services, and shared DTOs, and fail with a clear MISSING/not-implemented signal"
  artifacts:
    - path: apps/web/components.json
      provides: "shadcn configuration (New York, zinc, CSS vars)"
      contains: "new-york"
    - path: apps/web/src/app/(auth)/layout.tsx
      provides: "centered auth card shell"
      min_lines: 10
    - path: apps/api/src/auth/auth.service.spec.ts
      provides: "RED scaffold for AUTH-01/02/04"
      contains: "describe"
  key_links:
    - from: apps/web/src/app/layout.tsx
      to: Inter font
      via: "next/font/google"
      pattern: "Inter\\("
---

<objective>
Prepare the frontend design system and the Wave 0 test scaffolds in parallel with the contracts wave. This installs shadcn (New York / zinc / CSS variables per UI-SPEC), loads Inter, builds the (auth) route-group shell, and creates the failing test scaffolds the Validation Strategy requires before any feature code is written (Nyquist compliance).

Purpose: shadcn must be initialized before any Phase 2 UI component can be written (UI-SPEC initialization gate). The Wave 0 RED scaffolds satisfy the per-task `<automated>` verification contract so downstream TDD tasks have real test files to turn green.
Output: components.json, Inter-wired root layout, (auth) layout shell, and RED test scaffolds for auth/users/profile/shared.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-authentication-user-profile/02-UI-SPEC.md
@.planning/phases/02-authentication-user-profile/02-VALIDATION.md

<interfaces>
From apps/web/src/app/layout.tsx (exists — Phase 1 skeleton): root layout to be edited to load Inter via next/font/google and apply inter.variable on <html>.
From apps/web/src/app/globals.css (exists): shadcn init injects CSS custom properties here.
From apps/web/vitest.config.ts and apps/api/vitest.config.ts (Phase 1 Wave 0 configs): jsdom for web, node for api.
UI-SPEC mandates: New York style, Zinc base, CSS variables yes. Components to add (later plans): button input label form card badge avatar separator alert toast.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn + Inter font + (auth) layout shell</name>
  <read_first>
    - apps/web/src/app/layout.tsx (current root layout — preserve existing structure, add Inter)
    - apps/web/src/app/globals.css (shadcn injects vars here)
    - apps/web/tailwind.config.ts (confirm Tailwind 3.x; shadcn extends this)
    - .planning/phases/02-authentication-user-profile/02-UI-SPEC.md (Design System gate; Auth Layout Shell; Typography font loading; Color zinc tokens)
  </read_first>
  <action>
    In apps/web run `npx shadcn@latest init` selecting New York style, Zinc base color, CSS variables: yes — produces components.json and injects zinc CSS custom properties into globals.css. Confirm components.json exists with "style":"new-york". Edit apps/web/src/app/layout.tsx to load Inter via `next/font/google` (`Inter({ subsets:['latin'], variable:'--font-inter' })`), apply inter.variable on the <html> element, and ensure font-family resolves to var(--font-inter), system-ui per UI-SPEC Typography. Create apps/web/src/app/(auth)/layout.tsx: a centered single-column shell, no nav/sidebar, max content width 400px, white --background, with the app name "English Learning" (20px semibold) above the slot, linking to "/". Do not build login/register pages here — only the route-group shell. Add a .gitkeep to apps/web/src/components/ui/ if shadcn did not create the directory.
  </action>
  <verify>
    <automated>test -f apps/web/components.json && grep -q "new-york" apps/web/components.json && grep -q "Inter" apps/web/src/app/layout.tsx && test -f "apps/web/src/app/(auth)/layout.tsx" && pnpm --filter @repo/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: apps/web/components.json exists and contains `"style": "new-york"`
    - Source assertion: globals.css contains shadcn CSS variable tokens (e.g. `--background`, `--primary`)
    - Source assertion: layout.tsx imports Inter from next/font/google and applies a `--font-inter` variable
    - Source assertion: (auth)/layout.tsx constrains content to max-width 400px (e.g. `max-w-[400px]` or `max-w-sm`)
    - Behavior assertion: `pnpm --filter @repo/web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>shadcn initialized (New York/zinc/CSS vars), Inter loaded in root layout, (auth) centered card shell present; web type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 RED test scaffolds (Nyquist compliance)</name>
  <read_first>
    - apps/api/src/health/health.controller.spec.ts (existing Vitest pattern for api — describe/it/expect)
    - apps/web/src/app/api/health/route.test.ts (existing Vitest pattern for web)
    - .planning/phases/02-authentication-user-profile/02-VALIDATION.md (Wave 0 Requirements list + Per-Task Verification Map)
    - .planning/phases/02-authentication-user-profile/02-RESEARCH.md (Validation Architecture → Wave 0 Gaps; Code Examples for DTO shapes)
  </read_first>
  <action>
    Create the RED scaffolds named in VALIDATION.md Wave 0 Requirements, each with a describe block and at least one `it` that asserts the not-yet-implemented behavior and currently fails (use `it(...)` with an explicit `expect(false).toBe(true)` placeholder OR import the not-yet-existing symbol guarded so the suite reports red rather than erroring the whole run — prefer a failing assertion with a `// RED: implemented in Plan NN` comment naming the owning plan):
      - apps/api/src/auth/auth.service.spec.ts — RED tests for AUTH-01 (bcrypt hash stored, never plaintext), AUTH-02 (unverified user blocked), AUTH-04 (reset token expires 24h). Owned by Plans 03 + 05.
      - apps/api/src/auth/jwt.guard.spec.ts — RED tests for AUTH-05/AUTH-06 guard behavior: JwtAuthGuard.canActivate rejects a request with no Authorization header (throws UnauthorizedException / 401), rejects a request whose Bearer token is invalid or expired (401), and (RED placeholder) admits a request whose token decodes to a valid payload. Mock the @auth/core/jwt decode boundary. Owned by Plans 01 + 04 (guard built in Plan 01, exercised by the protected-route flow in Plan 04).
      - apps/api/src/users/users.service.spec.ts — RED tests for PROF-01 (GET /me returns profile fields), PROF-02 (PATCH /me updates name + avatarKey). Owned by Plan 06.
      - apps/api/src/profile/profile.service.spec.ts — RED tests for PROF-02 avatar upload constraints (2MB, JPEG/PNG/WebP -> reject others). Owned by Plan 06.
      - packages/shared/src/user.dto.test.ts — tests UserProfileDtoSchema / UpdateProfileDtoSchema / AvatarUploadUrlRequestSchema parse valid input and reject invalid (these can be GREEN immediately since the schemas exist after Plan 01; if Plan 01 has not merged, mark the import RED).
    Each scaffold's failing message must clearly name the owning plan so the executor of that plan knows which test to satisfy.
  </action>
  <verify>
    <automated>test -f apps/api/src/auth/auth.service.spec.ts && test -f apps/api/src/users/users.service.spec.ts && test -f apps/api/src/profile/profile.service.spec.ts && test -f apps/api/src/auth/jwt.guard.spec.ts && test -f packages/shared/src/user.dto.test.ts && grep -rq "RED" apps/api/src/auth/auth.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: all five scaffold files exist with at least one describe + it block each (auth.service, jwt.guard, users.service, profile.service, user.dto)
    - Source assertion: apps/api/src/auth/jwt.guard.spec.ts asserts JwtAuthGuard rejects a missing Bearer token (401) and an invalid/expired token (401)
    - Behavior assertion: running `pnpm --filter @repo/api test --run` reports the auth/jwt-guard/users/profile suites as RED (failing assertions), not as collection errors that abort the run
    - Source assertion: each RED test comment names its owning plan (Plan 01 / 03 / 04 / 05 / 06)
  </acceptance_criteria>
  <done>Five Wave 0 scaffolds created (incl. apps/api/src/auth/jwt.guard.spec.ts for AUTH-05/06); api suite runs and reports the new specs red with plan-attributed messages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npx shadcn → local files | shadcn writes components.json + edits globals.css |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC2 | Tampering | npx shadcn init | accept | Official shadcn CLI (UI-SPEC registry safety: official only, gate not required) |
</threat_model>

<verification>
- `test -f apps/web/components.json` — shadcn initialized
- `pnpm --filter @repo/web exec tsc --noEmit` — layout compiles
- `pnpm --filter @repo/api test --run` — scaffolds report RED
</verification>

<success_criteria>
- shadcn initialized (New York / zinc / CSS variables)
- Inter loaded; (auth) route-group shell renders centered 400px card
- Wave 0 RED scaffolds exist for auth/users/profile services + shared DTOs, attributed to owning plans
</success_criteria>

<output>
Create `.planning/phases/02-authentication-user-profile/02-02-SUMMARY.md` when done
</output>
