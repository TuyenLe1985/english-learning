---
phase: 02-authentication-user-profile
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - apps/web/src/app/(auth)/login/page.tsx
  - apps/web/src/middleware.ts
  - apps/web/src/app/(dashboard)/layout.tsx
  - apps/web/src/app/(dashboard)/dashboard/page.tsx
  - apps/web/e2e/auth.spec.ts
  - apps/web/playwright.config.ts
autonomous: true
requirements: [AUTH-05, AUTH-06]

must_haves:
  truths:
    - "A verified user can sign in with email/password and land on the dashboard"
    - "The session persists across browser refresh and a new tab (30-day JWT cookie)"
    - "An unauthenticated request to /dashboard or /profile redirects to /login"
    - "An already-authenticated user visiting /login is redirected to /dashboard"
  artifacts:
    - path: apps/web/src/app/(auth)/login/page.tsx
      provides: "login form per UI-SPEC with Google button placeholder + forgot-password link"
      min_lines: 30
    - path: apps/web/src/middleware.ts
      provides: "NextAuth route protection"
      contains: "matcher"
    - path: apps/web/src/app/(dashboard)/layout.tsx
      provides: "auth-gated dashboard layout with minimal top bar + sign-out"
      min_lines: 10
  key_links:
    - from: apps/web/src/middleware.ts
      to: apps/web/src/auth.ts
      via: "export { auth as middleware }"
      pattern: "auth as middleware"
    - from: apps/web/src/app/(auth)/login/page.tsx
      to: signIn credentials
      via: "next-auth signIn('credentials')"
      pattern: "signIn"
---

<objective>
Deliver the login + session-persistence + route-protection vertical slice: a verified user signs in (AUTH-05), the JWT session survives refresh/new-tab via the 30-day cookie configured in Plan 01, and unauthenticated visitors to protected routes are redirected to /login (AUTH-06).

Purpose: Completes the core sign-in loop and the security boundary every later phase relies on. After this plan a verified user can log in, stay logged in, and protected routes are enforced.
Output: login page, NextAuth middleware, (dashboard) auth-gated layout + placeholder dashboard page, Playwright E2E for the redirect behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authentication-user-profile/02-RESEARCH.md
@.planning/phases/02-authentication-user-profile/02-UI-SPEC.md

<interfaces>
From apps/web/src/auth.ts (Plan 01): exports { handlers, auth, signIn, signOut }; session strategy jwt, maxAge 30 days (AUTH-05); pages.signIn = "/login".
From Plan 02: (auth) layout shell; shadcn (button/input/label/form/card/separator/alert available via add).
RESEARCH Pattern 6: middleware.ts = `export { auth as middleware } from "@/auth"` with matcher ["/dashboard/:path*","/profile/:path*"]. Next.js 14 uses middleware.ts (NOT proxy.ts).
UI-SPEC Login Page section: title "Sign in to your account"; email + password (show/hide); full-width "Sign in"; "Continue with Google" outline button (handler wired in Plan 05 — render the button now, point its onClick at signIn('google')); "Forgot password?" link → /reset-password (page built in Plan 05); footer "Register" link → /register; destructive Alert on auth failure with copy "Incorrect email or password...".
Routing contract: /login + /register redirect to /dashboard if session exists.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Login page + redirect-if-authed</name>
  <read_first>
    - apps/web/src/auth.ts (signIn export + pages config)
    - apps/web/src/app/(auth)/layout.tsx (shell to render inside)
    - .planning/phases/02-authentication-user-profile/02-UI-SPEC.md (Login Page component inventory; Copywriting contract; Interaction States)
  </read_first>
  <behavior>
    - Submitting valid verified credentials calls signIn('credentials') and on success navigates to /dashboard.
    - Submitting invalid credentials renders the destructive Alert with copy "Incorrect email or password. Try again or reset your password."
    - Visiting /login with an active session redirects to /dashboard (server-side check via auth()).
  </behavior>
  <action>
    Build apps/web/src/app/(auth)/login/page.tsx per UI-SPEC: shadcn Card/Input/Label/Button/Separator/Alert; email (type=email) + password (type=password with show/hide toggle, aria-labels per Accessibility contract); full-width default Button "Sign in" calling signIn('credentials',{redirect handling}); outline Button "Continue with Google" wired to signIn('google') (provider configured in Plan 01; full OAuth flow validated in Plan 05); Separator with "or"; "Forgot password?" right-aligned link to /reset-password; footer "Don't have an account? Register" → /register; destructive Alert above fields on failure. Add a server-side guard (auth()) that redirects to /dashboard when a session already exists (routing contract). Add shadcn components as needed.
  </action>
  <verify>
    <automated>test -f "apps/web/src/app/(auth)/login/page.tsx" && grep -q "signIn" "apps/web/src/app/(auth)/login/page.tsx" && grep -q "Continue with Google" "apps/web/src/app/(auth)/login/page.tsx" && pnpm --filter @repo/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: login page calls signIn('credentials') and signIn('google')
    - Source assertion: error Alert uses exact copy "Incorrect email or password. Try again or reset your password."
    - Source assertion: page guards with auth() and redirects authenticated users to /dashboard
    - Behavior assertion: `pnpm --filter @repo/web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Login form renders per UI-SPEC, signs in via credentials, surfaces auth errors, redirects authed users away.</done>
</task>

<task type="auto">
  <name>Task 2: Route protection middleware + dashboard shell + E2E redirect test</name>
  <read_first>
    - apps/web/src/auth.ts (auth export used by middleware)
    - apps/web/src/app/api/health/route.test.ts (existing test conventions)
    - .planning/phases/02-authentication-user-profile/02-RESEARCH.md (Pattern 6 middleware; Validation → AUTH-06 E2E)
    - .planning/phases/02-authentication-user-profile/02-UI-SPEC.md (Profile Page note: Phase 2 uses minimal top bar with logo + sign-out)
  </read_first>
  <action>
    Create apps/web/src/middleware.ts as `export { auth as middleware } from "@/auth"` with `export const config = { matcher: ["/dashboard/:path*","/profile/:path*"] }` (RESEARCH Pattern 6; AUTH-06). Create apps/web/src/app/(dashboard)/layout.tsx: auth-gated layout calling auth() and redirecting to /login if no session; minimal top bar with "English Learning" logo + a sign-out Button calling signOut() (UI-SPEC — no full nav until Phase 8). Create apps/web/src/app/(dashboard)/dashboard/page.tsx as a placeholder ("Dashboard — fleshed out in Phase 8") so the redirect target exists. Create apps/web/playwright.config.ts (Playwright 1.x, baseURL localhost:3000) and apps/web/e2e/auth.spec.ts with a test: an unauthenticated GET to /dashboard ends at /login (AUTH-06). Mark the credentialed-login E2E as a follow-up if it requires a seeded verified user; the redirect test must run.
  </action>
  <verify>
    <automated>grep -q "auth as middleware" apps/web/src/middleware.ts && grep -q "matcher" apps/web/src/middleware.ts && test -f "apps/web/src/app/(dashboard)/layout.tsx" && test -f apps/web/e2e/auth.spec.ts && pnpm --filter @repo/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: middleware.ts re-exports auth as middleware with matcher covering /dashboard and /profile
    - Source assertion: (dashboard)/layout.tsx calls auth() and redirects unauthenticated users to /login
    - Behavior assertion: Playwright test asserts unauthenticated /dashboard navigation resolves to /login
    - Behavior assertion: `pnpm --filter @repo/web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Protected routes redirect to /login (AUTH-06); dashboard shell + sign-out present; E2E redirect test in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated request → protected route | middleware enforces auth boundary |
| login form → NextAuth | credential submission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Elevation of Privilege | protected route access | mitigate | NextAuth middleware matcher redirects unauthenticated users to /login (AUTH-06, Pattern 6) |
| T-02-09 | Spoofing | session token in cookie | accept | HttpOnly + Secure (prod) cookie set by Auth.js; not accessible to JS (Security Domain) |
| T-02-10 | Spoofing | brute-force login | accept | bcrypt time cost slows attacks; @nestjs/throttler deferred to a later hardening pass (no NestJS auth endpoint in this slice) |
</threat_model>

<verification>
- `grep -q "auth as middleware" apps/web/src/middleware.ts`
- `pnpm --filter @repo/web exec tsc --noEmit`
- Playwright: unauthenticated /dashboard → /login
</verification>

<success_criteria>
- Verified user signs in and reaches /dashboard (AUTH-05)
- Session persists across refresh/new tab via 30-day JWT cookie (AUTH-05)
- Unauthenticated access to /dashboard or /profile redirects to /login (AUTH-06)
- Authenticated user on /login redirected to /dashboard
</success_criteria>

<output>
Create `.planning/phases/02-authentication-user-profile/02-04-SUMMARY.md` when done
</output>
