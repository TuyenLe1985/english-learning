---
phase: 02-authentication-user-profile
plan: 05
type: tdd
wave: 3
depends_on: ["02-03", "02-04"]
files_modified:
  - apps/web/src/lib/auth-actions.ts
  - apps/web/src/app/(auth)/reset-password/page.tsx
  - apps/web/src/app/(auth)/reset-password/confirm/page.tsx
  - apps/web/src/app/api/reset-password/route.ts
  - apps/web/src/lib/auth-actions.test.ts
autonomous: false
requirements: [AUTH-03, AUTH-04]

must_haves:
  truths:
    - "A user can sign in with Google OAuth and land on the dashboard with no separate registration step"
    - "A Google OAuth user's emailVerified is set on account creation (they skip the verification gate)"
    - "A Google sign-in whose email matches an existing password account auto-links to that one user record"
    - "A user can request a password-reset link, receive it by email, and set a new password"
    - "Password-reset request returns the same response whether or not the email exists (no enumeration)"
    - "An expired reset token shows an error with a request-new-link option"
  artifacts:
    - path: apps/web/src/lib/auth-actions.ts
      provides: "createPasswordResetToken + resetPassword server actions"
      exports: ["createPasswordResetToken", "resetPassword"]
    - path: apps/web/src/app/(auth)/reset-password/page.tsx
      provides: "request-reset email form"
      min_lines: 20
    - path: apps/web/src/app/(auth)/reset-password/confirm/page.tsx
      provides: "set-new-password form with token from query param"
      min_lines: 20
  key_links:
    - from: apps/web/src/lib/auth-actions.ts
      to: VerificationToken table
      via: "identifier password-reset:{userId}"
      pattern: "password-reset:"
    - from: apps/web/src/app/(auth)/login/page.tsx
      to: signIn google
      via: "Continue with Google button (wired in Plan 04, validated here)"
      pattern: "signIn"
---

<objective>
Complete the two remaining auth methods: Google OAuth sign-in (AUTH-03) and email password-reset (AUTH-04). Google relies on the provider + PrismaAdapter auto-link configured in Plan 01; this plan validates the end-to-end flow and the emailVerified-on-creation behavior (D-10). Password reset is a custom Resend-based flow using the VerificationToken table.

Purpose: Closes ROADMAP success criteria 2 (Google) and 3 (password reset). After this plan all four auth entry methods work.
Output: createPasswordResetToken + resetPassword server actions, reset-password request + confirm pages, reset-password API route, and verification of the Google OAuth slice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authentication-user-profile/02-CONTEXT.md
@.planning/phases/02-authentication-user-profile/02-RESEARCH.md
@.planning/phases/02-authentication-user-profile/02-UI-SPEC.md

<interfaces>
From apps/web/src/auth.ts (Plan 01): Google provider registered; PrismaAdapter allowDangerousEmailAccountLinking:true (D-09); jwt callback sets emailVerified=new Date() for google provider (D-10); signIn callback returns true for google (skips verify gate).
From apps/web/src/lib/auth-actions.ts (Plan 03): existing file — ADD createPasswordResetToken + resetPassword without breaking registerUser/sendVerificationEmail. Resend SDK + email-templates already present.
From apps/web/src/app/(auth)/login/page.tsx (Plan 04): "Continue with Google" button already calls signIn('google'); "Forgot password?" links to /reset-password.
RESEARCH Pattern 5: VerificationToken with identifier `password-reset:{userId}`, crypto.randomBytes(32).hex token, expires now+24h; never reveal whether email exists; delete token after use (Security Domain T-02 reset reuse).
UI-SPEC Reset Password Request + Confirm pages define copy and the expired-token Alert.
Env: GOOGLE_CLIENT_ID/SECRET, RESEND_API_KEY, NEXTAUTH_URL.
</interfaces>
</context>

<feature>
  <name>Google OAuth + password reset (AUTH-03, AUTH-04)</name>
  <files>apps/web/src/lib/auth-actions.ts, apps/web/src/lib/auth-actions.test.ts, apps/web/src/app/(auth)/reset-password/page.tsx, apps/web/src/app/(auth)/reset-password/confirm/page.tsx, apps/web/src/app/api/reset-password/route.ts</files>
  <behavior>
    RED → GREEN:
    - createPasswordResetToken(email): if no user with that email, return the SAME success-shaped response as when a user exists (no enumeration — Security Domain T-02). When the user exists, upsert a VerificationToken row with identifier `password-reset:{userId}`, random token, expires now+24h (D-03), and send the reset email via Resend (check error — Pitfall 6).
    - resetPassword(token, newPassword): rejects newPassword < 8 chars; rejects unknown token; rejects expired token (with request-new-link signal); on success sets User.passwordHash = bcrypt(newPassword,12) and DELETES the token (single-use).
    - Google emailVerified: a Google sign-in creates/links a user whose emailVerified is non-null on creation (D-10) — assert via the jwt/adapter behavior with a mocked Google account.
    - Account linking: a Google sign-in with an email matching an existing password user resolves to a single User record (D-09) — assert one user, two Account rows.
  </behavior>
  <implementation>
    RED: Extend apps/web/src/lib/auth-actions.test.ts with failing tests for createPasswordResetToken (enumeration-safe response, token shape + 24h expiry), resetPassword (length/unknown/expired/success+delete), and the Google emailVerified + account-link behaviors (mock prisma + the auth callbacks). Run RED. Commit `test(02-05): failing tests for OAuth verify + password reset`.

    GREEN: Add createPasswordResetToken + resetPassword to auth-actions.ts (RESEARCH Pattern 5). Create apps/web/src/app/api/reset-password/route.ts (POST: validate token, set new hash, delete token). Build reset-password/page.tsx (email-only request form; on submit show enumeration-safe success copy "If that email is registered, a reset link is on its way. Check your inbox.") and reset-password/confirm/page.tsx (reads ?token=; new + confirm password fields; expired-token destructive Alert with "Request new reset link" → /reset-password; success state "Your password has been updated. Sign in with your new password." + Go to sign in). Use shadcn Card/Input/Label/Button/Alert. Run tests GREEN. Commit `feat(02-05): google oauth verification + password reset flow`.

    REFACTOR (if needed): share token-expiry checking between verify-email and reset flows. Keep green. Commit `refactor(02-05): ...`.
  </implementation>
</feature>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Google OAuth + password-reset email flows</name>
  <what-built>Google OAuth sign-in and password-reset email delivery — both require live external credentials (Google OAuth consent screen, Resend API key + real inbox) and cannot be fully verified by automated tests.</what-built>
  <how-to-verify>
    1. Google OAuth (AUTH-03, D-10): with GOOGLE_CLIENT_ID/SECRET set, click "Continue with Google" on /login, complete consent, confirm you land on /dashboard with no separate registration step. In the DB confirm the new user has emailVerified set (non-null).
    2. Account linking (D-09): register a password account with email X, then sign in with Google using the same email X. Confirm a single User row with two Account rows (credentials + google).
    3. Password reset (AUTH-04): from /reset-password submit a registered email, confirm the email arrives via Resend, click the link to /reset-password/confirm, set a new password, then sign in with it.
    4. Enumeration: submit an UNregistered email at /reset-password and confirm the response copy is identical to the registered case.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any flow that failed.</resume-signal>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google → NextAuth callback | OAuth identity assertion |
| reset form → server action | untrusted email / new password |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Information Disclosure | reset-password response | mitigate | Identical response for existing/non-existing email (RESEARCH Anti-Pattern; Security Domain) |
| T-02-12 | Elevation of Privilege | reset token reuse/expiry | mitigate | 24h expiry; token deleted after successful reset (single-use) |
| T-02-13 | Spoofing | OAuth account takeover via email link | accept | allowDangerousEmailAccountLinking safe only because Google verifies ownership; documented accepted risk per D-09 |
</threat_model>

<verification>
- `pnpm --filter @repo/web test --run` — reset + OAuth behavior tests GREEN
- `pnpm --filter @repo/web exec tsc --noEmit`
- Human checkpoint: Google OAuth + reset email delivery
</verification>

<success_criteria>
- Google OAuth sign-in lands on dashboard, no separate registration, emailVerified set (AUTH-03, D-10)
- Email-collision Google sign-in auto-links to existing user (D-09)
- Password reset: request → email → set new password works (AUTH-04)
- Reset response is enumeration-safe; expired token shows request-new-link (D-03)
</success_criteria>

<output>
Create `.planning/phases/02-authentication-user-profile/02-05-SUMMARY.md` when done
</output>
