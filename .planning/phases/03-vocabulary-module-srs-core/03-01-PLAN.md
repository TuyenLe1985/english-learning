---
phase: 03-vocabulary-module-srs-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/web/package.json
  - packages/database/package.json
  - packages/shared/src/vocabulary.dto.ts
  - packages/shared/src/index.ts
  - packages/database/prisma/seed.ts
  - packages/database/prisma/seed-data/vocabulary.json
  - apps/web/src/components/query-provider.tsx
  - apps/web/src/app/(dashboard)/layout.tsx
  - apps/web/src/middleware.ts
  - apps/web/.env.example
  - apps/api/src/vocabulary/vocabulary.service.spec.ts
  - apps/api/src/srs/srs.service.spec.ts
  - apps/web/e2e/vocabulary.spec.ts
autonomous: false
requirements: [VOCAB-01, VOCAB-02, VOCAB-03, VOCAB-04, VOCAB-05, VOCAB-06, VOCAB-07]
user_setup: []

must_haves:
  truths:
    - "ts-fsrs, framer-motion, @tanstack/react-query are installed and importable (D-03)"
    - "Vocabulary/SRS Zod DTOs are exported from @repo/shared"
    - "Running the seed populates 200 vocabulary words (25 per category x 8, D-13) with ~8-9 words per CEFR level per category (D-16) and a demo user with 5 past-due SRS cards (D-15); seed data lives at packages/database/prisma/seed-data/vocabulary.json (D-14)"
    - "React Query QueryClientProvider wraps the (dashboard) layout so useQuery works"
    - "Unauthenticated users visiting /vocabulary or /review are redirected to /login"
  artifacts:
    - path: "packages/shared/src/vocabulary.dto.ts"
      provides: "VocabularyWordDto, CategoryDto, PaginatedWordsDto, EnrollWordSchema, ReviewSubmitSchema, SessionCompleteSchema, MyWordDto"
      contains: "VocabularyWordDtoSchema"
    - path: "packages/database/prisma/seed.ts"
      provides: "Seed script: 200 words via createMany + demo user + 5 past-due SrsCards"
      contains: "createMany"
    - path: "packages/database/prisma/seed-data/vocabulary.json"
      provides: "200 vocabulary word records"
      contains: "category"
    - path: "apps/web/src/components/query-provider.tsx"
      provides: "QueryClientProvider wrapper"
      contains: "QueryClientProvider"
  key_links:
    - from: "apps/web/src/app/(dashboard)/layout.tsx"
      to: "apps/web/src/components/query-provider.tsx"
      via: "QueryProvider wrapping {children}"
      pattern: "QueryProvider"
    - from: "packages/database/prisma/seed.ts"
      to: "packages/database/prisma/seed-data/vocabulary.json"
      via: "import vocabularyData"
      pattern: "seed-data/vocabulary"
---

<objective>
Establish the shared foundation for the entire Vocabulary + SRS phase: install the three new packages, define all shared Zod DTOs, write the seed script + 200-word corpus, set up React Query in the dashboard layout, extend route-protection middleware, and lay down the Wave 0 RED test scaffolds that downstream TDD plans turn green.

Purpose: Every other Phase 3 plan depends on these artifacts existing. Without React Query the review queue and my-words pages cannot render; without DTOs the API and relay routes have no contracts; without the seed there is no content to browse (no-empty-states constraint).
Output: Installed deps, `@repo/shared` vocabulary DTOs, seed script + JSON, QueryProvider wired into dashboard layout, middleware matcher extended, env var added, and failing test scaffolds for VOCAB-01..07.

deviation_rationale: D-14 specifies seed data at `apps/api/prisma/seed-data/vocabulary.json`. Both seed.ts and vocabulary.json are placed under `packages/database/prisma/seed-data/` instead. Reason: Prisma `db seed` executes from the package owning the Prisma schema (`packages/database`); importing seed data JSON cross-package would create a fragile relative-path dependency. Consolidated under `packages/database/` per RESEARCH Open Question 1 resolution. Functionality (200-word seed corpus) is identical to D-14 intent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-vocabulary-module-srs-core/03-CONTEXT.md
@.planning/phases/03-vocabulary-module-srs-core/03-RESEARCH.md
@.planning/phases/03-vocabulary-module-srs-core/03-PATTERNS.md

<interfaces>
<!-- Phase 3 Prisma models (already migrated — read-only in Phase 3). From packages/database/prisma/schema.prisma -->

VocabularyWord: id, word (unique), definition, partOfSpeech?, examples String[], synonyms String[], pronunciationKey?, audioStorageKey?, cefrLevel (CefrLevel enum B1|B2|C1), cefrConfidence, topic?, category?, frequency, createdAt, updatedAt
UserVocabularyItem: id, userId, wordId, addedAt, contextSentence?  — @@unique([userId, wordId])
SrsCard: id, userId, wordId?, userVocabItemId? (@unique), due, stability, difficulty, elapsedDays, scheduledDays, reps, lapses, state (CardState enum New|Learning|Review|Relearning), lastReview?, createdAt, updatedAt
CardState enum values: New, Learning, Review, Relearning
CefrLevel enum values: B1, B2, C1

<!-- NOTE: SrsCard has NO learningSteps column. Phase 3 adds no schema. ts-fsrs learning_steps is defaulted to 0 on read and dropped on write (see RESEARCH Pitfall 1). -->

<!-- Prisma client import path (used by seed.ts): -->
import { PrismaClient } from "../generated/client";

<!-- @repo/shared barrel (packages/shared/src/index.ts) currently exports auth.types and user.dto. Add: export * from "./vocabulary.dto"; -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy before install (ts-fsrs, framer-motion, @tanstack/react-query)</name>
  <what-built>RESEARCH.md Package Legitimacy Audit tagged all three new packages as [ASSUMED] because slopcheck was unavailable. Per the package legitimacy gate, a human must confirm legitimacy before any package-manager install runs.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/ts-fsrs — confirm it links to github.com/open-spaced-repetition/ts-fsrs and latest version is ~5.4.1
    2. Open https://www.npmjs.com/package/framer-motion — confirm it links to github.com/motiondivision/motion and latest is ~12.x
    3. Open https://www.npmjs.com/package/@tanstack/react-query — confirm it links to github.com/TanStack/query and latest is ~5.x
    All three are named in CLAUDE.md / RESEARCH.md as the chosen libraries.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installs, or name any package to reject.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install packages + define shared DTOs + seed data + React Query + middleware + env</name>
  <files>apps/api/package.json, apps/web/package.json, packages/database/package.json, packages/shared/src/vocabulary.dto.ts, packages/shared/src/index.ts, packages/database/prisma/seed.ts, packages/database/prisma/seed-data/vocabulary.json, apps/web/src/components/query-provider.tsx, apps/web/src/app/(dashboard)/layout.tsx, apps/web/src/middleware.ts, apps/web/.env.example</files>
  <read_first>
    - packages/shared/src/user.dto.ts (Zod DTO + z.infer export pattern to replicate)
    - packages/shared/src/index.ts (barrel — add the new export after existing Phase 2 exports)
    - packages/database/package.json (add prisma.seed entry + bcryptjs/ts-node devDeps; do NOT remove existing scripts)
    - packages/database/src/index.ts (confirms generated client import path is ../generated/client)
    - apps/web/src/app/(dashboard)/layout.tsx (wrap the existing <main> children with QueryProvider; preserve header + signOut)
    - apps/web/src/middleware.ts (extend config.matcher only; leave export { auth as middleware } unchanged)
    - .planning/phases/03-vocabulary-module-srs-core/03-PATTERNS.md (query-provider.tsx, seed.ts, vocabulary.dto.ts, package.json patterns — copy verbatim)
    - .planning/phases/03-vocabulary-module-srs-core/03-RESEARCH.md (Pattern 6 QueryProvider, Pattern 7 seed, installation commands)
  </read_first>
  <action>
    Run installs with pnpm filters: `pnpm --filter @repo/api add ts-fsrs`; `pnpm --filter @repo/web add framer-motion @tanstack/react-query`; add `bcryptjs`, `@types/bcryptjs`, `ts-node` to packages/database devDependencies (per RESEARCH Pitfall 7 — use bcryptjs, not native bcrypt, in the database package). Then run the shadcn add command from apps/web: `pnpm dlx shadcn@latest add card badge progress dialog tabs pagination skeleton toast`.

    Create packages/shared/src/vocabulary.dto.ts following the user.dto.ts Zod pattern. Define and export: VocabularyWordDtoSchema (id, word, definition, partOfSpeech nullable, examples string[], synonyms string[], pronunciationKey nullable, audioStorageKey nullable, cefrLevel enum B1/B2/C1, category nullable, frequency number), CategoryDtoSchema (slug, name, wordCount, icon name string), PaginatedWordsDtoSchema (words array, total, page, limit, totalPages), EnrollWordSchema (wordId, contextSentence optional), ReviewSubmitSchema (cardId, rating enum Again/Hard/Good/Easy), SessionCompleteSchema (categorySlug, answers array of {wordId, exerciseType, isCorrect}, timeTakenMs optional), SessionResultDtoSchema (score, total, wrongWordIds string[], timeTakenMs), MyWordDtoSchema (wordId, word, definition, cefrLevel, status enum new/learning/reviewing/mastered, nextReviewDate nullable string). Export z.infer types for each. Add `export * from "./vocabulary.dto";` to packages/shared/src/index.ts after the Phase 2 exports.

    Create packages/database/prisma/seed-data/vocabulary.json with exactly 200 records: 25 words per category across the 8 categories business, travel, technology, education, health, daily-life, social-topics, academic-english (per VOCAB-01 + D-13). Each record: {word, definition, partOfSpeech, examples (2-3), synonyms (2-3), pronunciationKey, cefrLevel, category}. CEFR distribution per D-16: ~8-9 words each of B1/B2/C1 per category (e.g. 9 B1, 8 B2, 8 C1). Use real, plausible English words appropriate to each category and level — no placeholder/lorem text (no-empty-states constraint). `word` values must be globally unique (the VocabularyWord.word column is @unique).

    Create packages/database/prisma/seed.ts per RESEARCH Pattern 7 / PATTERNS seed.ts: import vocabularyData from "./seed-data/vocabulary.json", PrismaClient from "../generated/client", bcryptjs. createMany the 200 words (skipDuplicates: true) in a single call (200 < 500 batch limit, satisfies PIPE-06 batching intent). Guard demo-user block with `if (process.env.NODE_ENV !== 'production')` (D-15): upsert demo@example.com with bcrypt hash of 'demo1234' (12 rounds), emailVerified: new Date(), name 'Demo User', cefrLevel 'B1'; then upsert UserVocabularyItem + SrsCard for 5 words with due = new Date(Date.now() - 3600000) and state 'New'. Add `"prisma": { "seed": "ts-node --project tsconfig.json prisma/seed.ts" }` and a `"db:seed"` script to packages/database/package.json.

    Create apps/web/src/components/query-provider.tsx per RESEARCH Pattern 6: "use client", QueryClient created via useState lazy init, defaultOptions { queries: { staleTime: 30_000 } }, wrap children in QueryClientProvider. Modify apps/web/src/app/(dashboard)/layout.tsx to import QueryProvider and wrap the existing {children} inside <main> with <QueryProvider>. Preserve the existing header, logo, and signOut form exactly.

    Modify apps/web/src/middleware.ts: extend config.matcher to add "/vocabulary", "/vocabulary/:path*", "/review", "/review/:path*" alongside the existing dashboard/profile entries (RESEARCH Pitfall 3). Leave the `export { auth as middleware }` line unchanged.

    Add `NEXT_PUBLIC_MINIO_PUBLIC_URL=http://localhost:9000/english-learning` to apps/web/.env.example (RESEARCH Open Question 3) for client-side audio URL construction in later plans. If .env.example does not exist, create it with that single line.
  </action>
  <verify>
    <automated>pnpm --filter @repo/api exec node -e "require.resolve('ts-fsrs')" && pnpm --filter @repo/web exec node -e "require.resolve('framer-motion');require.resolve('@tanstack/react-query')" && pnpm --filter @repo/shared type-check && node -e "const d=require('./packages/database/prisma/seed-data/vocabulary.json'); if(d.length!==200) throw new Error('expected 200 words, got '+d.length); const cats={}; d.forEach(w=>cats[w.category]=(cats[w.category]||0)+1); console.log(cats)"</automated>
  </verify>
  <acceptance_criteria>
    - `require.resolve('ts-fsrs')` succeeds in apps/api; `framer-motion` and `@tanstack/react-query` resolve in apps/web
    - `pnpm --filter @repo/shared type-check` passes with vocabulary.dto.ts present
    - vocabulary.json has exactly 200 records; each of the 8 categories has exactly 25; all `word` values are unique
    - packages/database/package.json contains a `prisma.seed` entry and bcryptjs in devDependencies
    - apps/web/src/components/query-provider.tsx contains `QueryClientProvider` and `"use client"`
    - apps/web/src/app/(dashboard)/layout.tsx imports and renders `QueryProvider` around `{children}`
    - apps/web/src/middleware.ts config.matcher includes "/vocabulary" and "/review" entries
    - grep `NEXT_PUBLIC_MINIO_PUBLIC_URL` in apps/web/.env.example returns a match
  </acceptance_criteria>
  <done>All three packages install; shared DTOs type-check; seed JSON has 200 words balanced 25/category; QueryProvider wraps dashboard layout; middleware protects /vocabulary and /review; env var added.</done>
</task>

<task type="auto">
  <name>Task 3: Run seed against DB + write Wave 0 RED test scaffolds</name>
  <files>apps/api/src/vocabulary/vocabulary.service.spec.ts, apps/api/src/srs/srs.service.spec.ts, apps/web/e2e/vocabulary.spec.ts</files>
  <read_first>
    - apps/api/src/users/users.service.spec.ts (direct-instantiation mock pattern: new Service(mockPrisma); vi.fn() mocks — copy verbatim)
    - .planning/phases/03-vocabulary-module-srs-core/03-PATTERNS.md (vocabulary.service.spec.ts + srs.service.spec.ts sections including the ts-fsrs vi.mock block)
    - .planning/phases/03-vocabulary-module-srs-core/03-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map; Wave 0 Gaps list)
    - packages/database/package.json (db:seed script just added in Task 2)
  </read_first>
  <action>
    Run the seed against the running Postgres container: `pnpm --filter @repo/database db:seed` (requires `docker compose up` services running; if DB unreachable, surface the error — do not silently skip). Confirm 200 words + demo user + 5 past-due cards land.

    Create the RED test scaffolds that downstream TDD plans (02, 04) turn green. These tests reference services that do not yet exist, so they MUST fail at this point — that is the intended RED state. Use the direct-instantiation mock pattern from users.service.spec.ts (no NestJS DI).

    apps/api/src/vocabulary/vocabulary.service.spec.ts — describe blocks with failing/`it.todo`-then-real assertions for: getCategories() returns 8 categories with wordCount (VOCAB-01); getWordsByCategory() returns paginated words with correct total/totalPages (VOCAB-01); getWordDetail() returns word with definition/partOfSpeech/examples/synonyms/pronunciationKey (VOCAB-02); assignExerciseType() / session helpers cover the 6 exercise types and matching uses a 4-item grid (VOCAB-03); getMyWords() filters by status and includes nextReviewDate (VOCAB-07). Mock prisma.vocabularyWord (findMany, findUniqueOrThrow, count) and userVocabularyItem.

    apps/api/src/srs/srs.service.spec.ts — include the ts-fsrs vi.mock block from PATTERNS. Failing-until-implemented assertions for: enrollWord() upserts UserVocabularyItem then creates SrsCard, idempotent on second call (VOCAB-04); submitReview() maps DB card to ts-fsrs Card, calls fsrs.repeat, writes nextCard fields back, filters by userId (VOCAB-05); getDueQueue() returns only due<=now, max 20, ordered by due asc (VOCAB-06).

    apps/web/e2e/vocabulary.spec.ts — Playwright tests: unauthenticated GET /vocabulary redirects to /login; unauthenticated GET /review redirects to /login (VOCAB-06 / Pitfall 3). Use the existing Playwright config; mark page-content tests as test.skip with a TODO referencing the plan that implements them.

    Set the spec files so they FAIL now (RED) because the services are unimplemented. Do NOT create stub services to make them pass — that is the job of plans 02 and 04.
  </action>
  <verify>
    <automated>test -f apps/api/src/vocabulary/vocabulary.service.spec.ts && test -f apps/api/src/srs/srs.service.spec.ts && test -f apps/web/e2e/vocabulary.spec.ts && node -e "const {execSync}=require('child_process'); try { execSync('pnpm --filter @repo/database db:seed', {stdio:'inherit'}); } catch(e){ console.error('SEED FAILED — ensure docker compose services are up'); process.exit(1);} "</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @repo/database db:seed` exits 0 and the VocabularyWord table contains 200 rows (verifiable via `psql` count or a follow-up Prisma query)
    - Demo user demo@example.com exists with emailVerified set and 5 SrsCards with due in the past
    - vocabulary.service.spec.ts exists and references getCategories/getWordsByCategory/getWordDetail/getMyWords
    - srs.service.spec.ts exists, includes the ts-fsrs vi.mock, and references enrollWord/submitReview/getDueQueue
    - apps/web/e2e/vocabulary.spec.ts asserts /vocabulary and /review redirect to /login when unauthenticated
    - Running `pnpm --filter @repo/api test` shows the new specs FAILING (RED) — services not yet implemented
  </acceptance_criteria>
  <done>Seed populates the DB with 200 words + demo user + 5 due cards; RED test scaffolds for VOCAB-01..07 exist and fail because services are unimplemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → install | npm packages (ts-fsrs, framer-motion, @tanstack/react-query) cross into the build |
| browser → Next.js middleware | unauthenticated requests to /vocabulary, /review |
| seed script → database | demo user credentials written to DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm installs (ts-fsrs, framer-motion, @tanstack/react-query) | mitigate | Blocking human-verify checkpoint (Task 1) before any install; all three are [ASSUMED] per RESEARCH audit, verified against official npmjs/GitHub links |
| T-03-01 | Elevation of Privilege | /vocabulary, /review routes | mitigate | middleware.ts matcher extended so unauthenticated users are redirected to /login (RESEARCH Pitfall 3); E2E redirect tests added |
| T-03-02 | Information Disclosure | demo user seed credentials | accept | Demo user (demo@example.com / demo1234) is dev-only; seed guards with NODE_ENV !== 'production' so it never runs in prod (D-15) |
</threat_model>

<verification>
- `pnpm --filter @repo/shared type-check` passes
- `pnpm --filter @repo/database db:seed` populates 200 words + demo user + 5 due cards
- New service specs fail (RED) — confirms scaffolds reference not-yet-built services
- Middleware redirects /vocabulary and /review when unauthenticated (E2E)
</verification>

<success_criteria>
- ts-fsrs, framer-motion, @tanstack/react-query installed and importable
- vocabulary.dto.ts exports all DTOs/schemas; barrel re-exports them
- seed.ts + vocabulary.json produce 200 balanced words and a demo user with 5 past-due cards
- QueryProvider wraps the dashboard layout
- middleware protects /vocabulary and /review
- Wave 0 RED scaffolds exist for VOCAB-01..07
</success_criteria>

<output>
Create `.planning/phases/03-vocabulary-module-srs-core/03-01-SUMMARY.md` when done
</output>
