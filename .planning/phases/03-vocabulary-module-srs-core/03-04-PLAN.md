---
phase: 03-vocabulary-module-srs-core
plan: 04
type: execute
wave: 4
depends_on: ["03-02", "03-03"]
files_modified:
  - apps/web/src/app/(dashboard)/vocabulary/page.tsx
  - apps/web/src/app/(dashboard)/vocabulary/[category]/page.tsx
  - apps/web/src/app/(dashboard)/vocabulary/[category]/[wordId]/page.tsx
  - apps/web/src/app/api/vocabulary/categories/route.ts
  - apps/web/src/app/api/vocabulary/[category]/words/route.ts
  - apps/web/src/app/api/vocabulary/[category]/[wordId]/route.ts
  - apps/web/src/app/api/vocabulary/enroll/route.ts
  - apps/web/src/components/vocabulary/category-card.tsx
  - apps/web/src/components/vocabulary/word-list-item.tsx
  - apps/web/src/components/vocabulary/word-detail.tsx
  - apps/web/e2e/vocabulary.spec.ts
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-04]

must_haves:
  truths:
    - "User can open /vocabulary and see an 8-category grid (2x4) with icons and word counts (D-09)"
    - "User can open a category and see a paginated A-Z word list (20/page, D-12) with Prev/Next (D-09)"
    - "User can open a word and see definition, examples, synonyms, usage, phonetic key always shown + play button with speechSynthesis fallback (D-10, D-09)"
    - "User can click 'Mark as learned' on a word detail page and it enrolls the word into SRS — one of two SRS entry points (D-11)"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/vocabulary/page.tsx"
      provides: "Category grid page"
      contains: "categories"
    - path: "apps/web/src/app/(dashboard)/vocabulary/[category]/[wordId]/page.tsx"
      provides: "Word detail page"
    - path: "apps/web/src/components/vocabulary/word-detail.tsx"
      provides: "Word detail client component with pronunciation playback + mark-as-learned"
      contains: "speechSynthesis"
  key_links:
    - from: "apps/web/src/app/(dashboard)/vocabulary/page.tsx"
      to: "/api/vocabulary/categories"
      via: "fetch in server component"
      pattern: "vocabulary/categories"
    - from: "apps/web/src/components/vocabulary/word-detail.tsx"
      to: "/api/vocabulary/enroll"
      via: "fetch POST on Mark as learned"
      pattern: "vocabulary/enroll"
    - from: "apps/web/src/app/api/vocabulary/categories/route.ts"
      to: "NestJS /api/vocabulary/categories"
      via: "fetchWithAuth"
      pattern: "fetchWithAuth"
---

<objective>
Vertical slice: vocabulary browsing and word detail, end to end. After this plan a logged-in user can navigate /vocabulary -> category -> word detail and mark a word as learned (which enrolls it into SRS via the Plan 03 endpoint). This is the first user-visible capability of Phase 3.

Purpose: VOCAB-01 (browse 8 categories + paginated word list), VOCAB-02 (full word detail with pronunciation), VOCAB-04 (mark as learned -> SRS enrollment). Delivers a complete UI -> relay -> NestJS -> DB path for browse/detail/enroll.
Output: 3 vocabulary pages, 4 relay routes, 3 components, and the /vocabulary E2E page tests turned on.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-vocabulary-module-srs-core/03-CONTEXT.md
@.planning/phases/03-vocabulary-module-srs-core/03-PATTERNS.md
@.planning/phases/03-vocabulary-module-srs-core/03-UI-SPEC.md

<interfaces>
import type { VocabularyWordDto, CategoryDto, PaginatedWordsDto } from '@repo/shared';
import { fetchWithAuth, API_URL } from "@/lib/api-client";
import { CefrBadge } from "@/components/cefr-badge";

<!-- NestJS endpoints consumed (built in Plan 02/03):
  GET  /api/vocabulary/categories
  GET  /api/vocabulary/:category/words?page=&limit=20
  GET  /api/vocabulary/:category/:wordId
  POST /api/srs/enroll  { wordId, contextSentence? }  (proxied via /api/vocabulary/enroll relay) -->

<!-- Category icons (lucide), UI-SPEC: business Briefcase, travel Plane, technology Cpu, education GraduationCap, health Heart, daily-life Home, social-topics Users, academic-english BookOpen -->
<!-- Audio URL: const minioPublicUrl = process.env.NEXT_PUBLIC_MINIO_PUBLIC_URL ?? "http://localhost:9000/english-learning"; audioStorageKey is a KEY not URL -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relay routes (categories, words, word detail, enroll)</name>
  <files>apps/web/src/app/api/vocabulary/categories/route.ts, apps/web/src/app/api/vocabulary/[category]/words/route.ts, apps/web/src/app/api/vocabulary/[category]/[wordId]/route.ts, apps/web/src/app/api/vocabulary/enroll/route.ts</files>
  <behavior>
    - GET categories relay: auth() check -> 401 if no session; else fetchWithAuth to NestJS /api/vocabulary/categories; propagate status/body
    - GET words relay: forwards page/limit query params to NestJS /api/vocabulary/:category/words
    - GET word detail relay: forwards to /api/vocabulary/:category/:wordId
    - POST enroll relay: parses JSON body, forwards to NestJS /api/srs/enroll with cookie auth; 401 if unauthenticated
  </behavior>
  <action>
    Create the 4 relay routes following PATTERNS Next.js API Relay Routes (analogs apps/web/src/app/api/profile/me/route.ts for GET, profile/avatar/upload-url for POST). Every route calls auth() and returns 401 before proxying (RESEARCH Pitfall: relay without auth). For the words relay, construct the upstream URL with URLSearchParams (page from request searchParams default '1', limit '20'). The enroll relay proxies to `${API_URL}/api/srs/enroll` (the NestJS SRS endpoint) with method POST and JSON body. Use fetchWithAuth(cookieHeader, url) for all upstream calls.
  </action>
  <verify>
    <automated>test -f "apps/web/src/app/api/vocabulary/categories/route.ts" && test -f "apps/web/src/app/api/vocabulary/[category]/words/route.ts" && test -f "apps/web/src/app/api/vocabulary/[category]/[wordId]/route.ts" && test -f "apps/web/src/app/api/vocabulary/enroll/route.ts" && grep -lq "auth()" apps/web/src/app/api/vocabulary/enroll/route.ts && pnpm --filter @repo/web type-check</automated>
  </verify>
  <acceptance_criteria>
    - All 4 relay files exist and call `auth()` returning 401 when no session
    - words relay forwards page and limit query params to the upstream URL
    - enroll relay forwards POST body to `${API_URL}/api/srs/enroll`
    - All relays use fetchWithAuth (not raw fetch) for upstream calls
    - `pnpm --filter @repo/web type-check` passes
  </acceptance_criteria>
  <done>Four auth-gated relay routes proxy categories/words/word-detail/enroll to NestJS.</done>
  <read_first>
    - apps/web/src/app/api/profile/me/route.ts (GET relay pattern — auth, cookie header, fetchWithAuth)
    - apps/web/src/app/api/profile/avatar/upload-url/route.ts (POST relay pattern with JSON body)
    - apps/web/src/lib/api-client.ts (fetchWithAuth, API_URL exports)
    - .planning/phases/03-vocabulary-module-srs-core/03-PATTERNS.md (Next.js API Relay Routes section — GET-with-query and POST patterns)
  </read_first>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Browse pages + components (category grid, word list, word detail, enroll)</name>
  <files>apps/web/src/app/(dashboard)/vocabulary/page.tsx, apps/web/src/app/(dashboard)/vocabulary/[category]/page.tsx, apps/web/src/app/(dashboard)/vocabulary/[category]/[wordId]/page.tsx, apps/web/src/components/vocabulary/category-card.tsx, apps/web/src/components/vocabulary/word-list-item.tsx, apps/web/src/components/vocabulary/word-detail.tsx, apps/web/e2e/vocabulary.spec.ts</files>
  <behavior>
    - /vocabulary renders an 8-card 2-col (mobile) / 4-col (desktop) grid; each card shows the lucide icon, category name (Heading 20/600), word count (Label 14/400 muted); links to /vocabulary/[slug]
    - /vocabulary/[category] renders a paginated A-Z word list (20/page) with a "Practice this set" button (links to /vocabulary/[category]/practice) and shadcn Pagination Prev/Next; 5 skeleton rows while loading
    - /vocabulary/[category]/[wordId] renders Display word + phonetic + Volume2 play button, part-of-speech badge, CefrBadge, shadcn Tabs (Definition/Examples/Synonyms/Usage), and a "Mark as learned" primary button (shows "In your review schedule" disabled if already enrolled)
    - play button: if audioStorageKey present, new Audio(minioPublicUrl + '/' + key).play().catch(-> speechSynthesis); else speechSynthesis directly
    - Mark as learned: POST /api/vocabulary/enroll { wordId }; on success show toast "Word added to review schedule" and switch button to enrolled state
  </behavior>
  <action>
    Build the three pages as Server Components that auth()-check then fetch their relay routes with cache: 'no-store' (PATTERNS server-component patterns; layout already applies outer padding). category-card.tsx and word-list-item.tsx follow the cefr-badge.tsx component shape; word-list-item reuses CefrBadge and renders SRS status badge if provided. word-detail.tsx is a "use client" component implementing playPronunciation (PATTERNS word-detail.tsx, RESEARCH Pitfall 6: audioStorageKey is a key, build URL via NEXT_PUBLIC_MINIO_PUBLIC_URL) and the mark-as-learned mutation. Use the UI-SPEC color/typography/spacing tokens and copywriting ("Practice this set", "Mark as learned", "In your review schedule"). Add aria-label "Play pronunciation of {word}" on the play button; aria-labels per UI-SPEC accessibility.

    Turn on the previously-skipped /vocabulary E2E page tests in apps/web/e2e/vocabulary.spec.ts: assert the category grid shows 8 categories and that clicking through to a word detail shows definition content (use the seeded demo user session or a test login helper).
  </action>
  <verify>
    <automated>test -f "apps/web/src/app/(dashboard)/vocabulary/page.tsx" && test -f "apps/web/src/app/(dashboard)/vocabulary/[category]/[wordId]/page.tsx" && grep -q "speechSynthesis" apps/web/src/components/vocabulary/word-detail.tsx && grep -q "NEXT_PUBLIC_MINIO_PUBLIC_URL" apps/web/src/components/vocabulary/word-detail.tsx && pnpm --filter @repo/web type-check</automated>
  </verify>
  <acceptance_criteria>
    - /vocabulary page fetches /api/vocabulary/categories and maps 8 CategoryCards in a grid-cols-2 sm:grid-cols-4 layout
    - /vocabulary/[category] fetches the words relay with the page query param and renders Pagination + a "Practice this set" link to .../practice
    - word-detail.tsx builds the audio URL as `${NEXT_PUBLIC_MINIO_PUBLIC_URL}/${audioStorageKey}` and never passes the raw key to new Audio(); falls back to speechSynthesis
    - word-detail.tsx "Mark as learned" POSTs to /api/vocabulary/enroll and renders the enrolled/disabled state on success
    - play button has aria-label "Play pronunciation of {word}"
    - `pnpm --filter @repo/web type-check` passes; `pnpm --filter @repo/web test` passes for any added component tests
  </acceptance_criteria>
  <done>User can browse categories, page through a category's words, open a word's full detail, hear pronunciation, and mark it as learned.</done>
  <read_first>
    - apps/web/src/app/(dashboard)/profile/page.tsx (server component auth + fetch pattern)
    - apps/web/src/app/(dashboard)/profile/profile-form.tsx (client state, loading state, toast, storage-key->URL construction)
    - apps/web/src/components/cefr-badge.tsx (component shape + CEFR colors to reuse)
    - apps/web/src/app/(dashboard)/layout.tsx (outer padding already applied; do not duplicate)
    - .planning/phases/03-vocabulary-module-srs-core/03-PATTERNS.md (category-card, word-list-item, word-detail, page patterns)
    - .planning/phases/03-vocabulary-module-srs-core/03-UI-SPEC.md (Page Layouts /vocabulary, /vocabulary/[category], word detail; Copywriting; Color; Typography; Accessibility)
  </read_first>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Next.js relay routes | unauthenticated requests must be rejected before proxying |
| browser -> R2/MinIO audio URL | audioStorageKey resolved to public URL client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Elevation of Privilege | vocabulary relay routes | mitigate | Every relay calls auth() and returns 401 before proxying to NestJS (NestJS also enforces JwtAuthGuard) |
| T-03-11 | Information Disclosure | enroll relay | mitigate | wordId comes from client but enrollment is scoped server-side to req.user.userId in NestJS; relay never forwards a userId |
| T-03-12 | Tampering | audio URL construction | accept | audioStorageKey -> public read URL is a non-sensitive content key; no auth on public audio bucket (Phase 5 concern) |
</threat_model>

<verification>
- All 4 relay routes auth-gated and type-check
- 3 pages render and fetch their relays
- word-detail builds audio URL correctly and enrolls via POST
- /vocabulary E2E page tests pass
</verification>

<success_criteria>
- VOCAB-01: 8-category grid + paginated A-Z word list (20/page)
- VOCAB-02: word detail shows all fields + pronunciation playback
- VOCAB-04: "Mark as learned" enrolls the word into SRS
</success_criteria>

<output>
Create `.planning/phases/03-vocabulary-module-srs-core/03-04-SUMMARY.md` when done
</output>
