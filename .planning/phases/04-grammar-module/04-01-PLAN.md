---
phase: 04-grammar-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/prisma/schema.prisma
  - packages/shared/src/grammar.dto.ts
  - packages/shared/src/index.ts
  - packages/database/prisma/seed-data/grammar.json
  - packages/database/prisma/seed.ts
  - apps/web/package.json
  - apps/api/src/grammar/grammar.service.spec.ts
  - apps/web/src/components/grammar/exercises/multiple-choice-exercise.test.tsx
  - apps/web/src/components/grammar/exercises/fill-in-the-blank-exercise.test.tsx
  - apps/web/src/components/grammar/exercises/drag-and-drop-exercise.test.tsx
autonomous: false
requirements: [GRAM-01, GRAM-05]
user_setup: []

must_haves:
  truths:
    - "GrammarArea and GrammarLesson have unique slug fields in the DB schema"
    - "Grammar DTOs (area, topic, question, session-complete, session-result) are exported from @repo/shared"
    - "Seed populates 10 grammar areas, with ≥20 questions per topic across all lessons"
    - "@dnd-kit/core and @dnd-kit/sortable are installed in apps/web"
    - "RED test scaffolds exist for service + 3 exercise components and fail (no implementation yet)"
  artifacts:
    - path: "packages/database/prisma/schema.prisma"
      provides: "slug fields on GrammarArea and GrammarLesson"
      contains: "slug          String         @unique"
    - path: "packages/shared/src/grammar.dto.ts"
      provides: "Zod schemas + inferred types for all grammar API contracts"
      exports: ["GrammarAreaDtoSchema", "GrammarSessionCompleteSchema", "GrammarSessionResultDtoSchema"]
    - path: "packages/database/prisma/seed-data/grammar.json"
      provides: "Nested areas→topics→lessons→questions seed dataset"
      contains: "\"areas\""
    - path: "packages/database/prisma/seed.ts"
      provides: "seedGrammar() invoked from main()"
      contains: "seedGrammar"
    - path: "apps/api/src/grammar/grammar.service.spec.ts"
      provides: "RED unit tests for GrammarService (getAreas, getLessonDetail, completeSession, getWeakQuestions)"
      contains: "describe('GrammarService'"
  key_links:
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/grammar.dto.ts"
      via: "barrel re-export"
      pattern: "export \\* from \"\\./grammar\\.dto\""
    - from: "packages/database/prisma/seed.ts"
      to: "packages/database/prisma/seed-data/grammar.json"
      via: "static JSON import consumed by seedGrammar()"
      pattern: "seed-data/grammar\\.json"
---

<objective>
Establish the Phase 4 foundation: fix the BLOCKING schema slug gap, define shared grammar DTOs, install dnd-kit, build the grammar seed dataset + seed function, and lay down RED test scaffolds that downstream TDD plans turn GREEN.

Purpose: Every other Phase 4 plan depends on slug-resolvable routes, shared DTO types, seeded data, and dnd-kit. Without this wave nothing else can be built or verified.
Output: Migrated schema, grammar.dto.ts + barrel export, grammar.json seed data, seedGrammar() in seed.ts, dnd-kit installed, 4 failing RED test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-grammar-module/04-CONTEXT.md
@.planning/phases/04-grammar-module/04-RESEARCH.md
@.planning/phases/04-grammar-module/04-PATTERNS.md

<interfaces>
<!-- Existing schema (VERIFIED). GrammarTopic.slug ALREADY EXISTS. Only GrammarArea + GrammarLesson need slug added. -->
GrammarArea: id, name (@unique), description?, sortOrder, topics[]  — NO slug yet
GrammarTopic: id, areaId, title, slug (@unique), description?, cefrLevel (CefrLevel enum B1/B2/C1), sortOrder, createdAt, lessons[], progress[]  — slug EXISTS
GrammarLesson: id, topicId, title, explanation (@db.Text), examples (String[]), sortOrder, createdAt, questions[]  — NO slug yet
GrammarQuestion: id, lessonId, exerciseType (ExerciseType enum), prompt, answer, distractors (String[]), explanation?, difficulty (default 1), xpReward (default 10), createdAt
GrammarAttempt: id, questionId, userId, isCorrect, userAnswer?, attemptedAt  — @@index([questionId, userId])
GrammarProgress: id, userId, topicId, masteryPct (Float default 0), attempts (default 0), correct (default 0), lastAttemptAt?  — @@unique([userId, topicId]), accessor key userId_topicId
ExerciseType enum (already defined, line 51): MULTIPLE_CHOICE, FILL_IN_THE_BLANK, SENTENCE_TRANSFORMATION, ERROR_CORRECTION, DRAG_AND_DROP

<!-- Existing seed.ts shape (VERIFIED): static import at top, async main(), main().then($disconnect).catch(). seedGrammar() must be added and awaited inside main(). -->
<!-- packages/shared/src/index.ts barrel currently ends with: export * from "./vocabulary.dto"; -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify dnd-kit package legitimacy before install (T-04-SC)</name>
  <what-built>RESEARCH.md tagged @dnd-kit/core (6.3.1) and @dnd-kit/sortable (10.0.0) as [ASSUMED] because slopcheck was unavailable. These are the only new third-party dependencies in Phase 4. They must be human-verified before install.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@dnd-kit/core — confirm: maintained by clauderic, repository github.com/clauderic/dnd-kit, latest version ~6.3.1, weekly downloads in the millions, no "deprecated" banner.
    2. Open https://www.npmjs.com/package/@dnd-kit/sortable — confirm same repository/author, latest ~10.0.0.
    3. Confirm neither package page lists a postinstall script under "install scripts".
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add slug fields + migrate, define shared DTOs, install dnd-kit</name>
  <files>packages/database/prisma/schema.prisma, packages/shared/src/grammar.dto.ts, packages/shared/src/index.ts, apps/web/package.json</files>
  <read_first>
    - packages/database/prisma/schema.prisma (lines 225-315 — current grammar models; confirm GrammarArea/GrammarLesson lack slug, GrammarTopic has it)
    - packages/shared/src/vocabulary.dto.ts (Zod-schema-then-inferred-type structure to replicate exactly)
    - packages/shared/src/index.ts (barrel export; append after the vocabulary.dto line)
    - apps/web/package.json (confirm dnd-kit not already present)
    - .planning/phases/04-grammar-module/04-RESEARCH.md (Pattern 6 — full DTO field list; Critical Schema Gap section — exact slug field placement)
  </read_first>
  <action>
    [BLOCKING — schema migration] In packages/database/prisma/schema.prisma: add `slug String @unique` to model GrammarArea (place after `id`, before `name`). Add `slug String @unique` to model GrammarLesson (place after `topicId`, before `title`). Do NOT touch GrammarTopic (its slug already exists). Then apply the migration with the non-TTY-safe push command: run `pnpm --filter @repo/database db:push` (which executes `prisma db push --schema ./prisma/schema.prisma`), then `pnpm --filter @repo/database db:generate` to regenerate the Prisma client. The push command is required — slug-based URL navigation in plans 02/04/05 is blocked until slugs exist in both schema and DB.

    Create packages/shared/src/grammar.dto.ts following the vocabulary.dto.ts structure (Zod schema first, inferred type at bottom, section-separator comments). Define and export these Zod schemas and their inferred types per RESEARCH.md Pattern 6: GrammarAreaDtoSchema { id, slug, name, description (nullable), topicCount (number), sortOrder }; GrammarTopicDtoSchema { id, slug, title, description (nullable), cefrLevel (enum B1/B2/C1), lessonCount (number), masteryPct (number nullable), sortOrder }; GrammarLessonDtoSchema { id, slug, title, explanation, examples (string array), sortOrder }; GrammarQuestionDtoSchema { id, exerciseType (enum of the 5 ExerciseType values), prompt, answer, distractors (string array), explanation (nullable), difficulty (number), xpReward (number) }; GrammarLessonDetailDtoSchema = GrammarLessonDtoSchema extended with questions (array of GrammarQuestionDtoSchema); GrammarTopicDetailDtoSchema { topic: GrammarTopicDtoSchema, lessons: array of GrammarLessonDtoSchema with a questionCount number per lesson }; GrammarSessionCompleteSchema { lessonId (string), attempts (array of { questionId, isCorrect (boolean), userAnswer (string optional) }), timeTakenMs (number optional) }; GrammarSessionResultDtoSchema { score (number), total (number), masteryPct (number) }. Export inferred types: GrammarAreaDto, GrammarTopicDto, GrammarLessonDto, GrammarQuestionDto, GrammarLessonDetailDto, GrammarTopicDetailDto, GrammarSessionCompleteDto, GrammarSessionResultDto.

    In packages/shared/src/index.ts add `export * from "./grammar.dto";` after the existing `export * from "./vocabulary.dto";` line.

    Install dnd-kit (only after Task 1 approval): run `pnpm --filter @repo/web add @dnd-kit/core @dnd-kit/sortable`. Confirm apps/web/package.json now lists both under dependencies.
  </action>
  <verify>
    <automated>awk '/model GrammarArea[[:space:]]*{/{f=1} f&&/slug[[:space:]]+String[[:space:]]+@unique/{print "AREA_SLUG_OK"; f=0}' packages/database/prisma/schema.prisma | grep -q AREA_SLUG_OK && awk '/model GrammarLesson[[:space:]]*{/{f=1} f&&/slug[[:space:]]+String[[:space:]]+@unique/{print "LESSON_SLUG_OK"; f=0}' packages/database/prisma/schema.prisma | grep -q LESSON_SLUG_OK && grep -q 'export \* from "./grammar.dto"' packages/shared/src/index.ts && grep -q '@dnd-kit/core' apps/web/package.json && grep -q '@dnd-kit/sortable' apps/web/package.json && pnpm --filter @repo/shared build && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - GrammarArea model block contains a `slug String @unique` field (independent per-field assertion: `awk` scoped to the `model GrammarArea {` block matches `slug ... String ... @unique`)
    - GrammarLesson model block contains a `slug String @unique` field (independent per-field assertion: `awk` scoped to the `model GrammarLesson {` block matches `slug ... String ... @unique`)
    - GrammarTopic is left untouched (its slug already existed before this plan)
    - packages/shared/src/grammar.dto.ts contains `export const GrammarSessionCompleteSchema` and `export type GrammarSessionResultDto`
    - packages/shared/src/index.ts contains `export * from "./grammar.dto"`
    - apps/web/package.json dependencies contain both `@dnd-kit/core` and `@dnd-kit/sortable`
    - `pnpm --filter @repo/shared build` exits 0 (DTOs type-check)
  </acceptance_criteria>
  <done>Schema migrated with slug on both models, grammar DTOs exported from @repo/shared, dnd-kit installed.</done>
</task>

<task type="auto">
  <name>Task 3: Build grammar seed dataset + seedGrammar(), add RED test scaffolds</name>
  <files>packages/database/prisma/seed-data/grammar.json, packages/database/prisma/seed.ts, apps/api/src/grammar/grammar.service.spec.ts, apps/web/src/components/grammar/exercises/multiple-choice-exercise.test.tsx, apps/web/src/components/grammar/exercises/fill-in-the-blank-exercise.test.tsx, apps/web/src/components/grammar/exercises/drag-and-drop-exercise.test.tsx</files>
  <read_first>
    - packages/database/prisma/seed.ts (current main() structure, static import at top, main().then($disconnect) tail — seedGrammar must be awaited inside main before $disconnect)
    - packages/database/prisma/seed-data/vocabulary.json (JSON formatting/style reference)
    - .planning/phases/04-grammar-module/04-PATTERNS.md (seed.ts section lines 981-1034 — strict FK-ordered upsert/createMany loop; grammar.json structure lines 1050-1094)
    - .planning/phases/04-grammar-module/04-RESEARCH.md (Open Questions (RESOLVED) #2 — distribution ~7 questions × 3 lessons = 21 per topic; Pitfall 4 — FK insert order)
    - apps/api/src/vocabulary/vocabulary.service.spec.ts (Vitest mock-Prisma + direct-instantiation pattern)
    - apps/web/src/components/vocabulary/exercises/flashcard-exercise.test.tsx (component test pattern, jsdom)
  </read_first>
  <action>
    Create packages/database/prisma/seed-data/grammar.json with the nested structure areas→topics→lessons→questions (per D-01, GRAM-01, GRAM-05). Include all 10 areas with these exact slugs and names: verb-tenses/"Verb Tenses", modal-verbs/"Modal Verbs", conditionals/"Conditionals", passive-voice/"Passive Voice", relative-clauses/"Relative Clauses", reported-speech/"Reported Speech", gerunds-infinitives/"Gerunds & Infinitives", articles/"Articles", prepositions/"Prepositions", linking-words/"Linking Words". Each area: sortOrder, description, and ≥1 topic. Each topic: unique slug, title, cefrLevel (one of B1/B2/C1), sortOrder, and lessons[] totaling ≥20 questions across the topic (distribute as ~3 lessons × ~7 questions = 21 to satisfy GRAM-05). Each lesson: unique slug, title, explanation text, examples (≥2 strings), sortOrder, and questions[]. Across each topic's questions, use at least 3 distinct exerciseType values from the 5 (GRAM-03) and include at least one DRAG_AND_DROP question whose prompt uses `___` blank markers with the answer plus distractors[]. Question fields: exerciseType, prompt, answer, distractors (array — empty allowed for free-text), explanation, difficulty, xpReward.

    Extend packages/database/prisma/seed.ts: add static import `import grammarData from "./seed-data/grammar.json";` at the top alongside vocabularyData. Add an async `seedGrammar()` function implementing the strict FK-ordered loop from PATTERNS.md: for each area → `grammarArea.upsert` keyed on slug → for each topic → `grammarTopic.upsert` keyed on slug (with areaId=createdArea.id) → for each lesson → `grammarLesson.upsert` keyed on slug (with topicId=createdTopic.id) → `grammarQuestion.createMany` (with lessonId=createdLesson.id, skipDuplicates: true). Call `await seedGrammar();` inside main() before the demo-user block. Add a console.log reporting total questions seeded.

    Create RED test scaffolds (these MUST fail because implementations do not exist yet — they go GREEN in plans 02/03):
    - apps/api/src/grammar/grammar.service.spec.ts: Vitest suite using the mock-Prisma + `new GrammarService(mockPrisma)` direct-instantiation pattern. Write failing tests for: getAreas() returns areas mapped with topicCount; getLessonDetail(slug) returns lesson with questions / throws NotFoundException on P2025; completeSession() calls grammarAttempt.createMany then grammarProgress.upsert with masteryPct = newCorrect/newAttempts; getWeakQuestions() returns only questions whose most-recent attempt isCorrect=false. Import GrammarService from './grammar.service' (file does not exist yet → RED).
    - apps/web/src/components/grammar/exercises/multiple-choice-exercise.test.tsx: render MultipleChoiceExercise with mock props, assert correct option selection calls onCorrect and wrong calls onIncorrect. Import from '../multiple-choice-exercise' (does not exist → RED).
    - apps/web/src/components/grammar/exercises/fill-in-the-blank-exercise.test.tsx: assert case-insensitive trimmed answer comparison (input "  Has Been " matches answer "has been" → onCorrect). Import from '../fill-in-the-blank-exercise' (RED).
    - apps/web/src/components/grammar/exercises/drag-and-drop-exercise.test.tsx: assert word-bank chips and blank slots render from props (count of blanks matches `___` markers; word bank shows answer + distractors). Import from '../drag-and-drop-exercise' (RED).
  </action>
  <verify>
    <automated>node -e "const d=require('./packages/database/prisma/seed-data/grammar.json'); if(d.areas.length!==10) process.exit(1); for(const a of d.areas){for(const t of a.topics){const n=t.lessons.reduce((s,l)=>s+l.questions.length,0); if(n<20){console.error('topic '+t.slug+' has '+n); process.exit(1);}}} console.log('seed JSON OK')" && pnpm --filter @repo/database db:seed && node --experimental-vm-modules -e "import('@prisma/client').then(async({PrismaClient})=>{const p=new PrismaClient(); const grouped=await p.grammarQuestion.groupBy({by:['lessonId'],_count:{_all:true}}); const lessons=await p.grammarLesson.findMany({select:{id:true,topicId:true}}); const byLesson=Object.fromEntries(grouped.map(g=>[g.lessonId,g._count._all])); const perTopic={}; for(const l of lessons){perTopic[l.topicId]=(perTopic[l.topicId]||0)+(byLesson[l.id]||0);} const topics=await p.grammarTopic.findMany({select:{id:true,slug:true}}); let bad=topics.filter(t=>(perTopic[t.id]||0)<20); await p.\$disconnect(); if(topics.length===0){console.error('no topics seeded'); process.exit(1);} if(bad.length){console.error('topics under 20 questions: '+bad.map(t=>t.slug).join(',')); process.exit(1);} console.log('DB count OK: '+topics.length+' topics each ≥20 questions');})"</automated>
  </verify>
  <acceptance_criteria>
    - grammar.json `areas` array has exactly 10 entries with the 10 required slugs
    - every topic across the dataset has ≥20 questions summed over its lessons at the JSON level (GRAM-05) — verified by the node JSON-structure check exiting 0
    - DB query (run AFTER `db:seed`) confirms every grammar topic has ≥20 associated GrammarQuestion records — guards against a seed that silently truncates/skips records and still exits 0 on the JSON check alone (groupBy GrammarQuestion → roll up to topic via lesson.topicId → assert all topics ≥20, and assert topics.length > 0)
    - at least one question per topic has exerciseType "DRAG_AND_DROP" and a prompt containing `___`
    - packages/database/prisma/seed.ts contains `import grammarData` and `seedGrammar` and `await seedGrammar()`
    - All 4 RED test files exist; running `pnpm --filter @repo/api test -- grammar.service.spec` and `pnpm --filter @repo/web test -- grammar` FAILS with module-not-found / undefined-component errors (proving RED, not false-green)
  </acceptance_criteria>
  <done>grammar.json with 10 areas / ≥20 questions per topic (verified at JSON AND post-seed DB level), seedGrammar() wired into main(), 4 RED test scaffolds failing for the right reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → npm registry | dnd-kit packages fetched at install time |
| seed script → PostgreSQL | bulk insert of seed content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | @dnd-kit/core, @dnd-kit/sortable install | mitigate | Task 1 blocking-human checkpoint verifies both packages on npmjs.com before install; [ASSUMED] tag from RESEARCH.md cleared by human |
| T-04-01 | Tampering | grammar.json seed content | accept | Seed data is developer-authored static content, not user input; no injection surface |
| T-04-02 | Information Disclosure | schema migration | accept | Adding non-PII slug columns; no data exposure |
</threat_model>

<verification>
- `pnpm --filter @repo/database db:push` applies slug columns without error
- `pnpm --filter @repo/shared build` succeeds (DTOs type-check)
- `pnpm --filter @repo/database db:seed` runs without FK violation (P2003), then the DB-count check confirms every topic has ≥20 questions
- All 4 RED test files fail (implementations not yet built)
</verification>

<success_criteria>
- GrammarArea.slug and GrammarLesson.slug exist in schema + DB (unique)
- @repo/shared exports all grammar DTOs
- @dnd-kit/core + @dnd-kit/sortable installed in apps/web
- grammar.json: 10 areas, ≥20 questions/topic, ≥3 exercise types, ≥1 DRAG_AND_DROP per topic
- DB-level count confirms every topic has ≥20 GrammarQuestion records post-seed
- seedGrammar() invoked from main()
- 4 RED test scaffolds present and failing
</success_criteria>

<output>
Create `.planning/phases/04-grammar-module/04-01-SUMMARY.md` when done
</output>
