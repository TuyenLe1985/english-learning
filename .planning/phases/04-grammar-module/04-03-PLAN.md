---
phase: 04-grammar-module
plan: 03
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/web/src/components/grammar/exercises/multiple-choice-exercise.tsx
  - apps/web/src/components/grammar/exercises/fill-in-the-blank-exercise.tsx
  - apps/web/src/components/grammar/exercises/sentence-transformation-exercise.tsx
  - apps/web/src/components/grammar/exercises/error-correction-exercise.tsx
  - apps/web/src/components/grammar/exercises/drag-and-drop-exercise.tsx
  - apps/web/src/components/grammar/exercises/multiple-choice-exercise.test.tsx
  - apps/web/src/components/grammar/exercises/fill-in-the-blank-exercise.test.tsx
  - apps/web/src/components/grammar/exercises/drag-and-drop-exercise.test.tsx
  - apps/web/src/components/grammar/explanation-view.tsx
autonomous: true
requirements: [GRAM-02, GRAM-03]
user_setup: []

must_haves:
  truths:
    - "All 5 grammar exercise types render and emit onCorrect/onIncorrect"
    - "FillInTheBlank validates answers case-insensitively after trimming whitespace"
    - "DragAndDrop lets a user drag a word from the bank into a blank slot using dnd-kit (mouse + touch)"
    - "ExplanationView renders the lesson explanation rule card and example list"
  artifacts:
    - path: "apps/web/src/components/grammar/exercises/multiple-choice-exercise.tsx"
      provides: "MultipleChoiceExercise"
      contains: "onCorrect"
    - path: "apps/web/src/components/grammar/exercises/drag-and-drop-exercise.tsx"
      provides: "DragAndDropExercise using @dnd-kit/core"
      contains: "useDraggable"
    - path: "apps/web/src/components/grammar/explanation-view.tsx"
      provides: "ExplanationView rule card + examples"
      contains: "examples"
  key_links:
    - from: "apps/web/src/components/grammar/exercises/drag-and-drop-exercise.tsx"
      to: "@dnd-kit/core"
      via: "DndContext + useDraggable + useDroppable"
      pattern: "@dnd-kit/core"
    - from: "apps/web/src/components/grammar/exercises/multiple-choice-exercise.test.tsx"
      to: "multiple-choice-exercise.tsx"
      via: "component import under test"
      pattern: "multiple-choice-exercise"
---

<objective>
Build all 5 grammar exercise components plus the ExplanationView, turning the RED component tests from plan 01 GREEN. These are pure client components with no API dependency — they receive a question and emit correct/incorrect, ready for the lesson carousel (plan 05) to orchestrate.

Purpose: GRAM-03 (≥3 of 5 exercise types — we build all 5) and GRAM-02 (explanation + visual learning blocks). The lesson page slice in plan 05 cannot run a session without these.
Output: 5 exercise components, explanation-view.tsx, 3 component tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/04-grammar-module/04-UI-SPEC.md
@.planning/phases/04-grammar-module/04-PATTERNS.md
@.planning/phases/04-grammar-module/04-RESEARCH.md
@apps/web/src/components/grammar/exercises/multiple-choice-exercise.test.tsx
@apps/web/src/components/grammar/exercises/fill-in-the-blank-exercise.test.tsx
@apps/web/src/components/grammar/exercises/drag-and-drop-exercise.test.tsx

<interfaces>
<!-- @dnd-kit/core (installed plan 01): DndContext, PointerSensor, useSensor, useSensors, DragOverlay, useDraggable, useDroppable, type DragEndEvent. Do NOT use @dnd-kit/sortable. -->
<!-- GrammarQuestionDto from @repo/shared: { id, exerciseType, prompt, answer, distractors[], explanation?, difficulty, xpReward }. -->
<!-- cn() from @/lib/utils. shadcn Button from @/components/ui/button. -->
<!-- Common exercise prop contract: { prompt, answer, distractors/options, explanation?, onCorrect(): void, onIncorrect(): void }. -->
</interfaces>
</context>

<feature>
  <name>5 grammar exercise components + ExplanationView</name>
  <files>apps/web/src/components/grammar/exercises/multiple-choice-exercise.tsx, apps/web/src/components/grammar/exercises/fill-in-the-blank-exercise.tsx, apps/web/src/components/grammar/exercises/sentence-transformation-exercise.tsx, apps/web/src/components/grammar/exercises/error-correction-exercise.tsx, apps/web/src/components/grammar/exercises/drag-and-drop-exercise.tsx, apps/web/src/components/grammar/explanation-view.tsx, apps/web/src/components/grammar/exercises/multiple-choice-exercise.test.tsx, apps/web/src/components/grammar/exercises/fill-in-the-blank-exercise.test.tsx, apps/web/src/components/grammar/exercises/drag-and-drop-exercise.test.tsx</files>
  <behavior>
    - MultipleChoiceExercise: renders prompt + 2×2 options grid (answer + distractors shuffled). Tap selects → reveal correct/wrong coloring → "Next" → selected===answer ? onCorrect() : onIncorrect().
    - FillInTheBlankExercise: text input + "Check". Comparison `input.trim().toLowerCase() === answer.toLowerCase()` → onCorrect/onIncorrect. Wrong shows "The correct answer is: {answer}".
    - SentenceTransformationExercise: textarea + "Check"; case-insensitive compare against answer; wrong shows "Model answer: {answer}".
    - ErrorCorrectionExercise: textarea + "Check"; case-insensitive compare; wrong shows "Corrected: {answer}". (May share a base with SentenceTransformation per Claude discretion — keep prompt labels distinct.)
    - DragAndDropExercise: parse prompt for `___` blank markers; render word bank (answer tokens + distractors) as DraggableWord chips and inline DroppableBlank slots; PointerSensor activationConstraint distance 5; placing a word in a filled blank returns the prior occupant to the bank; "Check" disabled until all blanks filled; correct → green borders + onCorrect, wrong → red borders + onIncorrect.
    - ExplanationView: renders explanation in a zinc-100/dark:zinc-800 rule card + examples as an italic bulleted list (D-05).
  </behavior>
  <implementation>
    RED→GREEN→REFACTOR. The 3 component test scaffolds from plan 01 fail (components missing). Implement components to GREEN. If a scaffold under-specifies a behavior above, extend the test FIRST (RED) then implement.

    Follow UI-SPEC.md "Exercise Component Contracts" exactly for layout/classes/copy and PATTERNS.md exercise sections for structure. MultipleChoice + FillInTheBlank mirror ClozeExercise. SentenceTransformation/ErrorCorrection use textarea (UI-SPEC specs). DragAndDrop has NO codebase analog — follow RESEARCH.md Pattern 4 + PATTERNS.md drag-and-drop section: `"use client"`, DndContext + PointerSensor, useDraggable/useDroppable from @dnd-kit/core only (NOT sortable), DragOverlay; do NOT wrap the exercise card in overflow:hidden (Pitfall 3). All interactive elements `min-h-[44px]`; answer feedback uses color AND text (accessibility contract).

    All 5 exercise files and explanation-view.tsx require `"use client"`.
  </implementation>
</feature>

<read_first>
- apps/web/src/components/grammar/exercises/multiple-choice-exercise.test.tsx (RED scaffold)
- apps/web/src/components/grammar/exercises/fill-in-the-blank-exercise.test.tsx (RED scaffold)
- apps/web/src/components/grammar/exercises/drag-and-drop-exercise.test.tsx (RED scaffold)
- apps/web/src/components/vocabulary/exercises/cloze-exercise.tsx (MC + fill-blank analog — option grid + reveal coloring)
- apps/web/src/components/vocabulary/exercises/recall-exercise.tsx (textarea/free-text analog for transformation)
- apps/web/src/components/vocabulary/exercises/flashcard-exercise.test.tsx (component test pattern, jsdom)
- apps/web/src/lib/utils.ts (cn helper)
- .planning/phases/04-grammar-module/04-UI-SPEC.md (Exercise Component Contracts + Accessibility Contract)
- .planning/phases/04-grammar-module/04-PATTERNS.md (exercise + drag-and-drop + explanation-view sections)
- .planning/phases/04-grammar-module/04-RESEARCH.md (Pattern 4 dnd-kit, Pitfall 3)
</read_first>

<verify>
  <automated>pnpm --filter @repo/web test -- grammar && pnpm --filter @repo/web build</automated>
</verify>

<acceptance_criteria>
  - `pnpm --filter @repo/web test -- grammar` exits 0 (MC, fill-blank, drag-drop tests GREEN)
  - All 5 exercise .tsx files exist and begin with `"use client"`
  - multiple-choice-exercise.tsx contains both `onCorrect` and `onIncorrect`
  - fill-in-the-blank-exercise.tsx contains `.trim().toLowerCase()` (case-insensitive compare)
  - drag-and-drop-exercise.tsx imports from `@dnd-kit/core` and uses `useDraggable` and `useDroppable`; contains NO import from `@dnd-kit/sortable`
  - explanation-view.tsx renders `examples` and a `bg-zinc-100` / `dark:bg-zinc-800` rule card
  - `pnpm --filter @repo/web build` exits 0
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → client component | textarea/input answer text stays client-side; compared in-memory, posted later via lesson orchestrator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | client-side answer grading | accept | Client grades exercises for UX; authoritative GrammarAttempt.isCorrect is posted and could be spoofed, but grammar carries no privilege/payment value — mastery % is self-reported learning progress only. No server-side regrade in Phase 4 scope. |
| T-04-09 | Cross-Site Scripting | rendering prompt/explanation/examples | mitigate | All seed-sourced strings rendered as React text children (no dangerouslySetInnerHTML) — React auto-escapes |
</threat_model>

<verification>
- grammar component tests GREEN
- apps/web builds
- Manual (deferred to plan 05 checkpoint): drag-and-drop works with mouse + touch
</verification>

<success_criteria>
- All 5 exercise types implemented (GRAM-03)
- ExplanationView renders rule card + examples (GRAM-02)
- 3 component tests GREEN, no @dnd-kit/sortable usage
</success_criteria>

<output>
Create `.planning/phases/04-grammar-module/04-03-SUMMARY.md` when done
</output>
