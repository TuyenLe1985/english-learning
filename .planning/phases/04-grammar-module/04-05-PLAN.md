---
phase: 04-grammar-module
plan: 05
type: execute
wave: 4
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - apps/web/src/components/grammar/grammar-lesson-page.tsx
  - apps/web/src/components/grammar/grammar-session-results.tsx
  - apps/web/src/app/(dashboard)/grammar/[area]/[topic]/[lesson]/page.tsx
  - apps/web/src/app/api/grammar/lessons/[lessonSlug]/route.ts
  - apps/web/src/app/api/grammar/sessions/complete/route.ts
  - apps/web/src/app/api/grammar/topics/[topicSlug]/weak-questions/route.ts
autonomous: false
requirements: [GRAM-02, GRAM-03, GRAM-04, GRAM-06]
user_setup: []

must_haves:
  truths:
    - "User can open a lesson, read the explanation, click Start Practice, answer a one-at-a-time exercise carousel, and see a completion screen"
    - "Completing a session POSTs the batch to /api/grammar/sessions/complete and the topic mastery updates"
    - "User can launch a 'Review weak exercises' session containing only their previously-wrong questions"
    - "The lesson carousel renders all 5 exercise types based on each question's exerciseType"
  artifacts:
    - path: "apps/web/src/components/grammar/grammar-lesson-page.tsx"
      provides: "Session orchestrator: explanation → exercises → results"
      contains: "Start Practice"
    - path: "apps/web/src/app/(dashboard)/grammar/[area]/[topic]/[lesson]/page.tsx"
      provides: "Lesson page Server Component fetching lesson detail + questions"
      min_lines: 20
    - path: "apps/web/src/app/api/grammar/sessions/complete/route.ts"
      provides: "POST relay for session completion"
      contains: "POST"
  key_links:
    - from: "apps/web/src/components/grammar/grammar-lesson-page.tsx"
      to: "/api/grammar/sessions/complete"
      via: "fetch POST batch on completion"
      pattern: "/api/grammar/sessions/complete"
    - from: "apps/web/src/app/(dashboard)/grammar/[area]/[topic]/[lesson]/page.tsx"
      to: "/api/grammar/lessons/[lessonSlug]"
      via: "Server Component fetch of lesson detail"
      pattern: "/api/grammar/lessons"
    - from: "apps/web/src/components/grammar/grammar-lesson-page.tsx"
      to: "exercises/*"
      via: "renderExercise switch on exerciseType"
      pattern: "exerciseType"
---

<objective>
Build the lesson page vertical slice — the heart of the grammar module. A Server Component fetches the lesson + questions; the GrammarLessonPage orchestrator drives explanation → one-at-a-time exercise carousel → completion screen, batch-submitting attempts; the weak-review entry point launches a filtered session. This is the slice that makes GRAM-02/03/04/06 fully exercisable end to end, with a human verification checkpoint closing the phase.

Purpose: GRAM-02 (explanation before exercises), GRAM-03 (carousel runs all 5 exercise types), GRAM-04 (session completion stores score + updates mastery), GRAM-06 (weak-exercise re-attempt session).
Output: grammar-lesson-page.tsx, grammar-session-results.tsx, lesson page, 3 relay routes (lesson detail GET, session-complete POST, weak-questions GET), end-to-end checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-grammar-module/04-UI-SPEC.md
@.planning/phases/04-grammar-module/04-PATTERNS.md
@.planning/phases/04-grammar-module/04-CONTEXT.md

<interfaces>
<!-- GrammarLessonDetailDto from @repo/shared: { id, slug, title, explanation, examples[], sortOrder, questions: GrammarQuestionDto[] }. -->
<!-- GrammarSessionCompleteSchema body: { lessonId, attempts: [{ questionId, isCorrect, userAnswer? }], timeTakenMs? }. Response GrammarSessionResultDto: { score, total, masteryPct }. -->
<!-- Exercise components (plan 03) all in apps/web/src/components/grammar/exercises/ with prop contract { prompt, answer, distractors/options, explanation?, onCorrect, onIncorrect }. ExplanationView in apps/web/src/components/grammar/explanation-view.tsx. -->
<!-- Reuse: practice-session.tsx orchestrator pattern, session-results.tsx, framer-motion, Progress, Button. -->
<!-- Weak-review entry: plan 04's topic page ALREADY deep-links the weak CTA to the lesson route `/grammar/[area]/[topic]/[lesson]?review=weak` (the topic's first lesson slug). This plan only CONSUMES that `review=weak` search param on the lesson page — it does NOT re-wire plan 04. When review=weak, the lesson Server Component fetches the weak question set from /api/grammar/topics/[topicSlug]/weak-questions and the orchestrator runs only those questions, skipping the explanation phase. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lesson-detail + session-complete + weak-questions relay routes</name>
  <files>apps/web/src/app/api/grammar/lessons/[lessonSlug]/route.ts, apps/web/src/app/api/grammar/sessions/complete/route.ts, apps/web/src/app/api/grammar/topics/[topicSlug]/weak-questions/route.ts</files>
  <read_first>
    - apps/web/src/app/api/vocabulary/[category]/[wordId]/route.ts (GET relay with dynamic segment)
    - apps/web/src/app/api/vocabulary/session/complete/route.ts (POST relay pattern — req.json try/catch, await headers, fetchWithAuth POST)
    - apps/web/src/lib/api-client.ts (fetchWithAuth, API_URL)
    - .planning/phases/04-grammar-module/04-PATTERNS.md (GET + POST relay route sections)
  </read_first>
  <action>
    Create lessons/[lessonSlug]/route.ts GET relay: auth() → 401; await params for lessonSlug; await headers() cookieHeader; fetchWithAuth(cookieHeader, `${API_URL}/api/grammar/lessons/${lessonSlug}`); pass through status/json.

    Create sessions/complete/route.ts POST relay (mirror vocabulary session complete): auth() → 401; `let body; try { body = await req.json(); } catch { return 400 "Invalid JSON" }`; await headers() cookieHeader; fetchWithAuth(cookieHeader, `${API_URL}/api/grammar/sessions/complete`, { method: "POST", body: JSON.stringify(body) }); on !res.ok return res.text() error with status; else return res.json(). Do NOT inject or read userId in the relay — NestJS derives it from the JWT.

    Create topics/[topicSlug]/weak-questions/route.ts GET relay: auth() → 401; await params; await headers() cookieHeader; fetchWithAuth(cookieHeader, `${API_URL}/api/grammar/topics/${topicSlug}/weak-questions`); pass through.
  </action>
  <verify>
    <automated>grep -q 'method: "POST"' apps/web/src/app/api/grammar/sessions/complete/route.ts && grep -q 'fetchWithAuth' apps/web/src/app/api/grammar/lessons/'[lessonSlug]'/route.ts && grep -q 'fetchWithAuth' apps/web/src/app/api/grammar/topics/'[topicSlug]'/weak-questions/route.ts && pnpm --filter @repo/web build</automated>
  </verify>
  <acceptance_criteria>
    - sessions/complete/route.ts contains `method: "POST"`, a `req.json()` try/catch returning 400, and forwards via fetchWithAuth; contains no `userId` assignment
    - lessons/[lessonSlug]/route.ts and topics/[topicSlug]/weak-questions/route.ts both use `await headers()` + `fetchWithAuth` and return 401 unauthenticated
    - `pnpm --filter @repo/web build` exits 0
  </acceptance_criteria>
  <done>Three relay routes proxy lesson detail, session completion, and weak questions to NestJS.</done>
</task>

<task type="auto">
  <name>Task 2: GrammarSessionResults + GrammarLessonPage orchestrator + lesson Server Component page</name>
  <files>apps/web/src/components/grammar/grammar-session-results.tsx, apps/web/src/components/grammar/grammar-lesson-page.tsx, apps/web/src/app/(dashboard)/grammar/[area]/[topic]/[lesson]/page.tsx</files>
  <read_first>
    - apps/web/src/components/vocabulary/practice-session.tsx (session orchestrator: currentIndex, answers[], batch submit on complete, progress bar)
    - apps/web/src/components/vocabulary/session-results.tsx (results screen + framer-motion entrance)
    - apps/web/src/components/grammar/explanation-view.tsx (built plan 03 — explanation phase)
    - apps/web/src/components/grammar/exercises/multiple-choice-exercise.tsx (and the other 4 — prop contract for renderExercise switch)
    - apps/web/src/app/(dashboard)/vocabulary/[category]/[wordId]/page.tsx (Server Component detail page pattern)
    - .planning/phases/04-grammar-module/04-UI-SPEC.md (Screen 4 sub-states A/B/C, exercise sub-prompts, completion copy)
    - .planning/phases/04-grammar-module/04-PATTERNS.md (grammar-lesson-page + grammar-session-results sections)
    - .planning/phases/04-grammar-module/04-CONTEXT.md (D-04 linear flow, D-06 carousel, D-07 exercises-are-assessment, D-09 weak re-attempt)
  </read_first>
  <action>
    Create grammar-session-results.tsx (`"use client"`) per PATTERNS grammar-session-results section + UI-SPEC sub-state C: framer-motion entrance (opacity 0→1, scale 0.95→1, 0.3s easeOut); "Session complete!" label; "{score}/{total} correct" `text-[28px] font-semibold`; "{N}% · {Xs}" accuracy+time; mastery Progress bar `value={masteryPct*100}`; "Practice again" (onRestart) and "Back to topic" (onBackToTopic) outline Buttons `w-full min-h-[44px]`. No SRS dialog.

    Create grammar-lesson-page.tsx (`"use client"`) session orchestrator mirroring practice-session.tsx (D-06): props { lesson: GrammarLessonDetailDto, areaSlug, topicSlug, weakQuestions?: GrammarQuestionDto[] }. Three phases via useState: "explanation" | "exercises" | "results" (D-04 linear). Explanation phase renders ExplanationView(explanation, examples) + question-count hint + "Start Practice" Button (full-width min-h-[44px]) → sets phase "exercises". Exercises phase: in-memory state currentIndex, attempts[] (no mid-session API calls — D-06), startTime ref; progress section "{n} of {total}" + Progress bar; exercise card `rounded-xl border border-border bg-card p-6 shadow-sm min-h-[280px]` (no overflow:hidden — DnD Pitfall 3); renderExercise(question) switches on question.exerciseType across all 5 components, passing onCorrect/onIncorrect that push an Attempt { questionId, isCorrect, userAnswer? } and advance currentIndex (or submit when last). On completion call submitSession(finalAttempts): POST /api/grammar/sessions/complete { lessonId: lesson.id, attempts, timeTakenMs }; store returned { score, total, masteryPct }; set phase "results" even on fetch failure (show "Could not save your progress..." copy). Pass results to GrammarSessionResults; onRestart resets to "exercises" with currentIndex 0; onBackToTopic navigates to /grammar/${areaSlug}/${topicSlug}.

    Weak-review mode (D-09): the orchestrator accepts an optional `weakQuestions` prop (the set to run instead of the lesson's full question set). When `weakQuestions` is non-empty, the orchestrator skips the explanation phase and starts directly in "exercises" running ONLY the provided weak questions. Drive this from the lesson Server Component reading the `review` search param (see below) — the orchestrator itself just consumes the prop.

    Create app/(dashboard)/grammar/[area]/[topic]/[lesson]/page.tsx Server Component: params Promise<{ area, topic, lesson }>, searchParams Promise<{ review?: string }>; auth gate; await params + searchParams; fetch lesson detail via getSessionToken + `${API_URL}/api/grammar/lessons/${lesson}` (GrammarLessonDetailDto with questions). If searchParams.review === "weak": also fetch `${API_URL}/api/grammar/topics/${topic}/weak-questions` and pass the returned array as the `weakQuestions` prop (which makes the orchestrator skip the explanation phase). Otherwise pass no weakQuestions. Render `<GrammarLessonPage lesson={...} areaSlug={area} topicSlug={topic} weakQuestions={...} />`. Handle empty/missing lesson with the API-failure copy.

    Note: plan 04's topic page already constructs the weak CTA as `/grammar/${area}/${topic}/${lessons[0].slug}?review=weak`, so this lesson page is reached directly with the correct lesson slug and `review=weak`. This plan does NOT modify any plan-04 file — it only reads the search param here.
  </action>
  <verify>
    <automated>grep -q 'Start Practice' apps/web/src/components/grammar/grammar-lesson-page.tsx && grep -q '/api/grammar/sessions/complete' apps/web/src/components/grammar/grammar-lesson-page.tsx && grep -q 'exerciseType' apps/web/src/components/grammar/grammar-lesson-page.tsx && grep -q 'review' "apps/web/src/app/(dashboard)/grammar/[area]/[topic]/[lesson]/page.tsx" && pnpm --filter @repo/web build</automated>
  </verify>
  <acceptance_criteria>
    - grammar-lesson-page.tsx begins with `"use client"`, contains phase state with "explanation"/"exercises"/"results", a renderExercise switch on `exerciseType` covering all 5 types, and a POST to `/api/grammar/sessions/complete`
    - grammar-lesson-page.tsx accepts an optional `weakQuestions` prop and, when it is non-empty, skips the explanation phase and runs only those questions
    - grammar-session-results.tsx renders a mastery `Progress` and "Back to topic" + "Practice again" buttons, with framer-motion entrance
    - lesson page Server Component awaits params + searchParams, fetches lesson detail, and conditionally fetches weak-questions when `review=weak`, passing them as `weakQuestions`
    - this plan modifies NO file owned by plan 04 (no edits to `[area]/[topic]/page.tsx`)
    - exercise card wrapper does NOT include `overflow-hidden`
    - `pnpm --filter @repo/web build` exits 0
  </acceptance_criteria>
  <done>Full lesson flow (explanation → carousel → results) works; weak-review launches a filtered session via the review=weak search param; mastery updates on completion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end grammar phase verification</name>
  <what-built>Complete grammar module: 10-area browse, topic/lesson navigation with mastery, lesson explanation, 5-type exercise carousel, session completion with mastery update, and weak-exercise re-attempt — all backed by seeded data.</what-built>
  <how-to-verify>
    1. Ensure stack is running (`docker compose up` for DB/Redis, then `pnpm dev`) and the grammar seed has run (`pnpm --filter @repo/database db:seed`). Log in as demo@example.com / demo1234.
    2. Visit http://localhost:3000/grammar — confirm a grid of 10 area cards with distinct icons and "{N} topics" counts.
    3. Click an area → confirm its topic list with CEFR badges. Click a topic → confirm lesson list and (initially) no mastery bar.
    4. Open a lesson → confirm the explanation rule card + examples render BEFORE any exercise (GRAM-02). Click "Start Practice".
    5. Answer the carousel; confirm you encounter at least 3 distinct exercise types and that the DragAndDrop exercise works with BOTH mouse drag and (resize to mobile / touch emulation) touch (GRAM-03). Confirm the word chip does not clip when dragged outside the card.
    6. Finish the session → confirm the completion screen shows score and a mastery percentage. Return to the topic page → confirm the mastery bar now shows a value (GRAM-04).
    7. Intentionally answer some questions wrong, complete, then on the topic page click "Review weak exercises" → confirm the URL deep-links to the topic's first lesson with `?review=weak`, the explanation phase is skipped, and the session contains only previously-wrong questions (GRAM-06).
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass, or describe failures (which step, what happened).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next.js relay (POST sessions/complete) | client-submitted attempt batch crosses here |
| Next.js relay → NestJS | Bearer token forwarded; userId derived server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering | POST sessions/complete relay | mitigate | relay forwards body unchanged and never sets userId; NestJS (plan 02) takes userId from JWT and validates body with Zod |
| T-04-14 | Elevation of Privilege | lesson / weak-questions relays | mitigate | auth() gate returns 401 before proxy; weak-questions scoped to req.user.userId in NestJS |
| T-04-15 | Denial of Service | malformed POST body | mitigate | relay try/catch on req.json returns 400 before forwarding |
</threat_model>

<verification>
- apps/web builds
- `pnpm turbo test` green across the phase
- Human checkpoint (Task 3) passes all 7 checks
</verification>

<success_criteria>
- Lesson flow explanation→carousel→results complete (GRAM-02/03)
- Session completion stores attempts + updates mastery (GRAM-04)
- Weak-exercise re-attempt session works via review=weak search param (GRAM-06)
- 3 relay routes auth-gated; POST forwards without client userId
- End-to-end checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/04-grammar-module/04-05-SUMMARY.md` when done
</output>
