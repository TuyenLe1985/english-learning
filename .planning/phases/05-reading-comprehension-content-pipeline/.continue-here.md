---
context: phase
phase: 05-reading-comprehension-content-pipeline
task: 9
total_tasks: 9
status: in_progress
last_updated: 2026-06-14T14:23:22.705Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_No blocking constraints this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Worktree base-commit race | Parallel worktree agents in wave 2 got wrong base commits — agents returned exit 0 with no commits. | advisory | Dispatch agents one at a time (run_in_background: true, separate messages). Verify HEAD before each dispatch. Use sequential mode for safety. |
| TDD gate RED-in-wrong-plan | MVP+TDD gate tripped on 05-02/03/04/05 because RED tests are in 05-01. | advisory | Include explicit `<tdd_gate_override>` block in executor prompts for plans 05-02 through 05-05. |
| onClick in Server Component | Passing onClick prop from Server Component causes Next.js runtime error. | advisory | Browse grid bookmark is now display-only. All interactive handlers must be in Client Components (`"use client"`). |

<current_state>
All 9 Phase 5 plans are COMPLETE (9/9 SUMMARY.md files exist). Phase execution done.
Pending: user UI approval → phase.complete → code-review fixes.

Crawler fixes committed this session (f741646). Pipeline needs re-run to confirm VOA/BBC/NewsInLevels work.
Verification returned `human_needed` (13/16 must-haves) — waiting on user to approve 6 browser UI journeys.
</current_state>

<completed_work>

All Phase 5 plans complete:
- **05-01** (94bd53d): Foundation — packages installed, reading.dto.ts, TDD RED scaffolds (12+6+2 tests)
- **05-02** (1fae691): ReadingModule GREEN — 7 endpoints, 12/12 tests green
- **05-03** (04104f4): VocabularyService.lookupByWord + GET /api/vocabulary/lookup — 18/18 tests green
- **05-04** (2efff93): ClassifierService + PipelineModule + 923-word CEFR word list — 6/6 tests green
- **05-05** (e64d96e): CrawlerService (4 sources) + SeedService + pipeline CLI
- **05-06** (fd1e98d): Next.js reading browse page + passage detail Server Components
- **05-07** (3ace398): PassageRenderer + HighlightTooltip + QuestionsSection + NotesPanel + ReadingPageClient
- **05-08** (3ef9714): WordPopover + bookmark toggle wiring
- **05-09** (4f71121): E2E checkpoint — automated verification done (13/16 must-haves)

Bug fixes this session:
- fix(pipeline): tsconfig.pipeline.json moduleResolution node16 (020ae3b)
- fix(reading): ForbiddenException on highlight ownership mismatch / CR-05 (95f5afb)
- fix(reading): removed onClick from Server Component browse card (60f9c2d)
- fix(crawler): networkidle + Playwright live DOM for JS-rendered listing pages (b209adc)
- fix(crawler): plain type cast for href — HTMLAnchorElement not in Node types (f741646)
</completed_work>

<remaining_work>

1. **Confirm pipeline seeded 2000+ passages**: Re-run `pnpm --filter @repo/api pipeline:run` after crawler fix
2. **User approves 6 UI journeys** at http://localhost:3000/reading (see 05-09-PLAN.md Task 2)
3. **Mark phase complete**: `gsd-sdk query phase.complete 05`
4. **Apply code review fixes**: `/gsd-code-review 5 --fix` for:
   - CR-01: getPassageById() missing isPublished filter
   - CR-02: Zod .parse() unhandled → HTTP 500
   - CR-03: toggleBookmark() TOCTOU race
   - CR-04: HTML not sanitized server-side in crawler
   - WR-05: getPassages() returns raw Prisma fields instead of DTO fields
5. **Commit uncommitted files**: 05-REVIEW.md, 05-VERIFICATION.md, STATE.md
</remaining_work>

<decisions_made>

- **Sequential execution** for all Phase 5 waves — worktree parallel mode caused wrong base commits in wave 2
- **TDD architecture**: RED tests in 05-01, GREEN in 05-02/03/04/05 — executor agents need `<tdd_gate_override>` blocks
- **cefr-word-list.json** at `packages/database/prisma/seed-data/` (not apps/api/prisma/) — ClassifierService references via relative path `../../../../packages/database/prisma/seed-data/cefr-word-list.json`
- **Browse grid bookmark**: display-only amber icon — onClick forbidden in Server Components
- **tsconfig.pipeline.json**: moduleResolution must be `node16` to resolve `@repo/database` exports field
- **Crawler listing pages**: must use `networkidle` + `page.$$eval()` not `domcontentloaded` + Cheerio for JS-rendered sites
</decisions_made>

<blockers>

- **Pipeline seeding**: Only 10 sample passages in DB. Need internet-connected `pipeline:run` to seed 2000+ real passages. Crawler fix committed.
- **Pre-existing RED stubs**: 4 failing tests (jwt.guard.spec.ts × 2 from Phase 4, auth.service.spec.ts × 2 from Phase 2 labeled "Plan 05"). These are NOT Phase 5 regressions — safe to ignore for Phase 5 verification.
</blockers>

## Required Reading (in order)
1. `.planning/phases/05-reading-comprehension-content-pipeline/05-09-PLAN.md` — UI journey checklist for human verification
2. `.planning/phases/05-reading-comprehension-content-pipeline/05-REVIEW.md` — 5 critical issues to fix
3. `.planning/phases/05-reading-comprehension-content-pipeline/05-VERIFICATION.md` — verification report (human_needed, 13/16)
4. `.planning/STATE.md` — current progress

## Infrastructure State
- Docker: `el_postgres`, `el_redis_bullmq`, `el_redis_cache`, `el_minio` — all running
- Next.js web: port 3000 (already running before this session)
- NestJS API: port 3001 (started this session via turbo dev)
- DB: 10 passages (B1×5, B2×3, C1×2), all isPublished=true — need pipeline:run for real volume
- Branch: `worktree-agent-a98aff1badd8ed2b5` (this IS the working branch — not a worktree artifact)

<context>
Phase 5 fully implemented. All 36 Phase 5 tests green. The remaining work is operational: seed real data, get user UI approval, close the phase, and fix the 5 code review issues found. The code review issues are real bugs (missing isPublished filter, Zod error handling, bookmark race condition, server-side HTML sanitization, DTO field mapping) — they should be fixed before Phase 6.
</context>

<next_action>
1. Check if background pipeline:run completed: `pnpm --filter @repo/api pipeline:run 2>&1 | tail -20`
2. If complete with 2000+ passages: visit http://localhost:3000/reading and verify 6 UI journeys
3. Type "approved" to close phase, then run `/gsd-code-review 5 --fix`
</next_action>
