---
phase: 05-reading-comprehension-content-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/web/package.json
  - packages/shared/src/reading.dto.ts
  - packages/shared/src/index.ts
  - apps/api/src/reading/reading.service.spec.ts
  - apps/api/src/pipeline/classifier.service.spec.ts
  - apps/api/src/vocabulary/vocabulary.service.spec.ts
autonomous: true
requirements:
  - READ-01
  - READ-02
  - READ-03
  - READ-04
  - READ-05
  - READ-06
  - READ-07
  - PIPE-03
  - PIPE-04
  - VOCAB-08

must_haves:
  truths:
    - "reading.service.spec.ts exists with RED stubs for all 7 reading service methods (getPassages, getPassageById, completeSession, createHighlight, deleteHighlight, upsertNote, toggleBookmark)"
    - "classifier.service.spec.ts exists with RED stubs for CEFR level correctness (PIPE-03) and flaggedForReview=true when confidence < 0.65 (PIPE-04)"
    - "vocabulary.service.spec.ts has a lookupByWord RED stub (word found returns VocabularyWordDto; word not found returns null)"
    - "packages/shared/src/reading.dto.ts exports all 6 reading Zod schemas and inferred types"
    - "packages/shared/src/index.ts re-exports reading.dto.ts"
    - "new packages (natural, cheerio, isomorphic-dompurify, dom-anchor-text-position, @types/natural) are installed in apps/api"
    - "new packages (isomorphic-dompurify, dom-anchor-text-position) are installed in apps/web"
    - "shadcn popover, sheet, separator, select, textarea, tooltip components installed in apps/web"
  artifacts:
    - path: "apps/api/src/reading/reading.service.spec.ts"
      provides: "TDD RED scaffolds for ReadingService"
      contains: "describe('ReadingService'"
    - path: "apps/api/src/pipeline/classifier.service.spec.ts"
      provides: "TDD RED scaffolds for ClassifierService"
      contains: "describe('ClassifierService'"
    - path: "packages/shared/src/reading.dto.ts"
      provides: "All Phase 5 shared DTOs"
      exports:
        - "ReadingPassageDtoSchema"
        - "ReadingSessionCompleteSchema"
        - "HighlightCreateSchema"
        - "NoteUpsertSchema"
        - "BookmarkToggleSchema"
  key_links:
    - from: "packages/shared/src/reading.dto.ts"
      to: "packages/shared/src/index.ts"
      via: "export * from './reading.dto'"
      pattern: "reading\\.dto"

user_setup: []
---

<objective>
Wave 0 foundation: install new packages, generate the CEFR word list JSON from the CSV dataset, create shared reading DTOs, and write TDD RED test scaffolds for the three services that require TDD coverage in Phase 5. All downstream waves depend on these artifacts existing first.

Purpose: Establishes the test gate (RED scaffolds run and fail before implementation), the DTO contracts consumed by NestJS and Next.js, and ensures new packages are available before any code uses them.
Output: 5 new files, 3 modified files, package installs confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-reading-comprehension-content-pipeline/05-CONTEXT.md
@.planning/phases/05-reading-comprehension-content-pipeline/05-RESEARCH.md
@.planning/phases/05-reading-comprehension-content-pipeline/05-VALIDATION.md
@.planning/phases/05-reading-comprehension-content-pipeline/05-PATTERNS.md
@packages/shared/src/grammar.dto.ts
@apps/api/src/vocabulary/vocabulary.service.spec.ts
@apps/api/src/grammar/grammar.service.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install new packages (API + Web) and shadcn components</name>
  <files>apps/api/package.json, apps/web/package.json</files>
  <read_first>
    apps/api/package.json — verify current dependencies before adding new ones; confirm natural/cheerio/isomorphic-dompurify/dom-anchor-text-position are NOT already present
    apps/web/package.json — verify isomorphic-dompurify/dom-anchor-text-position are NOT already present
    .planning/phases/05-reading-comprehension-content-pipeline/05-RESEARCH.md §Package Legitimacy Audit — [ASSUMED] verdict applies to all four new packages; all passed audit
    .planning/phases/05-reading-comprehension-content-pipeline/05-RESEARCH.md §Standard Stack (New Packages)
  </read_first>
  <action>
    1. Install API packages: run `pnpm --filter @repo/api add isomorphic-dompurify dom-anchor-text-position natural cheerio` and `pnpm --filter @repo/api add -D @types/natural`. These are all [ASSUMED]-approved packages from the legitimacy audit. Do NOT install `@hypothesis/anchoring` — it does not exist on npm (404 confirmed in RESEARCH.md Pitfall 2).

    2. Install Web packages: run `pnpm --filter @repo/web add isomorphic-dompurify dom-anchor-text-position`. The `isomorphic-dompurify` package must only be used in "use client" components (RESEARCH.md Pitfall 6 — jsdom ESM conflict in Server Components).

    3. Install shadcn UI components in apps/web: run `cd apps/web && npx shadcn add popover sheet separator select textarea tooltip`. These come from the official shadcn registry only (UI-SPEC.md §Registry Safety). If the command asks for confirmation, accept defaults.

    4. Verify that TypeScript compilation still passes: run `pnpm --filter @repo/api type-check` and `pnpm --filter @repo/web type-check` after installs. If `@types/natural` causes issues, check the version aligns with natural@8.1.1.
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && node -e "const pkg=require('./apps/api/package.json'); ['natural','cheerio','isomorphic-dompurify','dom-anchor-text-position'].forEach(p=>{if(!pkg.dependencies[p]) throw new Error('Missing: '+p)}); console.log('API deps OK')" && node -e "const pkg=require('./apps/web/package.json'); ['isomorphic-dompurify','dom-anchor-text-position'].forEach(p=>{if(!pkg.dependencies[p]) throw new Error('Missing: '+p)}); console.log('Web deps OK')" && ls apps/web/src/components/ui/popover.tsx apps/web/src/components/ui/sheet.tsx apps/web/src/components/ui/separator.tsx apps/web/src/components/ui/select.tsx apps/web/src/components/ui/textarea.tsx apps/web/src/components/ui/tooltip.tsx</automated>
  </verify>
  <done>All 4 new API packages installed, 2 new web packages installed, 6 shadcn components present in apps/web/src/components/ui/. TypeScript compilation passes for both apps.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared reading.dto.ts and update barrel export</name>
  <files>packages/shared/src/reading.dto.ts, packages/shared/src/index.ts</files>
  <read_first>
    packages/shared/src/grammar.dto.ts — exact DTO file structure to replicate: imports, Zod schema naming conventions (Schema suffix), inferred type naming, export ordering
    packages/shared/src/index.ts — current barrel; add reading.dto.ts export after grammar.dto.ts line
    packages/shared/src/vocabulary.dto.ts — verify EnrollWordSchema requires wordId: z.string() (non-optional per RESEARCH.md Pitfall 5)
    .planning/phases/05-reading-comprehension-content-pipeline/05-PATTERNS.md §packages/shared/src/reading.dto.ts — complete DTO schema definitions to use verbatim
    packages/database/prisma/schema.prisma — verify ContentType enum values (ARTICLE, NEWS, BLOG_POST, ACADEMIC, STORY, OPINION) and CefrLevel enum values (B1, B2, C1) match
  </read_first>
  <action>
    Create packages/shared/src/reading.dto.ts with the following Zod schemas and TypeScript types. Follow the grammar.dto.ts structure exactly (z import, schema-first, inferred types at bottom).

    Schemas to define (exact names — NestJS and Next.js import these):
    - ReadingPassageDtoSchema: id, title, contentType (enum ARTICLE/NEWS/BLOG_POST/ACADEMIC/STORY/OPINION), cefrLevel (enum B1/B2/C1), cefrConfidence (number), topic (string nullable), wordCount (number), questionCount (number), isBookmarked (boolean)
    - ReadingPassageDetailDtoSchema: extends ReadingPassageDtoSchema, adds content (string — sanitized HTML), questions (ReadingQuestionDtoSchema array), highlights (HighlightDtoSchema array), note (string nullable), progress (object with score/accuracy/readingTimeSec nullable)
    - ReadingQuestionDtoSchema: id, questionType (string), prompt, answer, distractors (string array), explanation (string nullable), xpReward (number), sortOrder (number)
    - HighlightDtoSchema: id, passageId, startOffset (number), endOffset (number), text (string)
    - ReadingSessionCompleteSchema (input DTO): passageId (string), score (number), accuracy (number), readingTimeSec (number), attempts (array of {questionId: string, isCorrect: boolean, userAnswer: string optional})
    - HighlightCreateSchema (input DTO): passageId (string), startOffset (number), endOffset (number), text (string)
    - NoteUpsertSchema (input DTO): passageId (string), content (string)
    - BookmarkToggleSchema (input DTO): passageId (string)

    Export all inferred TypeScript types at bottom (ReadingPassageDto, ReadingPassageDetailDto, ReadingQuestionDto, HighlightDto, ReadingSessionCompleteDto, HighlightCreateDto, NoteUpsertDto, BookmarkToggleDto).

    Update packages/shared/src/index.ts: add `export * from "./reading.dto";` on the line after the grammar.dto export. Do not remove or reorder existing exports.
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && pnpm --filter @repo/shared type-check 2>/dev/null || pnpm --filter @repo/shared build && grep -c "ReadingPassageDtoSchema\|ReadingSessionCompleteSchema\|HighlightCreateSchema\|NoteUpsertSchema\|BookmarkToggleSchema" packages/shared/src/reading.dto.ts && grep -c "reading\.dto" packages/shared/src/index.ts</automated>
  </verify>
  <done>packages/shared/src/reading.dto.ts contains all 8 Zod schemas and inferred types. packages/shared/src/index.ts exports reading.dto.ts. TypeScript compilation passes for @repo/shared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write TDD RED scaffolds for ReadingService, ClassifierService, and VocabularyService.lookupByWord</name>
  <files>
    apps/api/src/reading/reading.service.spec.ts,
    apps/api/src/pipeline/classifier.service.spec.ts,
    apps/api/src/vocabulary/vocabulary.service.spec.ts
  </files>
  <read_first>
    apps/api/src/grammar/grammar.service.spec.ts — exact test file structure to replicate: vi.fn() mock setup, mockPrisma construction, describe/it nesting, beforeEach vi.clearAllMocks(), direct instantiation pattern (new Service(mockPrisma))
    apps/api/src/vocabulary/vocabulary.service.spec.ts — existing spec to ADD lookupByWord test to (do NOT overwrite the file — append one new describe block)
    .planning/phases/05-reading-comprehension-content-pipeline/05-PATTERNS.md §apps/api/src/reading/reading.service.spec.ts — mock prisma construction and describe structure
    .planning/phases/05-reading-comprehension-content-pipeline/05-VALIDATION.md §Wave 0 Requirements — exact stubs required per requirement
    .planning/phases/05-reading-comprehension-content-pipeline/05-RESEARCH.md §Validation Architecture §Phase Requirements → Test Map
  </read_first>
  <behavior>
    ReadingService RED stubs (all must FAIL because ReadingService does not exist yet):
    - getPassages(): returns paginated passages object with { passages, total, page, limit, totalPages }
    - getPassageById(): returns passage detail with questions, highlights, note, progress; throws NotFoundException for missing id
    - completeSession(): calls prisma.readingProgress.upsert with where: { userId_passageId: { userId, passageId } }
    - createHighlight(): calls prisma.highlight.create; returns created highlight with id
    - deleteHighlight(): calls prisma.highlight.delete; throws NotFoundException if not found or userId mismatch (IDOR protection)
    - upsertNote(): calls prisma.note.upsert with where: { userId_passageId: { userId, passageId } }
    - toggleBookmark(): creates bookmark if not exists, deletes if exists; returns { bookmarked: true/false }

    ClassifierService RED stubs (all must FAIL because ClassifierService does not exist yet):
    - classifyPassage() with a simple B1-vocabulary passage → returns { cefrLevel: 'B1', cefrConfidence: number ≥ 0.0 }
    - classifyPassage() with high-confidence result (≥ 0.65) → { flaggedForReview: false }
    - classifyPassage() with ambiguous passage → confidence < 0.65 → { flaggedForReview: true, isPublished: false }

    VocabularyService.lookupByWord RED stub (append to existing describe block):
    - lookupByWord() with a known word → returns a VocabularyWordDto (not null)
    - lookupByWord() with an unknown word → returns null (not a 404 exception per D-13)
  </behavior>
  <action>
    Create apps/api/src/reading/reading.service.spec.ts from scratch following the grammar.service.spec.ts pattern exactly:
    - Import from 'vitest': describe, it, expect, vi, beforeEach
    - Import NotFoundException from '@nestjs/common'
    - Mock all prisma methods needed: readingPassage.findMany, readingPassage.count, readingPassage.findUnique, readingProgress.upsert, readingProgress.findUnique, highlight.create, highlight.findMany, highlight.delete, note.upsert, note.findFirst, bookmark.upsert, bookmark.delete, bookmark.findUnique
    - Construct mockPrisma as unknown as PrismaService
    - Use direct instantiation: `service = new ReadingService(mockPrisma)` in beforeEach
    - Each test in RED state: mockResolvedValue(something appropriate), call the method, expect the mock to have been called with the right arguments
    - These tests MUST fail because ReadingService does not exist yet — the import itself will cause a compile error

    Create apps/api/src/pipeline/classifier.service.spec.ts from scratch following same pattern:
    - No prisma mocks needed (classifier is a pure function service)
    - Mock the cefr-word-list.json import OR pass a mock word map via constructor injection
    - RED state: tests fail because ClassifierService does not exist yet

    Modify apps/api/src/vocabulary/vocabulary.service.spec.ts — ADD (do not remove or rewrite):
    - Add a new `describe('lookupByWord()', () => { ... })` block at the end of the outer VocabularyService describe block
    - Two tests: (1) mock prisma.vocabularyWord.findFirst to return a mock word object → expect service.lookupByWord('negotiate') resolves to that object; (2) mock prisma.vocabularyWord.findFirst to return null → expect service.lookupByWord('xyzabc') resolves to null
    - These tests will be RED because lookupByWord does not exist on VocabularyService yet

    Run `pnpm --filter @repo/api test` and confirm it exits non-zero (RED state confirmed). The test failures are expected — they prove RED before GREEN.
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && pnpm --filter @repo/api test 2>&1 | grep -E "FAIL|failed|Cannot find module|reading.service.spec|classifier.service.spec" | head -10 && echo "RED state confirmed — tests fail as expected"</automated>
  </verify>
  <done>reading.service.spec.ts exists with ≥7 RED test cases. classifier.service.spec.ts exists with ≥3 RED test cases. vocabulary.service.spec.ts has lookupByWord describe block with ≥2 RED cases. pnpm --filter @repo/api test exits non-zero (RED confirmed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package registry → project | All 5 new packages cross from npm into the codebase at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-W0-01 | Tampering | npm package installs (natural, cheerio, isomorphic-dompurify, dom-anchor-text-position, @types/natural) | mitigate | All packages tagged [ASSUMED] in RESEARCH.md audit — human must verify via npmjs.com before install proceeds; see checkpoint gate in slopcheck protocol |
| T-05-W0-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck blocked at research time; planner tagged all 5 packages [ASSUMED]; blocking checkpoint required before first install |
</threat_model>

<verification>
After completing all three tasks:
1. `pnpm --filter @repo/api test` exits non-zero (RED state — reading.service.spec + classifier.service.spec fail to import non-existent modules)
2. `pnpm --filter @repo/shared build` exits 0 (reading.dto.ts compiles cleanly)
3. `ls apps/web/src/components/ui/{popover,sheet,separator,select,textarea,tooltip}.tsx` — all 6 files exist
4. `grep "reading\.dto" packages/shared/src/index.ts` returns 1 match
</verification>

<success_criteria>
Wave 0 foundation complete when:
- 5 new API packages installed (natural, cheerio, isomorphic-dompurify, dom-anchor-text-position, @types/natural)
- 2 new web packages installed (isomorphic-dompurify, dom-anchor-text-position)
- 6 shadcn components present (popover, sheet, separator, select, textarea, tooltip)
- packages/shared/src/reading.dto.ts exports 8 Zod schemas and 8 TypeScript types
- 3 spec files contain RED scaffolds covering all 10 test behaviors from VALIDATION.md Wave 0 Requirements
- pnpm --filter @repo/api test exits non-zero confirming RED state
</success_criteria>

<output>
Create `.planning/phases/05-reading-comprehension-content-pipeline/05-01-SUMMARY.md` when done
</output>
