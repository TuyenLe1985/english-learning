---
phase: 05-reading-comprehension-content-pipeline
plan: 05
type: execute
wave: 3
depends_on:
  - 05-04
files_modified:
  - apps/api/src/pipeline/crawler.service.ts
  - apps/api/src/pipeline/seed.service.ts
  - apps/api/src/pipeline/pipeline.cli.ts
  - apps/api/src/pipeline/pipeline.module.ts
  - apps/api/package.json
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-05
  - PIPE-06

must_haves:
  truths:
    - "pnpm pipeline:validate runs the crawler against 50-URL sample per source and reports extraction success rate ≥80% before bulk crawl is allowed"
    - "CrawlerService extracts title + plaintext content from VOA, BBC, NewsInLevels, Simple English Wikipedia using Playwright + Cheerio"
    - "PIPE-02 quality gate: passages below 150 words OR with low unique-word ratio are filtered out before seeding"
    - "PIPE-02 dedup: URL and contentHash (SHA-256 of cleaned content) prevent duplicate passages"
    - "SeedService.seedFromFile() uses createMany() in 500-record batches (PIPE-06); sets isPublished and flaggedForReview based on cefrConfidence threshold 0.65"
    - "pipeline.cli.ts bootstrap with NestFactory.createApplicationContext — no HTTP server, full DI"
    - "pnpm pipeline:validate, pipeline:crawl, pipeline:seed, pipeline:run scripts exist in apps/api/package.json"
    - "polite rate limiting: random delay 300–1200ms between page fetches"
  artifacts:
    - path: "apps/api/src/pipeline/crawler.service.ts"
      provides: "Playwright + Cheerio crawler with 4-source extractors"
      contains: "CrawlerService"
    - path: "apps/api/src/pipeline/seed.service.ts"
      provides: "SeedService with 500-record batch createMany"
      contains: "seedInBatches"
    - path: "apps/api/src/pipeline/pipeline.cli.ts"
      provides: "NestFactory.createApplicationContext CLI bootstrap"
      contains: "createApplicationContext"
  key_links:
    - from: "apps/api/src/pipeline/pipeline.cli.ts"
      to: "apps/api/src/pipeline/pipeline.module.ts"
      via: "NestFactory.createApplicationContext(PipelineModule)"
      pattern: "PipelineModule"
    - from: "apps/api/src/pipeline/seed.service.ts"
      to: "apps/api/src/pipeline/classifier.service.ts"
      via: "DI injection"
      pattern: "ClassifierService"
    - from: "apps/api/src/pipeline/seed.service.ts"
      to: "prisma.readingPassage"
      via: "createMany in 500-record batches"
      pattern: "createMany.*skipDuplicates"
---

<objective>
Content pipeline implementation: CrawlerService (Playwright + Cheerio, 4 sources), SeedService (CEFR classify + 500-record batch createMany), and pipeline CLI bootstrap. This plan delivers the standalone NestJS CLI script that populates the database with 2,000+ reading passages.

Purpose: Without this plan, the reading browse page has zero passages. The seed pipeline is the only way to populate content at the required scale (PIPE-05: 2,000 passages).
Output: 3 new files, 2 modified files (pipeline.module.ts adds seed+crawler, apps/api/package.json adds scripts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reading-comprehension-content-pipeline/05-CONTEXT.md
@.planning/phases/05-reading-comprehension-content-pipeline/05-RESEARCH.md
@.planning/phases/05-reading-comprehension-content-pipeline/05-PATTERNS.md
@apps/api/src/pipeline/pipeline.module.ts
@apps/api/src/pipeline/classifier.service.ts
@packages/database/prisma/seed.ts
@apps/api/src/main.ts

<interfaces>
Pipeline CLI pattern (from 05-PATTERNS.md):
```typescript
// pipeline.cli.ts — NestFactory.createApplicationContext, no HTTP server
async function bootstrap() {
  const app = await NestFactory.createApplicationContext(PipelineModule);
  const flag = process.argv[2]; // --validate | --crawl | --seed | --run
  const crawler = app.get(CrawlerService);
  const seeder = app.get(SeedService);
  if (flag === '--validate') await crawler.validateSelectors();
  else if (flag === '--crawl') await crawler.crawlAll();
  else if (flag === '--seed') await seeder.seedFromFile('./crawled-passages.json');
  else if (flag === '--run') { await crawler.crawlAll(); await seeder.seedFromFile('./crawled-passages.json'); }
  await app.close();
}
```

pnpm script pattern (from 05-PATTERNS.md — follows db:seed ts-node pattern):
```json
"pipeline:validate": "TS_NODE_PROJECT=tsconfig.json node -r ts-node/register src/pipeline/pipeline.cli.ts -- --validate",
"pipeline:crawl":   "TS_NODE_PROJECT=tsconfig.json node -r ts-node/register src/pipeline/pipeline.cli.ts -- --crawl",
"pipeline:seed":    "TS_NODE_PROJECT=tsconfig.json node -r ts-node/register src/pipeline/pipeline.cli.ts -- --seed",
"pipeline:run":     "TS_NODE_PROJECT=tsconfig.json node -r ts-node/register src/pipeline/pipeline.cli.ts -- --run"
```

seedInBatches pattern (from 05-PATTERNS.md):
```typescript
const BATCH_SIZE = 500;
for (let i = 0; i < items.length; i += BATCH_SIZE) {
  const batch = items.slice(i, i + BATCH_SIZE);
  const result = await batchFn(batch);
  console.log(`batch ${Math.floor(i/BATCH_SIZE)+1}/${Math.ceil(items.length/BATCH_SIZE)} — ${result.count} inserted`);
}
```

ClassifierService.classifyPassage signature (from 05-04):
```typescript
classifyPassage(text: string): { cefrLevel: 'B1'|'B2'|'C1', cefrConfidence: number }
```

ReadingPassage schema fields needed for createMany:
title, content (sanitized HTML), sourceUrl, contentHash (SHA-256), contentType (ContentType enum), cefrLevel, cefrConfidence, topic (string nullable), wordCount, isPublished (boolean), flaggedForReview (boolean)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CrawlerService — Playwright + Cheerio 4-source extractor</name>
  <files>apps/api/src/pipeline/crawler.service.ts</files>
  <read_first>
    apps/api/src/pipeline/classifier.service.ts — verify ClassifierService exists and has classifyPassage() before wiring
    .planning/phases/05-reading-comprehension-content-pipeline/05-RESEARCH.md §Common Pitfalls Pitfall 7 — VOA/BBC selector instability; validateSelectors must check ≥80% extraction before bulk crawl
    .planning/phases/05-reading-comprehension-content-pipeline/05-RESEARCH.md §Code Examples §Polite Crawler Rate Limiting — random 200–1000ms delay between fetches
    .planning/phases/05-reading-comprehension-content-pipeline/05-CONTEXT.md §D-11 — 50-URL --validate-selectors step is MANDATORY before bulk crawl
  </read_first>
  <action>
    Create apps/api/src/pipeline/crawler.service.ts as @Injectable() class CrawlerService.

    Source configurations (4 sources, per PIPE-01):
    - VOA Learning English: base URL learningenglish.voanews.com, article selectors (to validate): '.content-body p', '.article-content p', or closest available. Sitemap/listing pages to crawl: learningenglish.voanews.com/sitemap.xml or the level-based listing pages (Level 1, Level 2, Level 3).
    - BBC Learning English: bbc.co.uk/learningenglish, article body selectors: '.text p', '.story-body__inner p', or closest available.
    - News In Levels: newsinlevels.com, article body: '.entry-content p'.
    - Simple English Wikipedia: simple.wikipedia.org, article body: '#mw-content-text p' (filter out 'stub' and short disambiguation pages).

    validateSelectors() method:
    - For each source, fetch 50 sample URLs (from source's listing/sitemap)
    - For each URL, try each selector candidate; count successes (text extracted with ≥150 words)
    - Report: { source, successRate, bestSelector } for each source
    - Log to console; return results array
    - If any source has successRate < 0.80, log WARNING and continue (human must inspect before bulk crawl)

    crawlAll() method:
    - For each source, crawl ≥625 URLs (4 sources × 625 = 2,500 target raw passages → ~80% pass quality gate = ~2,000 seeded)
    - Use Playwright to fetch pages (chromium, headless: true)
    - Extract: title (from h1 or title element), content (inner HTML of body selector), sourceUrl (page URL)
    - Apply polite delay: 300 + Math.random() * 900 ms between each page fetch
    - Apply quality gate (PIPE-02): skip if word count < 150 or unique word ratio < 0.4 (low = boilerplate)
    - Compute contentHash: SHA-256 of cleaned content (crypto.createHash('sha256').update(cleanText).digest('hex'))
    - Map content type from source: VOA → NEWS, BBC → NEWS, NewsInLevels → NEWS, Wikipedia → ARTICLE
    - Write all extracted passages to ./crawled-passages.json (for SeedService to consume)
    - Log progress: total fetched, passed quality gate, written

    HTML content handling: store the raw inner HTML of the article body (sanitized by SeedService before DB insert). Do NOT strip HTML here — the DB stores formatted HTML (D-05). Do strip script/style tags.
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && grep -c "validateSelectors\|crawlAll\|contentHash\|politeDelay\|createHash" apps/api/src/pipeline/crawler.service.ts && pnpm --filter @repo/api type-check</automated>
  </verify>
  <done>crawler.service.ts exists with validateSelectors() and crawlAll() methods. Contains polite delay, quality gate (word count ≥ 150, unique word ratio ≥ 0.4), contentHash SHA-256 computation, 4-source configurations. TypeScript compilation passes.</done>
</task>

<task type="auto">
  <name>Task 2: SeedService + PipelineModule + CLI bootstrap + pnpm scripts</name>
  <files>
    apps/api/src/pipeline/seed.service.ts,
    apps/api/src/pipeline/pipeline.cli.ts,
    apps/api/src/pipeline/pipeline.module.ts,
    apps/api/package.json
  </files>
  <read_first>
    apps/api/src/pipeline/pipeline.module.ts — current state from 05-04 (has ClassifierService); add CrawlerService and SeedService to providers/exports
    packages/database/prisma/seed.ts — exact createMany + skipDuplicates pattern to follow
    apps/api/src/main.ts — NestFactory.create pattern; pipeline CLI uses NestFactory.createApplicationContext instead
    apps/api/package.json — current scripts section; add 4 pipeline scripts
    .planning/phases/05-reading-comprehension-content-pipeline/05-CONTEXT.md §D-09, D-12 — standalone script, isPublished=false for confidence < 0.65
  </read_first>
  <action>
    Create apps/api/src/pipeline/seed.service.ts as @Injectable() class SeedService:
    - Constructor: inject ClassifierService (DI) and PrismaService
    - seedFromFile(filePath: string): void method:
      1. Read crawled-passages.json from filePath
      2. For each raw passage: call classifierService.classifyPassage(plainTextContent) where plainTextContent = passage content stripped of HTML tags (use cheerio.load(html).text())
      3. Set isPublished = cefrConfidence >= 0.65; flaggedForReview = cefrConfidence < 0.65 (per D-12 and PIPE-04)
      4. Detect topic from content: simple keyword presence map (technology→technology, health→health, business→business, travel→travel, education→education, science→academic, default→null)
      5. Call seedInBatches() with prisma.readingPassage.createMany({ data: batch, skipDuplicates: true }) in chunks of 500 (PIPE-06)
      6. After passages seeded, seed stub ReadingQuestion rows (6 per passage, one of each type per READ-02): 1 MAIN_IDEA, 1 DETAIL, 1 INFERENCE, 1 VOCAB_IN_CONTEXT, 1 TRUE_FALSE, 1 SUMMARY — with answer/distractors set from passage content (simplified: use first sentence as answer context for all types). All 6 question types from READ-02 must be represented.
      7. Log total passages seeded (isPublished=true count) and flagged count

    Create apps/api/src/pipeline/pipeline.cli.ts from the pattern in 05-PATTERNS.md. Import 'reflect-metadata' at the top (required for NestJS DI decorators). Handle process.argv[2] flags: --validate, --crawl, --seed, --run. Exit 0 on success, exit 1 on error.

    Modify apps/api/src/pipeline/pipeline.module.ts (from 05-04): add CrawlerService and SeedService to providers[] and exports[]. Add PrismaModule to imports[] if not already there.

    Modify apps/api/package.json scripts: add the 4 pipeline scripts using ts-node/register pattern (from 05-PATTERNS.md). The scripts run from the apps/api directory so the path is `src/pipeline/pipeline.cli.ts`.
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && grep -c "seedInBatches\|createMany\|skipDuplicates\|BATCH_SIZE.*500\|500" apps/api/src/pipeline/seed.service.ts && grep -c "createApplicationContext\|PipelineModule" apps/api/src/pipeline/pipeline.cli.ts && node -e "const p=require('./apps/api/package.json'); ['pipeline:validate','pipeline:crawl','pipeline:seed','pipeline:run'].forEach(s=>{if(!p.scripts[s]) throw new Error('Missing script: '+s)}); console.log('scripts OK')" && pnpm --filter @repo/api type-check</automated>
  </verify>
  <done>seed.service.ts exists with seedInBatches() using BATCH_SIZE=500 and createMany+skipDuplicates. pipeline.cli.ts uses NestFactory.createApplicationContext(PipelineModule). PipelineModule includes CrawlerService, ClassifierService, SeedService. 4 pipeline:* scripts in apps/api/package.json. TypeScript compilation passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external websites → CrawlerService | Crawled HTML content enters from VOA/BBC/NewsInLevels/Wikipedia |
| CrawlerService output → SeedService → DB | Crawled content persisted via Prisma createMany |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | Crawled HTML containing XSS payloads | mitigate | SeedService calls isomorphic-dompurify (clearWindow() after each passage) to sanitize HTML before storing to DB; second sanitization in PassageRenderer before DOM render |
| T-05-05-02 | Tampering | SQL injection via content fields | mitigate | All content passed to Prisma createMany as parameterized data objects — never string interpolation |
| T-05-05-03 | DoS | CrawlerService hammering source sites | mitigate | Polite delay 300–1200ms random between fetches; total crawl time ~625 URLs × 750ms avg = ~8 min per source |
| T-05-05-04 | Repudiation | Seeded passages without audit trail | accept | Pipeline is developer tool; no user-facing audit needed for seed operations |
| T-05-05-SC | Tampering | npm installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
1. `grep -c "seedInBatches\|createMany.*skipDuplicates\|BATCH_SIZE" apps/api/src/pipeline/seed.service.ts` returns ≥3
2. `grep -c "createApplicationContext" apps/api/src/pipeline/pipeline.cli.ts` returns 1
3. `node -e "const p=require('./apps/api/package.json'); console.log(p.scripts['pipeline:validate'])"` prints the ts-node script
4. `pnpm --filter @repo/api type-check` exits 0
5. `grep -c "isPublished.*cefrConfidence\|flaggedForReview" apps/api/src/pipeline/seed.service.ts` returns ≥1 (D-12 threshold logic present)
</verification>

<success_criteria>
- CrawlerService has validateSelectors() (50-URL sample, reports ≥80% success) and crawlAll() (4 sources, polite delay, quality gate, contentHash)
- SeedService uses seedInBatches with BATCH_SIZE=500, createMany+skipDuplicates, classifies each passage before seeding, sets isPublished/flaggedForReview per PIPE-04
- pipeline.cli.ts uses NestFactory.createApplicationContext — no HTTP server bootstrapped
- 4 pnpm pipeline:* scripts in apps/api/package.json
- SeedService seeds stub ReadingQuestion rows (6 per passage) per READ-02 contract
</success_criteria>

<output>
Create `.planning/phases/05-reading-comprehension-content-pipeline/05-05-SUMMARY.md` when done
</output>
