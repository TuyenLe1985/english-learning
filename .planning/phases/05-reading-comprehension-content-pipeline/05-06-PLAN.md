---
phase: 05-reading-comprehension-content-pipeline
plan: 06
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - apps/web/src/app/(dashboard)/reading/page.tsx
  - apps/web/src/app/(dashboard)/reading/[passageId]/page.tsx
autonomous: true
requirements:
  - READ-01
  - READ-06

must_haves:
  truths:
    - "GET /reading renders a Server Component browse page with CEFR filter tabs (All/B1/B2/C1), topic Select, content type Select, and a responsive 3-column passage card grid"
    - "Each passage card shows CefrBadge, title (line-clamp-2), topic Badge, word count, Bookmark toggle icon — entire card is a Link to /reading/[passageId]"
    - "Filter state round-trips via URL search params: ?level=B2&topic=technology&type=ARTICLE — browser Back works correctly"
    - "GET /reading/[passageId] renders a Server Component that fetches passage detail from NestJS and passes data to PassageRenderer and QuestionsSection client components"
    - "Passage detail page handles 404 gracefully (passage not found error state)"
    - "Auth check: both pages redirect to /login if session is null"
    - "Empty state rendered when filter returns 0 passages"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/reading/page.tsx"
      provides: "Reading browse Server Component"
      contains: "ReadingPage"
    - path: "apps/web/src/app/(dashboard)/reading/[passageId]/page.tsx"
      provides: "Passage detail Server Component"
      contains: "ReadingPassagePage"
  key_links:
    - from: "apps/web/src/app/(dashboard)/reading/page.tsx"
      to: "GET /api/reading/passages"
      via: "fetchWithAuth + INTERNAL_API_URL"
      pattern: "api/reading/passages"
    - from: "apps/web/src/app/(dashboard)/reading/[passageId]/page.tsx"
      to: "GET /api/reading/passages/:id"
      via: "fetchWithAuth + INTERNAL_API_URL"
      pattern: "api/reading/passages/"
---

<objective>
Next.js Server Component routes for reading browse and passage detail pages. These Server Components do the auth check, fetch from NestJS API, and render the initial HTML shell — client components (PassageRenderer, QuestionsSection) are added in plan 05-07.

Purpose: Delivers the navigable reading module — users can browse the passage list, filter by CEFR level/topic/type, and open a passage page (even before client components are wired in the next plan).
Output: 2 new Next.js page files. Reading navigation is functional end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reading-comprehension-content-pipeline/05-CONTEXT.md
@.planning/phases/05-reading-comprehension-content-pipeline/05-UI-SPEC.md
@.planning/phases/05-reading-comprehension-content-pipeline/05-PATTERNS.md
@apps/web/src/app/(dashboard)/grammar/page.tsx
@apps/web/src/lib/api-client.ts
@apps/web/src/components/cefr-badge.tsx

<interfaces>
fetchWithAuth pattern (from apps/web/src/lib/api-client.ts):
```typescript
import { fetchWithAuth, INTERNAL_API_URL } from "@/lib/api-client";
const res = await fetchWithAuth(cookieHeader, `${INTERNAL_API_URL}/api/reading/passages`);
if (!res.ok) return { passages: [], total: 0 };
```

Server Component auth + header pattern (from grammar/page.tsx):
```typescript
import { auth } from "@/auth";
import { headers } from "next/headers";
import { redirect } from "next/navigation";
const session = await auth();
if (!session) redirect("/login");
const reqHeaders = await headers();
const cookieHeader = reqHeaders.get("cookie") ?? "";
```

Browse page searchParams type (Next.js 14 App Router):
```typescript
interface Props {
  searchParams: Promise<{ level?: string; topic?: string; type?: string; page?: string }>;
}
```

Passage detail page params type:
```typescript
interface Props {
  params: Promise<{ passageId: string }>;
}
```

ReadingPassageDto from @repo/shared:
- id, title, contentType, cefrLevel, topic (nullable), wordCount, questionCount, isBookmarked
ReadingPassageDetailDto from @repo/shared:
- extends ReadingPassageDto with: content (HTML string), questions[], highlights[], note (nullable), progress (nullable)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reading browse page Server Component (/reading)</name>
  <files>apps/web/src/app/(dashboard)/reading/page.tsx</files>
  <read_first>
    apps/web/src/app/(dashboard)/grammar/page.tsx — exact Server Component structure to replicate: auth check, fetchWithAuth, grid rendering with empty state, searchParams consumption
    apps/web/src/components/cefr-badge.tsx — CefrBadge import and usage (already built, reuse directly)
    .planning/phases/05-reading-comprehension-content-pipeline/05-UI-SPEC.md §Screen 1 — passage card anatomy, filter bar layout, grid responsive classes
    .planning/phases/05-reading-comprehension-content-pipeline/05-UI-SPEC.md §Copywriting Contract — browse page h1, empty state copy, error state copy
  </read_first>
  <action>
    Create apps/web/src/app/(dashboard)/reading/page.tsx as an async Server Component following the grammar/page.tsx pattern exactly.

    Structure:
    1. Auth check + redirect (standard pattern)
    2. fetchPassages(cookieHeader, { cefrLevel, topic, contentType, page }): builds URL from INTERNAL_API_URL/api/reading/passages with query params from searchParams; calls fetchWithAuth; returns { passages: ReadingPassageDto[], total: number } or empty on error
    3. Page header: h1 "Reading Passages", subtitle "Browse passages at your CEFR level" (text-sm text-muted-foreground)
    4. Filter bar: bg-muted rounded-lg p-3 container with:
       - CEFR Tabs: shadcn Tabs component with values "All"/"B1"/"B2"/"C1". Active tab maps to ?level= URL param. Client-side URL update via Next.js Link with updated searchParams (NOT client state — use URL-driven navigation for SSR filter state).
       - Topic Select: placeholder "All topics", values from UI-SPEC topic list. URL param: ?topic=
       - Content type Select: placeholder "All types", values ARTICLE/NEWS/BLOG_POST/ACADEMIC/STORY/OPINION. URL param: ?type=
    5. Passage grid: responsive grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6. Each passage rendered as a Card with:
       - Entire card wrapped in Next.js Link href="/reading/{passage.id}" (no explicit "Start Reading" CTA per UI-SPEC)
       - Top row: CefrBadge on left, Bookmark icon (Lucide Bookmark) on right. Bookmark toggle is a client action — for the Server Component, render bookmark state from passage.isBookmarked (pass to a thin client BookmarkButton component if needed, or simply show the filled/empty icon state statically on initial render)
       - Title: text-sm font-semibold line-clamp-2
       - Metadata row: topic Badge (secondary variant) + "~{wordCount} words" text-xs text-muted-foreground
    6. Pagination: use existing shadcn Pagination component. 20 passages per page.
    7. Empty state: centered div with "No passages match your filters" heading + "Try adjusting your filters..." body text (from UI-SPEC copywriting)

    IMPORTANT: Filter URL updates (tab clicks, select changes) must use Next.js router.push() or Link with updated searchParams. Since this is a Server Component, filter UI elements that need interactivity must be client components. Create a minimal ReadingFilters "use client" component in the same file or as a co-located file for the filter bar controls that pushes URL updates. The grid/data itself renders server-side.
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && grep -c "searchParams\|fetchWithAuth\|cefrLevel\|isPublished\|CefrBadge\|ReadingPage" apps/web/src/app/\(dashboard\)/reading/page.tsx && pnpm --filter @repo/web type-check 2>&1 | tail -5</automated>
  </verify>
  <done>apps/web/src/app/(dashboard)/reading/page.tsx exists as async Server Component. Auth redirect present. fetchWithAuth call to /api/reading/passages present. Passage grid with CefrBadge, title, topic Badge, word count rendered. Empty state present. Filter bar with Tabs (CEFR) and Select (topic, type) present. Type check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Passage detail Server Component (/reading/[passageId])</name>
  <files>apps/web/src/app/(dashboard)/reading/[passageId]/page.tsx</files>
  <read_first>
    apps/web/src/app/(dashboard)/grammar/[area]/[topic]/page.tsx — Server Component detail page pattern with fetchWithAuth, null check, params await
    .planning/phases/05-reading-comprehension-content-pipeline/05-PATTERNS.md §apps/web/src/app/(dashboard)/reading/[passageId]/page.tsx — exact server component pattern
    .planning/phases/05-reading-comprehension-content-pipeline/05-UI-SPEC.md §Screen 2 §2a — passage header anatomy (back link, title, metadata row, action row)
    .planning/phases/05-reading-comprehension-content-pipeline/05-UI-SPEC.md §Copywriting Contract — "Back to Reading" link text
  </read_first>
  <action>
    Create apps/web/src/app/(dashboard)/reading/[passageId]/page.tsx as an async Server Component.

    Structure:
    1. Auth check + redirect
    2. params await (Next.js 14 App Router: params is a Promise)
    3. fetchPassageDetail(cookieHeader, passageId): GET fetchWithAuth INTERNAL_API_URL/api/reading/passages/{passageId}; returns ReadingPassageDetailDto or null on error/404
    4. Null/error state: centered "Could not load this passage. Try refreshing the page." message (from UI-SPEC copywriting)
    5. Passage header (2a from UI-SPEC):
       - Back link: ChevronLeft icon + "Back to Reading" text (text-sm text-muted-foreground) linking to /reading
       - Passage title: text-xl font-semibold text-foreground
       - Metadata row: CefrBadge + topic Badge (secondary) + "~{wordCount} words" + contentType label (text-xs text-muted-foreground, separated by ·)
       - Action row: Clock icon + elapsed time display (placeholder "0m 0s" — timer starts client-side in PassageRenderer), Bookmark toggle button, Notes toggle button. These are client-interactive — render as placeholder divs or a thin ReadingActions "use client" component that receives initial state.
    6. Passage body area: render a PassageRenderer "use client" component placeholder. Since PassageRenderer is not yet implemented in this plan (comes in 05-07), render the passage HTML as dangerouslySetInnerHTML with basic sanitization check as a temporary fallback: `<div dangerouslySetInnerHTML={{ __html: data.content }} />`. PassageRenderer will replace this in 05-07.
    7. Separator (shadcn Separator) between passage body and questions section.
    8. Questions section area: render a QuestionsSection "use client" component placeholder. Since it is not yet built, render a static "Comprehension Questions ({count} questions)" heading with the count from data.questions.length. Full interactive implementation in 05-07.
    9. Page layout: max-w-3xl mx-auto px-4 py-8 container.

    Note: In 05-07, PassageRenderer and QuestionsSection client components will replace the placeholder dangerouslySetInnerHTML and static heading. The Server Component wires data down; client components add interactivity.
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && grep -c "fetchWithAuth\|passageId\|ReadingPassagePage\|dangerouslySetInnerHTML\|Back to Reading" apps/web/src/app/\(dashboard\)/reading/\[passageId\]/page.tsx && pnpm --filter @repo/web type-check 2>&1 | tail -5</automated>
  </verify>
  <done>apps/web/src/app/(dashboard)/reading/[passageId]/page.tsx exists. Fetches from /api/reading/passages/:id. Renders passage header (title, CefrBadge, back link, metadata). Null/error state renders. Temporary dangerouslySetInnerHTML fallback for passage body. TypeScript passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component → NestJS API | fetchWithAuth forwards session JWT; NestJS validates |
| NestJS response → dangerouslySetInnerHTML (temporary) | Raw HTML from DB rendered without full client-side sanitization in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering (XSS) | dangerouslySetInnerHTML in passage detail temporary fallback | mitigate | Content was sanitized by isomorphic-dompurify in SeedService before DB storage; 05-07 replaces this with PassageRenderer which runs DOMPurify again client-side |
| T-05-06-02 | Information Disclosure | Unauthenticated route access | mitigate | auth() check + redirect("/login") on both Server Components before any data fetch |
| T-05-06-SC | Tampering | npm installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
1. `ls apps/web/src/app/\(dashboard\)/reading/page.tsx apps/web/src/app/\(dashboard\)/reading/\[passageId\]/page.tsx` — both files exist
2. `grep -c "redirect.*login\|auth()" apps/web/src/app/\(dashboard\)/reading/page.tsx` returns ≥1
3. `grep -c "redirect.*login\|auth()" apps/web/src/app/\(dashboard\)/reading/\[passageId\]/page.tsx` returns ≥1
4. `pnpm --filter @repo/web type-check` exits 0
5. `grep -c "api/reading/passages" apps/web/src/app/\(dashboard\)/reading/page.tsx` returns ≥1
</verification>

<success_criteria>
- Both pages exist as async Server Components under apps/web/src/app/(dashboard)/reading/
- Auth check + redirect present on both pages
- Browse page: passage grid with CefrBadge, title, topic badge, word count, bookmark icon; filter bar with Tabs + Select; empty state; pagination
- Detail page: passage header with back link, title, metadata, action row; passage body placeholder; questions section placeholder
- Both pages call fetchWithAuth to INTERNAL_API_URL
- TypeScript compilation passes
</success_criteria>

<output>
Create `.planning/phases/05-reading-comprehension-content-pipeline/05-06-SUMMARY.md` when done
</output>
