---
phase: 05-reading-comprehension-content-pipeline
plan: 07
type: execute
wave: 4
depends_on:
  - 05-06
  - 05-02
  - 05-03
files_modified:
  - apps/web/src/app/(dashboard)/reading/[passageId]/page.tsx
  - apps/web/src/components/reading/passage-renderer.tsx
  - apps/web/src/components/reading/highlight-tooltip.tsx
  - apps/web/src/components/reading/questions-section.tsx
  - apps/web/src/components/reading/passage-score-card.tsx
  - apps/web/src/components/reading/notes-panel.tsx
autonomous: true
requirements:
  - READ-02
  - READ-03
  - READ-04
  - READ-05
  - READ-07

must_haves:
  truths:
    - "PassageRenderer sanitizes passage HTML with isomorphic-dompurify (client-side only) before rendering"
    - "Each word in the passage is wrapped in <span data-word='{normalized}'> with role=button and tabIndex=0 per D-14"
    - "Text selection in the passage body triggers a floating Highlight tooltip (D-07); clicking it saves the highlight via POST /api/reading/highlights"
    - "Existing highlights are restored on mount using dom-anchor-text-position toRange() → bg-amber-100/70 overlay (D-06, READ-04)"
    - "QuestionsSection shows all questions inline below passage; each answered question reveals immediate feedback (D-01, D-02)"
    - "Reading timer starts on mount, stops when last question is answered, and sends readingTimeSec in session complete payload (D-03, READ-03)"
    - "POST /api/reading/sessions/complete called on last question answered; inline score card with framer-motion appears below last question (D-04, READ-07)"
    - "Notes panel (shadcn Sheet on mobile, right sidebar on desktop) auto-saves on textarea blur (D-08, READ-05)"
  artifacts:
    - path: "apps/web/src/components/reading/passage-renderer.tsx"
      provides: "PassageRenderer client component — HTML sanitize + word-span + highlight restore"
      contains: "PassageRenderer"
    - path: "apps/web/src/components/reading/highlight-tooltip.tsx"
      provides: "HighlightTooltip client component — selection → save"
      contains: "HighlightTooltip"
    - path: "apps/web/src/components/reading/questions-section.tsx"
      provides: "QuestionsSection client component — per-question state + session submit"
      contains: "QuestionsSection"
    - path: "apps/web/src/components/reading/notes-panel.tsx"
      provides: "NotesPanel client component — auto-save on blur"
      contains: "NotesPanel"
    - path: "apps/web/src/components/reading/passage-score-card.tsx"
      provides: "PassageScoreCard with framer-motion entrance"
      contains: "PassageScoreCard"
  key_links:
    - from: "apps/web/src/components/reading/passage-renderer.tsx"
      to: "dom-anchor-text-position"
      via: "import { fromRange, toRange } from 'dom-anchor-text-position'"
      pattern: "dom-anchor-text-position"
    - from: "apps/web/src/components/reading/highlight-tooltip.tsx"
      to: "POST /api/reading/highlights"
      via: "fetch on tooltip click"
      pattern: "reading/highlights"
    - from: "apps/web/src/components/reading/questions-section.tsx"
      to: "POST /api/reading/sessions/complete"
      via: "fetch on last answer"
      pattern: "sessions/complete"
---

<objective>
Interactive reading passage client components: PassageRenderer (HTML sanitize, word-span, highlight save/restore), HighlightTooltip (selection → save), QuestionsSection (inline questions, timer, session submit, score card), and NotesPanel (auto-save). Replaces the placeholder dangerouslySetInnerHTML from plan 05-06 with full interactive functionality.

Purpose: Delivers READ-02 (questions), READ-03 (timer), READ-04 (highlight persistence), READ-05 (notes), READ-07 (score storage). This plan closes the functional gap left by the Server Component placeholders in 05-06.
Output: 5 new component files, 1 page modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reading-comprehension-content-pipeline/05-CONTEXT.md
@.planning/phases/05-reading-comprehension-content-pipeline/05-UI-SPEC.md
@.planning/phases/05-reading-comprehension-content-pipeline/05-PATTERNS.md
@.planning/phases/05-reading-comprehension-content-pipeline/05-RESEARCH.md
@apps/web/src/components/grammar/grammar-lesson-page.tsx
@apps/web/src/components/grammar/grammar-session-results.tsx
@apps/web/src/app/(dashboard)/reading/[passageId]/page.tsx

<interfaces>
dom-anchor-text-position API (from RESEARCH.md Pattern 2 — ASSUMED A1):
```typescript
import { fromRange, toRange } from 'dom-anchor-text-position';
// SAVE: const { start, end } = fromRange(root, selectionRange);
// RESTORE: const range = toRange(root, { start: h.startOffset, end: h.endOffset });
// CRITICAL: browser-only — only use inside "use client" components, within useEffect/event handlers
```

Highlight restore pattern (walk text nodes → apply bg-amber-100/70):
```typescript
// After toRange() returns a DOM Range, wrap its contents:
// 1. Create <mark class="bg-amber-100/70 rounded-sm" aria-label="Highlighted text">
// 2. range.surroundContents(markElement)
// Note: surroundContents throws if range spans multiple elements — use extractContents + insertNode instead
```

QuestionsSection per-question state:
```typescript
interface QuestionState { answered: boolean; selectedAnswer: string | null; isCorrect: boolean; }
const [questionStates, setQuestionStates] = useState<Record<string, QuestionState>>({});
// Questions appear in sequence; options disabled until the question above is answered
```

Reading timer (passive per D-03):
```typescript
const startTime = useRef<number>(Date.now());
const timerRef = useRef<ReturnType<typeof setInterval> | null>(null);
// Stop timer: clearInterval(timerRef.current) when last question answered
// readingTimeSec = Math.floor((Date.now() - startTime.current) / 1000)
```

NoteUpsertSchema input: { passageId: string, content: string }
HighlightCreateSchema input: { passageId: string, startOffset: number, endOffset: number, text: string }
ReadingSessionCompleteSchema input: { passageId, score, accuracy, readingTimeSec, attempts[] }

Next.js route handlers needed (relay to NestJS — these do NOT exist yet, use direct /api/* fetch pattern via api-client or create relay routes):
- POST /api/reading/highlights → NestJS POST /api/reading/highlights
- POST /api/reading/notes → NestJS POST /api/reading/notes
- POST /api/reading/sessions/complete → NestJS POST /api/reading/sessions/complete
Check apps/web/src/app/api/ for existing relay pattern before creating new ones.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PassageRenderer + HighlightTooltip client components</name>
  <files>
    apps/web/src/components/reading/passage-renderer.tsx,
    apps/web/src/components/reading/highlight-tooltip.tsx,
    apps/web/src/app/(dashboard)/reading/[passageId]/page.tsx
  </files>
  <read_first>
    apps/web/src/app/(dashboard)/reading/[passageId]/page.tsx — current state (has dangerouslySetInnerHTML placeholder); this task replaces it with PassageRenderer
    .planning/phases/05-reading-comprehension-content-pipeline/05-RESEARCH.md §Pattern 2 — dom-anchor-text-position fromRange/toRange usage
    .planning/phases/05-reading-comprehension-content-pipeline/05-RESEARCH.md §Pattern 5 — word-span wrapping approach (tokenizePassageHTML)
    .planning/phases/05-reading-comprehension-content-pipeline/05-RESEARCH.md §Pitfall 4 — dom-anchor-text-position is browser-only; ONLY import inside "use client" components
    .planning/phases/05-reading-comprehension-content-pipeline/05-RESEARCH.md §Pitfall 6 — isomorphic-dompurify client-only, do NOT import in Server Components
    .planning/phases/05-reading-comprehension-content-pipeline/05-UI-SPEC.md §2b — passage body layout: max-w-[65ch] mx-auto, text-[18px] leading-[1.75], word span classes
    .planning/phases/05-reading-comprehension-content-pipeline/05-UI-SPEC.md §2f — highlight tooltip dark pill appearance, Highlighter icon, position at selection end
    .planning/phases/05-reading-comprehension-content-pipeline/05-UI-SPEC.md §Accessibility Contract — span data-word: role=button, tabIndex=0, onKeyDown Enter/Space
  </read_first>
  <action>
    Create apps/web/src/components/reading/passage-renderer.tsx as a "use client" component.

    Props: { html: string; passageId: string; highlights: HighlightDto[]; onHighlightCreated: (h: HighlightDto) => void }

    Implementation steps:
    1. Sanitize: `const cleanHtml = DOMPurify.sanitize(html, { ALLOWED_TAGS: ['p','b','i','strong','em','br','ul','ol','li','blockquote'], ALLOWED_ATTR: [] });` — import DOMPurify from 'isomorphic-dompurify' (works in "use client" per Pitfall 6).

    2. Word tokenization: after DOMPurify sanitization, use a temporary div (`document.createElement('div')`) to parse the clean HTML, walk all text nodes, split each text node on word boundaries, wrap each word token in a React span element with data-word={normalized} role="button" tabIndex={0} class="cursor-pointer hover:bg-muted/50 rounded-sm px-0.5". Normalized word: word.toLowerCase().replace(/[.,!?;:'"()[\]]/g, ''). This must run inside useEffect (DOM access) — on initial render, use dangerouslySetInnerHTML with cleanHtml, then replace with React-controlled content after mount.

    3. Highlight restoration: inside useEffect, after mount. For each highlight in props.highlights, call `toRange(passageBodyRef.current, { start: highlight.startOffset, end: highlight.endOffset })`. If range is valid, create a `<mark>` element with class "bg-amber-100/70 rounded-sm" and aria-label="Highlighted text". Insert using range.surroundContents(mark) — if that throws (multi-element range), use extractContents() + insertNode() pattern instead.

    4. mouseup event handler on the passage body container: if selection is non-empty, non-collapsed, and anchorNode is within the passage container — call fromRange(passageBodyRef.current, range) to get { start, end }. Pass to HighlightTooltip via positioning state.

    5. Word click handler: if click target has data-word attribute, extract the normalized word, find the surrounding sentence (split text on /[.!?](?:\s|$)/ boundary), call onWordTap(word, sentence) prop — WordPopover is added in 05-08.

    6. Ref the passage body container with useRef<HTMLDivElement>(null).

    Create apps/web/src/components/reading/highlight-tooltip.tsx as a "use client" component.
    Props: { selection: { start: number; end: number; text: string } | null; position: { x: number; y: number } | null; passageId: string; onSaved: (highlight: HighlightDto) => void; onDismiss: () => void }
    - Renders a dark pill tooltip at the given position: bg-foreground text-background rounded-md px-2 py-1 shadow-lg text-xs
    - Contains Highlighter icon + "Highlight" label
    - On click: POST to reading/highlights relay (or direct fetch to NestJS /api/reading/highlights via api-client pattern), on success call onSaved, on error show Toast
    - Positioned absolutely relative to the viewport using position:fixed + top/left from mouse event coords

    Update apps/web/src/app/(dashboard)/reading/[passageId]/page.tsx: replace the dangerouslySetInnerHTML div with `<PassageRenderer html={data.content} passageId={passageId} highlights={data.highlights} onHighlightCreated={...} />`. PassageRenderer must be imported as a dynamic import with ssr:false (since it uses DOMPurify and dom-anchor-text-position which are browser-only): `const PassageRenderer = dynamic(() => import('@/components/reading/passage-renderer'), { ssr: false });`
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && grep -c '"use client"\|DOMPurify\|dom-anchor-text-position\|data-word\|PassageRenderer' apps/web/src/components/reading/passage-renderer.tsx && grep -c '"use client"\|HighlightTooltip\|Highlighter' apps/web/src/components/reading/highlight-tooltip.tsx && pnpm --filter @repo/web type-check 2>&1 | tail -5</automated>
  </verify>
  <done>passage-renderer.tsx: "use client", DOMPurify sanitization, word-span wrapping, highlight restoration using dom-anchor-text-position toRange, mouseup selection handler. highlight-tooltip.tsx: positioned pill with Highlighter icon + "Highlight" label, POST on click. Reading passage page imports PassageRenderer as dynamic(ssr:false). TypeScript passes.</done>
</task>

<task type="auto">
  <name>Task 2: QuestionsSection + PassageScoreCard + NotesPanel</name>
  <files>
    apps/web/src/components/reading/questions-section.tsx,
    apps/web/src/components/reading/passage-score-card.tsx,
    apps/web/src/components/reading/notes-panel.tsx,
    apps/web/src/app/(dashboard)/reading/[passageId]/page.tsx
  </files>
  <read_first>
    apps/web/src/components/grammar/grammar-lesson-page.tsx — per-question state pattern, session submit fetch, non-blocking try/catch/finally, timer ref pattern
    apps/web/src/components/grammar/grammar-session-results.tsx — framer-motion entrance pattern (opacity 0→1, scale 0.95→1, duration 0.3s) for score card
    .planning/phases/05-reading-comprehension-content-pipeline/05-UI-SPEC.md §2c — question card anatomy, answer option states (emerald/red), feedback expansion
    .planning/phases/05-reading-comprehension-content-pipeline/05-UI-SPEC.md §2d — inline score card layout, "Try another passage" + "Browse all passages" buttons
    .planning/phases/05-reading-comprehension-content-pipeline/05-UI-SPEC.md §2g — notes panel Sheet, Textarea, auto-save on blur
    .planning/phases/05-reading-comprehension-content-pipeline/05-UI-SPEC.md §Question Answer States — exact Tailwind classes for each state
    .planning/phases/05-reading-comprehension-content-pipeline/05-UI-SPEC.md §Animation Contract — question feedback height 0→auto
  </read_first>
  <action>
    Create apps/web/src/components/reading/questions-section.tsx as a "use client" component.
    Props: { questions: ReadingQuestionDto[]; passageId: string; onTimerStop: () => number; }

    Per-question state: `Record<string, { answered: boolean; selectedAnswer: string | null; isCorrect: boolean }>`. All questions visible in sequence; options on unanswered questions below the current answered question are disabled (opacity-50 cursor-not-allowed) until the question above is answered — use questionStates to determine which questions are "unlocked" (all questions with index ≤ last-answered-index + 1 are unlocked).

    Answer handler: on option click for question[i]: update questionStates[q.id], check isCorrect (selectedAnswer === q.answer). If all questions answered: call onTimerStop() to get readingTimeSec, compute score (correctCount) and accuracy, call submitSession(). Show PassageScoreCard after submit.

    submitSession(): POST to /api/reading/sessions/complete with { passageId, score, accuracy, readingTimeSec, attempts }. Non-blocking (try/catch/finally). Show score card regardless of submit success.

    Question card rendering per UI-SPEC §2c:
    - Card container, question number (text-xs text-muted-foreground), prompt (text-sm font-semibold)
    - Answer options: Button variant="outline" for each option
    - After answering: correct option → bg-emerald-50 border-emerald-400 text-emerald-800; incorrect selected → bg-red-50 border-red-400 text-red-700; correct option when user picked wrong → bg-emerald-50/50 border-emerald-300 text-emerald-700
    - Feedback expansion (framer-motion height 0→auto): explanation text with CheckCircle (emerald) or XCircle (red) icon
    - Section header: "Comprehension Questions" + live score counter "N / M correct" (text-sm font-semibold)
    - Score counter updates as each question is answered

    Create apps/web/src/components/reading/passage-score-card.tsx as a "use client" component.
    Props: { score: number; total: number; readingTimeSec: number; passageId: string }
    - framer-motion motion.div: initial={{ opacity:0, scale:0.95 }}, animate={{ opacity:1, scale:1 }}, transition={{ duration: 0.3, ease:'easeOut' }}
    - Card mx-auto max-w-lg mt-8
    - Display headline: "{score}/{total} correct" at text-[28px] font-semibold
    - Sub-line: "{pct}% · {readingTimeSec}s reading time" at text-sm text-muted-foreground
    - Two buttons min-h-[44px] w-full: "Try another passage" (primary, links to /reading), "Browse all passages" (outline, links to /reading)
    - No redirect on complete per D-04 — user stays on page; score card appears inline

    Create apps/web/src/components/reading/notes-panel.tsx as a "use client" component.
    Props: { passageId: string; initialContent: string | null; isOpen: boolean; onClose: () => void }
    - Desktop: right sidebar w-80 fixed with translate-x-full→translate-x-0 CSS transition 200ms ease-out when isOpen
    - Mobile: shadcn Sheet side="bottom" h-[60vh]
    - Use useMediaQuery or responsive class approach to switch between sidebar and sheet
    - Textarea: min-h-[200px] max-h-[400px], placeholder "Jot down your notes about this passage..."
    - Auto-save on blur: call POST /api/reading/notes with { passageId, content }. Show "Saved" + Check icon (text-xs text-muted-foreground) for 2 seconds after successful save.
    - Error state: "Could not save note. Changes may not persist." (from UI-SPEC copywriting)

    Update apps/web/src/app/(dashboard)/reading/[passageId]/page.tsx: replace static questions heading placeholder with `<QuestionsSection questions={data.questions} passageId={passageId} onTimerStop={...} />`. Wire NotesPanel with isOpen state and toggle button. Add reading timer ref in a thin ReadingPageClient wrapper if needed (since the timer must coordinate between PassageRenderer and QuestionsSection).
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && grep -c '"use client"\|QuestionsSection\|questionStates\|submitSession\|sessions\/complete' apps/web/src/components/reading/questions-section.tsx && grep -c '"use client"\|PassageScoreCard\|framer-motion\|motion.div' apps/web/src/components/reading/passage-score-card.tsx && grep -c '"use client"\|NotesPanel\|handleBlur\|auto-save\|onBlur' apps/web/src/components/reading/notes-panel.tsx && pnpm --filter @repo/web type-check 2>&1 | tail -5</automated>
  </verify>
  <done>questions-section.tsx: per-question state, immediate feedback (emerald/red), framer-motion feedback expansion, session submit on last answer, PassageScoreCard rendered below last question. passage-score-card.tsx: framer-motion entrance, score display, two CTA buttons. notes-panel.tsx: auto-save on blur, Sheet on mobile, right sidebar on desktop. TypeScript passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crawled HTML → PassageRenderer | User's browser renders sanitized HTML content |
| User input (notes textarea) → NestJS | Free-text note content must be stored as plain text (not rendered as HTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Tampering (XSS) | PassageRenderer dangerouslySetInnerHTML fallback | mitigate | DOMPurify.sanitize() runs before any HTML is set on the DOM; allowed tags whitelist (p,b,i,strong,em,br,ul,ol,li,blockquote) — no script/style/iframe/object allowed |
| T-05-07-02 | Tampering (Stored XSS) | NotesPanel textarea content | accept | Notes rendered as plain text value in textarea; NestJS stores as text; never rendered as innerHTML — no sanitization needed |
| T-05-07-03 | Information Disclosure | Highlight offsets in POST body (could leak passage structure) | accept | Offset integers are non-sensitive; passage content is already visible to the authenticated user |
| T-05-07-04 | DoS | Rapid POST /api/reading/highlights from selection spam | accept | Throttler on NestJS is global; client highlights only save on explicit tooltip click |
| T-05-07-SC | Tampering | npm installs | mitigate | No new packages in this plan — all installed in 05-01 |
</threat_model>

<verification>
1. `grep -c "dom-anchor-text-position\|fromRange\|toRange" apps/web/src/components/reading/passage-renderer.tsx` returns ≥2
2. `grep -c "isomorphic-dompurify\|DOMPurify" apps/web/src/components/reading/passage-renderer.tsx` returns ≥1
3. `grep -c "sessions/complete\|submitSession" apps/web/src/components/reading/questions-section.tsx` returns ≥1
4. `grep -c "motion.div\|framer-motion" apps/web/src/components/reading/passage-score-card.tsx` returns ≥1
5. `grep -c "onBlur\|handleBlur" apps/web/src/components/reading/notes-panel.tsx` returns ≥1
6. `pnpm --filter @repo/web type-check` exits 0
</verification>

<success_criteria>
- PassageRenderer: DOMPurify sanitize, word-span wrapping (data-word, role=button, tabIndex=0), highlight restoration via dom-anchor-text-position toRange(), mouseup → HighlightTooltip
- HighlightTooltip: positions at selection endpoint, Highlighter icon + "Highlight" label, POST on click, dismiss on outside click
- QuestionsSection: all questions inline (no carousel), per-question feedback (emerald/red), timer stops on last answer, POST sessions/complete, shows PassageScoreCard
- PassageScoreCard: framer-motion entrance, score/accuracy/time display, "Try another passage" + "Browse all passages" CTAs, no redirect
- NotesPanel: Textarea auto-saves on blur, "Saved" indicator 2s, Sheet on mobile, sidebar on desktop
</success_criteria>

<output>
Create `.planning/phases/05-reading-comprehension-content-pipeline/05-07-SUMMARY.md` when done
</output>
