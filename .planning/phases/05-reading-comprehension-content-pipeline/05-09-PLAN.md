---
phase: 05-reading-comprehension-content-pipeline
plan: 09
type: execute
wave: 5
depends_on:
  - 05-05
  - 05-07
  - 05-08
autonomous: false
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
  - PIPE-05
  - PIPE-06
  - READ-01
  - READ-02
  - READ-03
  - READ-04
  - READ-05
  - READ-06
  - READ-07
  - VOCAB-08
files_modified: []

must_haves:
  truths:
    - "pnpm pipeline:validate reports ≥80% extraction success for each of VOA, BBC, NewsInLevels, Wikipedia before bulk crawl"
    - "pnpm pipeline:seed completes in ≤10 minutes with ≥2,000 passages seeded with isPublished=true"
    - "Each CEFR level shelf (B1, B2, C1) has at least 1 passage in the database after seeding"
    - "User can browse /reading, filter by CEFR level/topic/type, click a passage, see questions, answer them, get inline score"
    - "Highlight text in passage → persists on page reload"
    - "Notes panel opens → auto-saves → note visible on reload"
    - "Bookmark passage → appears bookmarked after reload"
    - "Tap a known word → WordPopover shows definition → Add to SRS → success"
    - "pnpm test (full suite) exits 0 — all tests green"
  artifacts:
    - path: "apps/api/prisma/seed-data/cefr-word-list.json"
      provides: "CEFR word frequency map (full or representative)"
      contains: "word"
    - path: ".planning/phases/05-reading-comprehension-content-pipeline/05-09-SUMMARY.md"
      provides: "Phase 5 completion summary"
      contains: "Phase 5"
  key_links:
    - from: "pnpm pipeline:seed output"
      to: "prisma.readingPassage (isPublished=true)"
      via: "createMany in 500-record batches"
      pattern: "isPublished.*true"
---

<objective>
Phase 5 end-to-end checkpoint and pipeline execution. Runs the crawler selector validation, executes the full content pipeline to seed 2,000+ passages, then verifies the complete reading module works end-to-end via manual user journeys.

Purpose: Validates that all 14 Phase 5 requirements (READ-01–07, PIPE-01–06, VOCAB-08) are satisfied with real seeded data and real browser interaction before the phase is marked complete.
Output: Seeded database, full test suite green, human sign-off on key user flows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-reading-comprehension-content-pipeline/05-CONTEXT.md
@.planning/phases/05-reading-comprehension-content-pipeline/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite verification and type checks</name>
  <files></files>
  <read_first>
    .planning/phases/05-reading-comprehension-content-pipeline/05-VALIDATION.md §Sampling Rate — full suite must be green before verify-work
    apps/api/src/reading/reading.service.spec.ts — confirm all tests green (READ-01–07)
    apps/api/src/pipeline/classifier.service.spec.ts — confirm all tests green (PIPE-03, PIPE-04)
    apps/api/src/vocabulary/vocabulary.service.spec.ts — confirm lookupByWord tests green (VOCAB-08)
  </read_first>
  <action>
    Run the full test suite and type checks to confirm all plans in waves 1–4 completed cleanly before the human verification checkpoint:

    1. `pnpm test` — full Turborepo test suite (API + shared + web). Confirm exits 0 with all tests green. If any tests fail: investigate and fix before proceeding. The reading.service.spec.ts, classifier.service.spec.ts, and vocabulary.service.spec.ts tests from Plan 05-01 RED scaffolds MUST now be GREEN.

    2. `pnpm --filter @repo/api type-check` — confirm exits 0
    3. `pnpm --filter @repo/web type-check` — confirm exits 0
    4. `pnpm --filter @repo/shared type-check` — confirm exits 0

    5. Confirm all required files exist:
       - apps/api/src/reading/reading.module.ts, reading.controller.ts, reading.service.ts
       - apps/api/src/pipeline/pipeline.module.ts, classifier.service.ts, crawler.service.ts, seed.service.ts, pipeline.cli.ts
       - apps/api/prisma/seed-data/cefr-word-list.json
       - apps/web/src/components/reading/passage-renderer.tsx, highlight-tooltip.tsx, questions-section.tsx, passage-score-card.tsx, notes-panel.tsx, word-popover.tsx
       - apps/web/src/app/(dashboard)/reading/page.tsx
       - apps/web/src/app/(dashboard)/reading/[passageId]/page.tsx
       - packages/shared/src/reading.dto.ts

    6. Confirm ReadingModule is in AppModule: `grep -c "ReadingModule" apps/api/src/app.module.ts` returns ≥1
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && pnpm test && pnpm --filter @repo/api type-check && pnpm --filter @repo/web type-check && ls apps/api/src/reading/reading.service.ts apps/api/src/pipeline/classifier.service.ts apps/web/src/components/reading/passage-renderer.tsx packages/shared/src/reading.dto.ts && grep -c "ReadingModule" apps/api/src/app.module.ts</automated>
  </verify>
  <done>All tests green. All type checks pass. All required files exist. ReadingModule in AppModule.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 5 reading module with:
    - NestJS ReadingModule (7 endpoints: browse, detail, sessions, highlights, notes, bookmarks)
    - VocabularyModule lookup endpoint (GET /api/vocabulary/lookup?word=)
    - CEFR classifier + content pipeline CLI (pnpm pipeline:validate/crawl/seed/run)
    - Next.js reading browse page (/reading) and passage detail page (/reading/[passageId])
    - PassageRenderer with DOMPurify sanitization, word-span wrapping, highlight save/restore
    - QuestionsSection with inline feedback, reading timer, session complete
    - NotesPanel with auto-save
    - WordPopover with vocabulary lookup and Add to SRS (VOCAB-08)
    - Bookmark toggle (READ-06)
  </what-built>
  <how-to-verify>
    BEFORE running the human verification, execute the content pipeline:

    Step A — Validate crawler selectors:
    1. `cd apps/api && pnpm pipeline:validate`
    2. Confirm each source reports ≥80% extraction success rate
    3. If any source is below 80%: fix its selector in crawler.service.ts and re-run validate before proceeding

    Step B — Seed reading passages:
    4. `cd apps/api && pnpm pipeline:seed` (or `pnpm pipeline:run` for crawl + seed in one step)
    5. Confirm seed completes in ≤10 minutes (PIPE-05, PIPE-06)
    6. Confirm ≥2,000 passages in database: `pnpm --filter @repo/database db:studio` or direct SQL check

    Step C — Manual UI verification (requires running app: `pnpm dev`):

    Journey 1 — Browse and filter (READ-01):
    7. Navigate to http://localhost:3000/reading
    8. Confirm passage grid shows cards with CefrBadge, title, topic badge, word count
    9. Click "B2" tab — confirm grid updates to show only B2 passages
    10. Select "Technology" topic — confirm further filtering
    11. Confirm empty state renders correctly when filters return no results

    Journey 2 — Read passage + answer questions (READ-02, READ-03, READ-04, READ-07):
    12. Click any passage card to open /reading/[passageId]
    13. Confirm passage text renders with correct formatting
    14. Confirm reading timer starts and counts up in the header
    15. Answer all comprehension questions; confirm immediate per-question feedback (emerald correct, red incorrect)
    16. After last answer: confirm inline score card appears with framer-motion entrance
    17. Confirm timer stopped and readingTimeSec is nonzero in score card sub-line

    Journey 3 — Highlight persistence (READ-04):
    18. Select text in the passage body — confirm highlight tooltip appears
    19. Click "Highlight" — confirm amber-100/70 overlay appears immediately
    20. Reload the page — confirm highlight is still visible

    Journey 4 — Notes (READ-05):
    21. Click notes icon in passage header
    22. Type text in textarea
    23. Click outside (blur) — confirm "Saved" indicator appears
    24. Reload — confirm note text is still present

    Journey 5 — Bookmark (READ-06):
    25. Click bookmark icon on passage header — confirm BookmarkCheck (amber-400) appears and "Passage bookmarked." toast shows
    26. Reload — confirm bookmark state persists (BookmarkCheck icon)
    27. Click again — confirm "Bookmark removed." toast and icon returns to Bookmark

    Journey 6 — Word tap to SRS (VOCAB-08):
    28. Tap/click a single word in the passage body
    29. Confirm WordPopover appears with word + definition (or fallback message)
    30. Confirm sentence context is shown below definition
    31. If word is in vocabulary table: click "Add to SRS" — confirm "Added to SRS" confirmation
    32. If word not in vocabulary table: confirm "Add to SRS" is disabled
  </how-to-verify>
  <resume-signal>Type "approved" when all journeys pass, or describe any specific failures with the journey number</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → external crawl targets | Pipeline validation fetches live URLs |
| Seeded content → production database | Crawled + classified content enters the read-only content store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Tampering | Crawler selector failure producing boilerplate content | mitigate | validateSelectors() step with ≥80% threshold gate; human reviews selector report before approving bulk crawl |
| T-05-09-02 | Tampering | Seeded content with malicious HTML | mitigate | isomorphic-dompurify sanitization in SeedService + PassageRenderer double-sanitization |
| T-05-09-03 | DoS | pipeline:crawl hammering source sites beyond polite limits | mitigate | 300–1200ms random delay per request; ~8 min per source for 625 URLs is polite |
| T-05-09-SC | Tampering | npm installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
Full phase verification via human checkpoint above. Automated pre-gate:
1. `pnpm test` exits 0
2. All 3 spec files (reading.service, classifier.service, vocabulary.service) have passing tests
3. ≥2,000 readingPassage rows with isPublished=true in database after seed
4. B1 + B2 + C1 each have ≥1 passage (no empty shelf)
</verification>

<success_criteria>
Phase 5 complete when:
- pnpm test exits 0 (full suite green)
- pnpm pipeline:seed completes ≤10 min with ≥2,000 passages seeded across B1/B2/C1
- Human approves all 6 journey checkpoints
- READ-01–07, PIPE-01–06, VOCAB-08 all verified functional
</success_criteria>

<output>
Create `.planning/phases/05-reading-comprehension-content-pipeline/05-09-SUMMARY.md` when done
</output>
