---
phase: 06-listening-comprehension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/prisma/schema.prisma
  - docker-compose.yml
  - .env
  - packages/shared/src/listening.dto.ts
  - packages/shared/src/index.ts
  - apps/api/src/listening/listening.service.spec.ts
  - apps/api/src/pipeline/listening-seed.service.spec.ts
  - apps/web/src/hooks/use-audio-player.test.ts
  - apps/web/src/components/listening/transcript-panel.test.tsx
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
  - LIST-04
  - LIST-05
  - LIST-06
  - LIST-07

must_haves:
  truths:
    - "wordTimestamps Json? column exists on ListeningContent in the Prisma schema"
    - "Prisma migration has been applied and the DB schema is up to date"
    - "whisper-worker service is declared in docker-compose.yml"
    - "WHISPER_WORKER_URL is present in .env"
    - "listening.dto.ts exports WordTimestampSchema, ListeningItemDetailDtoSchema, ListeningSessionCompleteSchema and all inferred types"
    - "listening.service.spec.ts contains failing RED tests for getItems() and completeSession()"
    - "listening-seed.service.spec.ts contains failing RED tests for generateExercises() exercise type coverage (LIST-02)"
    - "use-audio-player.test.ts contains failing RED tests for seek, setSpeed, and findActiveWordIndex"
    - "transcript-panel.test.tsx contains failing RED tests for blur locked state and WordPopover tap"
  artifacts:
    - path: "packages/database/prisma/schema.prisma"
      provides: "wordTimestamps Json? column on ListeningContent"
      contains: "wordTimestamps"
    - path: "docker-compose.yml"
      provides: "whisper-worker service declaration"
      contains: "whisper-worker"
    - path: "packages/shared/src/listening.dto.ts"
      provides: "All Phase 6 Zod schemas and TypeScript types"
      exports:
        - "WordTimestampSchema"
        - "ListeningSessionCompleteSchema"
        - "ListeningItemDetailDtoSchema"
    - path: "apps/api/src/listening/listening.service.spec.ts"
      provides: "TDD RED scaffolds for ListeningService"
      contains: "describe('ListeningService'"
    - path: "apps/api/src/pipeline/listening-seed.service.spec.ts"
      provides: "TDD RED scaffold for ListeningSeedService.generateExercises() — LIST-02 coverage"
      contains: "generateExercises"
    - path: "apps/web/src/hooks/use-audio-player.test.ts"
      provides: "TDD RED scaffolds for useAudioPlayer and findActiveWordIndex"
      contains: "findActiveWordIndex"
    - path: "apps/web/src/components/listening/transcript-panel.test.tsx"
      provides: "TDD RED scaffolds for TranscriptPanel blur and WordPopover"
      contains: "transcriptLocked"
  key_links:
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/listening.dto.ts"
      via: "barrel re-export"
      pattern: "export \\* from.*listening\\.dto"
    - from: "docker-compose.yml whisper-worker"
      to: "apps/api/src/pipeline/listening-crawler.service.ts"
      via: "WHISPER_WORKER_URL env var"
      pattern: "whisper-worker"
---

<objective>
Phase 6 Wave 1 foundation: add the `wordTimestamps Json?` column to `ListeningContent`, run the Prisma schema migration, add the `whisper-worker` Docker service, create all shared DTOs, and scaffold all failing (RED) TDD test files. No feature logic ships in this plan — only the schema, contracts, and test scaffolds that all subsequent plans depend on.

Purpose: Every downstream plan in Phase 6 depends on the DB column (plans 02, 05), the shared DTOs (plans 02, 03, 04), and the test scaffolds (plans 02, 03, 06). This plan must complete before any other Phase 6 work begins.

Output: Modified schema.prisma + migration applied, docker-compose.yml with whisper-worker, .env updated, listening.dto.ts + barrel re-export, four RED spec files (API service + seed service + hook + component).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-listening-comprehension/06-CONTEXT.md
@.planning/phases/06-listening-comprehension/06-RESEARCH.md
@.planning/phases/06-listening-comprehension/06-PATTERNS.md
@.planning/phases/06-listening-comprehension/06-VALIDATION.md

<interfaces>
<!-- Key types from existing codebase that this plan must match -->

From packages/database/prisma/schema.prisma — ListeningContent model (lines 426-450):
```prisma
model ListeningContent {
  id               String      @id @default(cuid())
  title            String
  transcriptText   String      @db.Text
  audioStorageKey  String?
  sourceUrl        String?     @unique
  contentHash      String?     @unique
  contentType      ContentType
  cefrLevel        CefrLevel
  cefrConfidence   Float       @default(0.0)
  topic            String?
  durationSec      Int?
  isPublished      Boolean     @default(false)
  flaggedForReview Boolean     @default(false)
  createdAt        DateTime    @default(now())
  updatedAt        DateTime    @updatedAt

  questions ListeningQuestion[]
  progress  ListeningProgress[]

  @@index([cefrLevel])
  @@index([topic])
  @@index([contentType])
  @@index([isPublished])
}
```

From packages/shared/src/reading.dto.ts — reference structure for listening.dto.ts:
(reading.dto.ts uses zod, exports Zod schemas and inferred types)

From packages/shared/src/index.ts — barrel export pattern:
(each phase's DTOs added as: export * from "./phase.dto";)

From docker-compose.yml — existing services on ports:
- postgres: 127.0.0.1:5432
- redis-bullmq: 127.0.0.1:6379
- redis-cache: 127.0.0.1:6380
- minio: 127.0.0.1:9000 (API) + 127.0.0.1:9001 (console)
whisper-worker must use external port 9002 to avoid collision with MinIO console on 9001.

From .env — existing MinIO keys already present:
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=english-learning
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add wordTimestamps to Prisma schema and run migration [BLOCKING]</name>
  <files>packages/database/prisma/schema.prisma</files>
  <read_first>
    packages/database/prisma/schema.prisma — read the full ListeningContent model (lines 426-450) to see current state before modifying
  </read_first>
  <action>
    Add `wordTimestamps Json?` as a new column to the `ListeningContent` model in schema.prisma. Insert it after the `updatedAt DateTime @updatedAt` field and before the relations block (`questions ListeningQuestion[]`). The comment must read: `// [{word: String, start: Float, end: Float}] from Whisper forced alignment — nullable for items without a Whisper run`.

    After editing schema.prisma, run the Prisma migration from the repo root:
    `cd /home/tuyen/Desktop/Apps/english-learning && npx prisma migrate dev --name add_word_timestamps_to_listening_content --schema packages/database/prisma/schema.prisma`

    If the migration requires interactive confirmation (shadow database creation), use the non-interactive fallback:
    `npx prisma db push --schema packages/database/prisma/schema.prisma --accept-data-loss`

    After migration succeeds, regenerate the Prisma client:
    `npx prisma generate --schema packages/database/prisma/schema.prisma`

    Per D-04: `wordTimestamps` is nullable (`Json?`) so existing rows without a Whisper run remain valid. Do NOT make it required.
  </action>
  <verify>
    <automated>grep -n "wordTimestamps" /home/tuyen/Desktop/Apps/english-learning/packages/database/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - schema.prisma contains `wordTimestamps Json?` inside the `ListeningContent` model block
    - Migration command exits with code 0 (no errors)
    - `npx prisma generate` exits with code 0
    - `grep "wordTimestamps" packages/database/prisma/schema.prisma` returns one result inside the ListeningContent block
  </acceptance_criteria>
  <done>wordTimestamps Json? column present in schema.prisma; Prisma migration applied to running DB; client regenerated.</done>
</task>

<task type="auto">
  <name>Task 2: Add whisper-worker to docker-compose.yml and update .env</name>
  <files>docker-compose.yml, .env</files>
  <read_first>
    docker-compose.yml — read the full file to understand existing services and volumes declarations before adding whisper-worker
    .env — read to verify existing MINIO_* keys and identify correct position for WHISPER_WORKER_URL
  </read_first>
  <action>
    In docker-compose.yml, add the `whisper-worker` service AFTER the `minio` service block, before the `volumes:` declaration. Use these exact settings:

    Service name: `whisper-worker`
    Image: `hwdsl2/whisper-server` (CPU-only, no :cuda tag)
    Container name: `el_whisper_worker`
    Environment variables:
      WHISPER_MODEL: base
      WHISPER_DEVICE: cpu
      WHISPER_COMPUTE_TYPE: int8
      WHISPER_THREADS: 2
      WHISPER_LANGUAGE: en
      WHISPER_WORD_TIMESTAMPS: "true"
      WHISPER_LOG_LEVEL: WARNING
    Port mapping: `"127.0.0.1:9002:9000"` — use external port 9002 because MinIO occupies 9000 (API) and 9001 (console)
    Volume: `whisper_data:/var/lib/whisper`
    Healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/docs"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

    Also add `whisper_data:` to the top-level `volumes:` map alongside the existing `postgres_data:`, `redis_bullmq_data:`, `minio_data:` entries.

    In .env, add at the end of the file:
    `WHISPER_WORKER_URL=http://whisper-worker:9000`
    (Docker internal network URL — pipeline services call this from within the Docker network)

    Also add to .env.example if that file exists.
  </action>
  <verify>
    <automated>grep -c "whisper-worker" /home/tuyen/Desktop/Apps/english-learning/docker-compose.yml && grep -c "WHISPER_WORKER_URL" /home/tuyen/Desktop/Apps/english-learning/.env</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains `whisper-worker:` service block with image `hwdsl2/whisper-server`
    - docker-compose.yml port mapping is `"127.0.0.1:9002:9000"` (not 9001, which is taken by MinIO console)
    - docker-compose.yml environment includes `WHISPER_WORD_TIMESTAMPS: "true"` (string, not bare boolean)
    - docker-compose.yml top-level volumes block includes `whisper_data:`
    - .env contains `WHISPER_WORKER_URL=http://whisper-worker:9000`
    - `grep "whisper-worker" docker-compose.yml` returns at least 2 lines (service name + container_name)
  </acceptance_criteria>
  <done>whisper-worker service declared in docker-compose.yml on port 9002; WHISPER_WORKER_URL added to .env.</done>
</task>

<task type="auto">
  <name>Task 3: Create listening.dto.ts shared DTOs and TDD RED test scaffolds</name>
  <files>
    packages/shared/src/listening.dto.ts,
    packages/shared/src/index.ts,
    apps/api/src/listening/listening.service.spec.ts,
    apps/api/src/pipeline/listening-seed.service.spec.ts,
    apps/web/src/hooks/use-audio-player.test.ts,
    apps/web/src/components/listening/transcript-panel.test.tsx
  </files>
  <read_first>
    packages/shared/src/reading.dto.ts — read to understand the exact DTO file structure (zod schema + type exports)
    packages/shared/src/index.ts — read to understand the barrel export pattern (where to add the listening.dto re-export)
    apps/api/src/pipeline/reading.service.spec.ts — read to understand the Vitest mock + describe structure for the RED scaffold
    apps/api/src/grammar/grammar.service.spec.ts — read for the vi.fn() mockPrisma pattern
  </read_first>
  <action>
    Create `packages/shared/src/listening.dto.ts` with the complete Phase 6 Zod schemas and exported TypeScript types as specified in RESEARCH.md Code Examples (lines 586-645) and PATTERNS.md Pattern Assignments for listening.dto.ts. Include:
    - `WordTimestampSchema` (word: z.string(), start: z.number(), end: z.number())
    - `ListeningItemDtoSchema` (id, title, contentType enum with all 5 values, cefrLevel, topic, durationSec, questionCount)
    - `ListeningQuestionDtoSchema` (id, exerciseType enum with MULTIPLE_CHOICE/FILL_MISSING_WORDS/DICTATION, prompt, answer, distractors, explanation, timestampSec, xpReward, sortOrder)
    - `ListeningItemDetailDtoSchema` extending ListeningItemDtoSchema with (audioUrl, transcriptText, wordTimestamps nullable array, questions array, progress nullable object)
    - `ListeningSessionCompleteSchema` (contentId, score, accuracy, attempts array of {questionId, isCorrect, userAnswer?})
    - `PaginatedListeningItemsDtoSchema` (items array, total, page, limit, totalPages)
    - `ListeningSessionResultDtoSchema` (score, accuracy, xpEarned, contentId)
    - All inferred TypeScript types via `z.infer<typeof ...>`

    Add to `packages/shared/src/index.ts` after the reading.dto re-export line:
    `export * from "./listening.dto";`

    Create `apps/api/src/listening/` directory and `listening.service.spec.ts` with failing RED tests. The file must use Vitest (`import { describe, it, expect, vi, beforeEach } from 'vitest'`). Mock PrismaService with vi.fn() for: `listeningContent.findMany`, `listeningContent.count`, `listeningContent.findUnique`, `listeningProgress.upsert`, `listeningProgress.findUnique`. Include failing test cases for:
    - `getItems()` returns `{ items, total, page, limit, totalPages }` shape (LIST-01)
    - `getItems()` applies cefrLevel/topic/contentType filters to the prisma where clause (LIST-01)
    - `getItemById()` throws `NotFoundException` when content not found (LIST-01)
    - `completeSession()` calls `listeningProgress.upsert` with `where: { userId_contentId: { userId, contentId } }` (LIST-07)
    - `completeSession()` creates an `XpEvent` row with `skillArea: 'LISTENING'` (LIST-07)
    Tests must FAIL initially because ListeningService does not exist yet.

    Create `apps/api/src/pipeline/listening-seed.service.spec.ts` with a failing RED test for LIST-02 exercise type coverage. The file must use Vitest (`import { describe, it, expect } from 'vitest'`). Import `ListeningSeedService` from `./listening-seed.service` (this module does not exist yet — the import will cause the test suite to fail to compile, which is the RED state). Include this test:

    ```
    describe('ListeningSeedService', () => {
      describe('generateExercises', () => {
        it('returns at least one exercise of each required type for a content item', async () => {
          // RED: ListeningSeedService does not exist yet — this test fails at import
          const service = new ListeningSeedService()
          const mockContent = {
            id: 'test-id',
            transcriptText: 'The quick brown fox jumps over the lazy dog. It was a bright sunny day.',
          }
          const exercises = await service.generateExercises(mockContent as any)
          const types = exercises.map((e: any) => e.exerciseType)
          expect(types).toContain('MULTIPLE_CHOICE')
          expect(types).toContain('FILL_MISSING_WORDS')
          expect(types).toContain('DICTATION')
        })
      })
    })
    ```

    This scaffold gives Plan 05 (ListeningSeedService implementation) a concrete RED test to turn GREEN.

    Create `apps/web/src/hooks/use-audio-player.test.ts` with failing RED tests. Use Vitest. Include failing tests for:
    - `findActiveWordIndex([{word:'hello',start:0,end:0.5}], 0.25)` returns `0` (LIST-05)
    - `findActiveWordIndex([{word:'hello',start:0,end:0.5}], 0.5)` returns `-1` (between words boundary — LIST-05)
    - `findActiveWordIndex([], 0)` returns `-1` (empty array guard — LIST-05)
    - `seek(time)` sets `audioRef.current.currentTime` to the provided value (LIST-03)
    - `setSpeed(rate)` sets `audioRef.current.playbackRate` to the provided rate (LIST-03)
    Import `findActiveWordIndex` from `@/hooks/use-audio-player` (the function does not exist yet — tests will fail at import).

    Create `apps/web/src/components/listening/` directory and `transcript-panel.test.tsx` with failing RED tests. Use Vitest + Testing Library (`@testing-library/react`). Include failing tests for:
    - When `transcriptLocked={true}`, the rendered output has `blur-[4px]` class on the transcript text container (LIST-04)
    - When `transcriptLocked={false}`, the `blur-[4px]` class is absent (LIST-04)
    - When `transcriptLocked={false}` and a word span is clicked, the `onWordClick` callback is called with the word text (LIST-06)
    Import `TranscriptPanel` from `@/components/listening/transcript-panel` (does not exist yet — tests fail at import).

    Do NOT create the implementation files (`listening.service.ts`, `listening-seed.service.ts`, `use-audio-player.ts`, `transcript-panel.tsx`). Only the spec/test files.
  </action>
  <verify>
    <automated>pnpm --filter @repo/shared build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/listening.dto.ts` exists and exports `WordTimestampSchema`, `ListeningSessionCompleteSchema`, `ListeningItemDetailDtoSchema`, `PaginatedListeningItemsDtoSchema`, `ListeningSessionResultDtoSchema`, and all inferred types
    - `packages/shared/src/index.ts` contains `export * from "./listening.dto"`
    - `grep "export \* from.*listening" packages/shared/src/index.ts` returns 1 result
    - `apps/api/src/listening/listening.service.spec.ts` contains `describe('ListeningService'` and `listeningProgress.upsert`
    - `apps/api/src/pipeline/listening-seed.service.spec.ts` exists AND contains `generateExercises` AND `pnpm --filter @repo/api test -- listening-seed` exits non-zero (RED state — ListeningSeedService not yet implemented)
    - `apps/web/src/hooks/use-audio-player.test.ts` contains `findActiveWordIndex` and `seek` and `setSpeed`
    - `apps/web/src/components/listening/transcript-panel.test.tsx` contains `transcriptLocked` and `blur-[4px]`
    - `pnpm --filter @repo/shared build` exits 0 (shared package compiles with new DTOs)
    - Running `pnpm --filter @repo/api test -- listening.service.spec` shows FAILING tests (RED — ListeningService not yet implemented)
    - Running `pnpm --filter @repo/web test -- use-audio-player.test` shows FAILING tests (RED)
  </acceptance_criteria>
  <done>listening.dto.ts created and barrel-exported; four RED test files created with failing tests for LIST-01, LIST-02, LIST-03, LIST-04, LIST-05, LIST-06, LIST-07; shared package compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker internal network → whisper-worker | Pipeline calls POST to whisper-worker:9000 on Docker internal network; no auth required because it is not exposed to the public internet |
| localhost → MinIO | MINIO_* credentials in .env; Docker port only bound to 127.0.0.1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-W1-01 | Tampering | docker-compose.yml whisper-worker | mitigate | Port bound to 127.0.0.1:9002 only — not 0.0.0.0; internal Docker network only; matches existing service pattern |
| T-06-W1-02 | Information Disclosure | .env WHISPER_WORKER_URL | accept | .env is gitignored; Docker internal URL is not a secret (non-routable internal hostname) |
| T-06-W1-SC | Tampering | npm/pip/cargo installs | mitigate | No new npm packages installed in this plan; hwdsl2/whisper-server is a Docker image, not an npm package — slopcheck not applicable |
</threat_model>

<verification>
After all three tasks complete:
1. `grep "wordTimestamps" packages/database/prisma/schema.prisma` — returns one line inside ListeningContent
2. `grep "whisper-worker" docker-compose.yml` — returns multiple lines (service block)
3. `grep "WHISPER_WORKER_URL" .env` — returns the env var line
4. `grep "export \* from.*listening" packages/shared/src/index.ts` — returns 1 result
5. `pnpm --filter @repo/shared build` — exits 0
6. `pnpm --filter @repo/api test -- listening.service.spec` — tests FAIL (RED — expected, implementation not yet written)
7. `pnpm --filter @repo/api test -- listening-seed` — tests FAIL (RED — ListeningSeedService not yet written)
8. `pnpm --filter @repo/web test -- use-audio-player.test` — tests FAIL (RED)
9. `pnpm --filter @repo/web test -- transcript-panel.test` — tests FAIL (RED)
</verification>

<success_criteria>
- wordTimestamps column present in Prisma schema and migrated to DB
- whisper-worker declared in docker-compose.yml with correct port (9002) and WHISPER_WORD_TIMESTAMPS: "true"
- WHISPER_WORKER_URL=http://whisper-worker:9000 in .env
- listening.dto.ts exports all required schemas and types; barrel-exported from shared/index.ts
- Four RED test files exist with failing assertions for LIST-01, LIST-02, LIST-03, LIST-04, LIST-05, LIST-06, LIST-07
</success_criteria>

<output>
Create `.planning/phases/06-listening-comprehension/06-01-SUMMARY.md` when done
</output>
