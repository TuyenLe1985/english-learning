---
phase: 06-listening-comprehension
plan: 03
type: tdd
wave: 3
depends_on:
  - "06-01"
files_modified:
  - apps/web/src/hooks/use-audio-player.ts
  - apps/web/src/hooks/use-audio-player.test.ts
  - apps/web/src/components/listening/audio-player.tsx
  - apps/web/src/components/listening/transcript-panel.tsx
  - apps/web/src/components/listening/transcript-panel.test.tsx
autonomous: true
requirements:
  - LIST-03
  - LIST-04
  - LIST-05
  - LIST-06

must_haves:
  truths:
    - "useAudioPlayer hook manages currentTime, duration, isPlaying, playbackRate, hasListenedEnough via requestAnimationFrame loop"
    - "findActiveWordIndex performs O(log n) binary search returning correct index or -1"
    - "AudioPlayer renders sticky player bar with play/pause, seek bar (shadcn Slider), and four speed toggle pills"
    - "TranscriptPanel renders transcript blurred when transcriptLocked=true, clear when false"
    - "TranscriptPanel renders word spans with data-active attribute for karaoke in unlocked state"
    - "All use-audio-player.test.ts and transcript-panel.test.tsx tests pass GREEN"
  artifacts:
    - path: "apps/web/src/hooks/use-audio-player.ts"
      provides: "useAudioPlayer hook + exported findActiveWordIndex function"
      exports: ["useAudioPlayer", "findActiveWordIndex", "AudioPlayerState"]
    - path: "apps/web/src/components/listening/audio-player.tsx"
      provides: "AudioPlayer component with sticky player bar UI"
      exports: ["AudioPlayer"]
    - path: "apps/web/src/components/listening/transcript-panel.tsx"
      provides: "TranscriptPanel component with lock/unlock + karaoke word spans"
      exports: ["TranscriptPanel"]
  key_links:
    - from: "apps/web/src/components/listening/audio-player.tsx"
      to: "apps/web/src/hooks/use-audio-player.ts"
      via: "useAudioPlayer hook consumption"
      pattern: "useAudioPlayer"
    - from: "apps/web/src/components/listening/transcript-panel.tsx"
      to: "apps/web/src/hooks/use-audio-player.ts"
      via: "currentTime prop drives findActiveWordIndex"
      pattern: "findActiveWordIndex"
---

<objective>
TDD implementation of the audio player hook and transcript panel — the two most novel technical components in Phase 6. The RED scaffolds from Plan 01 (use-audio-player.test.ts, transcript-panel.test.tsx) become GREEN.

Purpose: Delivers the core audio playback primitives (LIST-03), karaoke word highlighting algorithm (LIST-05), transcript lock/unlock UI (LIST-04), and WordPopover integration surface (LIST-06). These components are consumed by the ListeningItemPage page in Plan 04.

Output: useAudioPlayer hook, findActiveWordIndex (exported pure function), AudioPlayer component (sticky bar), TranscriptPanel component (locked/unlocked + word spans). All plan-01 RED tests pass GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-listening-comprehension/06-CONTEXT.md
@.planning/phases/06-listening-comprehension/06-RESEARCH.md
@.planning/phases/06-listening-comprehension/06-UI-SPEC.md
@.planning/phases/06-listening-comprehension/06-PATTERNS.md

<interfaces>
<!-- Key interfaces this plan implements and produces -->

From packages/shared/src/listening.dto.ts (Plan 01):
- WordTimestamp = { word: string; start: number; end: number }
- ListeningItemDetailDto includes wordTimestamps: Array<WordTimestamp> | null

From RESEARCH.md Pattern 2 — useAudioPlayer hook signature:
- audioRef: RefObject<HTMLAudioElement>
- state: AudioPlayerState (currentTime, duration, isPlaying, playbackRate, hasListenedEnough)
- play(), pause(), seek(time: number), setSpeed(rate: number)
- startRafLoop(), stopRafLoop() — called on play/pause events

From RESEARCH.md Pattern 3 — findActiveWordIndex signature:
- findActiveWordIndex(words: WordTimestamp[], currentTime: number): number
- Returns index where words[i].start <= currentTime < words[i].end
- Returns -1 when between words (currentTime at exact .end boundary is -1)
- MUST be exported as a named export for testability

From UI-SPEC.md:
- AudioPlayer sticky bar: `sticky top-0 z-40 bg-background border-b border-border h-16 flex items-center gap-4 px-4`
- Speed toggles: `rounded-full px-3 py-1 h-7`, active: `bg-primary text-primary-foreground border-transparent`
- Seek bar: shadcn Slider, `flex-1`, min=0, max=duration, step=0.1
- Time display: `text-sm text-muted-foreground tabular-nums w-[84px]`
- Transcript locked: blur-[4px] on text + absolute overlay with LockKeyhole icon
- Transcript word span: `data-[active=true]:bg-amber-200 data-[active=true]:text-amber-900`
- Transcript word span: DO NOT use dangerouslySetInnerHTML — render {word.word} as text node inside span

Performance anti-pattern (RESEARCH Pitfall 2):
- DO NOT call setState inside rAF tick for activeWordIndex
- Use ref: `activeWordIndexRef.current = newIndex`
- Directly update DOM: `wordRefs[newIndex].current?.setAttribute('data-active', 'true')`
- Remove from previous: `wordRefs[prevIdx].current?.removeAttribute('data-active')`

From UI-SPEC.md — speed toggle ARIA:
- Wrap in `<div role="group" aria-label="Playback speed">`
- Each button: `aria-pressed={state.playbackRate === speed}`

From apps/web/src/components/reading/word-popover.tsx (Phase 5, may or may not exist):
- TranscriptPanel imports WordPopover only if it exists; otherwise renders word spans without popover
- WordPopover is called with: word (string), contextSentence (string)
- If WordPopover does not exist yet, TranscriptPanel accepts an `onWordClick?: (word: string, sentence: string) => void` prop and renders the word spans as interactive elements that call onWordClick
</interfaces>
</context>

<feature>
  <name>useAudioPlayer hook + findActiveWordIndex + AudioPlayer + TranscriptPanel</name>
  <files>
    apps/web/src/hooks/use-audio-player.ts,
    apps/web/src/hooks/use-audio-player.test.ts,
    apps/web/src/components/listening/audio-player.tsx,
    apps/web/src/components/listening/transcript-panel.tsx,
    apps/web/src/components/listening/transcript-panel.test.tsx
  </files>
  <behavior>
    RED phase (already failing from Plan 01 scaffolds):
    - `findActiveWordIndex([{word:'hello',start:0,end:0.5}], 0.25)` returns 0
    - `findActiveWordIndex([{word:'hello',start:0,end:0.5}], 0.5)` returns -1
    - `findActiveWordIndex([], 0)` returns -1
    - `seek(0.5)` sets `audioRef.current.currentTime` to 0.5
    - `setSpeed(1.5)` sets `audioRef.current.playbackRate` to 1.5
    - TranscriptPanel with transcriptLocked=true renders element with blur-[4px] class
    - TranscriptPanel with transcriptLocked=false does NOT have blur-[4px] class
    - TranscriptPanel unlocked word click calls onWordClick with the word text

    GREEN phase (implement to pass all):
    - findActiveWordIndex: binary search algorithm from RESEARCH Pattern 3 — boundary: words[mid].end <= currentTime moves lo right (currentTime at exact .end = -1)
    - useAudioPlayer: rAF loop updates state.currentTime and state.hasListenedEnough; does NOT update activeWordIndex in state
    - seek: sets audioRef.current.currentTime directly
    - setSpeed: sets audioRef.current.playbackRate + updates state.playbackRate
    - AudioPlayer: `<audio>` element with ref={audioRef}; onPlay=startRafLoop, onPause=stopRafLoop, onLoadedMetadata updates duration
    - TranscriptPanel locked: blur-[4px] class on transcript wrapper div + absolute overlay (LockKeyhole icon + label); word spans aria-hidden
    - TranscriptPanel unlocked: no blur class; word spans rendered from wordTimestamps array with ref={wordRefs[i]}, data-word-index={i}, onClick calls onWordClick
    - TranscriptPanel null wordTimestamps: renders plain <p> with static text + "Word-level sync is unavailable" banner
    - TranscriptPanel cue-click in unlocked mode: each paragraph/sentence is a clickable region calling seek(cue.start)
  </behavior>
  <implementation>
    Step 1 (RED): Confirm test failures in both test files.

    Step 2 (GREEN — useAudioPlayer hook):
    Create `apps/web/src/hooks/use-audio-player.ts` with 'use client' directive.
    Export `AudioPlayerState` interface and `useAudioPlayer(audioUrl: string)` function.
    Export `findActiveWordIndex(words: WordTimestamp[], currentTime: number): number` as a NAMED TOP-LEVEL EXPORT (not inside the hook) so it can be imported directly in tests.
    The rAF tick reads `audioRef.current.currentTime` and updates state. It sets `hasListenedEnough` to `true` once `currentTime >= duration * 0.5` (never sets back to false). Cancel rAF on `stopRafLoop`. Clean up rAF in a useEffect return on unmount.
    The `play()` callback calls `audioRef.current?.play()`. The `pause()` callback calls `audioRef.current?.pause()`. These should also call `startRafLoop` / `stopRafLoop` respectively — or attach those to `onPlay` / `onPause` audio element events.
    Import `WordTimestamp` type from `@repo/shared`.

    Step 3 (GREEN — AudioPlayer component):
    Create `apps/web/src/components/listening/audio-player.tsx` with 'use client'.
    Props: `audioRef: RefObject<HTMLAudioElement>`, `state: AudioPlayerState`, `play: () => void`, `pause: () => void`, `seek: (t: number) => void`, `setSpeed: (r: number) => void`, `startRafLoop: () => void`, `stopRafLoop: () => void`.
    Outer div: `sticky top-0 z-40 bg-background border-b border-border h-16 flex items-center gap-4 px-4`.
    Include `<audio ref={audioRef} src={audioUrl} onPlay={startRafLoop} onPause={stopRafLoop} onLoadedMetadata={...} preload="metadata" />` — hidden, not displayed.
    Play/Pause button: `Button variant="ghost" size="icon"` with Play/Pause Lucide icons (aria-label switches dynamically).
    Time display: `{formatTime(state.currentTime)} / {formatTime(state.duration)}` at `text-sm text-muted-foreground tabular-nums w-[84px]`.
    Slider (seek bar): shadcn Slider, `flex-1`, value={[state.currentTime]}, max={state.duration || 1}, step={0.1}, onValueChange={([v]) => seek(v)}, aria-label="Audio seek bar".
    Speed toggles: `SPEEDS = [0.75, 1, 1.25, 1.5]` as const. Wrapped in `<div role="group" aria-label="Playback speed" className="flex gap-1">`. Each: `Button variant="ghost" size="sm"` with active class override `bg-primary text-primary-foreground border-transparent` when `state.playbackRate === speed`. Base class: `rounded-full px-3 py-1 h-7 text-sm border border-border`. aria-pressed per speed.
    Helper: `formatTime(sec: number): string` formats as "M:SS".

    Step 4 (GREEN — TranscriptPanel component):
    Create `apps/web/src/components/listening/transcript-panel.tsx` with 'use client'.
    Props: `transcriptText: string`, `wordTimestamps: WordTimestamp[] | null`, `transcriptLocked: boolean`, `currentTime: number`, `onWordClick?: (word: string, sentence: string) => void`, `onCueSeek?: (time: number) => void`.
    Section header: `<h2 className="text-sm font-semibold text-foreground mb-3">Transcript</h2>`.
    Container: `relative mt-4 rounded-xl border border-border bg-card p-6`.
    LOCKED state: Apply `blur-[4px] transition-[filter] duration-300 ease-in-out select-none pointer-events-none` to the transcript text wrapper div. Render absolute overlay: `absolute inset-0 flex flex-col items-center justify-center gap-3 bg-background/80 rounded-xl` containing `LockKeyhole` icon (`size-8 text-muted-foreground`) + `<p className="text-sm text-muted-foreground text-center max-w-[240px]">Transcript is locked until you complete the exercises.</p>`. Add `aria-hidden="true"` to the blurred text div. Add `role="status" aria-live="polite"` to the overlay div.
    UNLOCKED state — no wordTimestamps: render `<p className="text-base leading-[1.75] text-foreground">{transcriptText}</p>` + `<p className="text-sm text-muted-foreground italic mt-2">Word-level sync is unavailable for this item.</p>`.
    UNLOCKED state — with wordTimestamps: render word spans. Create `wordRefs = useRef<Array<React.RefObject<HTMLSpanElement>>>([])` sized to wordTimestamps.length. Each `<span ref={wordRefs[i].current} data-word-index={i} data-active="false" className="cursor-pointer rounded-sm px-0.5 transition-colors duration-75 hover:bg-muted/70 data-[active=true]:bg-amber-200 data-[active=true]:text-amber-900" onClick={() => onWordClick?.(word.word, getSentenceContext(i))} role="button" tabIndex={0} aria-label={`Word: ${word.word}`}>{word.word}</span>{' '}`.
    The rAF-driven karaoke update comes from the parent (Plan 06 ListeningSession passes currentTime as a prop). In TranscriptPanel, a useEffect watches `currentTime` changes when unlocked: call `findActiveWordIndex(wordTimestamps, currentTime)` to get `newIdx`; if different from `prevIdxRef.current`, remove data-active from previous span and set on new span, call `wordRefs[newIdx].current?.scrollIntoView({ behavior: 'smooth', block: 'nearest' })`.
    The `getSentenceContext(wordIndex)` helper finds the sentence containing the word (split transcript on ". " boundaries) and returns it as a string.

    Step 5 (GREEN — run tests):
    Run `pnpm --filter @repo/web test -- use-audio-player.test`. All tests pass GREEN.
    Run `pnpm --filter @repo/web test -- transcript-panel.test`. All tests pass GREEN.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wordTimestamps (from API) → TranscriptPanel | Data came from DB (plan 02); transcript words rendered as text nodes, not innerHTML |
| User input (word click) → onWordClick callback | Passes plain word string; no HTML injection possible because span contains {word.word} text node |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | XSS | TranscriptPanel word spans | mitigate | `{word.word}` rendered as React text node — NOT `dangerouslySetInnerHTML`; no HTML tags can inject from wordTimestamps |
| T-06-07 | XSS | transcriptText plain paragraph (null wordTimestamps path) | mitigate | `{transcriptText}` rendered as React text node in `<p>` — not dangerouslySetInnerHTML |
| T-06-08 | DoS | rAF loop infinite | mitigate | stopRafLoop called on pause and useEffect cleanup; `if (!audioRef.current) return` guard in tick |
| T-06-W3-SC | Tampering | npm/pip/cargo installs | mitigate | No new npm installs in this plan (Slider was already installed in prior phases, or installed in Plan 01 via `npx shadcn add slider`) |
</threat_model>

<verification>
After implementation:
1. `pnpm --filter @repo/web test -- use-audio-player.test` — exits 0, all tests GREEN
2. `pnpm --filter @repo/web test -- transcript-panel.test` — exits 0, all tests GREEN
3. `grep "export function findActiveWordIndex" apps/web/src/hooks/use-audio-player.ts` — returns 1 result (exported, not private)
4. `grep "dangerouslySetInnerHTML" apps/web/src/components/listening/transcript-panel.tsx` — returns 0 results (XSS guard)
5. `grep "blur-\[4px\]" apps/web/src/components/listening/transcript-panel.tsx` — returns 1 result (locked state class)
6. `grep "data-\[active=true\]:bg-amber-200" apps/web/src/components/listening/transcript-panel.tsx` — returns 1 result (karaoke highlight)
7. `grep "sticky top-0 z-40" apps/web/src/components/listening/audio-player.tsx` — returns 1 result
</verification>

<success_criteria>
- All use-audio-player.test.ts tests pass GREEN (findActiveWordIndex binary search, seek, setSpeed — LIST-03, LIST-05)
- All transcript-panel.test.tsx tests pass GREEN (blur locked state, WordPopover tap — LIST-04, LIST-06)
- findActiveWordIndex is a named top-level export from use-audio-player.ts
- TranscriptPanel uses text nodes only (no dangerouslySetInnerHTML)
- AudioPlayer renders shadcn Slider as seek bar and four speed toggle pills
- rAF loop manages state.currentTime but does NOT call setState for activeWordIndex (uses refs + direct DOM setAttribute)
</success_criteria>

<output>
Create `.planning/phases/06-listening-comprehension/06-03-SUMMARY.md` when done
</output>
