---
phase: 06-listening-comprehension
plan: 04
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - apps/web/src/app/(dashboard)/listening/page.tsx
  - apps/web/src/app/(dashboard)/listening/[itemId]/page.tsx
  - apps/web/src/components/listening/listening-item-card.tsx
  - apps/api/src/app/api/listening/sessions/complete/route.ts
autonomous: true
requirements:
  - LIST-01
  - LIST-06

must_haves:
  truths:
    - "GET /listening renders a browse page with CEFR, topic, and content type filter controls"
    - "GET /listening/[itemId] renders item detail with back link, title, metadata, and ListeningItemClient placeholder"
    - "ListeningItemCard renders title, CEFR badge, content type badge, duration, and exercise count"
    - "Both pages redirect to /login if session is absent"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/listening/page.tsx"
      provides: "Server Component browse page with filters"
      contains: "fetchWithAuth"
    - path: "apps/web/src/app/(dashboard)/listening/[itemId]/page.tsx"
      provides: "Server Component item detail page"
      contains: "ListeningItemDetailDto"
    - path: "apps/web/src/components/listening/listening-item-card.tsx"
      provides: "Browse card component"
      contains: "CefrBadge"
  key_links:
    - from: "apps/web/src/app/(dashboard)/listening/page.tsx"
      to: "apps/api/src/listening/listening.controller.ts"
      via: "fetchWithAuth to INTERNAL_API_URL/api/listening/items"
      pattern: "api/listening/items"
    - from: "apps/web/src/app/(dashboard)/listening/[itemId]/page.tsx"
      to: "apps/api/src/listening/listening.controller.ts"
      via: "fetchWithAuth to INTERNAL_API_URL/api/listening/items/:id"
      pattern: "api/listening/items"
---

<objective>
Next.js Server Component pages for the Listening browse and item detail views, plus the ListeningItemCard component.

Purpose: Delivers the browsable UI (LIST-01) and the detail page shell that will host the audio player + transcript + exercises (added in Plan 06).

Output: Two Server Component pages, ListeningItemCard, and a Next.js API relay route for session completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-listening-comprehension/06-CONTEXT.md
@.planning/phases/06-listening-comprehension/06-RESEARCH.md
@.planning/phases/06-listening-comprehension/06-UI-SPEC.md
@.planning/phases/06-listening-comprehension/06-PATTERNS.md

<interfaces>
<!-- Key interfaces and patterns the executor must follow -->

From apps/web/src/app/(dashboard)/grammar/page.tsx — Server Component pattern:
- `import { auth } from "@/auth"` + `if (!session) redirect("/login")`
- `import { headers } from "next/headers"`; `const reqHeaders = await headers()`; `const cookieHeader = reqHeaders.get("cookie") ?? ""`
- `import { fetchWithAuth, INTERNAL_API_URL } from "@/lib/api-client"`
- Wrap fetchWithAuth in try/catch; return empty data on failure

From apps/web/src/components/cefr-badge.tsx — CefrBadge usage (already exists):
- `<CefrBadge level={item.cefrLevel} />` renders colored badge for B1/B2/C1

From packages/shared/src/listening.dto.ts (Plan 01):
- `ListeningItemDto`: { id, title, contentType, cefrLevel, topic, durationSec, questionCount }
- `ListeningItemDetailDto`: extends ListeningItemDto + audioUrl, transcriptText, wordTimestamps, questions, progress
- `PaginatedListeningItemsDto`: { items: ListeningItemDto[], total, page, limit, totalPages }

From UI-SPEC.md browse page:
- Page header h1: "Listening" at `text-[28px] font-semibold text-foreground` — wait, UI-SPEC says 20px heading for item page title; browse page uses same 28px pattern as Grammar page
- Filter bar: Tabs (CEFR levels: All/B1/B2/C1) + Select (topic) + Select (content type)
- Grid: 1 col mobile, 2 col md, 3 col xl, gap-6
- Card: `hover:shadow-md transition-shadow cursor-pointer rounded-xl border border-border bg-card p-5`
- Card uses `<Link href="/listening/{item.id}">` wrapping the whole card
- Duration format: `"{m}m {s}s"` from durationSec; omit if durationSec is null
- Empty state: "No listening items match your filters" + "Try adjusting your filters..."

From UI-SPEC.md item page header (above sticky player):
- Back link: `← Back to Listening` with ChevronLeft icon
- Title: `text-[20px] font-semibold text-foreground`
- Metadata: CefrBadge + content-type Badge + duration + exercise count

Content type display label map (UI-SPEC):
CONVERSATION→Conversation, INTERVIEW→Interview, PODCAST→Podcast, LECTURE→Lecture, NEWS_REPORT→News Report

Search params pattern for browse page (Next.js 14 App Router):
- `interface Props { searchParams: Promise<{ level?: string; topic?: string; type?: string; page?: string }> }`
- `const { level, topic, type, page } = await searchParams`

Next.js API relay route for session completion:
- `apps/web/src/app/api/listening/sessions/complete/route.ts`
- Pattern: POST handler extracts JWE cookie, calls fetchWithAuth to NestJS
- Follow pattern from existing `apps/web/src/app/api/grammar/sessions/complete/route.ts` if it exists, or from `apps/web/src/app/api/srs/review/route.ts`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create listening browse page + item card</name>
  <files>
    apps/web/src/app/(dashboard)/listening/page.tsx,
    apps/web/src/components/listening/listening-item-card.tsx
  </files>
  <read_first>
    apps/web/src/app/(dashboard)/grammar/page.tsx — read to understand Server Component auth + fetchWithAuth pattern to replicate
    apps/web/src/components/cefr-badge.tsx — read to understand CefrBadge props and import path
    apps/web/src/lib/api-client.ts — read to confirm fetchWithAuth and INTERNAL_API_URL exports
  </read_first>
  <action>
    Create `apps/web/src/app/(dashboard)/listening/page.tsx` as a Next.js Server Component. Pattern mirrors grammar/page.tsx exactly. Key implementation:
    - Import auth, headers, fetchWithAuth, INTERNAL_API_URL from their respective paths
    - Declare `interface Props { searchParams: Promise<{ level?: string; topic?: string; type?: string; page?: string }> }`
    - Await `searchParams` and build a URLSearchParams from level→cefrLevel, topic, type→contentType, page
    - `fetchListeningItems(cookieHeader, params)` → `{ items: ListeningItemDto[], total, page, limit, totalPages }` with try/catch returning empty data on failure
    - Return JSX: `<div className="mx-auto max-w-screen-xl px-4 py-8">` containing:
      - Page header: `<h1 className="text-[28px] font-semibold text-foreground">Listening</h1>` + subtitle "Practice your listening comprehension" at `text-sm text-muted-foreground`
      - Filter bar: wrapped in `<div className="bg-muted rounded-lg p-3 mb-6 flex flex-wrap gap-4 items-center">`. Use `<Link>` based CEFR tab buttons (All/B1/B2/C1) since Tabs is a client component — use simple styled Link buttons instead. Topic and content-type selects: these require client state, so use a `<ListeningFilters>` client component (create it inline in the page file or as a separate small file). Pass current filter values as props.
      - Grid: `<div className="grid grid-cols-1 gap-6 md:grid-cols-2 xl:grid-cols-3">`. Map items to `<ListeningItemCard key={item.id} item={item} />`
      - Empty state when items.length === 0: centered `<p>No listening items match your filters</p>` + `<p>Try adjusting your filters, or check back after the content pipeline has run.</p>`
      - Pagination: simple prev/next links if total > limit (or defer to Plan 06 if too complex — minimum viable is page 1 working)

    Create `apps/web/src/components/listening/listening-item-card.tsx` as a client component (needs Link). Props: `{ item: ListeningItemDto }`.
    - Outer: `<Link href={"/listening/" + item.id}>`
    - Container: `<div className="rounded-xl border border-border bg-card p-5 hover:shadow-md transition-shadow cursor-pointer">`
    - Top row: `CefrBadge level={item.cefrLevel}` + `<Badge variant="secondary">{contentTypeLabel(item.contentType)}</Badge>`
    - Title: `<p className="text-sm font-semibold text-foreground line-clamp-2 mt-2">{item.title}</p>`
    - Metadata row: duration (formatted "{m}m {s}s" from durationSec, omit if null) + separator `·` + `"{item.questionCount} exercises"`
    - `contentTypeLabel(type)` maps enum values to display labels (CONVERSATION→Conversation, NEWS_REPORT→News Report, etc.)
    - Duration format: `const m = Math.floor(durationSec / 60); const s = durationSec % 60; return \`${m}m ${s}s\``
  </action>
  <verify>
    <automated>ls /home/tuyen/Desktop/Apps/english-learning/apps/web/src/app/'(dashboard)'/listening/page.tsx && grep "fetchWithAuth" /home/tuyen/Desktop/Apps/english-learning/apps/web/src/app/'(dashboard)'/listening/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/app/(dashboard)/listening/page.tsx` exists and contains `fetchWithAuth` and `redirect("/login")`
    - `apps/web/src/components/listening/listening-item-card.tsx` exists and contains `CefrBadge`
    - Page imports `ListeningItemDto` from `@repo/shared`
    - ListeningItemCard maps `NEWS_REPORT` to "News Report" display label
    - `grep "redirect.*login" apps/web/src/app/(dashboard)/listening/page.tsx` returns 1 result
  </acceptance_criteria>
  <done>Browse page renders filter bar + grid + empty state; ListeningItemCard renders CEFR badge + content type + duration.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create listening item detail page + Next.js session relay route</name>
  <files>
    apps/web/src/app/(dashboard)/listening/[itemId]/page.tsx,
    apps/web/src/app/api/listening/sessions/complete/route.ts
  </files>
  <read_first>
    apps/web/src/app/(dashboard)/grammar/[area]/[topic]/[lesson]/page.tsx — read to understand the Server Component detail page pattern (params handling, fetchWithAuth, null item handling)
    apps/web/src/app/api/grammar/sessions/complete/route.ts — read to understand the relay POST route pattern (JWE cookie extraction + fetchWithAuth to NestJS)
    apps/web/src/lib/api-client.ts — read to confirm extractRawToken or cookie forwarding pattern for relay routes
  </read_first>
  <action>
    Create `apps/web/src/app/(dashboard)/listening/[itemId]/page.tsx` as a Server Component.
    - `interface Props { params: Promise<{ itemId: string }> }`
    - Await `params` to get `itemId`
    - Auth check: `const session = await auth(); if (!session) redirect("/login")`
    - Fetch: `fetchItemDetail(cookieHeader, itemId)` → `ListeningItemDetailDto | null`
    - If null: render error state with "← Back to Listening" link + "Could not load this item. Try refreshing." message
    - If found: render the page shell:
      - Back link: `<Link href="/listening" className="mb-4 inline-flex items-center gap-1 text-sm text-muted-foreground"><ChevronLeft className="size-4" />Back to Listening</Link>`
      - Item title: `<h1 className="text-[20px] font-semibold text-foreground mb-1">{item.title}</h1>`
      - Metadata row: `<div className="flex items-center gap-2 text-sm text-muted-foreground mb-4"><CefrBadge level={item.cefrLevel} /><Badge variant="secondary">{contentTypeLabel(item.contentType)}</Badge>{item.durationSec && <span>{formatDuration(item.durationSec)}</span>}<span>·</span><span>{item.questions.length} exercises</span></div>`
      - Placeholder for client components: `<ListeningItemClient item={item} />` — this is a placeholder import. Create a minimal `apps/web/src/components/listening/listening-item-client.tsx` stub that renders `<div>Player coming in Plan 06</div>` for now. The full implementation is in Plan 06.

    Create `apps/web/src/app/api/listening/sessions/complete/route.ts` as a Next.js Route Handler (POST).
    Follow the relay pattern: extract JWE cookie from the request, call `fetchWithAuth` to `INTERNAL_API_URL/api/listening/sessions/complete` with the request body forwarded as JSON. Return the response body with the same status code. Follow the exact same relay pattern as the grammar or srs equivalent route handlers.

    Also install shadcn Slider if not already installed:
    `npx shadcn add slider` (run from apps/web directory)
    Check first: `ls apps/web/src/components/ui/slider.tsx` — if it exists, skip install.
  </action>
  <verify>
    <automated>ls /home/tuyen/Desktop/Apps/english-learning/apps/web/src/app/'(dashboard)'/listening/'[itemId]'/page.tsx && ls /home/tuyen/Desktop/Apps/english-learning/apps/web/src/app/api/listening/sessions/complete/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/app/(dashboard)/listening/[itemId]/page.tsx` exists and contains `ListeningItemDetailDto` import from `@repo/shared`
    - Detail page contains `ChevronLeft` import and "Back to Listening" link
    - Detail page null-item state renders "Could not load this item" message
    - `apps/web/src/app/api/listening/sessions/complete/route.ts` exists and exports a `POST` function
    - Relay route calls `fetchWithAuth` to `INTERNAL_API_URL/api/listening/sessions/complete`
    - `apps/web/src/components/ui/slider.tsx` exists (shadcn Slider installed)
    - `pnpm --filter @repo/web build` exits 0 (or `pnpm --filter @repo/web typecheck` exits 0)
  </acceptance_criteria>
  <done>Listening item detail page renders item header + metadata + ListeningItemClient stub; relay route forwards POST to NestJS; shadcn Slider installed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js API relay route | Relay extracts JWE cookie from browser request; forwards as Bearer header to NestJS |
| Next.js Server Component → NestJS | fetchWithAuth forwards JWE cookie; NestJS JwtAuthGuard validates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Spoofing | Relay route sessions/complete | mitigate | Relay does not inject or modify userId — forwards raw JWE cookie; NestJS extracts userId from JWT |
| T-06-10 | Information Disclosure | ListeningItemDetail audioUrl | mitigate | audioUrl is a 1-hour presigned URL (Plan 02 mitigated); page does not expose audioStorageKey |
| T-06-W4-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages installed in this plan; fastest-levenshtein removed — web app uses native editDistance (Plan 06); NestJS does not require Levenshtein server-side |
</threat_model>

<verification>
After all tasks complete:
1. `ls apps/web/src/app/(dashboard)/listening/page.tsx` — file exists
2. `ls apps/web/src/app/(dashboard)/listening/'[itemId]'/page.tsx` — file exists
3. `grep "redirect.*login" apps/web/src/app/(dashboard)/listening/page.tsx` — 1 result
4. `ls apps/web/src/app/api/listening/sessions/complete/route.ts` — file exists
5. `ls apps/web/src/components/ui/slider.tsx` — Slider component exists
</verification>

<success_criteria>
- Browse page `/listening` renders with CEFR filter tabs, topic select, content type select, item grid, empty state (LIST-01)
- ListeningItemCard shows CefrBadge, content type label (CONVERSATION→Conversation etc.), duration, question count (LIST-01)
- Item detail page `/listening/[itemId]` shows back link, title, metadata, and ListeningItemClient stub
- Next.js relay route `POST /api/listening/sessions/complete` forwards to NestJS (LIST-07 prerequisite for client-side submit)
- shadcn Slider installed for use in AudioPlayer (Plan 03 prerequisite)
</success_criteria>

<output>
Create `.planning/phases/06-listening-comprehension/06-04-SUMMARY.md` when done
</output>
