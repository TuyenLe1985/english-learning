---
phase: 06-listening-comprehension
plan: 06
type: execute
wave: 5
depends_on:
  - "06-03"
  - "06-04"
files_modified:
  - apps/web/src/components/listening/listening-session.tsx
  - apps/web/src/components/listening/listening-score-card.tsx
  - apps/web/src/components/listening/exercises/fill-missing-words.tsx
  - apps/web/src/components/listening/exercises/dictation-exercise.tsx
  - apps/web/src/components/listening/listening-item-client.tsx
  - apps/web/src/app/(dashboard)/listening/[itemId]/page.tsx
autonomous: true
requirements:
  - LIST-02
  - LIST-03
  - LIST-04
  - LIST-05
  - LIST-06
  - LIST-07

must_haves:
  truths:
    - "ListeningItemClient wires together AudioPlayer, TranscriptPanel, and ListeningSession"
    - "Start Exercises button activates only after hasListenedEnough (50% audio threshold) per D-13"
    - "Submitting all exercises triggers transcriptLocked=false state transition atomically (D-15)"
    - "Score card renders inline below exercises after session submit — no page redirect (D-16)"
    - "FillMissingWords exercise renders blanked sentence with 3 option pill buttons"
    - "Dictation exercise renders mini clip player (seeks main audioRef to timestampSec) + Textarea + Check Answer"
    - "Dictation scoring uses fastest-levenshtein distance <= 2 for minor typo tolerance"
    - "WordPopover is available on unlocked transcript word spans"
  artifacts:
    - path: "apps/web/src/components/listening/listening-item-client.tsx"
      provides: "Client Component orchestrating AudioPlayer + TranscriptPanel + ListeningSession"
      exports: ["ListeningItemClient"]
    - path: "apps/web/src/components/listening/listening-session.tsx"
      provides: "Session orchestrator with state accumulation and batch submit"
      exports: ["ListeningSession"]
    - path: "apps/web/src/components/listening/listening-score-card.tsx"
      provides: "Inline score card with framer-motion entrance"
      exports: ["ListeningScoreCard"]
    - path: "apps/web/src/components/listening/exercises/fill-missing-words.tsx"
      provides: "FillMissingWords exercise component"
      exports: ["FillMissingWordsExercise"]
    - path: "apps/web/src/components/listening/exercises/dictation-exercise.tsx"
      provides: "Dictation exercise with mini clip player and Levenshtein scoring"
      exports: ["DictationExercise"]
  key_links:
    - from: "apps/web/src/components/listening/listening-item-client.tsx"
      to: "apps/web/src/hooks/use-audio-player.ts"
      via: "useAudioPlayer hook"
      pattern: "useAudioPlayer"
    - from: "apps/web/src/components/listening/listening-item-client.tsx"
      to: "apps/web/src/components/listening/transcript-panel.tsx"
      via: "transcriptLocked prop + currentTime prop"
      pattern: "transcriptLocked"
    - from: "apps/web/src/components/listening/exercises/dictation-exercise.tsx"
      to: "fastest-levenshtein"
      via: "distance() import"
      pattern: "fastest-levenshtein"
---

<objective>
Full interactive experience: ListeningItemClient wires AudioPlayer + TranscriptPanel + ListeningSession, implementing the complete listen-first flow. FillMissingWords and Dictation exercise components. Inline score card. Replaces the Plan 04 stub with the full client-side implementation.

Purpose: Delivers the user-facing listening comprehension workflow: D-13 (listen-first 50% gate), D-14 (3 exercise types), D-15 (atomic transcript unlock), D-16 (inline score card). Satisfies LIST-02 (exercise types), LIST-03 (speed/replay), LIST-04 (locked transcript), LIST-05 (karaoke), LIST-06 (tap-to-SRS), LIST-07 (session submit).

Output: Five new component files + update to listening-item-client.tsx stub from Plan 04. Full listen-then-exercise-then-reveal flow working end to end in the browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-listening-comprehension/06-CONTEXT.md
@.planning/phases/06-listening-comprehension/06-RESEARCH.md
@.planning/phases/06-listening-comprehension/06-UI-SPEC.md
@.planning/phases/06-listening-comprehension/06-PATTERNS.md

<interfaces>
<!-- Key interfaces this plan consumes (from prior plans) -->

From apps/web/src/hooks/use-audio-player.ts (Plan 03):
- `useAudioPlayer(audioUrl: string)` returns `{ audioRef, state: AudioPlayerState, play, pause, seek, setSpeed, startRafLoop, stopRafLoop }`
- `AudioPlayerState.hasListenedEnough: boolean` — true after currentTime >= duration * 0.5

From apps/web/src/components/listening/audio-player.tsx (Plan 03):
- Props: `{ audioRef, state, audioUrl, play, pause, seek, setSpeed, startRafLoop, stopRafLoop }`

From apps/web/src/components/listening/transcript-panel.tsx (Plan 03):
- Props: `{ transcriptText, wordTimestamps, transcriptLocked, currentTime, onWordClick?, onCueSeek? }`

From packages/shared/src/listening.dto.ts (Plan 01):
- `ListeningItemDetailDto.questions: ListeningQuestionDto[]`
- `ListeningQuestionDto.exerciseType: 'MULTIPLE_CHOICE' | 'FILL_MISSING_WORDS' | 'DICTATION'`
- `ListeningQuestionDto.timestampSec: number | null` — used by DictationExercise for clip start
- `ListeningSessionCompleteSchema` — used to POST session completion

From apps/web/src/components/grammar/ — MultipleChoice reuse:
- `MultipleChoiceExercise` component already exists from Phase 4; import and reuse for MULTIPLE_CHOICE type

From UI-SPEC.md — ListeningSession / exercise layout:
- Section header: "Exercises" + progress counter "{n} of {total} answered"
- Start Exercises button: `Button variant="default" size="lg" disabled={!hasListenedEnough} className="mt-6 min-h-[44px] w-full max-w-xs"`
- After click: exercises section appears with `framer-motion opacity:0→1, y:8→0, duration:0.2s easeOut`
- Submit button: `Button variant="default" size="lg" disabled={!allAnswered} className="mt-6 min-h-[44px] w-full"`
- Submit loading: `Loader2` icon with `animate-spin`

From UI-SPEC.md — FillMissingWords:
- Prompt label: `text-sm text-muted-foreground mb-2` "Fill in the missing word"
- Blanked pill: `inline-block min-w-[80px] text-center rounded-md border-2 border-primary bg-primary/5 px-2 py-0.5 font-semibold`
- Option pills: `Button variant="outline" size="sm" min-h-[36px] rounded-full px-4 text-sm`
- Correct answer pill: `bg-emerald-50 border-emerald-500 text-emerald-800`
- Incorrect answer pill: `bg-red-50 border-red-500 text-red-700`

From UI-SPEC.md — DictationExercise:
- Mini player container: `flex items-center gap-3 rounded-lg bg-muted p-3 mb-4`
- Clip label: `"Audio clip · {n}s"` from `question.timestampSec`
- Textarea: `rows={2}` `resize-none text-sm` full width
- Check Answer button: `Button variant="default" size="sm" min-h-[36px]`
- Correct: `border-emerald-400 bg-emerald-50` on Textarea + `"Correct!"` in emerald
- Incorrect: `border-red-400 bg-red-50` on Textarea + `"Correct: \"{answer}\""` in muted

From UI-SPEC.md — ListeningScoreCard:
- framer-motion: `initial={{ opacity: 0, scale: 0.95 }} animate={{ opacity: 1, scale: 1 }} transition={{ duration: 0.3, ease: 'easeOut' }}`
- Score headline: `text-[28px] font-semibold text-foreground text-center`
- Sub-line: `text-sm text-muted-foreground text-center`
- Exercise breakdown: dl with 3 rows (Multiple Choice, Fill in the Blank, Dictation)
- Transcript reminder (when wordTimestamps not null): "The transcript is now unlocked..."
- Action buttons: "Try another item" (default) + "Browse all listening" (outline)

From UI-SPEC.md — WordPopover integration:
- Import WordPopover from `@/components/reading/word-popover` (Phase 5)
- If WordPopover does not exist yet, TranscriptPanel's `onWordClick` callback is the integration point; no WordPopover import needed in this plan
- Pass `onWordClick` from ListeningItemClient down to TranscriptPanel

Dictation scoring (RESEARCH Pattern 5):
- `import { distance } from 'fastest-levenshtein'`
- normalizeDictation: lowercase, strip punctuation `[.,!?;:"']`, collapse whitespace
- isCorrect: `distance(normalized(userAnswer), normalized(correctAnswer)) <= 2`
- Note: fastest-levenshtein is in apps/api; for client-side use, import from the package directly
  OR implement a 20-line native edit distance in the component (avoids bundle dependency)
  Decision: implement native `editDistance(a, b)` in dictation-exercise.tsx — simpler, no bundle risk
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FillMissingWords and Dictation exercise components</name>
  <files>
    apps/web/src/components/listening/exercises/fill-missing-words.tsx,
    apps/web/src/components/listening/exercises/dictation-exercise.tsx
  </files>
  <behavior>
    FillMissingWords:
    - Renders prompt with blanked word replaced by styled pill `<span>___</span>`
    - Renders 3 option buttons in flex-wrap row
    - After correct selection: pill shows answer word, options disabled, correct option turns emerald
    - After incorrect selection: selected option turns red, correct option revealed in emerald/50

    DictationExercise:
    - Renders mini clip player (Play/Pause button, "Audio clip · {clipDuration}s" label)
    - `onPlayClip()` calls `seek(question.timestampSec)` on the main audioRef then plays
    - Textarea with placeholder "Type what you hear..."
    - Check Answer button disabled until textarea has content
    - On check: `normalizeDictation(userAnswer)` vs `normalizeDictation(answer)`; editDistance <= 2 → correct
    - Correct state: Textarea border-emerald-400 bg-emerald-50, CheckCircle icon + "Correct!"
    - Incorrect state: Textarea border-red-400 bg-red-50, XCircle icon + `Correct: "{answer}"`
  </behavior>
  <read_first>
    apps/web/src/components/grammar/multiple-choice-exercise.tsx — read to understand the answer state pattern (correct/incorrect class application) and disabled-after-answer pattern
    apps/web/src/components/vocabulary/exercises/cloze-exercise.tsx — read to understand the fill-in-blank option button pattern
  </read_first>
  <action>
    Create `apps/web/src/components/listening/exercises/fill-missing-words.tsx` as a 'use client' component.
    Props: `{ question: ListeningQuestionDto; questionNumber: number; total: number; onAnswer: (isCorrect: boolean) => void }`.
    State: `selected: string | null`.
    The prompt string `question.prompt` contains `___` where the blank is. Render by splitting on `___` and inserting the styled blank pill `<span className="inline-block min-w-[80px] text-center rounded-md border-2 border-primary bg-primary/5 px-2 py-0.5 font-semibold">___</span>` before answer is selected, and `<span className="inline-block ... border-emerald-400 bg-emerald-50 text-emerald-800 px-2 py-0.5">{question.answer}</span>` after correct answer selected.
    Options: mix `[question.answer, ...question.distractors]` shuffled once on mount (useMemo with empty dep array). Render as flex-wrap row of `Button variant="outline" size="sm"` with `rounded-full px-4 min-h-[36px]`. On click: set `selected`, call `onAnswer(selected === question.answer)`, disable all buttons.
    Correct option after selection: `className="bg-emerald-50 border-emerald-500 text-emerald-800"`. Incorrect selected: `"bg-red-50 border-red-500 text-red-700"`. Wrong options after answer revealed: if user selected wrong, also show correct option with `"bg-emerald-50/50 border-emerald-300 text-emerald-700"`.
    Include explanation row: if `question.explanation` is not null and answer is selected, show `<p className="text-sm text-muted-foreground mt-3">{question.explanation}</p>`.

    Create `apps/web/src/components/listening/exercises/dictation-exercise.tsx` as a 'use client' component.
    Props: `{ question: ListeningQuestionDto; questionNumber: number; total: number; onAnswer: (isCorrect: boolean) => void; onPlayClip: (startSec: number) => void }`.
    Implement `editDistance(a: string, b: string): number` as a native 20-line Wagner-Fischer implementation (2D array DP). This avoids a client-side bundle dependency on fastest-levenshtein (which lives in apps/api).
    Implement `normalizeDictation(text: string): string` — lowercase, strip punctuation `[.,!?;:"'-]`, collapse whitespace, trim.
    State: `userAnswer: string`, `isChecked: boolean`, `isCorrect: boolean | null`.
    Mini player: `<div className="flex items-center gap-3 rounded-lg bg-muted p-3 mb-4">`. Play button: `Button variant="ghost" size="icon" className="size-8"` with Play icon. On click: `onPlayClip(question.timestampSec ?? 0)`. Label: `<span className="text-sm text-muted-foreground">Audio clip · {estimateDuration(question.timestampSec)}s</span>`. `estimateDuration` returns 10 (default clip length estimate).
    Textarea: `<Textarea rows={2} className={cn("resize-none text-sm", isChecked && isCorrect ? "border-emerald-400 bg-emerald-50" : isChecked ? "border-red-400 bg-red-50" : "")} value={userAnswer} onChange={(e) => setUserAnswer(e.target.value)} disabled={isChecked} placeholder="Type what you hear..." aria-label="Type what you hear" />`.
    Check Answer button: `Button variant="default" size="sm" className="min-h-[36px]"` disabled when `!userAnswer.trim() || isChecked`. On click: compute editDistance(normalize(userAnswer), normalize(question.answer)); `isCorrect = distance <= 2`; set isChecked = true; call `onAnswer(isCorrect)`.
    After check: if correct — `<div className="flex items-center gap-1 mt-2"><CheckCircle className="size-4 text-emerald-500" /><span className="text-sm text-emerald-700">Correct!</span></div>`. If incorrect — `<div className="flex items-center gap-1 mt-2"><XCircle className="size-4 text-red-500" /><span className="text-sm text-muted-foreground">Correct: "{question.answer}"</span></div>`.
  </action>
  <verify>
    <automated>ls /home/tuyen/Desktop/Apps/english-learning/apps/web/src/components/listening/exercises/fill-missing-words.tsx && ls /home/tuyen/Desktop/Apps/english-learning/apps/web/src/components/listening/exercises/dictation-exercise.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `fill-missing-words.tsx` exists with `'use client'` directive, exports `FillMissingWordsExercise`
    - `fill-missing-words.tsx` splits `question.prompt` on `___` to render the blank pill inline
    - `dictation-exercise.tsx` exists with `'use client'` directive, exports `DictationExercise`
    - `dictation-exercise.tsx` implements `editDistance` as a native function (no `import from 'fastest-levenshtein'` in web app)
    - `dictation-exercise.tsx` calls `onPlayClip(question.timestampSec ?? 0)` when mini player is clicked
    - `grep "dangerouslySetInnerHTML" apps/web/src/components/listening/exercises/*.tsx` returns 0 results
    - `pnpm --filter @repo/web typecheck` exits 0
  </acceptance_criteria>
  <done>FillMissingWords exercise with inline blank rendering and 3 option pills; Dictation exercise with mini clip player, native edit distance scoring, and answer reveal.</done>
</task>

<task type="auto">
  <name>Task 2: ListeningSession orchestrator + ListeningScoreCard + ListeningItemClient (full implementation)</name>
  <files>
    apps/web/src/components/listening/listening-session.tsx,
    apps/web/src/components/listening/listening-score-card.tsx,
    apps/web/src/components/listening/listening-item-client.tsx,
    apps/web/src/app/(dashboard)/listening/[itemId]/page.tsx
  </files>
  <read_first>
    apps/web/src/components/vocabulary/practice-session.tsx — read lines 118-210 to understand the accumulate-then-submit session state pattern
    apps/web/src/components/grammar/grammar-session-results.tsx — read to understand the framer-motion score card entrance pattern (initial/animate/transition props)
    apps/web/src/components/listening/listening-item-client.tsx — read current state (Plan 04 stub) before replacing
    apps/web/src/app/(dashboard)/listening/[itemId]/page.tsx — read current state (passes item to ListeningItemClient stub) before updating
  </read_first>
  <action>
    Create `apps/web/src/components/listening/listening-score-card.tsx` as a 'use client' component.
    Props: `{ score: number; total: number; xpEarned: number; breakdown: { multipleChoice: [number,number]; fillMissingWords: [number,number]; dictation: [number,number] }; hasWordTimestamps: boolean; onReset: () => void }`.
    Use framer-motion: `import { motion } from 'framer-motion'`. Outer: `<motion.div initial={{ opacity: 0, scale: 0.95 }} animate={{ opacity: 1, scale: 1 }} transition={{ duration: 0.3, ease: 'easeOut' }} className="max-w-lg mx-auto mt-8" role="region" aria-label="Exercise results">`.
    Inside Card:
    - Score headline: `<p className="text-[28px] font-semibold text-foreground text-center">{score}/{total} correct</p>`
    - Sub-line: `<p className="text-sm text-muted-foreground text-center">{pct}% accuracy · {xpEarned} XP earned</p>`
    - Exercise breakdown dl: 3 rows (Multiple Choice, Fill in the Blank, Dictation) each as `<div className="flex items-center justify-between text-sm"><dt className="text-muted-foreground">{label}</dt><dd className="text-foreground font-medium">{correct}/{total}</dd></div>`
    - Transcript reminder (conditional `hasWordTimestamps`): `<p className="text-sm text-muted-foreground text-center mt-2">The transcript is now unlocked. Tap any word to add it to your vocabulary.</p>`
    - Action buttons: `<Button variant="default" size="lg" className="min-h-[44px] w-full" onClick={() => router.push('/listening')}>Try another item</Button>` + `<Button variant="outline" size="lg" className="min-h-[44px] w-full" onClick={() => router.push('/listening')}>Browse all listening</Button>`

    Create `apps/web/src/components/listening/listening-session.tsx` as a 'use client' component.
    Props: `{ item: ListeningItemDetailDto; audioRef: RefObject<HTMLAudioElement>; hasListenedEnough: boolean; seek: (t: number) => void }`.
    State: `exercisesActivated: boolean`, `answers: SessionAnswer[]`, `submitting: boolean`, `submitError: string`, `isComplete: boolean`.
    `type SessionAnswer = { questionId: string; isCorrect: boolean; userAnswer?: string }`.

    "Start Exercises" button section (before exercisesActivated):
    `<Button disabled={!hasListenedEnough} size="lg" className="mt-6 min-h-[44px] w-full max-w-xs" onClick={() => setExercisesActivated(true)}>Start Exercises</Button>`. Below button, when not hasListenedEnough: `<p className="text-sm text-muted-foreground text-center mt-2">Listen to at least half the audio to unlock exercises.</p>`. aria-disabled + title on button when disabled.

    After exercisesActivated (wrapped in framer-motion `opacity:0→1, y:8→0, duration:0.2s easeOut`):
    - Exercise section header: `<h2 className="text-sm font-semibold text-foreground">Exercises</h2><p className="text-sm text-muted-foreground">{answeredCount} of {total} answered</p>`
    - Map `item.questions` sorted by `sortOrder`. For each question, render the appropriate exercise component in a Card with `p-6`:
      - `MULTIPLE_CHOICE`: `<MultipleChoiceExercise>` (import from `@/components/grammar/multiple-choice-exercise` — reuse from Phase 4 per D-14)
      - `FILL_MISSING_WORDS`: `<FillMissingWordsExercise>`
      - `DICTATION`: `<DictationExercise onPlayClip={(startSec) => seek(startSec)}>` — passes seek to DictationExercise for mini clip playback
    - Each exercise calls `onAnswer(questionId, isCorrect, userAnswer?)` which pushes to answers state
    - allAnswered: `answers.length >= item.questions.length`
    - Submit button (visible when exercisesActivated, disabled when not allAnswered): see UI-SPEC section 2f
    - On submit: POST to `/api/listening/sessions/complete` with `{ contentId: item.id, score: correctCount, accuracy: (correctCount/total)*100, attempts: answers }`. On success: call `onSubmitComplete()`. On error: show toast error, re-enable button.

    If isComplete: render `<ListeningScoreCard>` with computed breakdown from answers array.

    Create the full `apps/web/src/components/listening/listening-item-client.tsx` replacing the Plan 04 stub.
    Props: `{ item: ListeningItemDetailDto }`.
    Uses `useAudioPlayer(item.audioUrl)` hook.
    State: `transcriptLocked: boolean` (starts true), `sessionComplete: boolean`.
    Layout (single column):
    - `<AudioPlayer audioRef={audioRef} state={state} audioUrl={item.audioUrl} play={play} pause={pause} seek={seek} setSpeed={setSpeed} startRafLoop={startRafLoop} stopRafLoop={stopRafLoop} />`
    - `<TranscriptPanel transcriptText={item.transcriptText} wordTimestamps={item.wordTimestamps} transcriptLocked={transcriptLocked} currentTime={state.currentTime} onWordClick={handleWordClick} onCueSeek={seek} />`
    - `<ListeningSession item={item} audioRef={audioRef} hasListenedEnough={state.hasListenedEnough} seek={seek} onSubmitComplete={() => setTranscriptLocked(false)} />`
    Note: `onSubmitComplete` is the D-15 atomic unlock — setting transcriptLocked=false triggers the TranscriptPanel blur→clear transition and activates karaoke sync.
    `handleWordClick(word: string, sentence: string)`: if Phase 5 WordPopover exists at `@/components/reading/word-popover`, open it; otherwise use a toast notification as a placeholder ("Added to SRS" feedback).

    Finally, update `apps/web/src/app/(dashboard)/listening/[itemId]/page.tsx` to import the new full `ListeningItemClient` (replacing the stub reference). No other changes needed to the page file.
  </action>
  <verify>
    <automated>grep -c "transcriptLocked" /home/tuyen/Desktop/Apps/english-learning/apps/web/src/components/listening/listening-item-client.tsx && grep -c "hasListenedEnough" /home/tuyen/Desktop/Apps/english-learning/apps/web/src/components/listening/listening-session.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `listening-item-client.tsx` contains `useAudioPlayer` import and `transcriptLocked` state
    - `listening-item-client.tsx` passes `onSubmitComplete={() => setTranscriptLocked(false)}` to ListeningSession (D-15 atomic unlock)
    - `listening-session.tsx` renders "Start Exercises" button with `disabled={!hasListenedEnough}` (D-13)
    - `listening-session.tsx` dispatches MULTIPLE_CHOICE to MultipleChoiceExercise, FILL_MISSING_WORDS to FillMissingWordsExercise, DICTATION to DictationExercise
    - `listening-session.tsx` POSTs to `/api/listening/sessions/complete` on submit
    - `listening-score-card.tsx` uses framer-motion with `initial={{ opacity: 0, scale: 0.95 }}`
    - `listening-score-card.tsx` renders 3-row exercise type breakdown (Multiple Choice, Fill in the Blank, Dictation)
    - `pnpm --filter @repo/web typecheck` exits 0
    - `grep "dangerouslySetInnerHTML" apps/web/src/components/listening/**/*.tsx` returns 0 results
  </acceptance_criteria>
  <done>Full listening item experience wired: AudioPlayer + TranscriptPanel + ListeningSession with all 3 exercise types + atomic transcript unlock on submit + inline score card.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client ListeningSession → Next.js relay route → NestJS | JWT forwarded via cookie; NestJS validates and extracts userId from JWT — not from body |
| User transcript tap → WordPopover → SRS enroll | Word string passed as plain text; POST /api/srs/enroll validates wordId server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | XSS | FillMissingWords prompt rendering | mitigate | `question.prompt` split on literal `___` string, not parsed as HTML; each part rendered as `{text}` React text node |
| T-06-15 | XSS | DictationExercise answer reveal | mitigate | `question.answer` rendered as `{question.answer}` text node in feedback span — not dangerouslySetInnerHTML |
| T-06-16 | Tampering | Client-side Dictation score | accept | `isCorrect` computed client-side and sent in `attempts[]`; NestJS server recomputes accuracy from attempts[] in Plan 02 — client score is not blindly trusted for accuracy calculation |
| T-06-W6-SC | Tampering | npm/pip/cargo installs | mitigate | Native editDistance implementation avoids client-side fastest-levenshtein import; no new npm installs in this plan |
</threat_model>

<verification>
After all tasks complete:
1. `grep "transcriptLocked" apps/web/src/components/listening/listening-item-client.tsx` — at least 2 results (state declaration + prop pass)
2. `grep "hasListenedEnough" apps/web/src/components/listening/listening-session.tsx` — at least 1 result in disabled condition
3. `grep "opacity: 0" apps/web/src/components/listening/listening-score-card.tsx` — 1 result (framer-motion)
4. `grep "editDistance" apps/web/src/components/listening/exercises/dictation-exercise.tsx` — at least 2 results (definition + call)
5. `grep "dangerouslySetInnerHTML" apps/web/src/components/listening/exercises/fill-missing-words.tsx` — 0 results
6. `grep "FILL_MISSING_WORDS\|DICTATION\|MULTIPLE_CHOICE" apps/web/src/components/listening/listening-session.tsx` — 3 results
7. `pnpm --filter @repo/web typecheck` exits 0
</verification>

<success_criteria>
- FillMissingWords exercise renders blank pill inline + 3 option pills with correct/incorrect state (LIST-02)
- DictationExercise mini clip player seeks main audioRef + Textarea + Check Answer with edit distance ≤ 2 scoring (LIST-02)
- ListeningSession activates exercises only after 50% audio threshold (D-13, LIST-03)
- Submit triggers atomic transcriptLocked=false (D-15, LIST-04)
- TranscriptPanel receives current transcriptLocked=false after submit and reveals blur (LIST-04, LIST-05)
- ListeningScoreCard renders inline with framer-motion, exercise breakdown, and transcript reminder (D-16, LIST-07)
- All 3 exercise types dispatch correctly in ListeningSession (LIST-02)
</success_criteria>

<output>
Create `.planning/phases/06-listening-comprehension/06-06-SUMMARY.md` when done
</output>
