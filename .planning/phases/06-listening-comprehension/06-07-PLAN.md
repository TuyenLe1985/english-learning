---
phase: 06-listening-comprehension
plan: 07
type: execute
wave: 6
depends_on:
  - "06-05"
  - "06-06"
files_modified: []
autonomous: false
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
  - LIST-04
  - LIST-05
  - LIST-06
  - LIST-07

must_haves:
  truths:
    - "Full test suite (API + web) passes GREEN"
    - "Pipeline validate:listening passes ≥90% success on at least 3 of 4 sources"
    - "At least 200 listening items seeded in the database with exercises"
    - "Human confirms audio plays, transcript unlocks, karaoke highlights words"
    - "Human confirms Start Exercises gate enforces 50% listen threshold"
    - "Human confirms word tap opens WordPopover and Add to SRS works"
  artifacts:
    - path: ".planning/phases/06-listening-comprehension/06-07-SUMMARY.md"
      provides: "Phase completion summary"
      contains: "Phase 6 complete"
  key_links:
    - from: "pnpm turbo test"
      to: "all LIST-01 through LIST-07 test assertions"
      via: "Vitest test runner"
      pattern: "LIST-0[1-7]"
---

<objective>
Phase 6 end-to-end verification: run the full test suite, validate crawl selectors, execute the seed pipeline, and human-verify the complete listening comprehension experience in the browser.

Purpose: Final gate before marking Phase 6 complete. Ensures all 7 requirements (LIST-01 through LIST-07) pass both automated tests and human verification.

Output: Green full test suite, 200+ seeded items, human approval of the listening experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-listening-comprehension/06-CONTEXT.md
@.planning/phases/06-listening-comprehension/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and pipeline validation</name>
  <files></files>
  <read_first>
    .planning/phases/06-listening-comprehension/06-VALIDATION.md — read to confirm all required test assertions and commands
  </read_first>
  <action>
    Run the full test suite: `pnpm turbo test`. All tests must pass GREEN. If any test fails, fix the root cause before proceeding.

    Run the pipeline validation: `pnpm --filter @repo/api pipeline:validate:listening`. This tests 20 sample URLs per source. Acceptable outcome: ≥3 of 4 sources pass at ≥90% success rate. If a source fails validation, log it as a known gap (selector may have changed) but do not block the phase — proceed with the passing sources only.

    Run specific Phase 6 test assertions to confirm all requirements:
    - `pnpm --filter @repo/api test -- --reporter verbose listening.service.spec` — confirms LIST-01, LIST-07 GREEN
    - `pnpm --filter @repo/web test -- --reporter verbose use-audio-player.test` — confirms LIST-03, LIST-05 GREEN
    - `pnpm --filter @repo/web test -- --reporter verbose transcript-panel.test` — confirms LIST-04, LIST-06 GREEN

    Run the seed pipeline for a sample (not full bulk — use limit 25 per source for verification):
    Set a temporary env var `CRAWL_LIMIT=25` or edit the crawl CLI entry point limit, then:
    `pnpm --filter @repo/api pipeline:crawl:listening`
    `pnpm --filter @repo/api pipeline:seed:listening`

    Confirm seed results: query the DB to verify at least 50 items seeded (25 per source × 2 working sources minimum):
    `npx prisma studio --schema packages/database/prisma/schema.prisma` — open in browser, check ListeningContent table count.
    Or use: `node -e "const { PrismaClient } = require('./packages/database/generated/client'); const p = new PrismaClient(); p.listeningContent.count().then(n => { console.log('Items:', n); p.$disconnect(); })"`

    MinIO CORS check: run the storage setup script if it exists, or manually verify:
    `docker exec el_minio mc alias set local http://localhost:9000 minioadmin minioadmin && docker exec el_minio mc admin config get local/ site` (to confirm MinIO is running)
    If CORS is not configured, add the CORS policy for the audio bucket so browser `<audio>` tags can load presigned URLs without CORS errors.
  </action>
  <verify>
    <automated>pnpm turbo test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm turbo test` exits 0 (all workspaces green)
    - `pnpm --filter @repo/api test -- listening.service.spec` shows all tests GREEN
    - `pnpm --filter @repo/web test -- use-audio-player.test` shows all tests GREEN
    - `pnpm --filter @repo/web test -- transcript-panel.test` shows all tests GREEN
    - Pipeline validate produces output showing at least 3/4 sources with ≥80% success rate
    - `ListeningContent` table has at least 50 rows after sample seed run
  </acceptance_criteria>
  <done>Full test suite green; pipeline selector validation completed; sample seed run populated ≥50 items with exercises.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete listening experience</name>
  <what-built>
    The complete Phase 6 listening comprehension feature:
    - Browse page at /listening with CEFR, topic, and content type filters showing seeded items
    - Item page at /listening/[itemId] with sticky audio player, locked transcript, exercise section
    - Audio playback with speed controls (0.75×/1×/1.25×/1.5×) and seek bar
    - Listen-first flow: Start Exercises button activates after 50% listen threshold
    - Three exercise types: Multiple Choice, Fill in the Blank, Dictation
    - Submit all exercises → transcript unlocks atomically → karaoke word highlighting
    - Inline score card with exercise type breakdown
    - Tap any unlocked transcript word → WordPopover for SRS enrollment
  </what-built>
  <how-to-verify>
    Run `docker compose up -d` and then `pnpm dev` from the project root. Verify all 7 requirements:

    **LIST-01 — Browse + Filters:**
    1. Visit http://localhost:3000/listening
    2. Confirm the page loads with a grid of listening items
    3. Select B2 from CEFR tabs — grid updates to B2 items only
    4. Select "News Report" from content type dropdown — grid updates
    5. Confirm at least one item card shows: CEFR badge, content type label, duration, exercise count

    **LIST-02 — 3 Exercise Types:**
    6. Click any item card to open the listening item page
    7. Start playing the audio (skip ahead to 50%+ if needed using the seek bar)
    8. Click "Start Exercises"
    9. Confirm at least one Multiple Choice, one Fill in the Blank, and one Dictation exercise is visible

    **LIST-03 — Speed Control + Section Replay:**
    10. In the sticky player bar, click the "1.5×" speed toggle — audio should speed up
    11. Confirm "1.5×" button becomes highlighted (active state)
    12. Click "0.75×" — audio slows down, 0.75× button highlighted
    13. After transcript unlocks (post-submit), click any sentence — audio seeks to that sentence start

    **LIST-04 — Transcript Locked During Exercise:**
    14. Before submitting exercises, confirm transcript text is blurred and shows "Transcript is locked until you complete the exercises." overlay
    15. Submit all exercises — confirm transcript reveals with a blur→clear animation (≈300ms transition)

    **LIST-05 — Karaoke Sync:**
    16. After transcript unlocks, resume audio playback
    17. Confirm the currently-spoken word is highlighted in amber (`bg-amber-200`) as audio plays
    18. Confirm the highlight advances word by word, not sentence by sentence
    19. Confirm the transcript auto-scrolls to keep the active word visible

    **LIST-06 — Tap Word → SRS:**
    20. After transcript unlocks, click any word in the transcript
    21. Confirm a WordPopover opens showing the word definition (or "Definition not yet in our vocabulary library" if not found)
    22. Click "Add to SRS" — confirm success feedback ("Added to SRS")

    **LIST-07 — Score Stored:**
    23. Submit exercises — confirm the inline score card appears with "{n}/{total} correct"
    24. Navigate away and return to the same item — confirm prior score/accuracy is displayed in the progress section
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 7 requirements pass.
    Type "partial [describe what failed]" if some requirements pass but others need fixes.
    Type "blocked [describe the issue]" if a critical issue prevents further verification.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → MinIO presigned URL | Audio loads directly from MinIO via presigned URL; CORS must be configured for localhost:3000 |
| Browser → /api/listening/sessions/complete relay → NestJS | JWT cookie forwarded; NestJS validates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | Information Disclosure | Browser Network tab — presigned audio URL | accept | 1-hour expiry; URL is a bearer credential for one audio file, limited blast radius |
| T-06-18 | Tampering | Pipeline crawled content | mitigate | ClassifierService normalizes and validates all crawled text before DB insertion; flaggedForReview for low-confidence items |
| T-06-W7-SC | Tampering | npm/pip/cargo installs | mitigate | No new installs in verification plan; all packages installed in prior plans |
</threat_model>

<verification>
Phase 6 complete when:
1. `pnpm turbo test` exits 0
2. `pnpm --filter @repo/api test -- listening.service.spec` all tests GREEN
3. `pnpm --filter @repo/web test -- use-audio-player.test` all tests GREEN
4. `pnpm --filter @repo/web test -- transcript-panel.test` all tests GREEN
5. Pipeline validate:listening ≥3/4 sources at ≥80% success
6. At least 50 seeded items in ListeningContent table
7. Human verification checkpoint returns "approved"
</verification>

<success_criteria>
- All 7 requirement IDs fully satisfied: LIST-01 (browse+filters), LIST-02 (3 exercise types), LIST-03 (speed+replay), LIST-04 (transcript locked), LIST-05 (karaoke sync), LIST-06 (tap-to-SRS), LIST-07 (score stored)
- Full Vitest suite passes GREEN
- 50+ listening items seeded with exercises (200-300 target after full seed run)
- Human verifies all 7 requirements in the browser
- Phase 6 ready to mark complete in STATE.md
</success_criteria>

<output>
Create `.planning/phases/06-listening-comprehension/06-07-SUMMARY.md` when done.
After human checkpoint approval, update `.planning/STATE.md` to mark Phase 6 complete and update `.planning/ROADMAP.md` Phase 6 entry to show completed plans.
</output>
