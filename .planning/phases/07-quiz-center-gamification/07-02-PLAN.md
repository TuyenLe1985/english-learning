---
phase: 07-quiz-center-gamification
plan: 02
type: tdd
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/api/src/gamification/gamification.service.ts
  - apps/api/src/gamification/gamification.service.spec.ts
autonomous: true
requirements: [GAME-01, GAME-02, GAME-03, GAME-04, GAME-05]
must_haves:
  truths:
    - "awardXp creates one XpEvent, increments User.xpTotal, updates User.level, and writes one ActivityLog atomically"
    - "awardXp returns levelUp:true exactly when crossing a 100-XP level boundary"
    - "CEFR multiplier is applied: B1 base, B2 1.5x, C1 2x, rounded"
    - "checkAchievements awards each of the 8 badges at most once (idempotent)"
    - "seedAchievements upserts all 8 Achievement rows"
  artifacts:
    - path: "apps/api/src/gamification/gamification.service.ts"
      provides: "awardXp(), checkAchievements(), seedAchievements(), streak detection"
      min_lines: 120
      contains: "awardXp"
  key_links:
    - from: "apps/api/src/gamification/gamification.service.ts"
      to: "prisma.user.update"
      via: "xpTotal increment + level write in $transaction"
      pattern: "xpTotal: \\{ increment"
    - from: "apps/api/src/gamification/gamification.service.ts"
      to: "prisma.userAchievement.upsert"
      via: "idempotent badge award"
      pattern: "userAchievement\\.upsert"
---

<objective>
Implement GamificationService — the single authoritative writer of XP, level, achievements, and activity log. `awardXp()` atomically creates an XpEvent, increments `User.xpTotal`, recomputes `User.level`, and writes an `ActivityLog` row. `checkAchievements()` evaluates the 8 milestone conditions idempotently. `seedAchievements()` upserts the 8 definitions. This drives the 07-01 RED tests to GREEN.

Purpose: GAME-01 through GAME-05 all depend on this service. The critical Phase 7 fix (RESEARCH Pitfall 2) is that existing modules create XpEvents but never increment User.xpTotal — this service is the centralized correction that all 5 modules will call (07-04).
Output: Fully implemented `gamification.service.ts` with passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-quiz-center-gamification/07-CONTEXT.md
@.planning/phases/07-quiz-center-gamification/07-RESEARCH.md
@.planning/phases/07-quiz-center-gamification/07-PATTERNS.md

<interfaces>
<!-- Verified schema facts (packages/database/prisma/schema.prisma) -->
User: xpTotal Int @default(0), level Int @default(1), cefrLevel CefrLevel @default(B1)
UserAchievement: @@unique([userId, achievementId]) — compound key name `userId_achievementId`
CardState enum: New | Learning | Review | Relearning  (no "mastered" — use state Review for vocab-100/500 count)
UserVocabularyItem: @@unique([userId, wordId]) — count enrolled words per user
ActivityLog: activityType String, skillArea SkillArea?, loggedAt DateTime @default(now())
SrsCard: userId, state CardState — count where state = Review for "mastered word" semantics

<!-- From 07-01: gamification.constants.ts exports XP_RATES, CEFR_MULTIPLIERS, calculateXp, levelForXp, ACHIEVEMENT_DEFINITIONS -->
<!-- Service return shape: { xpEarned, oldLevel, newLevel, levelUp } -->
</interfaces>

@apps/api/src/gamification/gamification.constants.ts
@apps/api/src/gamification/gamification.service.spec.ts
@apps/api/src/grammar/grammar.service.ts
@apps/api/src/listening/listening.service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement awardXp with atomic XP + level + activity log</name>
  <files>apps/api/src/gamification/gamification.service.ts</files>
  <read_first>
    - apps/api/src/gamification/gamification.service.spec.ts (the RED tests this must satisfy)
    - apps/api/src/gamification/gamification.constants.ts (levelForXp, calculateXp — use these, do not re-derive)
    - .planning/phases/07-quiz-center-gamification/07-PATTERNS.md (awardXp pattern lines 273-308)
    - apps/api/src/listening/listening.service.ts (lines 232-242 — the OLD direct xpEvent.create pattern being replaced)
  </read_first>
  <behavior>
    - awardXp(userId, amount, reason, skillArea, sourceRef?) reads current { xpTotal, level } via user.findUniqueOrThrow.
    - Computes oldLevel = user.level, newXpTotal = xpTotal + amount, newLevel = levelForXp(newXpTotal).
    - Runs prisma.$transaction([ xpEvent.create, user.update({ xpTotal: { increment: amount }, level: newLevel }), activityLog.create ]) — exactly these three writes.
    - activityLog.create writes { userId, activityType: reason maps to 'LESSON_COMPLETE' for lesson/quiz events, skillArea }. Use activityType derived from reason (lesson/quiz/session → 'LESSON_COMPLETE'; srs_review → 'SRS_REVIEW') so streak detection (Task 2) counts active days.
    - Returns { xpEarned: amount, oldLevel, newLevel, levelUp: newLevel > oldLevel }.
    - Edge: amount 0 still writes XpEvent and ActivityLog (no level change).
  </behavior>
  <action>
    Implement `awardXp` in GamificationService per the PATTERNS.md awardXp pattern (lines 273-308) and the behavior above. Use `levelForXp` from constants for level calc (do NOT inline the formula). The three writes go in a single `this.prisma.$transaction([...])` array form. The `activityType` is determined from `reason`: any reason matching lesson/quiz/session → `'LESSON_COMPLETE'`; `'srs_review'` → `'SRS_REVIEW'`. sourceRef defaults to null when undefined. Keep the method signature returning the 4-field object the quiz/module callers consume.
  </action>
  <verify>
    <automated>pnpm --filter api test -- --run gamification.service 2>&1 | grep -iE "awardXp|level|multiplier|pass|fail" | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - awardXp tests in gamification.service.spec.ts pass (single XpEvent, xpTotal increment, level write, one activityLog, all in one $transaction).
    - Level-up boundary test passes: xpTotal=98 + amount=5 → newLevel 2, levelUp true.
    - awardXp source uses `levelForXp(` (grep confirms — no inlined `Math.floor(.../100)` duplicate of the formula).
    - awardXp source contains `xpTotal: { increment` inside a $transaction.
  </acceptance_criteria>
  <done>awardXp GREEN: atomic three-write transaction, correct level boundary detection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement checkAchievements (idempotent) + streak detection + seedAchievements</name>
  <files>apps/api/src/gamification/gamification.service.ts</files>
  <read_first>
    - apps/api/src/gamification/gamification.service.spec.ts (achievement idempotency + streak tests)
    - apps/api/src/gamification/gamification.constants.ts (ACHIEVEMENT_DEFINITIONS)
    - .planning/phases/07-quiz-center-gamification/07-PATTERNS.md (checkAchievements lines 311-342; streak detection lines 345-369)
    - .planning/phases/07-quiz-center-gamification/07-RESEARCH.md (Pitfall 3 race condition; A7 ActivityLog follow-up; D-13 conditions)
  </read_first>
  <behavior>
    - checkAchievements(userId, { type, metadata? }) returns AchievementDto[] of NEWLY awarded badges only.
    - Uses a `tryAward(slug)` helper: count UserAchievement before, upsert (create {}, update {}) on `userId_achievementId`, count after; if after>before push the AchievementDto and return true.
    - Conditions (D-13):
      - 'first-lesson': any LESSON_COMPLETE-type event (type === 'LESSON_COMPLETE' or 'QUIZ_COMPLETE').
      - 'vocab-100' / 'vocab-500': count SrsCard where { userId, state: 'Review' } >= 100 / 500 (CardState Review = mastered semantics; UserVocabularyItem count is the fallback if SrsCard query is empty — count UserVocabularyItem where userId).
      - 'grammar-master': metadata.masteryPct >= 80 OR any GrammarProgress for user with masteryPct >= 80.
      - 'reading-complete': first ReadingProgress with completedAt != null (type READING or count >= 1).
      - 'listening-complete': first ListeningProgress with completedAt != null.
      - 'streak-7' / 'streak-30': checkStreak(userId, 7) / checkStreak(userId, 30) returns true.
    - checkStreak: query ActivityLog last (target+1) days, dedupe to calendar days, count consecutive days from today backward using date-fns differenceInCalendarDays (PATTERNS lines 345-369).
    - seedAchievements(): upsert each ACHIEVEMENT_DEFINITIONS entry on `slug` (create with name/description/xpReward, update name/description/xpReward).
  </behavior>
  <action>
    Implement `checkAchievements`, the private `checkStreak`, and `seedAchievements` per the PATTERNS.md sections and behavior above. ALWAYS use `prisma.userAchievement.upsert` (never create) per RESEARCH Pitfall 3 — the count-before/count-after delta detects new awards. Import `differenceInCalendarDays` from `date-fns`. For vocab counts use `prisma.srsCard.count({ where: { userId, state: 'Review' } })`. Map each achievement slug to its condition; only call tryAward when the condition holds so already-earned badges are not re-pushed. seedAchievements loops ACHIEVEMENT_DEFINITIONS with upsert on slug.
  </action>
  <verify>
    <automated>pnpm --filter api test -- --run gamification.service 2>&1 | tail -12 && grep -c "userAchievement.upsert\|differenceInCalendarDays\|seedAchievements" apps/api/src/gamification/gamification.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - All gamification.service.spec.ts tests pass (GREEN).
    - 'first-lesson' awarded once: second checkAchievements call with same event returns empty array.
    - Source contains `userAchievement.upsert` (never a bare `userAchievement.create`).
    - Source imports and uses `differenceInCalendarDays`.
    - `seedAchievements` upserts on `slug` for all 8 definitions.
    - Full api suite for this file green: `pnpm --filter api test -- --run gamification.service` exits 0.
  </acceptance_criteria>
  <done>checkAchievements, streak detection, and seedAchievements GREEN and idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→GamificationService | userId is passed by trusted callers (controllers that already resolved it from JWT); service never reads userId from request body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering | awardXp concurrent calls | mitigate | Prisma `$transaction` + `increment` is atomic; concurrent awards cannot produce a lost update or negative XP (RESEARCH §Security) |
| T-07-03 | Tampering | Achievement double-award (race) | mitigate | `userAchievement.upsert` on `@@unique([userId, achievementId])` — second concurrent write no-ops (RESEARCH Pitfall 3) |
| T-07-04 | Elevation of Privilege | XP for another user | mitigate | awardXp callers pass JWT-derived userId only; verified in 07-03/07-04 wiring |
| T-07-SC | Tampering | npm installs | mitigate | No new packages (date-fns already installed); slopcheck N/A |
</threat_model>

<verification>
- `pnpm --filter api test -- --run gamification.service` exits 0 (all GREEN)
- awardXp uses single $transaction with three writes
- checkAchievements uses upsert (idempotent)
</verification>

<success_criteria>
- GAME-01: CEFR multiplier applied and rounded
- GAME-02: User.xpTotal + User.level updated atomically
- GAME-03: 8 achievements awarded idempotently
- GAME-05: exactly one XpEvent per awardXp call
</success_criteria>

<output>
Create `.planning/phases/07-quiz-center-gamification/07-02-SUMMARY.md` when done
</output>
