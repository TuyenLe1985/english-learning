---
phase: 07-quiz-center-gamification
plan: 04
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - apps/api/src/grammar/grammar.service.ts
  - apps/api/src/grammar/grammar.module.ts
  - apps/api/src/reading/reading.service.ts
  - apps/api/src/reading/reading.module.ts
  - apps/api/src/listening/listening.service.ts
  - apps/api/src/listening/listening.module.ts
  - apps/api/src/vocabulary/vocabulary.service.ts
  - apps/api/src/vocabulary/vocabulary.module.ts
  - apps/api/src/srs/srs.service.ts
  - apps/api/src/srs/srs.module.ts
autonomous: true
requirements: [GAME-01, GAME-02, GAME-03, GAME-04, GAME-05]
must_haves:
  truths:
    - "Completing a grammar lesson awards XP via GamificationService and increments User.xpTotal"
    - "Completing a reading passage awards XP and may unlock reading-complete achievement"
    - "Completing a listening exercise awards XP and may unlock listening-complete achievement"
    - "A Good/Easy SRS review awards 3 flat XP"
    - "No module writes XpEvent directly anymore — all XP flows through GamificationService.awardXp"
  artifacts:
    - path: "apps/api/src/grammar/grammar.service.ts"
      provides: "completeSession wired to gamification.awardXp + checkAchievements"
      contains: "gamification.awardXp"
    - path: "apps/api/src/listening/listening.service.ts"
      provides: "completeSession wired to gamification (replaces direct xpEvent.create)"
      contains: "gamification.awardXp"
  key_links:
    - from: "apps/api/src/listening/listening.service.ts"
      to: "gamification.awardXp"
      via: "replaces prisma.xpEvent.create"
      pattern: "gamification\\.awardXp"
    - from: "apps/api/src/srs/srs.service.ts"
      to: "gamification.awardXp"
      via: "3 XP flat on Good/Easy"
      pattern: "gamification\\.awardXp"
---

<objective>
Retrofit the four existing session-complete endpoints (Grammar, Vocabulary, Reading, Listening) and the SRS review endpoint to call GamificationService.awardXp() + checkAchievements(), replacing the broken direct xpEvent.create() pattern that never incremented User.xpTotal (RESEARCH Pitfall 2). Each module imports GamificationModule. SRS Good/Easy ratings award 3 flat XP.

Purpose: GAME-01/02/05 are only satisfied platform-wide if ALL modules route XP through the single authoritative writer (D-14). Without this, profile XP stays 0 for non-quiz activity and achievements never trigger. This is Phase 7's cross-cutting concern.
Output: Five service files wired, five module files importing GamificationModule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-quiz-center-gamification/07-CONTEXT.md
@.planning/phases/07-quiz-center-gamification/07-RESEARCH.md
@.planning/phases/07-quiz-center-gamification/07-PATTERNS.md

<interfaces>
From 07-02: GamificationService.awardXp(userId, amount, reason, skillArea, sourceRef?) returns { xpEarned, oldLevel, newLevel, levelUp }; checkAchievements(userId, { type, metadata? }) returns AchievementDto[].
VERIFIED current state of each service:
- listening.service.ts (lines 232-242): currently prisma.xpEvent.create with amount round(score*10), reason listening_session, skillArea LISTENING, sourceRef contentId. REPLACE this block.
- grammar.service.ts completeSession (lines 199-263): upserts GrammarProgress with masteryPct; does NOT create XpEvent currently. ADD awardXp + checkAchievements (grammar-master keys off masteryPct >= 80).
- reading.service.ts completeSession (lines 143-170): upserts ReadingProgress; NO XpEvent currently. ADD awardXp + checkAchievements (reading-complete).
- vocabulary.service.ts: has CATEGORY_MAP; the practice completeSession lives in srs.service.ts completeSession. ADD awardXp on vocabulary practice completion in the path the vocab UI calls.
- srs.service.ts submitReview(userId, cardId, rating) where rating is Again|Hard|Good|Easy (lines 158-190): ADD 3 flat XP on Good or Easy.
XP_RATES (07-01): QUIZ_CORRECT 5, QUIZ_SESSION_BONUS 10, LESSON_COMPLETE 20, SRS_REVIEW 3.
calculateXp(baseRate, cefrLevel) from gamification.constants. Lesson XP = calculateXp(LESSON_COMPLETE, userCefrLevel). SRS XP = XP_RATES.SRS_REVIEW flat (no multiplier, D-10).
userCefrLevel: resolve service-side via a User.cefrLevel lookup (authoritative). Do not trust a body-supplied level.
</interfaces>

@apps/api/src/listening/listening.service.ts
@apps/api/src/grammar/grammar.service.ts
@apps/api/src/reading/reading.service.ts
@apps/api/src/vocabulary/vocabulary.service.ts
@apps/api/src/srs/srs.service.ts
@apps/api/src/listening/listening.module.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire Grammar + Reading + Listening completeSession to gamification</name>
  <files>apps/api/src/grammar/grammar.service.ts, apps/api/src/grammar/grammar.module.ts, apps/api/src/reading/reading.service.ts, apps/api/src/reading/reading.module.ts, apps/api/src/listening/listening.service.ts, apps/api/src/listening/listening.module.ts</files>
  <read_first>
    - apps/api/src/listening/listening.service.ts (lines 232-242 — the direct xpEvent.create to REPLACE)
    - apps/api/src/grammar/grammar.service.ts (lines 199-263 — completeSession + GrammarProgress upsert + masteryPct)
    - apps/api/src/reading/reading.service.ts (lines 143-170 — completeSession upsert)
    - .planning/phases/07-quiz-center-gamification/07-PATTERNS.md (module-wiring change pattern lines 434-462)
    - apps/api/src/gamification/gamification.constants.ts (XP_RATES, calculateXp)
  </read_first>
  <behavior>
    Grammar.completeSession: after GrammarProgress upsert, resolve userCefrLevel (User.cefrLevel lookup), compute xp = calculateXp(XP_RATES.LESSON_COMPLETE, userCefrLevel), call gamification.awardXp(userId, xp, 'grammar_lesson', 'GRAMMAR', lessonId), then gamification.checkAchievements(userId, { type: 'LESSON_COMPLETE', metadata: { masteryPct: newMasteryPct, skillArea: 'GRAMMAR' } }). Extend the returned result additively with xpEarned, levelUp, newLevel, newAchievements.
    Reading.completeSession: after ReadingProgress upsert, xp = calculateXp(LESSON_COMPLETE, userCefrLevel), awardXp(userId, xp, 'reading_session', 'READING', passageId), checkAchievements(userId, { type: 'READING', metadata: { passageId } }). Extend return additively.
    Listening.completeSession: REMOVE the direct prisma.xpEvent.create block; replace with xp = calculateXp(LESSON_COMPLETE, userCefrLevel), awardXp(userId, xp, 'listening_session', 'LISTENING', contentId), checkAchievements(userId, { type: 'LISTENING', metadata: { contentId } }). Keep server-side accuracy recompute unchanged. Return xpEarned from the awardXp result.
    All three: constructor adds private readonly gamification: GamificationService; module imports GamificationModule.
  </behavior>
  <action>
    For grammar/reading/listening: (1) add GamificationService to the service constructor; (2) import GamificationModule in each module imports array; (3) import XP_RATES and calculateXp from ../gamification/gamification.constants; (4) in completeSession resolve the user CEFR level via a User.cefrLevel lookup, then call gamification.awardXp(...) and gamification.checkAchievements(...) per the behavior. For listening specifically, DELETE the existing prisma.xpEvent.create block (around lines 232-242) — awardXp now owns XpEvent creation. Extend each completeSession return object with xpEarned, levelUp, newLevel, newAchievements additively without breaking existing fields. Update the existing service spec files to mock GamificationService and assert awardXp is called with the correct skillArea and reason.
  </action>
  <verify>
    <automated>pnpm --filter api test -- --run grammar.service reading.service listening.service 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grammar.service.ts, reading.service.ts, listening.service.ts each contain `gamification.awardXp` and `gamification.checkAchievements`.
    - listening.service.ts no longer contains `prisma.xpEvent.create` (grep count is 0).
    - grammar.module.ts, reading.module.ts, listening.module.ts each import `GamificationModule`.
    - The three service spec files pass with the new wiring assertions.
  </acceptance_criteria>
  <done>Grammar/Reading/Listening completion routes XP through GamificationService; listening's broken direct XpEvent write removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Vocabulary practice + SRS review to gamification</name>
  <files>apps/api/src/vocabulary/vocabulary.service.ts, apps/api/src/vocabulary/vocabulary.module.ts, apps/api/src/srs/srs.service.ts, apps/api/src/srs/srs.module.ts</files>
  <read_first>
    - apps/api/src/srs/srs.service.ts (lines 158-215 — submitReview + completeSession; rating values Again/Hard/Good/Easy)
    - apps/api/src/vocabulary/vocabulary.service.ts (lines 1-60 — CATEGORY_MAP + structure)
    - apps/api/src/gamification/gamification.constants.ts (XP_RATES.SRS_REVIEW 3 flat, XP_RATES.LESSON_COMPLETE)
    - .planning/phases/07-quiz-center-gamification/07-PATTERNS.md (srs.service.ts change lines 465-477)
    - .planning/phases/07-quiz-center-gamification/07-CONTEXT.md (D-10 SRS Good/Easy = 3 XP flat, no CEFR multiplier)
  </read_first>
  <behavior>
    SRS.submitReview: after the FSRS card update write, if rating === 'Good' or rating === 'Easy', call gamification.awardXp(userId, XP_RATES.SRS_REVIEW, 'srs_review', 'VOCABULARY', cardId). Flat 3, no CEFR multiplier (D-10). Again/Hard award nothing.
    SRS.completeSession (vocabulary practice batch): on completion, award lesson-level XP once — xp = calculateXp(XP_RATES.LESSON_COMPLETE, userCefrLevel), awardXp(userId, xp, 'vocabulary_session', 'VOCABULARY', null), checkAchievements(userId, { type: 'LESSON_COMPLETE', metadata: { skillArea: 'VOCABULARY' } }). Resolve userCefrLevel via User lookup; note completeSession currently ignores _userId — change the signature to use userId.
  </behavior>
  <action>
    Add GamificationService to the SrsService constructor (and VocabularyService constructor if vocabulary practice completion is owned there). Import GamificationModule in srs.module.ts and vocabulary.module.ts. In srs.service.ts submitReview, award 3 flat XP on Good/Easy via gamification.awardXp using XP_RATES.SRS_REVIEW. In srs.service.ts completeSession, replace the unused `_userId` parameter with `userId`, resolve User.cefrLevel, and award vocabulary lesson XP + checkAchievements. Update srs.service.spec.ts: mock GamificationService and assert awardXp is called with 3 on Good/Easy and NOT called on Again/Hard. If completeSession's caller (controller) currently passes a placeholder for userId, update the controller call site to pass the real JWT userId.
  </action>
  <verify>
    <automated>pnpm --filter api test -- --run srs.service vocabulary.service 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - srs.service.ts submitReview calls `gamification.awardXp` with `XP_RATES.SRS_REVIEW` on Good/Easy and the spec asserts no award on Again/Hard.
    - srs.module.ts and vocabulary.module.ts import `GamificationModule`.
    - srs.service.ts completeSession uses a real `userId` parameter (no `_userId` placeholder).
    - srs.service.spec.ts and vocabulary.service.spec.ts pass.
  </acceptance_criteria>
  <done>SRS Good/Easy awards 3 flat XP; vocabulary practice completion awards lesson XP through GamificationService.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module controllers -> service -> GamificationService | userId always JWT-derived; cefrLevel resolved server-side from User table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Tampering | Body-supplied CEFR level inflating XP | mitigate | Services resolve User.cefrLevel from DB, never from request body |
| T-07-11 | Elevation of Privilege | Awarding XP to a foreign user | mitigate | userId passed from req.user.userId only; SRS card and progress lookups already scope by userId |
| T-07-12 | Repudiation | XP awarded without audit trail | mitigate | awardXp creates one XpEvent + one ActivityLog per award (GAME-05) |
| T-07-SC | Tampering | npm installs | mitigate | No new packages this phase; slopcheck N/A |
</threat_model>

<verification>
- `pnpm --filter api test -- --run grammar.service reading.service listening.service srs.service vocabulary.service` exits 0
- `grep -rn "prisma.xpEvent.create\|this.prisma.xpEvent.create" apps/api/src` returns only GamificationService (zero in module services)
- All five modules import GamificationModule
</verification>

<success_criteria>
- GAME-01: lesson XP CEFR-weighted across all modules
- GAME-02: User.xpTotal increments for every learning activity, not just quizzes
- GAME-03/04: achievements (reading-complete, listening-complete, vocab-100/500, grammar-master) trigger from real activity
- GAME-05: every award produces an XpEvent audit record
</success_criteria>

<output>
Create `.planning/phases/07-quiz-center-gamification/07-04-SUMMARY.md` when done
</output>
