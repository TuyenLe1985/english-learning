---
phase: 07-quiz-center-gamification
plan: 05
type: execute
wave: 3
depends_on: ["07-01"]
files_modified:
  - apps/web/src/app/(dashboard)/quiz/page.tsx
  - apps/web/src/app/(dashboard)/quiz/[sessionId]/page.tsx
  - apps/web/src/app/(dashboard)/quiz/[sessionId]/results/page.tsx
  - apps/web/src/app/api/quiz/sessions/start/route.ts
  - apps/web/src/app/api/quiz/sessions/[sessionId]/complete/route.ts
  - apps/web/src/app/api/quiz/sessions/[sessionId]/mistakes/route.ts
  - apps/web/src/components/quiz/quiz-type-selector.tsx
  - apps/web/src/components/quiz/quiz-session.tsx
  - apps/web/src/components/quiz/quiz-question.tsx
  - apps/web/src/components/quiz/quiz-progress-bar.tsx
  - apps/web/src/components/quiz/quiz-score-card.tsx
autonomous: true
requirements: [QUIZ-01, QUIZ-02, QUIZ-03]
must_haves:
  truths:
    - "User can open /quiz, pick Mixed Skill or a topic, and start a 10-question quiz"
    - "User answers one question at a time with a 'N of 10' progress bar; answered options lock and Next advances"
    - "Submitting the final question stores the session and shows a score card with accuracy and XP earned"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/quiz/page.tsx"
      provides: "Quiz browse page rendering QuizTypeSelector"
      contains: "QuizTypeSelector"
    - path: "apps/web/src/components/quiz/quiz-session.tsx"
      provides: "Session orchestrator: paging, answer accumulation, batch submit"
      contains: "currentIndex"
    - path: "apps/web/src/app/api/quiz/sessions/start/route.ts"
      provides: "Next.js relay to NestJS POST /api/quiz/sessions/start"
      contains: "quiz/sessions/start"
  key_links:
    - from: "apps/web/src/components/quiz/quiz-type-selector.tsx"
      to: "/api/quiz/sessions/start"
      via: "fetch on Start Quiz then router.push to /quiz/[sessionId]"
      pattern: "quiz/sessions/start"
    - from: "apps/web/src/components/quiz/quiz-session.tsx"
      to: "/api/quiz/sessions/[sessionId]/complete"
      via: "batch POST on question 10"
      pattern: "complete"
---

<objective>
Build the user-facing quiz vertical slice: the `/quiz` browse page (6 quiz-type cards), the `/quiz/[sessionId]` paginated session flow (one question at a time, progress bar, answer lock, Next/Submit), the `/quiz/[sessionId]/results` score card, and the three Next.js relay routes proxying to NestJS. After this plan, a user can take a full quiz end-to-end and see their score.

Purpose: QUIZ-01/02/03 from the user's perspective. This is the thinnest end-to-end slice that makes the quiz usable. Gamification UI overlays (XP toast, level-up modal, mistake review, profile) land in 07-06.
Output: 3 pages, 3 relay routes, 5 quiz components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-quiz-center-gamification/07-CONTEXT.md
@.planning/phases/07-quiz-center-gamification/07-PATTERNS.md
@.planning/phases/07-quiz-center-gamification/07-UI-SPEC.md

<interfaces>
From 07-01 shared (@repo/shared): QuizStartResponseDto { sessionId, questions: QuizQuestionDto[] }; QuizQuestionDto { questionRef, skillArea, prompt, answer, distractors[], explanation }; QuizCompleteResponseDto { score, accuracy, xpEarned, levelUp, newLevel, newAchievements[], incorrectAnswers[] }.
Quiz types (D-07, UI-SPEC Screen 1): 'MIXED' (Shuffle icon), 'technology' (Monitor), 'travel' (Plane), 'business' (Briefcase), 'daily-communication' (MessageCircle), 'education' (GraduationCap).
Reuse: MultipleChoiceExercise from @/components/grammar/multiple-choice-exercise (props per PATTERNS.md lines 727-746: question { id, exerciseType: 'MULTIPLE_CHOICE', prompt, answer, distractors, explanation, difficulty, xpReward }, onCorrect, onIncorrect). CefrBadge from @/components/cefr-badge. Progress, Card, Button, Skeleton, Badge from @/components/ui/*.
Relay pattern: fetchWithAuth + INTERNAL_API_URL from @/lib/api-client; auth() guard returning 401 if no session.
Session state (D-06, PATTERNS.md lines 657-702): hold { questions, currentIndex, answers[], startedAt } in client state; on question 10 answered, batch POST to /api/quiz/sessions/[sessionId]/complete with { timeTakenSec, answers }.
</interfaces>

@apps/web/src/app/(dashboard)/listening/page.tsx
@apps/web/src/app/(dashboard)/listening/[itemId]/page.tsx
@apps/web/src/app/api/listening/sessions/complete/route.ts
@apps/web/src/components/listening/listening-session.tsx
@apps/web/src/components/listening/listening-score-card.tsx
@apps/web/src/components/grammar/multiple-choice-exercise.tsx
@apps/web/src/lib/api-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the three Next.js relay routes</name>
  <files>apps/web/src/app/api/quiz/sessions/start/route.ts, apps/web/src/app/api/quiz/sessions/[sessionId]/complete/route.ts, apps/web/src/app/api/quiz/sessions/[sessionId]/mistakes/route.ts</files>
  <read_first>
    - apps/web/src/app/api/listening/sessions/complete/route.ts (exact relay pattern to copy)
    - apps/web/src/lib/api-client.ts (lines 69-93 — fetchWithAuth + INTERNAL_API_URL)
    - .planning/phases/07-quiz-center-gamification/07-PATTERNS.md (relay route lines 613-649)
  </read_first>
  <action>
    Create three relay routes following the listening relay pattern exactly. start/route.ts: POST, auth() guard (401 if no session), parse JSON body, fetchWithAuth to `${INTERNAL_API_URL}/api/quiz/sessions/start`, return JSON or error. complete/route.ts: POST with `[sessionId]` dynamic param — read params, relay to `${INTERNAL_API_URL}/api/quiz/sessions/${sessionId}/complete`. mistakes/route.ts: GET with `[sessionId]` — relay (no body, method GET) to `${INTERNAL_API_URL}/api/quiz/sessions/${sessionId}/mistakes`. All three use INTERNAL_API_URL (not NEXT_PUBLIC) and forward the cookie header.
  </action>
  <verify>
    <automated>pnpm --filter web exec tsc --noEmit 2>&1 | grep -i "quiz/sessions" | head -5; ls apps/web/src/app/api/quiz/sessions/start/route.ts apps/web/src/app/api/quiz/sessions/[sessionId]/complete/route.ts apps/web/src/app/api/quiz/sessions/[sessionId]/mistakes/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three route files exist and export the correct HTTP method handler (POST, POST, GET).
    - Each calls auth() and returns 401 JSON when there is no session.
    - Each uses fetchWithAuth + INTERNAL_API_URL targeting the matching /api/quiz/... path.
    - `pnpm --filter web exec tsc --noEmit` reports no errors in these files.
  </acceptance_criteria>
  <done>Three relay routes proxy quiz start/complete/mistakes to NestJS with auth enforcement.</done>
</task>

<task type="auto">
  <name>Task 2: Build quiz browse page + QuizTypeSelector + relay session start</name>
  <files>apps/web/src/app/(dashboard)/quiz/page.tsx, apps/web/src/components/quiz/quiz-type-selector.tsx</files>
  <read_first>
    - apps/web/src/app/(dashboard)/listening/page.tsx (Server Component auth-gate pattern)
    - .planning/phases/07-quiz-center-gamification/07-UI-SPEC.md (Screen 1 Quiz Browse — card grid, 6 types, icons, copy "Start Quiz")
    - .planning/phases/07-quiz-center-gamification/07-PATTERNS.md (quiz page lines 548-575; quiz-type-selector role-match)
    - apps/web/src/components/cefr-badge.tsx (CefrBadge usage)
  </read_first>
  <action>
    Create `quiz/page.tsx` as a Server Component: auth() guard (redirect /login if none); render page heading "Quiz Center" (20px semibold per UI-SPEC) + subtitle "Test your knowledge across all skills."; render `<QuizTypeSelector />`. Centered `max-w-3xl mx-auto`.
    Create `quiz-type-selector.tsx` ('use client'): a 2-col (md+) / 1-col card grid of the 6 quiz types with lucide icons (Shuffle/Monitor/Plane/Briefcase/MessageCircle/GraduationCap per UI-SPEC). Each card: icon 32x32, name (14px semibold), "10 questions" label (12px muted), CefrBadge for the user level, full card is the click target. On click: POST to /api/quiz/sessions/start with { type }, then `router.push(/quiz/${sessionId})`. Use the copy "Start Quiz" and the empty/error states from the Copywriting Contract. Show a loading state on the clicked card while the request is in flight. Pass the user's CEFR level into the selector (fetch from session or accept as prop from the Server Component).
  </action>
  <verify>
    <automated>pnpm --filter web exec tsc --noEmit 2>&1 | grep -iE "quiz/page|quiz-type" | head -5; grep -c "Shuffle\|Monitor\|Plane\|Briefcase\|MessageCircle\|GraduationCap" apps/web/src/components/quiz/quiz-type-selector.tsx; grep -c "quiz/sessions/start" apps/web/src/components/quiz/quiz-type-selector.tsx</automated>
  </verify>
  <acceptance_criteria>
    - quiz/page.tsx is a Server Component with auth() redirect guard and renders QuizTypeSelector.
    - quiz-type-selector.tsx references all 6 lucide icons (grep count >= 6).
    - On card click it POSTs to /api/quiz/sessions/start and navigates to /quiz/[sessionId] using the returned sessionId.
    - Heading text "Quiz Center" and CTA copy "Start Quiz" present (UI-SPEC Copywriting Contract).
  </acceptance_criteria>
  <done>User can open /quiz and start any of the 6 quiz types, landing on the session page.</done>
</task>

<task type="auto">
  <name>Task 3: Build session flow + results page (QuizSession, QuizQuestion, QuizProgressBar, QuizScoreCard)</name>
  <files>apps/web/src/app/(dashboard)/quiz/[sessionId]/page.tsx, apps/web/src/app/(dashboard)/quiz/[sessionId]/results/page.tsx, apps/web/src/components/quiz/quiz-session.tsx, apps/web/src/components/quiz/quiz-question.tsx, apps/web/src/components/quiz/quiz-progress-bar.tsx, apps/web/src/components/quiz/quiz-score-card.tsx</files>
  <read_first>
    - apps/web/src/components/listening/listening-session.tsx (lines 79-309 — orchestrator: accumulate answers, batch submit)
    - apps/web/src/components/listening/listening-score-card.tsx (lines 50-116 — score card + breakdown table + entrance animation)
    - apps/web/src/components/grammar/multiple-choice-exercise.tsx (props contract for delegation)
    - .planning/phases/07-quiz-center-gamification/07-UI-SPEC.md (Screen 2 session, Screen 3 results, Interaction Contract state machine, Animation Contract)
    - .planning/phases/07-quiz-center-gamification/07-PATTERNS.md (quiz-session lines 653-703, quiz-question lines 707-746, quiz-score-card lines 751-789)
  </read_first>
  <action>
    QuizProgressBar: shadcn Progress with value (currentIndex/10)*100 + label "Question N of 10" (UI-SPEC copy).
    QuizQuestion ('use client'): polymorphic renderer that delegates ALL question types to MultipleChoiceExercise (grammar/vocab/reading/listening questions are all MC in quiz context). Map QuizQuestionDto into the MC props (id=questionRef, exerciseType 'MULTIPLE_CHOICE', prompt, answer, distractors, explanation, difficulty 1, xpReward 5). Header shows skill-area Badge (variant secondary) + CefrBadge. onCorrect/onIncorrect emit a SessionAnswer { questionRef, skillArea, isCorrect, userAnswer, correctAnswer }.
    QuizSession ('use client'): the state machine from the Interaction Contract (IDLE/LOADING/ACTIVE/SUBMITTING/RESULTS). Hold { questions, currentIndex, answers[], startedAt }. The session page fetches questions — since startSession already returned questions in the type selector, pass them via sessionStorage or re-fetch is not available; instead the session page receives sessionId and the QuizSession reads the questions handed off from the start response. Implementation: store the QuizStartResponseDto in sessionStorage keyed by sessionId at start time (in QuizTypeSelector), and QuizSession reads it on mount; if missing, show the load-error copy. On answer: lock options, append answer, show "Next ->" (or "Submit Quiz" on question 10). On submit: POST to /api/quiz/sessions/[sessionId]/complete with { timeTakenSec, answers }, store the QuizCompleteResponseDto (sessionStorage keyed by sessionId), then router.push to /quiz/[sessionId]/results. No back-navigation once an answer is locked (D-06).
    QuizScoreCard ('use client'): Framer Motion entrance (initial opacity 0 scale 0.95, animate opacity 1 scale 1, 0.3s easeOut). Score headline "{score}/10 correct" at text-[28px] font-semibold; sub-line "{pct}% accuracy · +{xp} XP · {time}". Per-skill breakdown table (Grammar/Vocabulary/Reading/Listening correct/attempted). CTA buttons: "Review Mistakes" (primary, only if incorrect count > 0 — links to mistake review built in 07-06), "Try Another Quiz" (outline -> /quiz), "Back to Quiz Center" (ghost -> /quiz).
    quiz/[sessionId]/page.tsx and results/page.tsx: Server Components with auth() guard rendering the client components, passing sessionId.
    NOTE: XP toast and level-up modal are added in 07-06 — leave a clearly marked mount point (e.g. render results with the QuizCompleteResponseDto available) but do not implement the toast/modal here.
  </action>
  <verify>
    <automated>pnpm --filter web exec tsc --noEmit 2>&1 | grep -iE "quiz" | head -8; grep -c "MultipleChoiceExercise" apps/web/src/components/quiz/quiz-question.tsx; grep -c "currentIndex\|sessions/.*complete\|setAnswers\|answers" apps/web/src/components/quiz/quiz-session.tsx</automated>
  </verify>
  <acceptance_criteria>
    - QuizQuestion delegates to MultipleChoiceExercise (grep >= 1) and never re-implements MC option rendering.
    - QuizSession accumulates answers in state, shows progress, locks answered options, and on question 10 submits a single batch POST to /api/quiz/sessions/[sessionId]/complete.
    - QuizScoreCard shows "{score}/10 correct" at text-[28px] font-semibold with a Framer Motion entrance matching the Animation Contract, and renders the per-skill breakdown table.
    - quiz/[sessionId]/page.tsx and results/page.tsx are Server Components with auth() guards.
    - `pnpm --filter web exec tsc --noEmit` reports no quiz errors.
  </acceptance_criteria>
  <done>User completes a 10-question quiz one question at a time and sees a score card with accuracy, XP, and per-skill breakdown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Next.js relay routes | Quiz start/complete/mistakes payloads cross here; relay enforces auth and forwards to NestJS which re-validates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Spoofing | Relay routes without auth | mitigate | Every relay calls auth() and returns 401 before forwarding (NestJS JwtAuthGuard re-checks) |
| T-07-14 | Information Disclosure | Client-held answers/session in sessionStorage | accept | Session results contain only the user's own quiz data; cleared on navigation; no PII or secrets |
| T-07-15 | Tampering | Client-supplied accuracy/score on submit | mitigate | Server (07-03) recomputes accuracy/score; client values are advisory only |
| T-07-SC | Tampering | npm installs | mitigate | No new packages this phase (framer-motion/lucide already installed); slopcheck N/A |
</threat_model>

<verification>
- `pnpm --filter web exec tsc --noEmit` clean for quiz routes/pages/components
- Browse -> start -> answer 10 -> results works against the 07-03 API (verified end-to-end in 07-07)
- QuizQuestion delegates to MultipleChoiceExercise (no MC re-implementation)
</verification>

<success_criteria>
- QUIZ-01/02: user starts mixed-skill or topic quiz from /quiz
- QUIZ-03: completing the quiz stores the session and surfaces score, accuracy, time, per-skill breakdown
</success_criteria>

<output>
Create `.planning/phases/07-quiz-center-gamification/07-05-SUMMARY.md` when done
</output>
