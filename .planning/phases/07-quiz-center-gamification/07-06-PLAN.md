---
phase: 07-quiz-center-gamification
plan: 06
type: execute
wave: 4
depends_on: ["07-03", "07-04", "07-05"]
files_modified:
  - apps/web/src/components/gamification/xp-toast.tsx
  - apps/web/src/components/gamification/level-up-modal.tsx
  - apps/web/src/components/gamification/achievement-badge.tsx
  - apps/web/src/components/gamification/achievement-grid.tsx
  - apps/web/src/components/gamification/level-badge.tsx
  - apps/web/src/components/gamification/xp-progress-bar.tsx
  - apps/web/src/components/quiz/mistake-review.tsx
  - apps/web/src/app/(dashboard)/quiz/[sessionId]/results/page.tsx
  - apps/web/src/app/(dashboard)/profile/page.tsx
  - apps/web/src/app/(dashboard)/profile/profile-form.tsx
  - apps/api/src/gamification/gamification.controller.ts
  - apps/api/src/gamification/gamification.module.ts
  - apps/web/src/app/api/profile/achievements/route.ts
autonomous: true
requirements: [GAME-02, GAME-03, GAME-04, QUIZ-04]
must_haves:
  truths:
    - "On quiz completion, a +XP toast slides up bottom-right and auto-dismisses after 4 seconds"
    - "On level-up, a modal shows the new level and dismisses on click or timeout"
    - "User can review each incorrect quiz question with the correct answer and explanation"
    - "Profile shows level badge, XP progress bar toward next level, and an 8-badge achievement grid with earned dates"
  artifacts:
    - path: "apps/web/src/components/gamification/xp-toast.tsx"
      provides: "Framer Motion +XP toast, bottom-right, 4s auto-dismiss"
      contains: "AnimatePresence"
    - path: "apps/web/src/components/gamification/achievement-grid.tsx"
      provides: "8-badge grid with locked/unlocked state"
      contains: "AchievementBadge"
    - path: "apps/web/src/components/quiz/mistake-review.tsx"
      provides: "Incorrect question re-display with explanation"
      contains: "explanation"
  key_links:
    - from: "apps/web/src/app/(dashboard)/quiz/[sessionId]/results/page.tsx"
      to: "XpToast + LevelUpModal"
      via: "render on mount from QuizCompleteResponseDto flags"
      pattern: "XpToast"
    - from: "apps/web/src/app/(dashboard)/profile/profile-form.tsx"
      to: "AchievementGrid + XpProgressBar + LevelBadge"
      via: "render gamification UI from profile data + achievements fetch"
      pattern: "AchievementGrid"
---

<objective>
Build the gamification UI overlays and profile additions: the XP toast (Framer Motion slide-up), level-up modal (shadcn Dialog), mistake-review screen (QUIZ-04), and profile-page level badge + XP progress bar + achievement grid. Add a NestJS gamification endpoint to fetch a user's achievements (all 8 with locked/unlocked state) and a Next.js relay for it.

Purpose: Completes the gamification feedback loop (GAME-02/03/04) and mistake review (QUIZ-04). After this plan, the quiz results screen rewards the user visibly and the profile reflects real progress.
Output: 6 gamification components, mistake-review component, results-page wiring, profile additions, gamification achievements endpoint + relay.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-quiz-center-gamification/07-CONTEXT.md
@.planning/phases/07-quiz-center-gamification/07-PATTERNS.md
@.planning/phases/07-quiz-center-gamification/07-UI-SPEC.md

<interfaces>
From 07-01/07-03: QuizCompleteResponseDto { score, accuracy, xpEarned, levelUp, newLevel, newAchievements: AchievementDto[], incorrectAnswers: QuizQuestionDto[] }; AchievementDto { id, slug, name, description, iconUrl, xpReward }; QuizQuestionDto { questionRef, skillArea, prompt, answer, distractors[], explanation }.
From 07-02: GamificationService.seedAchievements() + the 8 ACHIEVEMENT_DEFINITIONS. Achievement table has slug/name/description/iconUrl/xpReward. UserAchievement has earnedAt.
D-09 level formula: progress within level = xpTotal % 100, out of 100, toward Math.min(100, level+1).
D-11 toast/modal: toast bottom-right (initial y50 opacity0, animate y0 opacity1, exit opacity0, 0.3s), auto-dismiss 4s. Level-up modal appears ~1s after toast, auto-closes ~5s. Achievement toast(s) 500ms after XP toast, stacked above.
UI-SPEC Screen 4 mistake review: user wrong answer bg-destructive/10 border-destructive/30; correct answer bg-green-50 border-green-300 text-green-700; explanation text-sm muted italic.
UI-SPEC Screen 5 profile: level badge w-12 h-12 bg-primary rounded-full; XP bar "Level {n} · {current}/{next} XP"; achievement grid 4-col md / 2-col mobile.
Existing profile: apps/web/src/app/(dashboard)/profile/page.tsx (Server Component) hands to profile-form.tsx (client). UserProfileDto includes xpTotal and level (verified packages/shared/src/user.dto.ts).
</interfaces>

@apps/web/src/components/listening/listening-session.tsx
@apps/web/src/components/vocabulary/session-results.tsx
@apps/web/src/components/cefr-badge.tsx
@apps/web/src/components/grammar/grammar-session-results.tsx
@apps/web/src/app/(dashboard)/profile/profile-form.tsx
@apps/api/src/gamification/gamification.module.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build gamification feedback components + results-page wiring + mistake review</name>
  <files>apps/web/src/components/gamification/xp-toast.tsx, apps/web/src/components/gamification/level-up-modal.tsx, apps/web/src/components/gamification/level-badge.tsx, apps/web/src/components/quiz/mistake-review.tsx, apps/web/src/app/(dashboard)/quiz/[sessionId]/results/page.tsx</files>
  <read_first>
    - apps/web/src/components/listening/listening-session.tsx (AnimatePresence + motion.div block, lines 207-215)
    - apps/web/src/components/vocabulary/session-results.tsx (Dialog block, lines 20-30)
    - apps/web/src/components/cefr-badge.tsx (badge color-tier structure for LevelBadge)
    - apps/web/src/components/reading/questions-section.tsx (correct/incorrect answer styling for mistake review)
    - .planning/phases/07-quiz-center-gamification/07-PATTERNS.md (xp-toast lines 794-835, level-up-modal lines 839-874, level-badge lines 878-916, mistake-review role-match)
    - .planning/phases/07-quiz-center-gamification/07-UI-SPEC.md (Animation Contract, Screen 4 mistake review, Achievement Toast Sequencing)
  </read_first>
  <action>
    XpToast ('use client'): AnimatePresence + motion.div fixed bottom-4 right-4 z-50, bg-primary text-primary-foreground, initial y50 opacity0 -> animate y0 opacity1 -> exit opacity0, 0.3s. useEffect 4s timeout to dismiss, onClick dismisses. Copy "+{n} XP". role status, aria-live polite.
    LevelUpModal ('use client'): shadcn Dialog, opens ~1s after mount, auto-closes ~5s. DialogTitle "Level {n}!" (UI-SPEC copy), body "You've reached Level {n}. Keep learning to unlock more!", renders LevelBadge size lg with ring-2 ring-primary.
    LevelBadge ('use client'): pill/circular badge showing "Lv. {level}" with color tiers (1-33 blue, 34-66 emerald, 67-100 violet) mirroring CefrBadge; sizes sm/md/lg.
    MistakeReview ('use client'): takes QuizQuestionDto[] of incorrect answers + the user's wrong answers; for each, re-render the full question (prompt + all options), highlight the user's wrong answer (bg-destructive/10 border-destructive/30) and the correct answer (bg-green-50 border-green-300 text-green-700), and show explanation (text-sm muted italic). Empty state copy "All correct! No mistakes to review." Apply the aria-labels from UI-SPEC Answer Option Accessibility.
    results/page.tsx wiring: read the QuizCompleteResponseDto handed off from 07-05 (sessionStorage keyed by sessionId). Render QuizScoreCard, then on mount fire XpToast (always, with xpEarned), LevelUpModal (only if levelUp true, ~1s delay), and achievement unlock toast(s) for each newAchievements entry (reuse XpToast styling or a small variant, 500ms after XP toast, stacked above per Sequencing). Render MistakeReview inline (or behind the "Review Mistakes" CTA) using incorrectAnswers. If sessionStorage is empty (refresh), fall back to GET /api/quiz/sessions/[sessionId]/mistakes for the mistake list and show a minimal score summary.
  </action>
  <verify>
    <automated>pnpm --filter web exec tsc --noEmit 2>&1 | grep -iE "gamification|mistake|results" | head -8; grep -c "AnimatePresence" apps/web/src/components/gamification/xp-toast.tsx; grep -c "Dialog" apps/web/src/components/gamification/level-up-modal.tsx; grep -c "explanation" apps/web/src/components/quiz/mistake-review.tsx</automated>
  </verify>
  <acceptance_criteria>
    - xp-toast.tsx uses AnimatePresence + motion.div, fixed bottom-right, 4s auto-dismiss; copy "+{n} XP".
    - level-up-modal.tsx uses shadcn Dialog, title "Level {n}!", renders LevelBadge.
    - mistake-review.tsx renders prompt + options + correct-answer highlight + explanation for each incorrect item, with the "All correct!" empty state.
    - results/page.tsx fires XpToast on mount, LevelUpModal only when levelUp is true, and achievement toasts for newAchievements.
    - `pnpm --filter web exec tsc --noEmit` clean for these files.
  </acceptance_criteria>
  <done>Quiz results reward the user with XP toast, level-up modal, achievement toasts, and reviewable mistakes.</done>
</task>

<task type="auto">
  <name>Task 2: Gamification achievements endpoint + relay + profile additions</name>
  <files>apps/api/src/gamification/gamification.controller.ts, apps/api/src/gamification/gamification.module.ts, apps/web/src/app/api/profile/achievements/route.ts, apps/web/src/components/gamification/achievement-badge.tsx, apps/web/src/components/gamification/achievement-grid.tsx, apps/web/src/components/gamification/xp-progress-bar.tsx, apps/web/src/app/(dashboard)/profile/page.tsx, apps/web/src/app/(dashboard)/profile/profile-form.tsx</files>
  <read_first>
    - apps/api/src/listening/listening.controller.ts (controller + JwtAuthGuard pattern)
    - apps/api/src/gamification/gamification.service.ts (from 07-02 — add a getUserAchievements method if absent)
    - apps/web/src/app/(dashboard)/profile/profile-form.tsx (current profile render — where to add gamification UI)
    - .planning/phases/07-quiz-center-gamification/07-PATTERNS.md (achievement-badge lines 955-993, achievement-grid lines 998-1027, xp-progress-bar lines 920-951)
    - .planning/phases/07-quiz-center-gamification/07-UI-SPEC.md (Screen 5 Profile additions)
  </read_first>
  <action>
    Backend: add a `GET /api/gamification/achievements` endpoint (JwtAuthGuard) on a new gamification.controller.ts that returns all 8 achievements merged with the user's UserAchievement.earnedAt (null when not earned). Add a `getUserAchievements(userId)` method to GamificationService: load all Achievement rows + the user's UserAchievement rows, return AchievementDto[] each with an `earnedAt: Date | null`. Register the controller in GamificationModule (add controllers array). Ensure seedAchievements() runs so the 8 rows exist — invoke it via an OnModuleInit hook in GamificationService OR document a one-time `pnpm db:seed:achievements` (prefer OnModuleInit for portfolio simplicity; idempotent upsert makes it safe on every boot).
    Relay: create apps/web/src/app/api/profile/achievements/route.ts (GET) relaying to ${INTERNAL_API_URL}/api/gamification/achievements with auth() guard.
    Frontend: AchievementBadge ('use client') — single tile with locked (opacity-40 grayscale + Lock icon) vs earned (full opacity + earned date) states per UI-SPEC. AchievementGrid — 4-col md / 2-col grid of the 8 badges ordered by definition sequence. XpProgressBar — shadcn Progress with xpIntoLevel = xpTotal % 100, label "Level {n} · {current}/{next} XP". Add to profile: render LevelBadge next to avatar, XpProgressBar below the CEFR badge, and AchievementGrid under an "Achievements" heading. Fetch achievements via /api/profile/achievements. Use empty-state copy "Complete lessons and quizzes to earn achievement badges." when none earned.
  </action>
  <verify>
    <automated>pnpm --filter api exec tsc --noEmit 2>&1 | grep -i gamification | head -5; pnpm --filter web exec tsc --noEmit 2>&1 | grep -iE "achievement|xp-progress|profile" | head -8; grep -c "gamification/achievements" apps/api/src/gamification/gamification.controller.ts apps/web/src/app/api/profile/achievements/route.ts; grep -c "AchievementGrid\|XpProgressBar\|LevelBadge" apps/web/src/app/(dashboard)/profile/profile-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - gamification.controller.ts exposes JwtAuthGuard-protected `GET /api/gamification/achievements` returning all 8 with earnedAt (null = locked).
    - GamificationService has getUserAchievements(userId) and seedAchievements runs on module init (idempotent upsert).
    - /api/profile/achievements relay exists with auth() guard.
    - profile-form.tsx renders LevelBadge, XpProgressBar, and AchievementGrid (grep count >= 3).
    - AchievementBadge shows locked vs earned state; XpProgressBar uses xpTotal % 100 for in-level progress.
    - Both tsc --noEmit checks clean for these files.
  </acceptance_criteria>
  <done>Profile shows level badge, XP progress bar, and an 8-badge achievement grid backed by a real achievements endpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /api/profile/achievements -> NestJS | Achievement fetch; userId from JWT, returns only the requesting user's earned state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-16 | Elevation of Privilege | achievements endpoint (IDOR) | mitigate | userId from req.user.userId only; never accept a userId path/query param |
| T-07-17 | Spoofing | profile achievements relay | mitigate | auth() guard returns 401 before forwarding; NestJS JwtAuthGuard re-checks |
| T-07-18 | Tampering | seedAchievements on every boot | accept | Upsert on slug is idempotent; no duplicate rows possible |
| T-07-SC | Tampering | npm installs | mitigate | No new packages this phase (framer-motion/lucide/shadcn already installed); slopcheck N/A |
</threat_model>

<verification>
- `pnpm --filter api exec tsc --noEmit` and `pnpm --filter web exec tsc --noEmit` clean for touched files
- GET /api/gamification/achievements returns 8 entries (verified in 07-07)
- Profile renders gamification UI; results page fires toast/modal
</verification>

<success_criteria>
- GAME-02: XP progress bar + level badge visible on profile
- GAME-03/04: 8 achievements displayed with locked/unlocked + earned date
- QUIZ-04: mistake review shows incorrect questions with explanations
</success_criteria>

<output>
Create `.planning/phases/07-quiz-center-gamification/07-06-SUMMARY.md` when done
</output>
