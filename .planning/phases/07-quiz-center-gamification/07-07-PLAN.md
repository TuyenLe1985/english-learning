---
phase: 07-quiz-center-gamification
plan: 07
type: execute
wave: 5
depends_on: ["07-03", "07-04", "07-05", "07-06"]
files_modified:
  - apps/api/src/gamification/gamification.service.ts
autonomous: false
requirements: [QUIZ-01, QUIZ-02, QUIZ-03, QUIZ-04, QUIZ-05, GAME-01, GAME-02, GAME-03, GAME-04, GAME-05]
must_haves:
  truths:
    - "Database schema is in sync (prisma db push reports no drift) — all Phase 7 tables exist"
    - "Full test suite passes (turbo test green)"
    - "8 achievement rows exist in the Achievement table after seeding"
    - "A human has verified all 5 phase success criteria end-to-end in the running app"
  artifacts:
    - path: ".planning/phases/07-quiz-center-gamification/07-07-SUMMARY.md"
      provides: "Phase 7 verification record"
      contains: "verification"
  key_links:
    - from: "running app"
      to: "User.xpTotal / User.level"
      via: "completing a quiz updates profile XP and level"
      pattern: "xpTotal"
---

<objective>
End-to-end verification of Phase 7: confirm database schema is in sync, run the full test suite, seed and verify the 8 achievements, then a human checkpoint walking all 5 phase success criteria in the running app. This is the phase gate before /gsd:verify-work.

Purpose: Prove QUIZ-01..05 and GAME-01..05 work together against a real database, and that the cross-cutting gamification retrofit (07-04) actually updates profile XP for non-quiz activity.
Output: A verified, smoke-tested Phase 7 and a SUMMARY recording results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-quiz-center-gamification/07-CONTEXT.md
@.planning/phases/07-quiz-center-gamification/07-RESEARCH.md

<interfaces>
Phase 7 success criteria (ROADMAP):
1. Mixed-skill quiz: answer grammar/vocab/reading/listening in sequence; session stores score, accuracy, time, incorrect items.
2. Topic-based quiz filtered by topic.
3. Mistake-review screen with explanations.
4. XP earned after lesson/quiz; harder/higher-CEFR award more; XP accumulates to a visible level 1-100 on profile + dashboard.
5. Achievement badges awarded at milestones, displayed on profile with earned date; every XP event recorded in audit log.
Schema is fully scaffolded from Phase 1 — no schema file changes were made in Phase 7. `prisma db push` should report no drift (verification, not migration).
</interfaces>

@packages/database/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Verify schema sync + seed achievements + run full suite</name>
  <files>apps/api/src/gamification/gamification.service.ts</files>
  <read_first>
    - packages/database/prisma/schema.prisma (Phase 7 models lines 486-590 — confirm no pending edits)
    - apps/api/src/gamification/gamification.service.ts (seedAchievements from 07-02/07-06)
  </read_first>
  <action>
    [BLOCKING] Schema push: run `npx prisma db push` from the repo root (or `pnpm --filter @repo/database exec prisma db push`) to confirm the database matches schema.prisma. Phase 7 added NO schema-file changes, so this is a sync/verification step and must report "already in sync" / "no changes" (or apply nothing). If the command requires non-interactive mode, use `npx prisma db push --skip-generate`; if it reports destructive drift, STOP and flag for manual review (do not pass --accept-data-loss — the schema should already match). After push, run `npx prisma generate` if the client is stale.
    Seed achievements: confirm the 8 achievements are present. If GamificationService.seedAchievements runs on OnModuleInit, starting the API seeds them; otherwise run the seed path directly. Verify with a query.
    Run the full suite: `turbo test` (or `pnpm --filter api test` + `pnpm --filter web test`). All quiz + gamification + retrofitted module specs must be green.
  </action>
  <verify>
    <automated>npx prisma db push --skip-generate 2>&1 | tail -5; turbo test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `prisma db push` reports the database is already in sync / no schema changes applied (no destructive drift).
    - `turbo test` exits 0 — all api and web specs green, including quiz.service, gamification.service, and the retrofitted grammar/reading/listening/srs/vocabulary specs.
    - After API boot, the Achievement table contains exactly 8 rows matching the ACHIEVEMENT_DEFINITIONS slugs (verify by querying the slugs).
  </acceptance_criteria>
  <done>Schema in sync, 8 achievements seeded, full test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all 5 phase success criteria</name>
  <files>(none — verification only)</files>
  <action>
    Pause for human verification. Do not modify code in this task; present the verification steps and wait for the resume signal. If the human reports issues, capture them as gap items for a follow-up `/gsd:plan-phase 07 --gaps` pass.
  </action>
  <what-built>
    The full Quiz Center + Gamification system: /quiz browse, paginated quiz session, results with score card + XP toast + level-up modal + mistake review, and profile additions (level badge, XP progress bar, 8-badge achievement grid). All 5 module session-complete endpoints now award XP through GamificationService.
  </what-built>
  <how-to-verify>
    Start the stack (`docker compose up` + `pnpm dev` / `turbo dev`) and sign in as the demo user, then verify each criterion:
    1. MIXED-SKILL QUIZ (QUIZ-01/03): Go to /quiz, click "Mixed Skill", start the quiz. Confirm exactly 10 questions appear one at a time across grammar/vocabulary/reading/listening, the "N of 10" progress bar advances, answered options lock, and "Next ->" advances. On question 10 the button reads "Submit Quiz". After submit, the results screen shows "{score}/10 correct", an accuracy %, time, XP earned, and a per-skill breakdown.
    2. TOPIC QUIZ (QUIZ-02): From /quiz, start a topic quiz (e.g. Technology). Confirm 10 questions appear and the quiz completes with a score.
    3. MISTAKE REVIEW (QUIZ-04): After a quiz where you answered some wrong, open "Review Mistakes". Confirm each incorrect question is re-shown with the correct answer highlighted and an explanation. If you got everything right, confirm the "All correct!" message.
    4. XP + LEVEL (GAME-01/02): On quiz completion confirm the "+{n} XP" toast slides up bottom-right and dismisses after ~4s. Open /profile and confirm the level badge and XP progress bar ("Level {n} · {current}/{next} XP") reflect the XP just earned. Complete a higher-CEFR or harder activity and confirm it awards more XP than an easy one. Cross a 100-XP boundary and confirm the level-up modal appears.
    5. ACHIEVEMENTS + AUDIT (GAME-03/04/05): On /profile confirm the 8-badge achievement grid shows locked vs earned states with earned dates. Complete your first quiz/lesson and confirm "first-lesson" (and any milestone hit) unlocks with a toast and shows on the profile. Confirm a non-quiz activity (e.g. complete a reading passage or a grammar lesson) ALSO increases profile XP — proving the 07-04 retrofit works, not just quizzes.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 criteria pass, or describe the issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> prisma db push | Schema sync runs against the dev/target database; verification-only this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-19 | Denial of Service | prisma db push destructive drift | mitigate | Push is sync-only (no schema changes this phase); destructive drift halts for manual review, never auto --accept-data-loss |
| T-07-20 | Tampering | Unseeded achievements break profile grid | mitigate | seedAchievements idempotent upsert on boot; Task 1 verifies 8 rows exist |
| T-07-SC | Tampering | npm installs | mitigate | No new packages this phase; slopcheck N/A |
</threat_model>

<verification>
- `prisma db push` reports in-sync (no drift)
- `turbo test` green
- 8 achievement rows present
- Human approves all 5 success criteria
</verification>

<success_criteria>
- All 5 ROADMAP success criteria verified in the running app
- Full test suite green
- Schema in sync, achievements seeded
- Cross-cutting XP retrofit confirmed (non-quiz activity increases profile XP)
</success_criteria>

<output>
Create `.planning/phases/07-quiz-center-gamification/07-07-SUMMARY.md` when done
</output>
