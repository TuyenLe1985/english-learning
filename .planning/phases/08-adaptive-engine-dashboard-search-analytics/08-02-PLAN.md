---
phase: 08-adaptive-engine-dashboard-search-analytics
plan: 02
type: tdd
wave: 2
depends_on: ["08-01a", "08-01b"]
files_modified:
  - apps/api/src/adaptive/adaptive.controller.ts
  - apps/api/src/adaptive/adaptive.service.ts
  - apps/api/src/adaptive/adaptive.service.spec.ts
  - apps/api/src/grammar/grammar.module.ts
  - apps/api/src/grammar/grammar.service.ts
  - apps/api/src/vocabulary/vocabulary.module.ts
  - apps/api/src/vocabulary/vocabulary.service.ts
  - apps/api/src/reading/reading.module.ts
  - apps/api/src/reading/reading.service.ts
  - apps/api/src/listening/listening.module.ts
  - apps/api/src/listening/listening.service.ts
  - apps/api/src/quiz/quiz.module.ts
  - apps/api/src/quiz/quiz.service.ts
  - apps/api/src/srs/srs.module.ts
  - apps/api/src/srs/srs.service.ts
autonomous: true
requirements: [ADPT-01, ADPT-02, ADPT-03, ADPT-04, ADPT-05, DASH-01, DASH-02, DASH-04]
user_setup: []

must_haves:
  truths:
    - "After completing a session in any of the 5 modules, the user's SkillScore for that skill area is updated (EMA blend)"
    - "A SkillScore with blended accuracy < 0.6 has isWeak=true"
    - "GET /api/adaptive/dashboard returns user XP/level/CEFR/streak, skill scores, recommendation, recently viewed, bookmarks, pending reviews"
    - "GET /api/adaptive/recommendation returns preThreshold=true until the user has >= 5 ActivityLog entries, then surfaces the lowest-accuracy weak skill"
    - "When User.cefrLevel changes, a CefrHistory row is recorded"
  artifacts:
    - path: "apps/api/src/adaptive/adaptive.service.ts"
      provides: "updateSkillScore, getDashboardData, getContinueLearningRecommendation, computeCurrentStreak"
      exports: ["AdaptiveService"]
    - path: "apps/api/src/adaptive/adaptive.controller.ts"
      provides: "GET /api/adaptive/dashboard, GET /api/adaptive/recommendation"
  key_links:
    - from: "apps/api/src/grammar/grammar.service.ts"
      to: "AdaptiveService.updateSkillScore"
      via: "inline call after awardXp"
      pattern: "adaptive\\.updateSkillScore"
    - from: "apps/api/src/quiz/quiz.service.ts"
      to: "AdaptiveService.updateSkillScore"
      via: "inline call after awardXp"
      pattern: "adaptive\\.updateSkillScore"
---

<objective>
TDD vertical slice: the adaptive engine backend. Fill in the AdaptiveService + AdaptiveController bodies (skeletons created in 08-01) implementing skill scoring, weak detection, dashboard aggregation, Continue Learning recommendation, streak computation, and CEFR history recording. Then wire `AdaptiveService.updateSkillScore()` into all 5 session-complete endpoints immediately after `gamificationService.awardXp()`.

Purpose: The data-producing core of the dashboard. Without it, SkillScore never updates and dashboard/recommendation/analytics have no data. Delivers ADPT-01–05 and the data behind DASH-01/02/04.
Output: AdaptiveService/Controller implemented; 5 session-complete endpoints wired; adaptive.service.spec.ts GREEN. (Does NOT touch app.module.ts — registration owned by 08-01.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-adaptive-engine-dashboard-search-analytics/08-RESEARCH.md
@.planning/phases/08-adaptive-engine-dashboard-search-analytics/08-PATTERNS.md

<interfaces>
AdaptiveModule + empty AdaptiveService/AdaptiveController skeletons already exist (created in 08-01b) and are registered in app.module.ts. CefrHistory model + GIN migration + deps come from 08-01a. This plan fills the service + controller bodies only — do NOT edit adaptive.module.ts or app.module.ts.

GamificationService.awardXp signature (apps/api/src/gamification/gamification.service.ts line 61):
  awardXp(userId: string, amount: number, reason: string, skillArea: SkillArea, sourceRef?: string)
GamificationModule exports GamificationService; consuming modules already import GamificationModule.

Existing awardXp call sites (insert updateSkillScore immediately AFTER each):
  - grammar.service.ts ~line 271: awardXp(..., 'GRAMMAR', lessonId); accuracy = correctCount/totalCount
  - reading.service.ts ~line 180: accuracy from reading session (ReadingProgress.accuracy)
  - listening.service.ts ~line 245: accuracy from listening session
  - quiz.service.ts ~line 415: awardXp(..., 'MIXED', sessionId); accuracy = computed session accuracy
  - srs.service.ts ~line 195: per-card awardXp on Good/Easy. For skill scoring, call updateSkillScore once per completed REVIEW SESSION (in completeSession) with skillArea 'VOCABULARY' and accuracy = session recall rate — NOT per card (avoid noisy single-card EMA)

SkillScore upsert (RESEARCH.md Pattern 1): EMA alpha=0.3; newAccuracy = existing ? existing.accuracy*0.7 + accuracy*0.3 : accuracy; isWeak = newAccuracy < 0.6 (D-08). @@unique → where { userId_skillArea: { userId, skillArea } }.

Streak helper (08-PATTERNS.md): copy GamificationService.checkStreak approach — query ActivityLog last 32 days, dedupe to calendar dates via date-fns differenceInCalendarDays.

Recommendation (RESEARCH.md Pattern 1 + D-03/D-05/D-07/D-08): activityCount < 5 → {preThreshold:true, recommendedModule: sensible default}. Else findFirst SkillScore where isWeak=true orderBy [{accuracy:'asc'},{updatedAt:'desc'}]. None weak → {preThreshold:false} (all healthy). ADPT-04 (recommendation-only, D-06): if a skill accuracy ≥ 0.8, include a recommended next-CEFR-tier informational field; NO content gating, NO guards.

Dashboard data (DASH-01/02/04): user {name,xpTotal,level,cefrLevel,streak}, skillScores[], lessonsCompleted (count completed sessions across modules), recommendation, recentlyViewed (ReadingProgress + ListeningProgress orderBy lastViewedAt desc, last 4 combined — RESEARCH Open Q #4 scope to reading+listening), bookmarked (Bookmark orderBy createdAt desc last 4 with passage title/cefrLevel), pendingReviews (SrsCard due count — reuse existing SRS due query).

CefrHistory (RESEARCH.md Pattern 7 + Pitfall 9): write a CefrHistory row only when User.cefrLevel differs from the most recent CefrHistory entry. Private recordCefrSnapshotIfChanged(userId, currentLevel) called from getDashboardData.

Controller pattern (08-PATTERNS.md): AdaptiveController already @Controller('adaptive'); add @UseGuards(JwtAuthGuard) @Get('dashboard') and @Get('recommendation'); userId ALWAYS from req.user.userId (IDOR prevention — never query param).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Implement AdaptiveService + controller bodies (GREEN the RED spec)</name>
  <files>apps/api/src/adaptive/adaptive.service.spec.ts, apps/api/src/adaptive/adaptive.service.ts, apps/api/src/adaptive/adaptive.controller.ts, apps/api/src/gamification/gamification.service.ts, apps/api/src/gamification/gamification.controller.ts, packages/shared/src/adaptive.dto.ts, apps/api/src/srs/srs.service.ts</files>
  <read_first>
    - apps/api/src/adaptive/adaptive.service.spec.ts (RED scaffold from 08-01b — contract to satisfy; includes ADPT-03 lowest-accuracy weak skill + ADPT-04 recommendedNextTier ≥0.8)
    - apps/api/src/adaptive/adaptive.service.ts (empty skeleton from 08-01b)
    - apps/api/src/adaptive/adaptive.controller.ts (empty @Controller('adaptive') skeleton)
    - apps/api/src/gamification/gamification.service.ts (lines 61-130 awardXp/checkAchievements; lines 262-298 checkStreak)
    - apps/api/src/gamification/gamification.controller.ts (AuthenticatedRequest interface lines 25-33)
    - packages/shared/src/adaptive.dto.ts (DashboardDto, ContinueLearningDto shapes)
    - apps/api/src/srs/srs.service.ts (existing due-card query to reuse for pendingReviews)
  </read_first>
  <behavior>
    - ADPT-01: updateSkillScore(userId,'GRAMMAR',0.9) on no existing → accuracy ≈ 0.9; then 0.5 → EMA ≈ 0.78
    - ADPT-02: blend to < 0.6 sets isWeak=true; recover to ≥ 0.6 sets isWeak=false
    - ADPT-05: getContinueLearningRecommendation returns {preThreshold:true, recommendedModule} when ActivityLog < 5
    - ADPT-03/D-07: ≥5 activities + a weak skill → lowest-accuracy isWeak skill (tie-break updatedAt desc)
    - Dashboard: getDashboardData returns all DashboardDto fields
  </behavior>
  <action>
    RED→GREEN→REFACTOR. Implement AdaptiveService per <interfaces>: `updateSkillScore(userId, skillArea: SkillArea, accuracy: number)` EMA alpha=0.3, isWeak = newAccuracy < 0.6, upsert via userId_skillArea; `getContinueLearningRecommendation(userId)` with ADPT-05 (<5 ActivityLog) gate + D-07 ordering + ADPT-04 informational tier field; `getDashboardData(userId)` aggregating user fields + skillScores + lessonsCompleted + recommendation + recentlyViewed (reading+listening lastViewedAt) + bookmarked + pendingReviews; private `computeCurrentStreak(userId)` (date-fns differenceInCalendarDays over ActivityLog); private `recordCefrSnapshotIfChanged(userId, currentLevel)` writing CefrHistory only on change (Pitfall 9). Add to AdaptiveController: @Get('dashboard') → getDashboardData(req.user.userId) and @Get('recommendation') → getContinueLearningRecommendation(req.user.userId), both @UseGuards(JwtAuthGuard), userId from JWT only. Do NOT edit adaptive.module.ts or app.module.ts (already registered in 08-01b).
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && pnpm --filter @repo/api test -- adaptive.service.spec 2>&1 | grep -Eq "passed|PASS" && ! grep -Eq "@Query\('userId'\)|params.userId" apps/api/src/adaptive/adaptive.controller.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @repo/api test -- adaptive.service.spec` passes (GREEN)
    - AdaptiveService exports updateSkillScore, getDashboardData, getContinueLearningRecommendation
    - adaptive.controller.ts: both endpoints @UseGuards(JwtAuthGuard), userId from req.user.userId (no userId query/path param)
    - adaptive.module.ts and app.module.ts UNCHANGED by this plan
  </acceptance_criteria>
  <done>AdaptiveService + controller implemented; adaptive.service.spec GREEN; registration untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AdaptiveService into all 5 session-complete endpoints</name>
  <files>apps/api/src/grammar/grammar.service.ts, apps/api/src/grammar/grammar.module.ts, apps/api/src/reading/reading.service.ts, apps/api/src/listening/listening.service.ts, apps/api/src/quiz/quiz.service.ts, apps/api/src/srs/srs.service.ts, apps/api/src/adaptive/adaptive.module.ts</files>
  <read_first>
    - apps/api/src/grammar/grammar.service.ts (lines 268-295 completeSession; accuracy = correctCount/totalCount)
    - apps/api/src/grammar/grammar.module.ts (imports [AuthModule, GamificationModule] — add AdaptiveModule)
    - apps/api/src/reading/reading.service.ts (line ~180 awardXp; session accuracy)
    - apps/api/src/listening/listening.service.ts (line ~245 awardXp; session accuracy)
    - apps/api/src/quiz/quiz.service.ts (line ~415 awardXp 'MIXED'; accuracy variable)
    - apps/api/src/srs/srs.service.ts (completeSession recall rate; line ~195 per-card awardXp)
    - apps/api/src/adaptive/adaptive.module.ts (exports AdaptiveService — import the module, not the file)
  </read_first>
  <action>
    For each of the 5 services: inject `AdaptiveService` via constructor (alongside existing `GamificationService`), add `AdaptiveModule` to the module's `imports` array, and call `await this.adaptive.updateSkillScore(userId, skillArea, accuracy)` IMMEDIATELY AFTER the existing `awardXp(...)` and before `checkAchievements(...)` (Phase 7 D-12 inline call-chain). Skill area + accuracy (0.0–1.0 float, NOT percentage): grammar → 'GRAMMAR', correctCount/totalCount; reading → 'READING', ReadingProgress accuracy; listening → 'LISTENING', session accuracy; quiz → 'MIXED', computed session accuracy; vocabulary/srs → 'VOCABULARY', session recall rate in completeSession (once per session, not per card). Do NOT remove/alter existing awardXp/checkAchievements. Do NOT create a circular import — AdaptiveModule must NOT import these 5 modules.
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && for f in grammar/grammar reading/reading listening/listening quiz/quiz srs/srs; do grep -q "adaptive.updateSkillScore" apps/api/src/$f.service.ts || { echo "MISSING $f"; exit 1; }; done && grep -l "AdaptiveModule" apps/api/src/grammar/grammar.module.ts apps/api/src/reading/reading.module.ts apps/api/src/listening/listening.module.ts apps/api/src/quiz/quiz.module.ts apps/api/src/srs/srs.module.ts | wc -l | grep -q 5 && pnpm --filter @repo/api build 2>&1 | tail -3 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 5 service files contain `adaptive.updateSkillScore(` after their awardXp call
    - All 5 module files import AdaptiveModule
    - `pnpm --filter @repo/api build` exits 0 — confirms no circular DI introduced by AdaptiveModule imports (AdaptiveModule must NOT import the 5 consumer modules)
    - accuracy passed as 0.0–1.0 (division, not *100)
  </acceptance_criteria>
  <done>All 5 session-complete endpoints update SkillScore inline after awardXp; API builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/adaptive/* | userId derived from JWT, never from request body/query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Elevation of Privilege (IDOR) | adaptive.controller dashboard/recommendation | mitigate | userId only from req.user.userId; no userId path/query param |
| T-08-03 | Spoofing | /api/adaptive endpoints | mitigate | @UseGuards(JwtAuthGuard) on every endpoint (V2) |
| T-08-04 | Tampering | accuracy influencing skill score | accept | accuracy server-computed from stored answers, not client-supplied directly |
</threat_model>

<verification>
- adaptive.service.spec GREEN (ADPT-01/02/05)
- All 5 modules wired; API builds (no circular dependency)
- /api/adaptive endpoints guarded; userId from JWT
- app.module.ts / adaptive.module.ts unchanged
</verification>

<success_criteria>
Skill scores update after every session; dashboard + recommendation endpoints return real data; CefrHistory records on level change.
</success_criteria>

<output>
Create `.planning/phases/08-adaptive-engine-dashboard-search-analytics/08-02-SUMMARY.md` when done.
</output>
