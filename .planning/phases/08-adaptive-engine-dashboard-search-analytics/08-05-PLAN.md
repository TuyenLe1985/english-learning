---
phase: 08-adaptive-engine-dashboard-search-analytics
plan: 05
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - apps/web/src/app/(dashboard)/layout.tsx
  - apps/web/src/components/navigation/sidebar.tsx
  - apps/web/src/components/search/top-nav-search.tsx
  - apps/web/src/app/(dashboard)/dashboard/page.tsx
  - apps/web/src/app/api/adaptive/dashboard/route.ts
  - apps/web/src/app/api/adaptive/recommendation/route.ts
  - apps/web/src/components/dashboard/dashboard-hero.tsx
  - apps/web/src/components/dashboard/dashboard-hero.test.tsx
  - apps/web/src/components/dashboard/skill-scores-card.tsx
  - apps/web/src/components/dashboard/skill-radar-chart.tsx
  - apps/web/src/components/dashboard/skill-radar-chart.test.tsx
  - apps/web/src/components/dashboard/activity-bar-chart.tsx
  - apps/web/src/components/dashboard/continue-learning-widget.tsx
  - apps/web/src/components/dashboard/recently-viewed-row.tsx
  - apps/web/src/components/dashboard/bookmarked-row.tsx
  - apps/web/src/components/dashboard/content-scroll-card.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, ADPT-03, ADPT-05]
user_setup: []

must_haves:
  truths:
    - "Visiting /dashboard shows the XP/level/streak hero, per-skill scores, skill radar, daily activity chart, Continue Learning widget, and Recently Viewed + Bookmarked rows — all from real user data"
    - "Continue Learning shows a pre-threshold default before 5 exercises, the weakest skill after, or an all-healthy state"
    - "The dashboard layout has a left-column Sidebar nav (Dashboard, Reading, Listening, Grammar, Vocabulary, Quiz, Analytics) with an Admin link visible only to ADMIN users (D-15), plus a top-bar search input that navigates to /search?q="
    - "dashboard-hero and skill-radar-chart component tests pass (GREEN)"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/dashboard/page.tsx"
      provides: "full dashboard page (replaces placeholder)"
    - path: "apps/web/src/components/navigation/sidebar.tsx"
      provides: "left-column sidebar nav with module links + role-gated Admin link (D-15)"
    - path: "apps/web/src/components/dashboard/dashboard-hero.tsx"
      provides: "XP/level/streak hero card"
    - path: "apps/web/src/app/api/adaptive/dashboard/route.ts"
      provides: "relay to NestJS GET /api/adaptive/dashboard"
  key_links:
    - from: "apps/web/src/app/(dashboard)/layout.tsx"
      to: "apps/web/src/components/navigation/sidebar.tsx"
      via: "Sidebar import + render in left column"
      pattern: "Sidebar"
    - from: "apps/web/src/components/navigation/sidebar.tsx"
      to: "/admin link gated on session.user.role === 'ADMIN'"
      via: "conditional render (D-15)"
      pattern: "role === 'ADMIN'"
    - from: "apps/web/src/app/api/adaptive/dashboard/route.ts"
      to: "NestJS GET /api/adaptive/dashboard"
      via: "fetchWithAuth relay"
      pattern: "/api/adaptive/dashboard"
    - from: "apps/web/src/components/dashboard/skill-radar-chart.tsx"
      to: "recharts RadarChart"
      via: "ResponsiveContainer in fixed-height div"
      pattern: "RadarChart"
---

<objective>
MVP vertical slice: the dashboard UI. Build the dashboard layout with a left-column Sidebar nav (module links + role-gated Admin link per D-15) and a top-bar search input, the dashboard page that replaces the placeholder, all dashboard components (hero, skill scores, radar, activity bar chart, Continue Learning widget, recently-viewed + bookmarked scroll rows), and the two relay routes to the adaptive endpoints. After this plan, a user can open /dashboard and see their full personalized dashboard end-to-end with sidebar navigation.

Purpose: Delivers DASH-01–04, the sidebar nav (D-15), and the visible surface of ADPT-03/05. Depends on 08-02 (adaptive endpoints).
Output: Working /dashboard page + Sidebar nav + top-bar search; dashboard-hero + skill-radar-chart tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-adaptive-engine-dashboard-search-analytics/08-UI-SPEC.md
@.planning/phases/08-adaptive-engine-dashboard-search-analytics/08-PATTERNS.md

<interfaces>
Adaptive endpoints (from 08-02): GET /api/adaptive/dashboard → DashboardDto {user:{name,xpTotal,level,cefrLevel,streak}, skillScores[], lessonsCompleted, recommendation:{preThreshold,weakestSkill?,accuracy?,recommendedModule?}, recentlyViewed[], bookmarked[], pendingReviews}. GET /api/adaptive/recommendation → ContinueLearningDto.

Relay pattern (08-PATTERNS.md, apps/web/src/app/api/profile/me/route.ts): auth()→401; headers()→cookieHeader; fetchWithAuth(cookieHeader, `${API_URL}/api/adaptive/dashboard`); res.ok check; NextResponse.json. Use API_URL for relay routes (client-facing); INTERNAL_API_URL for Server Component direct fetches.

Server Component page (08-PATTERNS.md, reading/page.tsx): auth()→redirect('/login'); headers cookie; fetchWithAuth(INTERNAL_API_URL + '/api/adaptive/dashboard'); graceful fallback res.ok ? json : null.

Reusable components: @/components/ui/{card,progress,badge,skeleton,separator,scroll-area}; @/components/cefr-badge (CefrBadge); @/components/gamification/level-badge (LevelBadge); @/components/gamification/xp-progress-bar (XpProgressBar — reuse in hero); lucide-react icons (Flame, BookOpen, Headphones, BookText, Layers, Search, Shield, LayoutDashboard, BarChart3).

Charts (RESEARCH.md Pattern 5 + Pitfalls 6/7): ALL recharts components MUST be "use client" and wrap ResponsiveContainer in a div with explicit height (e.g. h-[220px]). SkillRadarChart: RadarChart with PolarGrid/PolarAngleAxis(dataKey skill)/PolarRadiusAxis(domain 0-100)/Radar(dataKey accuracy). ActivityBarChart: BarChart {date,count} with --chart-1 fill.

UI-SPEC contract (08-UI-SPEC.md Screens 1-3 + Copywriting + Animation + Color):
  - Hero: full-width Card p-6 — greeting "Welcome back, {name}" (20px semibold), LevelBadge, CefrBadge, XpProgressBar, Flame icon (text-orange-500) + "{n} day streak" + "Keep it up!"
  - Two-column grid grid-cols-1 md:grid-cols-2 gap-6: left SkillScoresCard (min-h-[240px]) + SkillRadarChart (min-h-[220px]); right ActivityBarChart (min-h-[220px]) + ContinueLearningWidget (min-h-[120px])
  - Continue Learning 3 states (Screen 2 + Copywriting): pre-threshold "Begin your journey"/"Start {Module}"; weak "Work on {Skill}"/"Your lowest skill at {accuracy}%..."/"Start {Skill} Lesson"; all-healthy "You're on track"/"All skills above 60%..."/"Take a Quiz". CTA primary, min-h-[44px]
  - Horizontal rows (D-04): RecentlyViewedRow + BookmarkedRow via ScrollArea, cards w-[200px] flex-shrink-0, "View all →" link; empty-state copy per UI-SPEC
  - SkillScoresCard: 4 rows Grammar/Vocabulary/Reading/Listening with accuracy %, isWeak → text-destructive + bg-destructive/10 + "Needs work" badge
  - Sidebar (Screen 3 + Updated Dashboard Layout — D-15 REQUIRED): left-column nav listing Dashboard, Reading, Listening, Grammar, Vocabulary, Quiz, Analytics with lucide icons; active route highlighted; Admin link rendered ONLY when session.user.role === 'ADMIN' (D-15). Top bar: logo/title left, TopNavSearch (max-w-[320px], placeholder "Search lessons, vocabulary, passages...", submits /search?q=) center, right cluster LevelBadge(sm) + Sign out.
  - Framer Motion: hero entrance opacity+translateY (duration 0.3); Continue Learning opacity (delay 0.15)
  - Loading: Skeleton blocks; data fetched client-side via React Query useQuery (Interaction Contract)
  - Accessibility: sidebar nav role="navigation" aria-label; search form role="search" aria-label; isWeak rows aria-label; Continue Learning CTA aria-label

RED tests to GREEN (created in 08-01c): dashboard-hero.test.tsx (DASH-01), skill-radar-chart.test.tsx (DASH-03).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dashboard components + GREEN hero/radar tests</name>
  <files>apps/web/src/components/dashboard/dashboard-hero.tsx, apps/web/src/components/dashboard/dashboard-hero.test.tsx, apps/web/src/components/dashboard/skill-scores-card.tsx, apps/web/src/components/dashboard/skill-radar-chart.tsx, apps/web/src/components/dashboard/skill-radar-chart.test.tsx, apps/web/src/components/dashboard/activity-bar-chart.tsx, apps/web/src/components/dashboard/continue-learning-widget.tsx, apps/web/src/components/dashboard/recently-viewed-row.tsx, apps/web/src/components/dashboard/bookmarked-row.tsx, apps/web/src/components/dashboard/content-scroll-card.tsx</files>
  <read_first>
    - apps/web/src/components/dashboard/dashboard-hero.test.tsx (RED — DASH-01)
    - apps/web/src/components/dashboard/skill-radar-chart.test.tsx (RED — DASH-03)
    - apps/web/src/components/gamification/xp-progress-bar.tsx (reuse in hero; "use client" + Progress pattern)
    - apps/web/src/components/gamification/level-badge.tsx + apps/web/src/components/cefr-badge.tsx (badges)
    - apps/web/src/components/gamification/achievement-grid.tsx (grid/card map pattern analog for skill-scores + scroll rows)
    - apps/web/src/components/ui/scroll-area.tsx (installed in 08-01a; ScrollArea + ScrollBar)
    - packages/shared/src/adaptive.dto.ts (DashboardDto shapes)
    - .planning/phases/08-adaptive-engine-dashboard-search-analytics/08-UI-SPEC.md (Screens 1-2, Copywriting, Color, Animation)
    - .planning/phases/08-adaptive-engine-dashboard-search-analytics/08-RESEARCH.md (Pattern 5 chart shapes; Pitfalls 6/7)
  </read_first>
  <behavior>
    - DASH-01 (dashboard-hero.test): renders XP bar, level badge, CEFR badge, and "{n} day streak"
    - DASH-03 (skill-radar-chart.test): renders given 4 skill data points
  </behavior>
  <action>
    Build all "use client" dashboard components per the UI-SPEC contract in <interfaces>: DashboardHero (greeting + LevelBadge + CefrBadge + XpProgressBar + Flame streak; framer-motion entrance), SkillScoresCard (4 skill rows, isWeak → text-destructive + bg-destructive/10 + "Needs work" badge + aria-label), SkillRadarChart (recharts RadarChart in h-[220px] div — Pitfall 6; "use client" — Pitfall 7), ActivityBarChart (recharts BarChart {date,count} --chart-1 fill, h-[220px] wrapper), ContinueLearningWidget (3 states pre-threshold/weak/all-healthy with exact copy + module icon + primary CTA min-h-[44px] + framer-motion opacity), ContentScrollCard (title + CefrBadge + module icon + content-type label, w-[200px] flex-shrink-0), RecentlyViewedRow + BookmarkedRow (ScrollArea horizontal + "View all →" + empty-state copy). GREEN the two RED tests (dashboard-hero, skill-radar-chart). Use --chart-* CSS variables (not arbitrary colors) per UI-SPEC Color.
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && pnpm --filter @repo/web test -- dashboard-hero.test 2>&1 | grep -Eq "passed|PASS" && pnpm --filter @repo/web test -- skill-radar-chart.test 2>&1 | grep -Eq "passed|PASS" && for c in dashboard-hero skill-scores-card skill-radar-chart activity-bar-chart continue-learning-widget recently-viewed-row bookmarked-row content-scroll-card; do test -f apps/web/src/components/dashboard/$c.tsx || { echo "MISSING $c"; exit 1; }; done && grep -q "h-\[220px\]\|height: 220\|height={220}" apps/web/src/components/dashboard/skill-radar-chart.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - dashboard-hero.test + skill-radar-chart.test pass (GREEN)
    - All 8 dashboard component files exist
    - SkillRadarChart + ActivityBarChart are "use client" with explicit-height wrappers (Pitfalls 6/7)
    - ContinueLearningWidget implements all 3 states with exact UI-SPEC copy
    - isWeak skill rows use text-destructive + "Needs work" + aria-label
  </acceptance_criteria>
  <done>All dashboard components built; hero + radar tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard page + adaptive relay routes</name>
  <files>apps/web/src/app/(dashboard)/dashboard/page.tsx, apps/web/src/app/api/adaptive/dashboard/route.ts, apps/web/src/app/api/adaptive/recommendation/route.ts</files>
  <read_first>
    - apps/web/src/app/(dashboard)/dashboard/page.tsx (current placeholder to replace)
    - apps/web/src/app/(dashboard)/reading/page.tsx (Server Component auth + fetchWithAuth pattern)
    - apps/web/src/app/api/profile/me/route.ts (relay route pattern to copy)
    - apps/web/src/lib/api-client.ts (API_URL, INTERNAL_API_URL, fetchWithAuth exports)
    - apps/web/src/components/query-provider.tsx (QueryProvider — already wraps dashboard via layout)
    - packages/shared/src/adaptive.dto.ts (DashboardDto)
  </read_first>
  <action>
    Create relay routes: apps/web/src/app/api/adaptive/dashboard/route.ts and apps/web/src/app/api/adaptive/recommendation/route.ts — copy profile/me/route.ts exactly, changing only the NestJS URL to `${API_URL}/api/adaptive/dashboard` and `${API_URL}/api/adaptive/recommendation`. Replace dashboard/page.tsx placeholder with the full dashboard: Server Component fetches `${INTERNAL_API_URL}/api/adaptive/dashboard` (graceful fallback) for initial data and renders DashboardHero (full width) → two-column grid (SkillScoresCard + SkillRadarChart left; ActivityBarChart + ContinueLearningWidget right, grid-cols-1 md:grid-cols-2 gap-6) → RecentlyViewedRow + BookmarkedRow (mt-6 gap-6) per UI-SPEC Screen 1. Wire client components to React Query useQuery against the relay routes (Interaction Contract: client-side fetch, Skeleton loading states). Pass DashboardDto fields into the components. Set page title "Dashboard — English Learning".
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && test -f apps/web/src/app/api/adaptive/dashboard/route.ts && test -f apps/web/src/app/api/adaptive/recommendation/route.ts && grep -q "/api/adaptive/dashboard" apps/web/src/app/api/adaptive/dashboard/route.ts && grep -q "DashboardHero" apps/web/src/app/\(dashboard\)/dashboard/page.tsx && ! grep -q "fleshed out in Phase 8" apps/web/src/app/\(dashboard\)/dashboard/page.tsx && pnpm --filter @repo/web build 2>&1 | tail -5 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both adaptive relay routes exist and target the correct NestJS URLs
    - dashboard/page.tsx renders DashboardHero + two-column grid + scroll rows (placeholder copy removed)
    - `pnpm --filter @repo/web build` succeeds
  </acceptance_criteria>
  <done>Full /dashboard page renders from real data via relay routes; web builds.</done>
</task>

<task type="auto">
  <name>Task 3: Layout — Sidebar nav (D-15) + TopNavSearch</name>
  <files>apps/web/src/app/(dashboard)/layout.tsx, apps/web/src/components/navigation/sidebar.tsx, apps/web/src/components/search/top-nav-search.tsx</files>
  <read_first>
    - apps/web/src/app/(dashboard)/layout.tsx (current minimal top nav to restructure into sidebar + top bar)
    - apps/web/src/app/(dashboard)/reading/reading-filters.tsx (useRouter / usePathname navigation pattern)
    - apps/web/src/auth.ts (session.user.role shape — 'STUDENT' | 'ADMIN')
    - apps/web/src/components/gamification/level-badge.tsx (LevelBadge sm)
    - .planning/phases/08-adaptive-engine-dashboard-search-analytics/08-UI-SPEC.md (Screen 3 + Updated Dashboard Layout Navigation)
  </read_first>
  <action>
    Build the dashboard navigation chrome. (1) Create `apps/web/src/components/navigation/sidebar.tsx` ("use client", usePathname): a `<nav role="navigation" aria-label="Main">` left-column sidebar (fixed width e.g. w-56, full-height, border-r) listing these links with lucide icons and active-route highlighting (aria-current="page" on the active link): Dashboard (/dashboard), Reading (/reading), Listening (/listening), Grammar (/grammar), Vocabulary (/vocabulary), Quiz (/quiz), Analytics (/analytics). Accept a `role` prop (or read it from a server-passed value); render an Admin link (/admin, Shield icon) ONLY when `role === 'ADMIN'` (D-15 — use 'ADMIN' literal, Pitfall 3). (2) Create TopNavSearch ("use client", useRouter): a `<form role="search">` with `<input type="search" name="q" aria-label="Search platform content" placeholder="Search lessons, vocabulary, passages...">` (h-9, max-w-[320px], focus ring per UI-SPEC) that on submit navigates `router.push('/search?q=' + encodeURIComponent(value.trim()))` when non-empty. (3) Restructure layout.tsx: render `<Sidebar role={session.user?.role} />` as the left column and a main column with a top bar (logo/title left | TopNavSearch center | right cluster LevelBadge size sm + existing Sign out form) above `{children}`. Keep the existing QueryProvider wrapper and auth redirect. The Sidebar is REQUIRED (D-15) — do NOT make it optional.
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && test -f apps/web/src/components/navigation/sidebar.tsx && grep -q "role === 'ADMIN'" apps/web/src/components/navigation/sidebar.tsx && grep -q "/admin" apps/web/src/components/navigation/sidebar.tsx && grep -q "Sidebar" apps/web/src/app/\(dashboard\)/layout.tsx && test -f apps/web/src/components/search/top-nav-search.tsx && grep -q "/search?q=" apps/web/src/components/search/top-nav-search.tsx && grep -q "role=\"search\"" apps/web/src/components/search/top-nav-search.tsx && grep -q "TopNavSearch" apps/web/src/app/\(dashboard\)/layout.tsx && pnpm --filter @repo/web build 2>&1 | tail -3 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/navigation/sidebar.tsx exists and renders the 7 module nav links (Dashboard, Reading, Listening, Grammar, Vocabulary, Quiz, Analytics)
    - Sidebar renders a link to /admin ONLY when role === 'ADMIN' (D-15)
    - layout.tsx imports and renders `<Sidebar>` as the left column
    - TopNavSearch: role="search" form, aria-label input, navigates to /search?q= on non-empty submit; rendered in the top bar of layout.tsx
    - `pnpm --filter @repo/web build` succeeds
  </acceptance_criteria>
  <done>Dashboard layout has a required Sidebar nav with module links + role-gated Admin link (D-15) and a working top-bar search; web builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → relay route → NestJS | session validated by auth() in relay before forwarding |
| client role display (Admin link) | display-only; real enforcement at /admin server check + RolesGuard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Spoofing | adaptive relay routes | mitigate | auth() returns 401 if no session before fetchWithAuth forwards cookie |
| T-08-13 | Elevation of Privilege | Sidebar Admin link visibility | accept | client-side visibility only; authoritative gate is /admin server redirect (08-06) + RolesGuard (08-04). Hiding the link is UX, not security |
</threat_model>

<verification>
- dashboard-hero + skill-radar-chart tests GREEN
- /dashboard renders full layout from real data; placeholder removed
- Sidebar nav renders module links + role-gated Admin link (D-15); TopNavSearch navigates to /search?q=
- web builds
</verification>

<success_criteria>
A user opens /dashboard and sees their XP/level/streak, skill scores + radar, activity chart, Continue Learning recommendation, and recently-viewed + bookmarked rows; the left sidebar navigates the modules (with an ADMIN-only Admin link per D-15) and the top-bar search works.
</success_criteria>

<output>
Create `.planning/phases/08-adaptive-engine-dashboard-search-analytics/08-05-SUMMARY.md` when done.
</output>
</output>
