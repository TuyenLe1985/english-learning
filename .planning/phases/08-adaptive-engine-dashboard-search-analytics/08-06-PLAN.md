---
phase: 08-adaptive-engine-dashboard-search-analytics
plan: 06
type: execute
wave: 4
depends_on: ["08-03", "08-04", "08-05"]
files_modified:
  - apps/web/src/app/(dashboard)/search/page.tsx
  - apps/web/src/app/api/search/route.ts
  - apps/web/src/components/search/search-result-group.tsx
  - apps/web/src/components/search/search-result-item.tsx
  - apps/web/src/components/search/search-filters.tsx
  - apps/web/src/app/(dashboard)/analytics/page.tsx
  - apps/web/src/app/api/analytics/me/route.ts
  - apps/web/src/components/analytics/cefr-progression-chart.tsx
  - apps/web/src/components/analytics/vocab-retention-chart.tsx
  - apps/web/src/components/analytics/learning-time-chart.tsx
  - apps/web/src/components/analytics/activity-heatmap.tsx
  - apps/web/src/components/analytics/activity-heatmap.test.tsx
autonomous: true
requirements: [SRCH-01, SRCH-03, SRCH-04, ANLT-01]
user_setup: []

must_haves:
  truths:
    - "Submitting a search navigates to /search?q= and shows results grouped by content type with title, CEFR badge, topic, and a highlighted snippet"
    - "Search results can be filtered by CEFR level, topic, and skill via the filter bar (URL query params)"
    - "Visiting /analytics shows CEFR progression, vocabulary retention, learning time, and a GitHub-style activity heatmap from real data"
    - "activity-heatmap component test passes (GREEN)"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/search/page.tsx"
      provides: "search results page (Server Component reading URL params)"
    - path: "apps/web/src/app/(dashboard)/analytics/page.tsx"
      provides: "student analytics page"
    - path: "apps/web/src/components/analytics/activity-heatmap.tsx"
      provides: "react-activity-calendar heatmap"
  key_links:
    - from: "apps/web/src/app/api/search/route.ts"
      to: "NestJS GET /api/search"
      via: "fetchWithAuth relay forwarding query params"
      pattern: "/api/search"
    - from: "apps/web/src/app/api/analytics/me/route.ts"
      to: "NestJS GET /api/analytics/me"
      via: "fetchWithAuth relay"
      pattern: "/api/analytics/me"
---

<objective>
MVP vertical slices: the search UI and the student analytics UI. Build the /search page (grouped results, snippets, filters) with its query-forwarding relay, and the /analytics page (4 charts including the GitHub-style heatmap) with its relay. After this plan a user can search all content and view their analytics end-to-end.

Purpose: Delivers SRCH-01/03/04 (search UI surface) and ANLT-01 (student analytics). Depends on 08-03 (search API), 08-04 (analytics API), 08-05 (layout TopNavSearch already wired).
Output: Working /search and /analytics pages; activity-heatmap test GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-adaptive-engine-dashboard-search-analytics/08-UI-SPEC.md
@.planning/phases/08-adaptive-engine-dashboard-search-analytics/08-PATTERNS.md

<interfaces>
APIs: GET /api/search?q=&level=&topic=&skill= → SearchResponseDto {query,total,groups:[{type,count,results:[{id,type,title,snippet,cefrLevel,topic}]}]} (08-03). GET /api/analytics/me → AnalyticsDto {cefrProgression:[{month,level:1|2|3}], vocabRetention:[{week,rate}], learningTime:[{date,minutes}], activityHeatmap:[{date,count,level:0-4}]} (08-04).

Search relay (08-PATTERNS.md): copy profile/me/route.ts but FORWARD query params — `const {searchParams}=new URL(req.url); const nestUrl=new URL(`${API_URL}/api/search`); searchParams.forEach((v,k)=>nestUrl.searchParams.set(k,v)); fetchWithAuth(cookieHeader, nestUrl.toString())`. Analytics relay: plain copy targeting `${API_URL}/api/analytics/me`.

Search page (08-PATTERNS.md + UI-SPEC Screen 4): Server Component with `searchParams: Promise<{q?,level?,topic?,skill?}>`; auth()→redirect('/login'); only fetch when q present (skip empty); fetch `${INTERNAL_API_URL}/api/search` with forwarded params; render heading `Search results for "{query}"`, count `{n} results across {m} categories`, SearchFilters bar, result groups in order Vocabulary · Grammar Lessons · Reading Passages · Listening · Quizzes (Quiz omitted by API). No-results + error + loading states per UI-SPEC. snippet `<mark>` styled bg-yellow-100 text-yellow-800. framer-motion group stagger.

SearchFilters (08-PATTERNS.md, copy reading-filters.tsx; URL-driven): Select for CEFR (All/B1/B2/C1), Topic, Skill (All/Grammar/Vocabulary/Reading/Listening); active filters as dismissible Badge chips; "Clear all"; navigation target /search (not /reading); params ?level=&topic=&skill= preserving q.

Analytics page (UI-SPEC Screen 5): centered max-w-4xl, heading "Your Analytics"; 2-col grid (grid-cols-1 md:grid-cols-2 gap-6): CefrProgressionChart, VocabRetentionChart, LearningTimeChart (with Daily/Weekly/Monthly Select), ActivityHeatmap (col-span full). Empty-state copy per UI-SPEC. Charts client-fetched via React Query.

Charts (RESEARCH.md Pattern 5/6 + Pitfalls 6/7): ALL "use client", ResponsiveContainer in explicit-height div. CefrProgressionChart LineChart (Y B1/B2/C1 via 1/2/3 tickFormatter, --chart-2). VocabRetentionChart LineChart (--chart-3). LearningTimeChart BarChart (--chart-4) with time-range Select (local state, default Weekly). ActivityHeatmap: react-activity-calendar (installed 08-01) with Activity[] {date,count,level}, theme light array per UI-SPEC heatmap colors (D-16 mapping 0/1-3/4-7/8+), showWeekdayLabels, Tooltip cell labels; "use client".

RED test to GREEN (08-01): activity-heatmap.test.tsx (ANLT-01 renders 365 days).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search slice — relay + page + result/filter components</name>
  <files>apps/web/src/app/api/profile/me/route.ts, apps/web/src/app/, apps/web/src/components/cefr-badge.tsx, apps/web/src/lib/api-client.ts, packages/shared/src/search.dto.ts</files>
  <read_first>
    - apps/web/src/app/api/profile/me/route.ts (relay base)
    - apps/web/src/app/(dashboard)/reading/page.tsx (Server Component + searchParams pattern)
    - apps/web/src/app/(dashboard)/reading/reading-filters.tsx (URL-driven filter pattern to adapt for SearchFilters)
    - apps/web/src/components/cefr-badge.tsx + apps/web/src/components/ui/{card,badge,separator,select,skeleton,button}.tsx
    - apps/web/src/lib/api-client.ts (API_URL, INTERNAL_API_URL, fetchWithAuth)
    - packages/shared/src/search.dto.ts (SearchResponseDto shapes)
    - .planning/phases/08-adaptive-engine-dashboard-search-analytics/08-UI-SPEC.md (Screen 4 + Copywriting Search)
  </read_first>
  <action>
    Create apps/web/src/app/api/search/route.ts: relay forwarding ALL query params to `${API_URL}/api/search` (see <interfaces>). Create SearchResultItem ("use client": title 14px semibold, CefrBadge + topic Badge variant outline, snippet with `<mark>` styled bg-yellow-100 text-yellow-800 rounded-sm px-0.5, content-type label 12px muted). Create SearchResultGroup (Separator + module name 16px semibold + count Badge + 3-5 items + "Show {n} more →" link when >5). Create SearchFilters (adapt reading-filters.tsx: CEFR/Topic/Skill Selects, dismissible Badge chips, "Clear all", target /search, preserve q). Create search/page.tsx (Server Component, searchParams prop, auth redirect, skip fetch on empty q, fetch INTERNAL_API_URL/api/search with forwarded params, render heading + count + SearchFilters + groups in fixed order, no-results/error/loading states, framer-motion group stagger). Use exact UI-SPEC copy.
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && test -f apps/web/src/app/api/search/route.ts && grep -q "searchParams.forEach\|searchParams.set" apps/web/src/app/api/search/route.ts && test -f apps/web/src/app/\(dashboard\)/search/page.tsx && for c in search-result-group search-result-item search-filters; do test -f apps/web/src/components/search/$c.tsx || { echo "MISSING $c"; exit 1; }; done && grep -q "Search results for" apps/web/src/app/\(dashboard\)/search/page.tsx && pnpm --filter @repo/web build 2>&1 | tail -5 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - search relay forwards all query params to NestJS /api/search
    - search/page.tsx is a Server Component reading searchParams; skips fetch on empty q; renders grouped results + filters + states
    - SearchResultItem renders snippet `<mark>` highlight, CefrBadge, topic, content-type label
    - SearchFilters updates URL ?level=&topic=&skill= preserving q
    - `pnpm --filter @repo/web build` succeeds
  </acceptance_criteria>
  <done>/search shows grouped, filterable, snippet-bearing results end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Analytics slice — relay + page + 4 charts + GREEN heatmap test</name>
  <files>apps/web/src/components/analytics/activity-heatmap.test.tsx, apps/web/src/app/api/profile/me/route.ts, apps/web/src/app/, apps/web/src/components/ui/{card,select,skeleton}.tsx, packages/shared/src/analytics.dto.ts</files>
  <read_first>
    - apps/web/src/components/analytics/activity-heatmap.test.tsx (RED — ANLT-01)
    - apps/web/src/app/api/profile/me/route.ts (relay base)
    - apps/web/src/app/(dashboard)/reading/page.tsx (Server Component pattern)
    - apps/web/src/components/ui/{card,select,skeleton}.tsx + apps/web/src/components/ui/tooltip.tsx (installed 08-01)
    - packages/shared/src/analytics.dto.ts (AnalyticsDto)
    - .planning/phases/08-adaptive-engine-dashboard-search-analytics/08-UI-SPEC.md (Screen 5 + Copywriting Analytics + Color heatmap)
    - .planning/phases/08-adaptive-engine-dashboard-search-analytics/08-RESEARCH.md (Pattern 5 chart shapes; Pattern 6 react-activity-calendar; Pitfalls 6/7)
  </read_first>
  <behavior>
    - ANLT-01 (activity-heatmap.test): renders given 365 days of activity data
  </behavior>
  <action>
    Create apps/web/src/app/api/analytics/me/route.ts: relay copy targeting `${API_URL}/api/analytics/me`. Build "use client" charts per <interfaces>: CefrProgressionChart (recharts LineChart, Y B1/B2/C1 via 1/2/3 tickFormatter, --chart-2, h-[200px] wrapper — Pitfall 6), VocabRetentionChart (LineChart --chart-3), LearningTimeChart (BarChart --chart-4 + Daily/Weekly/Monthly Select local state default Weekly), ActivityHeatmap (react-activity-calendar with Activity[] {date,count,level}, theme.light array per UI-SPEC D-16 colors, showWeekdayLabels, cell Tooltip, role="grid" aria-label). GREEN the activity-heatmap RED test. Create analytics/page.tsx (Server Component, auth redirect, fetch INTERNAL_API_URL/api/analytics/me graceful fallback, max-w-4xl, heading "Your Analytics", 2-col grid with the 4 charts + heatmap col-span full, empty-state copy per UI-SPEC, framer-motion chart container fade). Title "Analytics — English Learning". All charts "use client" (Pitfall 7).
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && pnpm --filter @repo/web test -- activity-heatmap.test 2>&1 | grep -Eq "passed|PASS" && test -f apps/web/src/app/api/analytics/me/route.ts && test -f apps/web/src/app/\(dashboard\)/analytics/page.tsx && for c in cefr-progression-chart vocab-retention-chart learning-time-chart activity-heatmap; do test -f apps/web/src/components/analytics/$c.tsx || { echo "MISSING $c"; exit 1; }; done && grep -q "react-activity-calendar" apps/web/src/components/analytics/activity-heatmap.tsx && pnpm --filter @repo/web build 2>&1 | tail -5 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - activity-heatmap.test passes (GREEN)
    - analytics relay targets NestJS /api/analytics/me
    - analytics/page.tsx renders 4 chart cards (CEFR progression, vocab retention, learning time, heatmap)
    - All chart components "use client" with explicit-height wrappers; heatmap uses react-activity-calendar with D-16 theme colors
    - `pnpm --filter @repo/web build` succeeds
  </acceptance_criteria>
  <done>/analytics shows all 4 student analytics visualizations end-to-end; heatmap test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → search/analytics relay → NestJS | session validated by auth() before forwarding; userId never client-supplied for /analytics/me |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Spoofing | search + analytics relays | mitigate | auth() returns 401 if no session before fetchWithAuth forwards cookie |
| T-08-15 | Cross-site Scripting | search snippet `<mark>` rendering | mitigate | ts_headline output uses fixed StartSel/StopSel `<mark>` tags only; render snippet via a constrained highlighter (sanitize/allow-list `<mark>` only), not arbitrary dangerouslySetInnerHTML of raw HTML |
</threat_model>

<verification>
- activity-heatmap test GREEN (ANLT-01)
- /search end-to-end: grouped results, snippets, filters
- /analytics end-to-end: 4 charts from real data
- web builds
</verification>

<success_criteria>
A user searches all content with filters and views their full analytics page; snippet rendering is XSS-safe.
</success_criteria>

<output>
Create `.planning/phases/08-adaptive-engine-dashboard-search-analytics/08-06-SUMMARY.md` when done.
</output>
