---
phase: 08-adaptive-engine-dashboard-search-analytics
plan: 07
type: execute
wave: 5
depends_on: ["08-04", "08-06"]
files_modified:
  - apps/web/src/app/(dashboard)/admin/page.tsx
  - apps/web/src/app/api/admin/analytics/route.ts
  - apps/web/src/components/analytics/admin-stat-card.tsx
  - apps/web/src/components/analytics/user-growth-chart.tsx
  - apps/web/src/components/analytics/top-content-table.tsx
  - apps/web/src/components/analytics/module-completion-table.tsx
autonomous: false
requirements: [ANLT-02]
user_setup: []

must_haves:
  truths:
    - "An ADMIN user visiting /admin sees DAU/WAU/MAU, week-2 retention, a user growth chart, and a top-content table"
    - "A non-ADMIN user visiting /admin is redirected to /dashboard (server-side)"
    - "The full test suite passes and all 5 Phase 8 success criteria are verified by a human"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/admin/page.tsx"
      provides: "admin dashboard, server-side role-gated"
      contains: "redirect"
    - path: "apps/web/src/app/api/admin/analytics/route.ts"
      provides: "relay to NestJS GET /api/analytics/admin"
  key_links:
    - from: "apps/web/src/app/(dashboard)/admin/page.tsx"
      to: "session.user.role gate"
      via: "server-side redirect when role !== 'ADMIN'"
      pattern: "role !== 'ADMIN'"
    - from: "apps/web/src/app/api/admin/analytics/route.ts"
      to: "NestJS GET /api/analytics/admin"
      via: "fetchWithAuth relay"
      pattern: "/api/analytics/admin"
---

<objective>
MVP vertical slice: the admin dashboard, plus the phase end-to-end verification checkpoint. Build the role-gated /admin page (stat cards, user growth chart, top content table) with its relay, run the full test suite, seed an admin user, and run a human checkpoint over all 5 Phase 8 success criteria.

Purpose: Delivers ANLT-02 (admin UI surface) and closes the phase. Depends on 08-04 (admin API + RolesGuard) and 08-06 (analytics components reused/extended).
Output: Working /admin page; full suite green; phase verified by human.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-adaptive-engine-dashboard-search-analytics/08-UI-SPEC.md
@.planning/phases/08-adaptive-engine-dashboard-search-analytics/08-PATTERNS.md

<interfaces>
API: GET /api/analytics/admin → AdminAnalyticsDto {dau,wau,mau,retentionRate,topContent:[{title,module,completions}],completionRateByModule:[{module,rate}],userGrowth:[{date,total}],lastUpdated} (08-04, RolesGuard ADMIN-only, Redis-cached). completionRateByModule satisfies ANLT-02 "average completion rates by module".

Admin relay (08-PATTERNS.md): copy profile/me/route.ts targeting `${API_URL}/api/analytics/admin`. The NestJS RolesGuard enforces ADMIN; relay just forwards the session cookie (a non-admin gets 403 from NestJS).

Admin page (08-PATTERNS.md + UI-SPEC Screen 6): Server Component; auth()→redirect('/login'); ROLE GATE `if (session.user?.role !== 'ADMIN') redirect('/dashboard')` (use 'ADMIN' literal — Pitfall 3); fetch `${INTERNAL_API_URL}/api/analytics/admin`. Layout max-w-screen-xl, heading "Platform Overview", sub-heading "Last updated {relative time}". 4 AdminStatCard (DAU/WAU/MAU/Week-2 Retention) in grid-cols-2 md:grid-cols-4 gap-4; below (mt-6) 2-col grid: UserGrowthChart + TopContentTable, then a ModuleCompletionTable (ANLT-02 "average completion rates by module") showing completionRateByModule. framer-motion stat-card stagger (opacity+scale).

Components: AdminStatCard ("use client" or server — label 12px muted, value 28px semibold Display, delta badge green/destructive; use `<dl><dt><dd>` for a11y per UI-SPEC). UserGrowthChart (recharts LineChart cumulative {date,total}, --chart-5, "use client", h wrapper — Pitfalls 6/7). TopContentTable (Title truncate max-w-[200px], Module Badge variant secondary, Completions right-aligned). ModuleCompletionTable (ANLT-02): rows over completionRateByModule {module, rate} — Module Badge + a rate % bar or right-aligned percentage; small table or horizontal bar chart.

Admin seed (08-01b created seed-admin.ts + db:seed:admin): run `pnpm db:seed:admin` (or `pnpm --filter @repo/database run db:seed:admin`) to create the ADMIN user used to verify /admin.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin slice — relay + role-gated page + stat cards/chart/table</name>
  <files>apps/web/src/app/api/profile/me/route.ts, apps/web/src/app/, apps/web/src/auth.ts, apps/web/src/components/ui/{card,badge,table,skeleton}.tsx, packages/shared/src/analytics.dto.ts</files>
  <read_first>
    - apps/web/src/app/api/profile/me/route.ts (relay base)
    - apps/web/src/app/(dashboard)/reading/page.tsx (Server Component + auth pattern)
    - apps/web/src/auth.ts (session.user.role shape)
    - apps/web/src/components/ui/{card,badge,table,skeleton}.tsx (confirm table component exists; if not, render a plain styled table)
    - packages/shared/src/analytics.dto.ts (AdminAnalyticsDto)
    - .planning/phases/08-adaptive-engine-dashboard-search-analytics/08-UI-SPEC.md (Screen 6 + Copywriting Admin + Color --chart-5)
  </read_first>
  <action>
    Create apps/web/src/app/api/admin/analytics/route.ts: relay copy targeting `${API_URL}/api/analytics/admin` (forward session cookie; surface NestJS 403 status as-is for non-admins). Build AdminStatCard (label 12px muted via `<dt>`, value 28px semibold via `<dd>`, delta badge green if positive / text-destructive if negative — UI-SPEC), UserGrowthChart ("use client" recharts LineChart cumulative {date,total}, --chart-5, explicit-height wrapper — Pitfalls 6/7), TopContentTable (Title truncate, Module Badge secondary, Completions right-aligned), ModuleCompletionTable (ANLT-02 "average completion rates by module" — rows over completionRateByModule {module, rate} with Module Badge + rate % bar/right-aligned percentage). Create admin/page.tsx: Server Component with auth()→redirect('/login') and ROLE GATE `if (session.user?.role !== 'ADMIN') redirect('/dashboard')`; fetch INTERNAL_API_URL/api/analytics/admin (graceful fallback); render "Platform Overview" heading + "Last updated {relative time}" + 4 AdminStatCard (DAU/WAU/MAU/Week-2 Retention) + UserGrowthChart + TopContentTable + ModuleCompletionTable (ANLT-02) per UI-SPEC Screen 6 layout; framer-motion stat-card stagger. Use exact UI-SPEC copy.
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && test -f apps/web/src/app/api/admin/analytics/route.ts && grep -q "/api/analytics/admin" apps/web/src/app/api/admin/analytics/route.ts && grep -q "role !== 'ADMIN'" apps/web/src/app/\(dashboard\)/admin/page.tsx && grep -q "redirect" apps/web/src/app/\(dashboard\)/admin/page.tsx && for c in admin-stat-card user-growth-chart top-content-table module-completion-table; do test -f apps/web/src/components/analytics/$c.tsx || { echo "MISSING $c"; exit 1; }; done && pnpm --filter @repo/web build 2>&1 | tail -5 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - admin relay targets NestJS /api/analytics/admin and forwards the session cookie
    - admin/page.tsx redirects non-ADMIN to /dashboard server-side (role !== 'ADMIN')
    - 4 AdminStatCard + UserGrowthChart + TopContentTable + ModuleCompletionTable (ANLT-02 completion rates by module) render per UI-SPEC Screen 6
    - `pnpm --filter @repo/web build` succeeds
  </acceptance_criteria>
  <done>/admin renders platform stats for ADMIN users; non-admins redirected; web builds.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite + admin seed + type/build gate</name>
  <files>packages/database/package.json</files>
  <read_first>
    - .planning/phases/08-adaptive-engine-dashboard-search-analytics/08-RESEARCH.md (Validation Architecture: full suite command `pnpm test`; per-app commands)
    - packages/database/package.json (db:seed:admin script from 08-01b)
  </read_first>
  <action>
    Ensure Docker services are up (`docker compose up -d postgres redis-cache`). Run the admin seed: `pnpm --filter @repo/database run db:seed:admin` so an ADMIN user exists for the human checkpoint. Run the full test suite: `pnpm test` (Turborepo runs both apps). All Phase 8 specs (adaptive/search/analytics service specs, dashboard-hero/skill-radar-chart/activity-heatmap component tests) plus prior-phase suites must pass. Run type/build gate: `pnpm --filter @repo/api build` and `pnpm --filter @repo/web build`. Fix any failures (do NOT weaken assertions). Confirm the 4 GIN indexes and CefrHistory table still present (from 08-01a migration).
  </action>
  <verify>
    <automated>cd /home/tuyen/Desktop/Apps/english-learning && pnpm test 2>&1 | tail -20 && pnpm --filter @repo/api build 2>&1 | tail -3 && pnpm --filter @repo/web build 2>&1 | tail -3 && echo SUITE_DONE</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 (all API + web specs pass, including the 6 Phase 8 tests)
    - Both apps build with no type errors
    - ADMIN user seeded (db:seed:admin ran successfully)
  </acceptance_criteria>
  <done>Full suite green; both apps build; admin user seeded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 8 end-to-end human verification</name>
  <action>Run the human verification over all 5 ROADMAP Phase 8 success criteria using the steps below. Do not auto-advance — this is a blocking checkpoint.</action>
  <what-built>
    The complete Phase 8 surface: adaptive engine (skill scoring wired into all 5 modules), personalized /dashboard, global /search (GIN FTS), student /analytics, and admin /admin dashboard with role gating.
  </what-built>
  <how-to-verify>
    Prereq: `docker compose up -d` (postgres + redis-cache), `pnpm dev` running both apps. Sign in as a regular (STUDENT) user that has completed ≥ 5 exercises, and separately as the seeded ADMIN user (email/password from ADMIN_EMAIL/ADMIN_PASSWORD env, default admin@example.com).
    1. Success criterion 1 (DASH-01/03): Visit `/dashboard`. Confirm the hero shows CEFR level, XP progress bar, level, and streak; the two-column grid shows per-skill scores + skill radar chart + a daily/weekly activity bar chart — all with real data (no placeholder text).
    2. Success criterion 2 (ADPT-03/05, DASH-02): Confirm "Continue Learning" surfaces the lowest weak skill ("Work on {Skill} — your lowest at X%") for a user past 5 exercises; for a brand-new user it shows the pre-threshold "Begin your journey" default.
    3. Success criterion 3 (ADPT-02/04): Complete a lesson/quiz, return to /dashboard, confirm the per-skill score updated and weak flags reflect < 60% accuracy; confirm no content is hard-locked (recommendation-only, D-06).
    4. Success criterion 4 (SRCH-01/04): Use the top-nav search to search a known term. Confirm results appear under `/search` grouped by content type (Vocabulary/Grammar/Reading/Listening) within ~300ms, each with content type, CEFR badge, topic, and a highlighted snippet; apply a CEFR/topic/skill filter and confirm results narrow. NOTE: Quiz search exclusion is by design (locked D-11) — SRCH-01 is satisfied for the 4 indexed content types: vocabulary, grammar, reading, listening (QuizSession has no stable tsvector-indexable text document).
    5. Success criterion 5 (ANLT-01/02): Visit `/analytics` (student) and confirm CEFR progression, vocabulary retention, learning time, and the GitHub-style activity heatmap render. Then as ADMIN visit `/admin` and confirm DAU/WAU/MAU, week-2 retention, user growth chart, and top-content table render. As a STUDENT, navigate to `/admin` directly and confirm you are redirected to `/dashboard`.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 success criteria pass, or describe the specific gaps observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-admin → /admin | server-side role gate redirects; API RolesGuard returns 403 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16 | Elevation of Privilege | /admin page | mitigate | Server Component checks session.user.role !== 'ADMIN' → redirect('/dashboard'); authoritative API enforcement via RolesGuard (08-04) returns 403 even if page were reached |
| T-08-17 | Spoofing | admin analytics relay | mitigate | auth() 401 if no session; NestJS RolesGuard 403 if not ADMIN — both server-side |
</threat_model>

<verification>
- /admin renders for ADMIN, redirects STUDENT
- `pnpm test` green; both apps build; admin seeded
- Human checkpoint confirms all 5 Phase 8 success criteria
</verification>

<success_criteria>
All 5 ROADMAP Phase 8 success criteria pass; admin dashboard role-gated; full suite green.
</success_criteria>

<output>
Create `.planning/phases/08-adaptive-engine-dashboard-search-analytics/08-07-SUMMARY.md` when done.
</output>
